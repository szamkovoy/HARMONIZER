- 2026-08-09 (427): Admin users list + cabinet — paid period/next charge from `current_period_end` (no 48h grace).
- 2026-08-09 (426): Admin Harmonizer header — tariff «с/до» from payment + `current_period_end` (no 48h grace).
- 2026-08-08 (425): Admin user card/search polish — name modal, GetCourse fields, Harmonizer header period; drop crm_legacy_id; email segment email_only + locale.
- 2026-08-08 (424): CRM import foundation — nullable profile fields, crm_products links, admin «Только рассылки», OTP cleanup skips CRM imports.
- 2026-08-07 (423): Admin users — «Отменил автоплатеж» after tier badge when paid access remains without active renew.
- 2026-08-07 (422): OTP ghosts TTL 1h; marketing contacts only after OTP confirm; welcome drip after onboarded_at.
- 2026-08-06 (421): Store-review ensure — do not overwrite birth place on every login (seed only when empty).
- 2026-08-06 (420): Store-review — restore sign-out/delete links; delete shows blocked dialog (no wipe); cabinet stays hidden.
- 2026-08-05 (419): Store-review login — fixed OTP secrets, skip email send, mint session, Master + hide cabinet/sign-out/delete.
- 2026-08-04 (418): Keep-awake for flash meditation + asana; calm unchanged; breath already covered.
- 2026-08-04 (417): Master upgrade bonus — restore oracle catalog 950₽; preview/fulfill use catalog price.
- 2026-08-04 (416): Cabinet billing geo — prefer `users.country_code`; stop sticky EUR timeout (RU → YooKassa).
- 2026-08-04 (415): App marketing version **1.2.2** (`app.json` / `package.json`; store codes via EAS autoIncrement).
- 2026-08-04 (414): OTP email slim (no guide block) + Auto-Submitted; SES_OTP_CONFIGURATION_SET separate from marketing.
- 2026-08-04 (413): Fix users access-sort; sort controls below date filters; dashboard KPI dig-down links.
- 2026-08-04 (412): Admin users UX — set_access tariff modal, email/push statuses, unified access filter + sort, segment «Не в гармонизаторе».
- 2026-08-04 (411): Refund success revokes membership; YooKassa marks refunded only on `succeeded` (+ manual fallback button).
- 2026-08-04 (410): Admin refunds (Lava mark / YooKassa API); YooKassa Mentor→Master bonus days + cabinet note; admin login error codes + «Восстановить связь».
- 2026-08-04 (409): Re-enable YooKassa (RU) + Lava (INT); drop legacy `YOOKASSA_ENABLED` / `PAYMENT_GATEWAY_FOR_RUB` / `YOOKASSA_RECURRING_ENABLED`; save_method with 403 retry; `.vercelignore` `/dist`.
- 2026-08-04 (408): Store versioning — `DEPLOY.md` + agent rule + `after-store-build.mjs` (EAS autoIncrement; no hand-bump; commit `app.json` once).
- 2026-08-04 (407): Android `versionCode` **30** (Play already consumed 27–29).
- 2026-08-04 (406): App marketing version **1.2.1** (`app.json` / `package.json`).
- 2026-08-03 (405): Expo Dev iOS splash — white underlay handoff (no black blink); stable day refresh + force-complete (no hang).
- 2026-08-03 (404): Opportunity Windows bell — close reminder modal before OS notification permission; localized `{appName}`; Settings CTA on deny.
- 2026-08-03 (403): App marketing version **1.2.0** (`app.json` / `package.json`).
- 2026-08-02 (402): Profile empty reports — ghost chart previews (bars/donut/matrix/line) under semi-transparent empty-copy.
- 2026-08-02 (401): Android splash — full-bleed windowBackground + immediate native hide (no mini→large jump; no white JS-wait hang).
- 2026-08-02 (400): Android splash — restore visible native icon (no white hang); keep splash on unsettled route; theme-colored root/Stack underlay.
- 2026-08-02 (399): Android splash — no early hideAsync race; wait theme.ready before EarlyCover/Stack (dark flash); Day pencil/trash tint; stories quiet cold placeholder.
- 2026-08-02 (398): Communicator mic — ignore Android AppState inactive + settle-grace after record start (Samsung shutter-jerk / dead mic).
- 2026-08-02 (397): Stories cold-start spinner + retry; breath QC fail dialog ~3s before ring «0».
- 2026-08-02 (396): Stories ring — keep warm feed on RPC/network errors (no brand-avatar flash over existing stories).
- 2026-08-02 (395): Breath camera QC — fail dialog aligned with countdown «0» (wall deadline + hopeless early fail at T−5s).
- 2026-08-02 (394): Breath camera activation — wall-clock hang watchdog so empty PPG no longer sticks on «waiting for signal».
- 2026-08-02 (393): Breath tempo on practice card + triangle phase presets; ComboBox header «Темп:»; per-practice prefs after start.
- 2026-08-02 (392): Breath camera results — R–R chart from live merged beats (guidance-only had empty analyzed beats).
- 2026-08-02 (391): Breath results — R–R interval chart under pulse (display-only; BLE + camera).
- 2026-08-02 (390): Meditation cards — «?» help for Flash/Calm (SurfaceHelpModal); EU PREMIUM fill.
- 2026-08-02 (389): Practices sound bed — localize Neuro-sync → «Нейро-ритмы» (+ EU overlays).
- 2026-08-02 (388): Asana phone player — faster Vimeo poster path; no RN spinner overlay (blocked play).
- 2026-08-02 (387): Communicator — unify assistant reveal: paced lines (not chars); defer all live turns.
- 2026-08-02 (386): Communicator mic cancel — circle `#bdbdbd` (lighter gray).
- 2026-08-02 (385): Mandala sound — shorter practice fade-in/out (~0.6 s / ~0.8 s).
- 2026-08-02 (384): Practices — «Спокойствие» (Calm): catalog card, image+sound screen, no reports/assistant; background audio.
- 2026-08-02 (383): Ambient beds — rebuild cuts from author table; RU/EN/EU bed names refreshed (PREMIUM fill).
- 2026-08-02 (382): Breath/meditation — ComboBox sound bed (Neuro-sync + 8 nature loops); fade in/out via MandalaSoundProvider.
- 2026-08-02 (381): Communicator mic — no warmup cancel on early release/AppState; alert on too-short takes (Samsung).
- 2026-08-01 (380): Math of the day — localize planet/aspect names to profile locale (cron + cached markdown).
- 2026-08-01 (379): Home ChakraFlower — dark center ring matches read-stories ring (`storiesRingStrokes.read`).
- 2026-08-01 (378): Communicator mic footer — pad ≈30% mic diameter; bg=`screenBg` (match header/chat); no top divider.
- 2026-08-01 (377): Home ChakraFlower — dark center ring uses surfaceBorder (frame), not surface/screenBg; light unchanged.
- 2026-08-01 (376): Breath catalog — «Когерентное дыхание» → «Полное дыхание»; phone-camera results show one measured pulse chart.
- 2026-08-01 (375): AppButton busy — fixed 3 dots color-pulse; summarizing — no empty auto-close; tighter silence STT guard.
- 2026-08-01 (374): Mic — unload Recording on Communicator unmount; gray X inert; unlock mic at deferred reveal; AppButton busy animated ellipsis.
- 2026-08-01 (373): Mic cancel — white `cancel.svg` on drawn `#a8a8a8` circle (`MicCancelButton`).
- 2026-08-01 (372): Mic button — white `microphone.svg` glyph on drawn `#0d74f1` circle; +20% diameter + soft aura while recording.
- 2026-08-01 (371): Mic soft volume aura + modest size; serialize expo-av Recording ops; hide Home BlockingStatusToast once communicator modal opens.
- 2026-08-01 (370): Android mic — GestureHandlerRootView inside AssistantModalShell (GH Pressable was dead in RN Modal; Pixel + Samsung).
- 2026-08-01 (369): iOS splash — soft first-frame veil on EarlySplashCover; Android mic — RNGH Pressable + GestureHandlerRootView (Samsung/One UI hold-to-talk); recording aura ~2×.
- 2026-08-01 (368): Android splash — restore transparent system icon + EarlySplashCover handoff (hide after Image.onLoadEnd) to kill mini→large jump.
- 2026-08-01 (367): iOS dev-client — add `NSLocalNetworkUsageDescription` + Bonjour services so Metro LAN works (Settings → Local Network appears).
- 2026-07-31 (366): iOS EAS AppCheckCore — stale CocoaPods CDN shard (etag stuck at 11.2.0); refresh script + Podfile preamble plugin; local iOS build scripts call refresh first.
- 2026-07-31 (365): iOS EAS — `eas-build-post-install` runs `pod repo update` so Firebase `AppCheckCore (~> 11.3)` resolves (fixes Install pods exit 31).
- 2026-07-31 (364): Android splash reset — iOS keeps EarlySplashCover; Android skips it (native→AppStartup); explicit window cover (no absoluteFill stretch).
- 2026-07-31 (363): Android cold start — revert transparent splash icon (white hang); hide native after image load; hold splash on profile gate; auth sets `profileLoading` before `initializing=false`.
- 2026-07-31 (362): Android cold start — keep splash while profile gate is open (fix white hang after splash handoff).
- 2026-07-31 (361): Splash — remove image opacity “breath” (white flicker); keep `EarlySplashCover` until JS splash paints.
- 2026-07-31 (360): Android splash — hide system centered icon via `withFinalizedMod` (`with-android-splash-hide-icon`); hand off to full-bleed `EarlySplashCover` after image load.
- 2026-07-31 (359): Profile crash — fix `expo-application` import in `LegalFooter` (`import * as Application`); add direct dependency.
- 2026-07-31 (358): iOS — disable RNFirebase autolink without plist (`react-native.config.js`) so `pod install` does not require AppCheckCore.
- 2026-07-31 (357): iOS EAS prebuild — gate RNFirebase on `GoogleService-Info.plist` / `GOOGLE_SERVICES_PLIST`; `.easignore` keeps Firebase client configs in build archives.
- 2026-07-31 (356): Account overview prefers active subscription over older cancelled; YooKassa webhook note in payment_gateways.md.
- 2026-07-31 (355): Cabinet cancelled-sub badge «отменена»; YooKassa recurring off until shop allows save_payment_method (403 mapped).
- 2026-07-31 (354): Communicator — reset stream on commit/initiate errors so mic is not stuck after empty/failed assistant hydration.
- 2026-07-31 (353): Payment gateways RU/INT profiles (Lava/YooKassa); YooKassa recurring (save method, daily cron, revoke on cancel/wipe); cabinet passes `country`.
- 2026-07-31 (352): Admin login — apply server session without browser→Auth `/user` (fixes «Failed to fetch» after successful password grant).
- 2026-07-31 (351): Trial no longer unlocks webinars (same paywall as free); account wipe cancels email automations; re-register restarts welcome drip + 1-day demo.
- 2026-07-31 (350): App version **1.1.0** (`appVersionSource: local`, autoIncrement preview+production); Profile footer shows localized name + `vX.Y.Z (build)` + © year/holder above legal links.
- 2026-07-31 (349): Breath practice «?» help — two new paragraphs (headphones for binaural sound; tap to adjust breathing-indicator speed); RU/EN + de–nl overlays.
- 2026-07-31 (348): `SurfaceHelpModal` — body starts at the top and scrolls (finger / large Dynamic Type); card capped to ~90% viewport so long «?» tips are not vertically clipped.
- 2026-07-31 (347): Asana credit — wall-clock + player duration when Vimeo bridge misses time/ended; own stop-confirm copy (was breath «не будут засчитаны»).
- 2026-07-30 (346): Profile practice stats / chakra reload on tab focus (fix stale totals vs Day); chart scrub defaults to latest day with practice.
- 2026-07-30 (345): Splash — `EarlySplashCover` before fonts (hide tiny native logo ASAP); iOS `enableFullScreenImage_legacy`; Android `imageWidth: 288`.
- 2026-07-30 (344): OTP send — shorter App Check budget + prefetch; CTA shows «Отправляется…» at full opacity while busy.
- 2026-07-30 (343): OTP App Check enforce temporarily off (`OTP_REQUIRE_APP_CHECK=false`) after store `otpAppCheckFailed`; client App Check getToken retries; rate limits still on.
- 2026-07-30 (342): EAS builds default to `--local` on this Mac (Android SDK + Xcode/fastlane); cloud only if explicitly asked — see `.cursor/rules/eas-local-builds.mdc` + `DEPLOY.md`.
- 2026-07-30 (341): OTP abuse protection — server rate limits (60s/10h/25d) + verify cap; `POST /api/auth/otp-gate` + Firebase App Check permit; UI cooldown; Turnstile skipped.
- 2026-07-30 (340): Marketing deliverability — ignore OTP Resend events; MailboxFull → suppress marketing; report filters historical OTP noise (Play tester bounce flood was sign-in codes, not campaigns).
- 2026-07-30 (339): Email transport profiles — `EMAIL_OTP` / `EMAIL_MARKETING` (Resend↔Amazon × yoga|ru); OTP default Amazon yoga; marketing default Resend ru; SES marketing webhook for future flip; shared delivery event apply.
- 2026-07-30 (338): Client geo place — sync `country_code`/`city` after onboarding GPS and on every `logAppOpen` (even when event is throttled).
- 2026-07-30 (337): Admin email segment — «Демо» = active trial (same as Users); new chip «Новые 24ч»; Навигатор excludes trial. Store app unchanged.
- 2026-07-30 (336): Admin users — list/card badge shows effective access («Демо» while trial active), not raw free→«Навигатор»; filter order clarified. No membership/access change.
- 2026-07-30 (335): App variants — `APP_VARIANT` gives side-by-side «Harmonizer Expo» / «Harmonizer Test» / store ids so QR, internal test, and Play/TestFlight no longer overwrite each other.
- 2026-07-30 (334): Store builds — bake `EXPO_PUBLIC_SUPABASE_*` into EAS production/preview; eas profile→environment + autoIncrement + iOS ascAppId; `check:eas-env` preflight; `babel-preset-expo` for Metro; `DEPLOY.md` three-lane workflow.
- 2026-07-29 (333): Expo doctor hygiene — remove project `eas-cli`, align SDK 54 deps, exclude intentional Sentry 8.x, add `expo-font`/`expo-web-browser` plugins (`expo-doctor` 18/18).
- 2026-07-29 (332): Breath `PracticeCard` — unified «?» help (`SurfaceCardTitleRow` / `SurfaceHelpModal`) on all seven breath cards; shared `breathHelp*` copy + typed overlays; card padding matches `SURFACE_CARD`.
- 2026-07-29 (331): Android Play AAB — fix duplicate LOCATION `maxSdkVersion` (BLE vs expo-location) via `with-android-location-permission-merge`; align Bluetooth≤30 and storage≤32.
- 2026-07-29 (330): Product UI cleanup vs `HARMONIZER_TEST_MODE`: remove Home birth/sign-out CTAs; Profile copy + drop access/DevTier/coming-soon; catalog footer gone; breath live diagnostics + activation export follow test flag; results Close centered when alone.
- 2026-07-29 (329): Unify product test UI under `HARMONIZER_TEST_MODE` (not `__DEV__`): DevTierSwitch, day-reset/DevLinks, local-notification probe, profile diagnostics, recommendation/communicator model badges, dialog export; AccessProvider ignores tier override when flag is off.
- 2026-07-29 (328): Pixel QA — (1) breath interpretation: no bilingual EN+RU timeout; `AppUserError` + silent retry; (2) post-login Home keeps prep overlay and auto-retries transient day-content failures instead of flashing «Could not reach the server»; birth CTA / dev tier copy i18n.
- 2026-07-29 (327): Breath results: hide «Время вхождения не определено» warning (keep «—» in the row).
- 2026-07-29 (326): Breath results UI by sensor mode (none/camera/BLE); drop debug time-base line from results.
- 2026-07-29 (325): Breath activation «Отменить» returns to catalog/Day (not legacy practice-type idle).
- 2026-07-29 (324): Breath catalog: late BLE probe no longer resets «пульс с телефона» back to «без пульсометра».
- 2026-07-29 (323): Breath catalog: unavailable BLE → default «без пульсометра»; no black-screen start; iOS prep cancel + hang watchdog.
- 2026-07-29 (322): Admin video «Перевести»: longer timeouts + chunked LLM; source=active tab; EN fills all except RU.
- 2026-07-29 (321): Admin video Save returns to list; globe icon only when real i18n content exists.
- 2026-07-29 (320): Video duration_seconds (HH/MM/SS in admin); YouTube-style badge on covers in app + admin.
- 2026-07-29 (319): Admin video editor: publish date field; list status Планируется for future published_at.
- 2026-07-28 (318): requireUser validates JWT via PostgREST + 8s timeout (avoids Auth 522 hanging all admin lists).
- 2026-07-28 (317): Admin lists unified to per-item cards; safe delete on lists; payments ledger avoids Auth 504 timeout.
- 2026-07-28 (316): Campaign/step «Редактировать» confirms and saves dirty title before opening editor.
- 2026-07-28 (315): Email segment «Вся база» vs installed; click/open redirect without awaiting DB; asset cache; drop preview footnote.
- 2026-07-28 (314): Campaign segment count = send eligibility (exact locale); email_contains without chips; no «примерно».
- 2026-07-28 (313): Users stats: countries/languages by selected period; aligned bars; API `by_locale`.
- 2026-07-28 (312): Email locale exact-match harden: sync + users.locale on automation due-send; skip missing translation, continue drip.
- 2026-07-28 (311): User-card chain sends link to step page; compact delivery KPI (Недоставлено, no Errors/hints).
- 2026-07-28 (310): User card: active email enrollments + cancel; letter history shows chain/letter/campaign names.
- 2026-07-28 (309): Marketing email HTML sanitize on save (paste bloat); automations list shows step names; backfill welcome chain.
- 2026-07-28 (308): Account wipe shared helper; reattach payment ledger by email on signup (migration `20260728144628`); membership restore from contracts.
- 2026-07-28 (307): Payments list flat (no nested History card); revenue stats «Всё время» + gap before Дни/Недели.
- 2026-07-28 (306): Stats activity 24/72/168h dig-down links (`active_hours` → user_event_log).
- 2026-07-28 (305): Geo: derive district centre (Осташковский→Осташков); stats dig-down links; users list access/addon URL filters.
- 2026-07-28 (304): User stats UX (onboarded total, tariffs/addons, all-time); geo prefer town via Nominatim zoom=10; location maps link; light chain button.
- 2026-07-28 (303): Admin user card (Общее/Гармонизатор), city repair, notifications list-first (F), users filters/stats onboarded_at; migration `20260728160000`.
- 2026-07-28 (302): Deliverability contact-status counts link to contacts list; drop open/click explainer.
- 2026-07-28 (301): First-party open/click tracking for zamkovoi.ru; deliverability KPI «Не доставлено».
- 2026-07-28 (300): Restore email image centering (`inline-block` + parent text-align).
- 2026-07-28 (299): Fix email image stretch: do not treat max-width:100% as width:100%.
- 2026-07-28 (298): Marketing email images emit width+height (reserve space; no text jump on load).
- 2026-07-28 (297): Email preview iframe height shrinks to content (no tall empty gap after shorter letters).
- 2026-07-28 (296): Fix automation step name persistence (GET omitted `name`); shared `emailNaming` for campaigns/steps.
- 2026-07-28 (295): Automation step name field + Copy (same chain, «(копия)»); list shows step name.
- 2026-07-28 (294): Marketing email preview matches send HTML (560px iframe); normalize paragraph spacing; default image width 240px.
- 2026-07-27 (293): Admin login via `POST /api/admin/login` (server password-grant); safe headers for `sb_*` API keys.
- 2026-07-27 (292): Admin login — reset hung Supabase client before sign-in; show real auth errors (not always «wrong password»).
- 2026-07-27 (291): Admin API — timeout getSession/refresh/fetch so Save cannot hang; keep near-expiry sessions for refresh.
- 2026-07-27 (290): Admin email editor — asset upload via adminFetch (JWT refresh); refresh timeout; no Unauthorized alert; Save loading label.
- 2026-07-27 (289): Marketing email block editor — remove «Логотип»; keep «Изображение» only (legacy logo → image on parse).
- 2026-07-27 (288): Marketing email — shared UI foundation for campaigns + automation steps (list row, message workspace, step page + test send); footer 12.5px; web-safe fonts; logo vs image defaults.
- 2026-07-27 (287): Admin email/notification UX parity — campaign «Рассылка» + RU status + KPI/read-only after send; segment registration dates; notification locale tabs; From Сергей Замковой / Sergei Zamkovoi; smaller footer.
- 2026-07-27 (286): Deliverability dashboard — drop Resend domain health + top bounce-domain blocks.
- 2026-07-27 (285): Resend suppressions → contacts sync moved to daily pg_cron (`sync_email_suppressions_daily` → `/api/cron/email-suppressions-sync`). Migration `20260727180000`.
- 2026-07-27 (284): User-card messaging UX — fix notification history query; light linked lists; send confirm; campaigns picker limit 10.
- 2026-07-27 (283): Deliverability UX (RU) + campaign/step stats in list/editor; automation step counters migration `20260727170000`.
- 2026-07-27 (282): Marketing email D — deliverability dashboard + auto-suppress (hard bounce/complaint → Resend suppressions); webhook events failed/suppressed/suppression.*; indexes `20260727160000`.
- 2026-07-27 (281): Marketing email B2+C1+C2 — chain editor, registration/expired/inactive triggers, template footer/placeholders, user-card messaging. Migration `20260727150000`.
- 2026-07-25 (280): Marketing email — inline preview (page flow), fix last_seen/email segment filters, B1 welcome runner + cron. Migration `20260725020000`.
- 2026-07-25 (279): Fix email contact sync (pgcrypto); auto-sync on segment refresh; full-height preview; drop list Sync button. Migration `20260725010000`.
- 2026-07-25 (278): Рассылки UX — light admin theme, block editor, campaign `name`/`blocks_i18n`, segment chips + refresh count. Migration `20260724210000`.
- 2026-07-24 (277): Marketing email module phase A — contacts, campaigns, Resend zamkovoi.ru, webhooks, admin `/admin/email`. Docs `marketing_email/*`, migration `20260724200000`.
- 2026-07-24 (276): Unified notification inbox (admin/opportunity/webinar_start) + auto webinar-start push at `starts_at` (Edge minutely, `users.locale`). Migration `20260724193000`.
- 2026-07-24 (275): Notification deliveries retention — weekly pg_cron prune rows older than 30d (`cleanup_stale_notification_deliveries`, low-priority batches). Migrations `20260724190000`/`190500`.
- 2026-07-24 (274): Admin notifications — exact `users.locale` only (no EN→RU); skip recipients without that tab; clear stale «Отправлено» on draft edit; client locale hydrate/push sync so shade language matches inbox.
- 2026-07-24 (273): Profile — legal modal +10pt top; matrixNotReady copy; practice stats 90d empty fix + lighter scrub line.
- 2026-07-24 (272): Support upload — fix signed PUT URL (bucket in path → InvalidSignature→slow base64); buffer fallback via fetch(uri).
- 2026-07-24 (271): Support attach — native `uploadAsync` (fix Android base64 hang); gallery warm + progress copy; no image-manipulator resize.
- 2026-07-24 (270): SupportModal Android — keyboard keeps Send visible; success via AppDialog (app theme). Inbox: digit-only link, title/hint, limit 10.
- 2026-07-24 (269): Profile — support copy refresh (8 locales); notifications card → «Уведомления: N» link in My data.
- 2026-07-24 (268): Support attach — View overlay instead of RN Modal so picker opens over the form (no flash / dead PHPicker buttons).
- 2026-07-24 (267): Android remote push — EAS file-env `GOOGLE_SERVICES_JSON` (gitignore ломал FCM в APK → 0 android tokens).
- 2026-07-24 (266): First-launch locale — supported → RU-cluster (be/uk/kk/ky/uz/tg) → EN; Profile legal links (`LegalFooter`).
- 2026-07-24 (265): Birth-place overlay Android — hostRect из onLayout корня (пустой absolute-слой давал height=0).
- 2026-07-24 (264): WizardTitle iOS `21→20` pt — заголовок шага 2 в одну строку; Android без изменений.
- 2026-07-24 (263): Birth-place suggestions — низ списка привязан к полю (`bottom`), при укорочении не отрывается вверх.
- 2026-07-24 (262): Birth-place suggestions — снова WizardOverlayHost (без RN Modal): на Android Modal прятал IME; coords = anchor − host.
- 2026-07-24 (261): Birth-place suggestions — overlay via WizardOverlayHost (no form shift); ignore abort; 401 refresh+retry remembers session.
- 2026-07-24 (260): Android birth-place search — in-flow city suggestions (absolute clipped); `EXPO_PUBLIC_COMMUNICATOR_API_URL` в `eas env`.
- 2026-07-24 (259): Android Maps — `EXPO_PUBLIC_GOOGLE_MAPS_API_KEY` в `eas env` (dev/preview/prod); без этого EAS build падает на BirthPlaceMapModal (`API key not found`).
- 2026-07-24 (258): Delete account — fix `recompute_user_daily_stats` null `local_date` on cascade wipe; `requireUser` email instead of flaky Admin `getUserById` with `sb_secret_*`.
- 2026-07-24 (257): Home day-cache scope — `birth_time` канон `HH:MM:SS` (+ legacy `HH:MM` на чтении); чинит trial/paid cold-start timeout после ошибочной нормализации к `HH:MM`.
- 2026-07-24 (256): Onboarding step 2 polish — title 21pt, place-focus scroll nudge, faster Next (natal∥geo + loading label), Maps API key wiring, warmup cache scope fix + force splash after finish.
- 2026-07-23 (255): OTP email → Resend (`AUTH_EMAIL_PROVIDER`, `RESEND_ZAMKOVOI_YOGA_API_KEY`); SES оставлен как switchable tail. Каналы yoga/ru разделены; DNS inventory — `docs/04_workspace/email_providers.md`.
- 2026-07-23 (254): `web_cabinet/{cabinet,tv}/` — единая структура (`index.html` + `icons/` + README); wordpress-snippet удалён.
- 2026-07-23 (253): Remote Play TV → standalone `web_cabinet/tv/` (ISPManager), без WordPress Custom HTML; `?fr`/`?pt` снова работают.
- 2026-07-23 (252): Favicon набора из `assets/images/icon.png` — админка (Vercel `/icons`), кабинет (`web_cabinet/icons`), TV snippet (inject с Vercel).
- 2026-07-23 (251): `google-services.json` убран из git (GitHub secret scanning); шаблон `.example` + gitignore. Нужна ротация Android API key в Google Cloud.
- 2026-07-23 (250): Locale switch — account `users.locale` over sticky SecureStore; morning source = generated RU; no overwrite of existing target; no silent LLM retry. Cabinet open failures → runtimeDiagnostics.
- 2026-07-23 (249): Android FCM — `google-services.json` + FCM V1 key в EAS (`upload-fcm-to-eas.mjs`); старт rebuild Android development.
- 2026-07-22 (248): Android FCM wiring — `app.config` подхватывает `google-services.json`; чекер `scripts/android-fcm-setup.mjs`; await Android notification channels. Осталось: файлы Firebase + EAS FCM key + rebuild.
- 2026-07-22 (247): Android remote push — колокольчик после grant делает `registerPushToken`; админ Expo payload с `channelId: harmonizer_remote`; в docs зафиксирован блокер FCM/`google-services.json`.
- 2026-07-22 (246): Paid смена языка — translate канонического morning `sourceTexts` (`localeSwitch`), не полная перегенерация до 2 мин.
- 2026-07-22 (245): TV URL короче (`tv?pt`); на пульте крупная ссылка + подпись «откройте эту страницу…»; название практики — обычный жирный.
- 2026-07-22 (244): TV Remote Play — UI locale из `?l=` (ссылка из приложения), не из браузера ТВ; audiotrack + unmute overlay; PT/DE/… строки пульта и «Завершить практику» через typed overlays (`asanaScreen`, `remotePlay`).
- 2026-07-22 (243): Android asana — `asana-embed.html` на zamkovoi.yoga + vimeoId в launch (без flash «нет Vimeo ID»); iOS html+baseUrl без изменений. Путь к TV HTML: `docs/remote-play/wordpress-snippet.html`.
- 2026-07-22 (242): Android asana WebView (чёрный экран); Connect TV keyboard lift; BLE picker connected copy + no scan-busy flash; probe hides forgotten Polar; TV unmute CTA в wordpress-snippet.
- 2026-07-22 (241): Android BLE — «Подключен» после устойчивого HR (не 1 пакет); статусы найден/подключение/подключен; без голого чёрного prep; Android PPG clamp fps (`invalid-fps` 30). iOS BLE без изменений.
- 2026-07-22 (240): Android BLE — связка в picker (`onConnectLive` + `androidOsLinkReady`); «Подключен» только после HR; старт без баннеров если ready; probe-miss→без пульсометра; короче чёрный prep при warm hold. iOS без изменений.
- 2026-07-22 (239): Android BLE — live HR до старта практики (`ensureWearableLiveLink`), без MTU/bond API; fail→Retry, не второй connect; iOS не трогаем.
- 2026-07-22 (238): BLE Android — без mid-practice reconnect/banner при живом HR; iOS без prep-экрана; панель практики плотнее на Android.
- 2026-07-22 (237): Day prefetch с mount tabs + disk paint; BLE — warm на «Начать практику», minimal prep, fix `connectedDevices` UUID.
- 2026-07-22 (236): BLE Android — connect на экране подготовки с подсказкой про системный баннер; старт практики только после ready; disconnect в конце.
- 2026-07-22 (235): Android BLE — без mid-session reconnect/stall-cancel (системный «Запрос подключения» не поднимается); connect `autoConnect`.
- 2026-07-22 (234): BLE picker без ложного «занят» + Подключен/Отключить; антиспам reconnect (системный «Запрос подключения»); audio без Android duck; кабинет Custom Tabs + Lava `lava_buyer_email_rejected`.
- 2026-07-22 (233): Stories ring единый контраст iOS/Android; BLE: Polar остаётся в комбо после «Подключить», retry scan busy; splash plugin без imageWidth 300 (меньше мелкого лого на Android).
- 2026-07-22 (232): «Что делать?» — спиннер без текста до первого ответа ассистента; Android PPG `analyzeFingerRoi` + torch/thermal Expo-модуль (нужен native rebuild).
- 2026-07-22 (231): Android: контраст кольца сторис; Day prefetch late-arrival + memory paint; «Что делать?» spinner + без HealthConnect crash; PPG камера — явный unavailable (не silent sim); BLE runtime permissions.
- 2026-07-22 (230): Android UI: DayWait-карточка по теме; палитра light/dark в Профиле (default light); фикс Home→Day prefetch; Health Connect guard + plugin. Стратегия — `docs/04_workspace/multiplatform_ui.md`.
- 2026-07-22 (229): Единая политика запроса push-разрешений: Home (мягкий), колокольчик окон, вебинар; cooldown; bridge без prompt. Docs `notifications/*`.
- 2026-07-22 (228): Админка — ЮКасса без «скоро»; каталог SKU (`/admin/payments/catalog`); история/удаление пользователя учитывают ЮКасса. Docs `admin_panel/*`.
- 2026-07-22 (227): ЮKassa для RUB рядом с Lava — `selectPaymentProvider`, `payment_catalog`, checkout/webhook/settle, cancel stub, кабинет `?paid=1`. Миграция `20260722120000`. Docs `account_web/*`.
- 2026-07-22 (226): Onboarding Android — видимые поля (`WizardTextInput` + light theme), подъём формы над IME, белый splash в dark. Docs `onboarding/*`.
- 2026-07-22 (225): Admin dashboard — длиннее горизонтальные полоски графиков (как в stats). Docs `admin_panel/history`.
- 2026-07-22 (224): Admin — дефолт 7 дн.; выручка шлюзов выше LLM/стран, воронки внизу; топ-3 по токенам. Docs `admin_panel/history`.
- 2026-07-22 (223): Admin stats — скрыть нулевые продукты; без дублей в динамике/странах; убрано «(net)»; длиннее полоски графиков. Docs `admin_panel/history`.
- 2026-07-22 (222): Admin — KPI «Выручка»; фикс диаграммы токенов; stats: тарифы→Lava.top/ЮКасса→общая динамика→страны; единые названия шлюзов. Docs `admin_panel/*`.
- 2026-07-22 (221): Admin — единый список платежей (Lava+гранты, gross); «Статистика выручки» с ₽/€/$ и блоками Lava/YuKassa; гранты с валютой и FX без комиссии. Миграция `20260722040000`. Docs `admin_panel/*`.
- 2026-07-22 (220): FX — суточный кэш курсов `fx_daily_quotes` (Москва); Т-Банк→ЦБ не чаще 1 раза/день. Миграция `20260722030000`. Docs `account_web/*`.
- 2026-07-22 (219): FX — убран резерв Альфа; цепочка Т-Банк→ЦБ. Docs `account_web/*`.
- 2026-07-22 (218): Payment FX nets — settlement на вебхуке Lava (`payment_settlements` + `net_amount_*`); курсы Т-Банк→ЦБ; админ выручка net с переключателем ₽/€/$. Миграция `20260722020000`. Docs `account_web/*`, `admin_panel/*`.
- 2026-07-22 (217): Geo city — Nominatim via `GET /api/geo/reverse` (town/city), порог 100 км + `geo_place_lat/lon`. Миграция `20260722010000`. Docs `profile/spec`.
- 2026-07-21 (216): Admin — порядок KPI, бренд «Гармонизатор»; фон `country_code`/`city` (reverse-geocode, порог 8 км) + backfill city. Docs `admin_panel/history`, `profile/spec`.
- 2026-07-21 (215): Admin pulse v3 (когортная конверсия, воронки 1–7 мес., KPI продления) + hourly cleanup неподтверждённых OTP-аккаунтов (строго never-confirmed). Миграция `20260721160000`. Docs `admin_panel/*`, `supabase/README`.
- 2026-07-21 (214): Admin pulse UX: KPI clarity, conversions, Lava+Yandex slots, all-time/week, geo by cohort, token series, user card location+delete. Миграция `20260721143000`. Docs `admin_panel/*`.
- 2026-07-21 (213): Admin dashboard «пульс»: `/admin` → KPI/период/графики + `GET /api/admin/dashboard` + RPC `admin_dashboard_pulse`; Lava vs гранты; trial-сегмент; geo/`last_seen_at`; dialog `dialog_turn`/`llm_prompt_size`; усилены users/payments stats. Миграция `20260721120000`. Docs `admin_panel/*`, `profile/*`, `infra/*`, `account_web/dependencies`.
- 2026-07-21 (212): Admin — устойчивый фикс overlay `AuthApiError: Invalid Refresh Token` на `/admin/*` (жёсткий prune localStorage + узкий filter console.error). Docs `admin_panel/*`.
- 2026-07-21 (211): Day «Психо-практики» — оптимистичные rename/delete действий (список сразу, без спиннера; после delete — тихий sync сфер). Docs `daily_forecast/*`.
- 2026-07-21 (210): Day «Выбрать практику»/отмена — оптимистичный UI (карточка сразу, без спиннера и полного reload); POST/PATCH в фоне с сериализацией. Docs `daily_forecast/*`.
- 2026-07-21 (209): Day tab — гейт запуска практики для Oracle как в каталоге (`AccountGateDialog` по kind); мутации дня — тихий `ActivityIndicator` вместо `BlockingStatusToast` «Обновляем день…». Docs `practices/*`, `subscription/*`, `daily_forecast/*`.
- 2026-07-21 (208): Day prefetch: не ждать `homeTextsLoading`; на focus при prefetch — `refresh({silent:true})` без toast «Обновляем день…» поверх уже видимого плана. Docs `daily_forecast/history`.
- 2026-07-21 (207): После полной готовности Home фоновый prefetch `/api/day` → `storePrefetchedDayPlan`, чтобы первое открытие вкладки «День» было мгновенным. Docs `daily_forecast/history`.
- 2026-07-21 (206): GeoGate на каждом открытии Home делал `checking→granted` → `refresh({forceRefresh:true})` → paid ждал LLM, хотя cron warm был готов. Fix: onGranted только после `denied→granted`; обычный refresh без force. Docs `daily_forecast/history`.
- 2026-07-21 (205): Paid morning cache больше не отбрасывается из‑за `modelUsed` ≠ текущего `AI_MODEL_*` (cron gemini vs Vercel deepseek → ложный cold LLM). Soft validity + sync Edge `AI_MODEL_*`. Docs `daily_forecast/history`.
- 2026-07-21 (204): Cron self-heal — `ensure_harmonizer_cron_jobs()` + watchdog `*/15 * * * *` (free/paid precompute, stories cleanup, membership reconcile); restore missing `invoke_reconcile_expired_memberships`. Миграции `20260721010000`, `20260721011000`. Docs `infra/*`, `daily_forecast/history`.
- 2026-07-21 (203): Paid midnight pre-warm — в pg_cron не было job для `precompute-daily-forecasts` (Edge был, schedule нет). Миграция `20260721003000_pg_cron_precompute_daily_forecasts_hourly.sql`. Docs `open_questions`.
- 2026-07-21 (202): Free midnight Home hang — крон уже готовил `global_daily_content`, но splash «Собираем общий настрой дня» держался из‑за abort при `refreshProfile`/`profileLoading` + `holdWarmForTexts` после strip locale. Fix: silent `refreshProfile`; free dismiss на renderable; direct Supabase до token/route; free без GPS-wait. Docs `daily_forecast/*`, `profile/*`.
- 2026-07-20 (201): Day add-flow: лишние card больше не invent’ятся keyword-inference поверх верного ответа модели — канон = DB rows беседы + model markers. Docs `assistant/*`.
- 2026-07-20 (200): Day tab — «Обновляем день…» при удалении/мутации через `BlockingStatusToast` поверх экрана (без прыжка списка). Docs `daily_forecast/*`.
- 2026-07-20 (199): Day add-flow: «подумал… из сферы отдыха» больше не card; stricter scaffold filter только при `noGreeting`. Docs `assistant/*`.
- 2026-07-20 (198): Day add-flow: вводная «добавить действие про…» больше не становится `planned_event` (`isMetaPlanningIntentDescription` + fix strip «Да,»). Docs `assistant/*`.
- 2026-07-20 (197): Planning spheres — корневой фикс вместо keyword-заплаток: убран конкурирующий Quick guide из planning prompt (тег только по `life_spheres_baseline`); sphere 3 сужена до работы/денег; удалены «НЕ грибы»/CRITICAL и `reconcilePlanningSpheres`. Docs `assistant/*`.
- 2026-07-20 (196): Splash QR hang: profile fetch 5s Abort → free global-content; timeout 15s + hold splash на cold retry. Dialog: sunset/эмоции/красота → close without clarifying. Docs `assistant/*`, `profile/history`.
- 2026-07-20 (195): Dialog summarizing: (1) reflective answers («задуматься о смыслах») больше не получают лишний clarifying; (2) empty-due больше не уводит тот же ход в planning с мусорными `PLANNED_EVENT`; snapshot rebuild после summary не блокирует SSE. Docs `assistant/*`.
- 2026-07-20 (194): Dialog: DevTierSwitch «Мастер» снова даёт practice-ветку (`devAccessTierOverride`); Oracle/Free soft-close — детерминированный scaffold `fallbackSoftPracticeClose` (раньше срезался при сборке финала). Docs `assistant/*`.
- 2026-07-20 (193): Dialog QA: (1) «дело + больше ничего» снова сохраняет действие и делает нормальный planning finalize; (2) blank при `profileLoading` только если `profile==null` — иначе foreground refresh размонтировал Communicator после Health. Docs `assistant/*`, `profile/*`.
- 2026-07-20 (192): Profile cold start — Master больше не мигает как «Навигатор». Root: UI открывался с `profile=null` до `syncProfile` → free Access + `global_daily_content`. Fix: blank пока `profileLoading`; retry fetch; не затирать stale profile; upsell только после известного free-профиля. Docs `profile/*`.
- 2026-07-20 (191): Assistant — Oracle/Free (и Free safety-net) не входят в ветку `practice`: FSM `softPracticeClose` + planning finalize с мягким абзацем (йога под чакру дня + каталог на уровне Мастер в Личном кабинете, без вопроса); Master/trial — прежний practice-offer; Day-tab add без upsell. Docs `assistant/*`, `open_questions`.
- 2026-07-20 (190): Профиль — кнопка «Личный кабинет» мигала и OTT падал при кратком PostgREST 503 (schema cache). Fix: персист kill-switch + ретраи; OTT/profile-reports retry на schema cache/503; LogBox demote `service_busy`. Docs `account_web/*`.
- 2026-07-20 (189): Личный кабинет — standalone HTML5 вне WordPress (`web_cabinet/cabinet.html`: DOCTYPE/viewport/fonts). Деплой на `zamkovoi.yoga/cabinet/` через ISPManager без WP-темы; API/оферта на Vercel. Docs `account_web/*`.
- 2026-07-20 (188): Личный кабинет — быстрее выход из белого экрана. Root cause: geo blocking `openURL`; ~57KB оферты в WP блокировали спиннер/`POST /session`; последовательные цены Lava; WP-шелл держит Safari progress. Fix: currency persist+800ms timeout; early boot; оферта только по клику (`/cabinet/offer/{lang}.json`); parallel Lava prices; далее (189) — вынос со страницы WP. Docs `account_web/*`.
- 2026-07-20 (187): Смена языка на Профиле — таймаут 120s + без ложного пересчёта эфемерид. Root cause: `ensureLocaleDayContent({forceRefresh})` пробрасывал force в `fetchDailyForecast` (25s default) → Astronomía + второй LLM при смене языка; UI показывал EN «timed out after 25s». Fix: `DAY_CONTENT_LLM_TIMEOUT_MS=120s` (общий бюджет monologue/onboarding/global force/home warm); `forceStructuralRefresh` только после смены натала; смена языка — monologue + cache forecast. Диалог rebuild целиком на целевом языке. Docs `daily_forecast/*`, `profile/*`.
- 2026-07-20 (186): OTP-письмо снова на языке мастера (не на языке первой регистрации). Root cause: GoTrue не обновляет `user_metadata.locale` для существующих пользователей; `send-auth-email` читал только metadata. Fix: `signin_name_hints.locale` + RPC с `p_locale`; edge предпочитает hint → metadata → ru; после verify — `updateUser({locale})`. Миграция применена, edge redeployed. Профиль: комбобокс языка перенесён в «Мои данные» под email (`profile.language.appLabel`). Docs `i18n/*`, `profile/*`.
- 2026-07-20 (185): Профиль — «Выйти» / «Удалить аккаунт» под «Личный кабинет» (центрированная строка ссылок + `AppDialog`). Delete: `DELETE /api/account/delete` отменяет active subscription во всех провайдерах (`cancelActiveSubscriptionsForUser`, сейчас Lava), пишет `buyer_email`, `auth.admin.deleteUser`; миграция `20260720092052` — payment ledger переживает wipe (`ON DELETE SET NULL`). i18n `profile.account.*` ×8. Docs `profile/*`, `account_web/*`. Нужен deploy Vercel (delete route).
- 2026-07-20 (184): `BirthPlacePicker` — при первом открытии редактора натала поле «Место рождения» больше не пустое, если место есть в профиле. `query` синхронизируется с `value` после mount; `geoPlaceFromProfileBirthPlace` принимает string lat/lon и `lng`. Docs `onboarding/*`, `profile/history`. tsc clean.
- 2026-07-20 (183): Mid-session ожидание дня — карточка «Готовим ваш день», не полная заставка. `AppStartupProvider`: presentation `splash` | `day_card`; после первого `completeHomeBootstrap` и при `refresh({ blockingReload })` (смена натала с Профиля / ролловер без кэша) — `DayWaitCardOverlay` (`wizard.warm.*` + спиннер). Холодный старт и auth по-прежнему splash (`startup.step.*`). Точка gate та же (`shouldBlockSplash` в `useDayContent`). Docs `daily_forecast/*`, `profile/history`. tsc clean.
- 2026-07-20 (182): Home day-content — фикс рассинхрона «диаграмма новая / слоган+рекомендация старые» после смены натала. Root cause: `POST /api/astro/natal` чистил `user_daily_forecasts`, но не `scenario_cache` (`morning_recommendation` keyed user+date+locale без fingerprint натала) → новый каркас (Венера) + старые LLM-тексты (Меркурий). Fix: natal route удаляет и morning cache; Profile Save — `clearDayContentCache` + фоновый `ensureLocaleDayContent({forceRefresh})` + `markHomeDayContentBlockingReload({forceRefresh:true})` без спиннера на Профиле; `ensure` передаёт `forceRefresh` в `fetchDailyForecast`; `useDayContent` при blockingReload сначала берёт warm/phone-cache, а при incomplete server texts всегда `stripHomeLlmTexts` + secondary monologue. Нужен deploy Vercel (natal route). tsc clean.
- 2026-07-20 (181): `NatalBirthDataModal` — форма почти касается клавиатуры + меньше артефактов выделения. Убран `KeyboardAvoidingView`+`justifyContent:center` (оставлял большой зазор над клавиатурой и сжимал карточку → «Место рождения» до зелёной линии). Вместо этого `Keyboard` listeners: `paddingBottom = keyboardHeight+8`, при открытой клавиатуре `justifyContent:flex-end`. Сегменты маски выше/шире + `overflow:visible` — iOS selection-handles не обрезаются. `BirthPlacePicker` `minHeight=INPUT_HEIGHT`. tsc clean.
- 2026-07-20 (180): `NatalBirthDataModal` — форма снова открывается + слоты маски не съезжают. Root cause «белой таблетки»: `modalCard`/`ScrollView` с `flex:1` внутри обёртки только с `maxHeight` (без height) → карточка схлопывалась. Layout упрощён: карточка по размеру контента (`flexGrow:0`/`flexShrink:1` на ScrollView), кнопки sticky внизу, `KeyboardAvoidingView` поднимает над клавиатурой. Root cause сдвига цифр (год → «968»): flat digit-string без padding — укорочение сегмента сжимало следующие; теперь слоты фиксированной длины (пробел = пусто), `toSlots` сохраняет позиции. tsc clean.
- 2026-07-20 (179): `NatalBirthDataModal` — фикс/layout. (1) `MaskedTextInput`: сегменты рендерятся плотно, по центру поля — каждый сегмент фиксированной ширины (`len*11+4` px), разделители с `paddingHorizontal:1`, `row` `justifyContent:"center"` → «21-11-1968» вместо «21 - 11 - 1968». Сегменты по-прежнему изолированы (отдельные `TextInput`), правка одного не сдвигает другой. (2) **Клавиатура больше не обрезает кнопки «Закрыть»/«Сохранить»**: кнопки вынесены из `ScrollView` (sticky внизу карточки); карточка обёрнута в `cardWrapper` с `maxHeight:"92%"`, `SurfaceCardView` `flex:1` заполняет его, `ScrollView` `flex:1, minHeight:0` — при клавиатуре карточка не схлопывается, а кнопки остаются видимыми внизу (при нехватке места скроллится только блок полей). tsc clean. Docs `profile/*`, `onboarding/*`.
- 2026-07-20 (178): Profile birth editor — сегментный ввод + центрированная модалка + честное ожидание пересчёта. (1) **`MaskedTextInput`** переписан на **сегментный ввод**: каждый сегмент маски (DD | MM | YYYY для даты, HH | MM для времени) — отдельный `TextInput` с `selectTextOnFocus`; сегменты изолированы, поэтому правка одного физически не может сдвинуть цифры в другом (баг «правлю месяц — год превращается в 968Y» невозможен). При фокусе сегмента его содержимое выделяется целиком — пользователь вводит сегмент заново; при заполнении фокус авто-переходит на следующий. (2) `NatalBirthDataModal` теперь **отцентрирована** на экране (а не прижата к низу): `modalBackdrop` `justifyContent:"center"`; `KeyboardAvoidingView` (`behavior:"padding"`, iOS) приподнимает карточку при появлении клавиатуры и опускает при уходе. (3) **No-change skip**: если при сохранении дата/время (цифры) и место (lat/lng/timezone) совпадают с исходными — `onClose()` без API-вызова и без пересчёта (пользователь мог открыть форму просто посмотреть). (4) **Пост-сейв поток исправлен**: раньше оверлей «пересчёт» ждал только `fetchDailyForecast` (который мог вернуться БЕЗ LLM-текстов) → закрывался слишком рано → при переходе на главную показывалась стартовая заставка «Считаем ваш личный день». Теперь оверлей ждёт **`ensureLocaleDayContent({ forceRefresh: true })`** (`services/localeDayContentEnsure.ts`) — она ждёт LLM morning monologue + daily forecast, мерджит, проверяет `isDayContentComplete` и пишет **полный** phone-кэш. После успеха `markHomeDayContentBlockingReload` **НЕ** вызывается → главная при фокусе читает свежий полный кэш (`instantCached fresh` → `shouldBlockSplash=false`) **без заставки**. `markHomeDayContentBlockingReload` остался только как fallback в случае падения/таймаута ensure. tsc clean, `i18n-sync check` green, vitest 95/96 (1 прежний flaky `globalContentClient`, не связан). Docs `profile/*`, `onboarding/*`.
- 2026-07-20 (177): Profile polish — блок «Мои данные» + модалка редактирования натальных данных. (1) Строка имя/тариф: «Роман / Мастер» вместо «Имя • Тариф: Название» (bullet и слово «Тариф» убраны). (2) Шрифт полей рождения (дата/время/место) увеличен с `technicalCaption` до `dialogBody` — как имя. (3) Время рождения показывается только «HH:MM» (секунды отбрасываются). (4) `NatalBirthDataModal` — клавиатура больше не перекрывает форму: контент обёрнут в `KeyboardAvoidingView` + `ScrollView` (`keyboardShouldPersistTaps="handled"`), карточка приподнимается над клавиатурой. (5) Новый компонент `modules/onboarding/MaskedTextInput.tsx` — ввод с фиксированной маской (слоты и разделители не сдвигаются при правке одной цифры: выделили «07» и ввели «2» → «2D-MM-YYYY», месяц/год остаются на местах; пустые слоты показывают плейсхолдер маски). `NatalBirthDataModal` переведён на него для даты («DD-MM-YYYY») и времени («HH:MM»); состояние хранится как строки цифр, конвертация ISO↔цифры — локально в модалке. (6) Пост-сейв поток: убран `Alert` «Готово» с кнопкой «К калибровке» (калибровка пока не развивается); вместо него после сохранения форма закрывается и показывается блокирующий оверлей «Готовим ваш день» (переиспользованы `wizard.warm.*`, новых i18n-ключей нет), профиль ждёт `fetchDailyForecast({ forceRefresh: true, timeoutMs: 90_000 })` — чтобы пользователь ушёл на главную уже с готовыми текстами; затем `markHomeDayContentBlockingReload({ forceRefresh: true })` (главная при фокусе всё равно сделает свой blocking reload через `consumeHomeDayContentBlockingReload` + `useDayContent.shouldBlockSplash`). Универсальная проверка готовности данных главной уже существует (`markHomeDayContentBlockingReload` + `useFocusEffect` в `app/(tabs)/index.tsx` + `shouldBlockSplash` в `useDayContent` — срабатывает после правки профиля и на ролловере дня); `useDayContent` намеренно не трогался. tsc clean, `i18n-sync check` green, vitest 95/96 (1 прежний flaky `globalContentClient` timeout-тест, не связан). Docs `profile/*`, `onboarding/*`.
- 2026-07-20 (176): Profile — новый блок «Мои данные» в самом верху вкладки Профиль (`app/(tabs)/profile.tsx`): имя • «Тариф: <локализованное имя>» (для trial — «Демо»), email одной строкой, кнопка «Личный кабинет» (перенесена сюда из карточки «Текущий доступ»), и — только для тарифов с персональным прогнозом (`canUseFeature("personal_daily_forecast")`, т.е. не для бесплатного «Навигатора»; демо-trial и платные показывают) — поля рождения в режиме просмотра (Дата/Время/Место рождения: <значение> или «не указано») + ряд из двух кнопок одинаковой ширины: «Карта» (открывает `BirthPlaceMapModal` для сверки места) и «Изменить» (открывает `NatalBirthDataModal`). 9 новых i18n-ключей `profile.myData.*` во всех 8 локалях (sync-meta обновлён, `i18n-sync check` green). Унификация формата ввода даты/времени рождения: `NatalBirthDataModal` теперь использует тот же маскированный ввод «DD-MM-YYYY» / «HH:MM», что и шаг 2 мастера регистрации — общие хелперы вынесены в `modules/onboarding/birthDateFormat.ts` (`formatDateMask`/`formatTimeMask`/`ddmmyyyyToIso`/`isoToDdmmyyyy`, `app/onboarding.tsx` переведён на них); `geoPlaceFromProfileBirthPlace` экспортирован из `NatalBirthDataModal` для переиспользования картой. tsc clean. Docs `profile/*`, `onboarding/*`.
- 2026-07-19 (175): Proactive refresh of expired access token in `getSupabaseAccessSession` (`services/supabase.ts`, модуль `profile`/auth). Симптом: кнопка «Личный кабинет» падала с `gate.cabinetError` после 20–30 мин в фоне, хотя вкладки работали. Корневая причина — не геолокация (`resolveBillingCurrency` ловит все geo-ошибки и fallback на `EUR`), а протухший JWT: `autoRefreshToken` в мобильном клиенте выключен, `startAutoRefresh` на foreground не успевал до тапа, а `getSupabaseAccessSession` только читал снимок/`getSession()` (supabase-js v2 не фильтрует и не обновляет протухшие токены) → `POST /api/account/ott` уходил с устаревшим JWT → 401. Фикс: при отсутствии/протухшем снимке `getSupabaseAccessSession` упреждающе зовёт `auth.refreshSession()` через сериализованный `refreshAccessSessionOnce` (один refresh за раз — иначе параллельные вызовы сжигают single-use refresh token → `SIGNED_OUT`), при неудаче fallback на `getSession()`. Лечит всех потребителей Bearer JWT: `openAccountCabinet`, `dayPlan`, `geoSearchClient`, `purchasesClient`. Текст ошибки оставлен прежним (`gate.cabinetError`) по запросу. tsc clean. Docs `profile/*`, `account_web/*`.
- 2026-07-19 (174): Cabinet footer — ссылка «Договор оферты» + модалка с полным текстом договора (8 локалей). В `web_cabinet/cabinet.html` под копирайтом добавлена ссылка, открывающая `#hz-offer-modal` с текстом «Договор публичной оферты и политика обработки персональных данных» (ред. 29 июля 2026). Модалка: `position:fixed` overlay без блокировки body-scroll (нет сдвига), тело `overflow-y:auto` + `-webkit-overflow-scrolling:touch` (прокрутка пальцем), `white-space:pre-wrap`; закрытие × / клик по фону / Escape; `hzCloseOffer` восстанавливает `window.scrollY` и фокус на ссылке (`focus({preventScroll:true})`) — кабинет остаётся как был в момент клика. Тексты зашиты в `HZ_OFFER` (8 локалей): RU-источник + 7 переводов через DeepSeek (`AI_MODEL_PREMIUM`); компания «ТОО Сергей Замковой» → «LLP Sergei Zamkovoi» в переводах (RU оставлен), URL/e-mail/БИН сохранены, link и title используют одинаковую фразу «оферты». JS-синтаксис проверен `node --check`. Docs `account_web/*`.
- 2026-07-19 (173): Legal — правки Пользовательского соглашения (`wizard.legal.termsBody`) на всех 8 языках. В RU-источнике (`modules/i18n/catalog/ru.json`) заменены: 1.1 (Приложение = бесплатный интерактивный кабинет для участников авторских образовательных курсов Сергея Замкового; технический доступ к учебным материалам, индивидуальным ежедневным рекомендациям, методическим материалам, видео и расписанию живых онлайн-консультаций); раздел 2 переименован из «ОПЛАТА И ПЛАТНЫЕ СЕРВИСЫ» в «ПОРЯДОК ДОСТУПА К МАТЕРИАЛАМ» (2.1 — бесплатное распространение приложения; 2.2 — доступ к разделам личного кабинета, авторским учебным программам и еженедельным вебинарам предоставляется исключительно лицам, зачисленным на обучение на `https://zamkovoi.yoga`; управление доступом, учётными записями и планами обучения осуществляется пользователем вне мобильного приложения); 3.3 (ежедневные персональные рекомендации, формируемые на основе данных Пользователя, являются частью авторской методики саморегуляции и носят исключительно ознакомительный, философский и информационный характер — заменил прежний текст про астрологические расчёты). 7 целевых локалей (EN, DE, FR, IT, ES, PT, NL) переведены `node scripts/i18n-sync.mjs fill --all` через модель `AI_MODEL_PREMIUM`; `i18n-sync check` green. `wizard.legal.privacyBody` без изменений. Docs `onboarding/history.md`.
- 2026-07-19 (172): Webinar thank-you modal on any cabinet entry + remove «Отменить запись». Bug: paying for a webinar from the cabinet opened via the Profile's «Личный кабинет» button (not the webinar card) showed no thank-you modal — effect 2b only fired for `cabinetVisit.ctx=webinar:<id>` (set only from the webinar screen), and effect 2c (book) filtered `kind==="book"` and ignored the webinar purchase. Fix: merged effects 2b+2c into one 2bc in `modules/account/ui/MembershipEventsBridge.tsx` — on foreground after a cabinet visit with **any** ctx, calls `GET /api/account/purchases/last`; if the latest one_time purchase is a webinar or book paid after `cabinetVisit.ts`, shows `gate.webinarPaid.*` / `gate.bookPaid.*` (10s retry for webhook delay; flag `purchaseThanksShown.<uid>.<contractId>`; visit cleared only after showing). Removed `isRegistered` import. `modules/webinars/ui/WebinarScreen.tsx`: removed the «Отменить запись» button (cancel without refund is meaningless; paid-webinar registration is set by the Lava webhook automatically, Master registration stays manual «Записаться» without cancel); `toggleRegistration` → `register` (false→true only). tsc clean. Docs `account_web/*`, `webinars/*`.
- 2026-07-19 (171): Admin dev-overlay `AuthApiError: Invalid Refresh Token: Refresh Token Not Found` на `/admin/*`. Root cause: в `localStorage` оставалась сессия с истёкшим `access_token` и отозванным `refresh_token` (повтор сценария 2026-07-08 (4) — задание пароля через Auth Admin API отзывает refresh-токены). supabase-js в `_initialize → _recoverAndRefresh` находил сессию, пытался refresh-нуть, получал `AuthApiError` и делал `console.error(error)` (`GoTrueClient.js:3818`); Next.js 15 dev overlay перехватывает `console.error` и рисует красный оверлей, перекрывая UI, хотя приложение само уходило бы на `/admin/login`. Fix: `_legacy_web/app/admin/_lib/supabaseBrowser.ts` — `getBrowserSupabase()` перед `createClient` подчищает из `localStorage` все `*-auth-token` сессии с истёкшим `access_token` (margin 90 c = `EXPIRY_MARGIN_MS` auth-js) → `_initialize` не находит сессию → нет refresh → нет `console.error` → `AdminChrome` чисто уводит на логин. Дополнительно `_legacy_web/app/admin/_lib/adminApi.ts` — `.catch(() => null)` на mutex `refreshAccessToken` от unhandled rejection при не-auth ошибке refresh-а. Docs `admin_panel/*`.
- 2026-07-19 (170): Kill-switch `account_links_enabled` — кнопка «Личный кабинет» пропадала на Профиле после переключения пользователя. Root cause: RLS на `app_config` разрешал select только `authenticated`, и в окне sign-out/sign-in (или при протухшем access_token / холодном старте) запрос от анона возвращал 0 строк, `useAccountLinksEnabled` кэшировал fail-safe `false` на 5 минут. К тарифу/пользователю не относилось (в БД `true`, `membership_tier=oracle`). Fix: миграция `20260719000000_app_config_anon_read_account_links.sql` — доп. политика select для `anon, authenticated` с `using (key='account_links_enabled')` (публичный boolean, остальные ключи gated); применена в remote. `modules/account/core/accountLinksConfig.ts` — `fetchAccountLinksEnabled` возвращает `boolean|null` (`null`=нет строк/ошибка), `getAccountLinksEnabled` кэширует только явное значение, при `null` возвращает прежний кэш без продления TTL (защита от отравления кэша транзиентным сбоем). Docs `account_web/*`.
- 2026-07-18 (169): Lava real offerIds for webinar/book + `feedVisibility=ALL`. Root cause of `Product with offer id not found` and null prices: `payment_offers` held Lava `productId` (from `app.lava.top/products/<productId>` URL), but `/api/v2/invoice` expects the **offerId**; and `GET /api/v2/products` without `feedVisibility` skips «Доступ только по ссылке» products (webinar/book) so prices never resolved. Fix keeps prices sourced from Lava cards (no «Цена по запросу API», no DB price duplication): `lava.ts` `fetchLavaProducts` → `/api/v2/products?feedVisibility=ALL&limit=100`; migration `20260718230000_fix_webinar_book_offer_ids.sql` updates `webinar/en` and `book/en` to real offerIds from `GET /api/v2/products?feedVisibility=ALL` (`offers[].id`). Verified: `POST /api/v2/invoice` ONE_TIME → 201 + `paymentUrl`, prices from cards (book 19.50 €, webinar 9.50 €). Docs `account_web/lava_integration.md` (§2 feedVisibility, §3.1 where to find offerId, §9 blocker resolved).
- 2026-07-18 (168): Cabinet UI fixes + one-time book purchase + Advisor rename. `web_cabinet/cabinet.html`: email in «My account» on its own line without leading dash; EN tier name «Mentor»→«Advisor» (app-wide via `modules/i18n/catalog/en.json` `tier.oracle`/`tier.practitioner`); EN book title → «Yoga - the Way of Wisdom»; footer note replaced with dynamic copyright `© <year> <name>` (`new Date().getFullYear()`, `I18N.copyrightName`: ru «Сергей Замковой», others «Sergei Zamkovoi»); webinar block rendered only when an upcoming webinar exists or `ctx=webinar:<id>` (no more «No upcoming webinar» error); buttons «Записаться — <price>» / «Купить — <price>» (`I18N.buyBtn`). One-time book (ONE_TIME): `checkout` `kind:"book"` → `startBookCheckout` (ONE_TIME invoice, `payment_contracts` product_kind=one_time,tier=book); webhook `payment.success` one_time generalized (webinar→upsert `webinar_registrations`, book→just `status=active`, no membership change); `overview-data.ts` adds `book:{purchase}`. New `GET /api/account/purchases/last` (Bearer JWT, service role) returns latest active one_time purchase; `modules/account/core/purchasesClient.ts`; `MembershipEventsBridge` thanks for a book purchase on foreground after a cabinet visit (`gate.bookPaid.*`, 8 locales, sync-meta updated; 10s retry). `i18n-sync check` green; tsc clean (app + `_legacy_web`). Docs `account_web/*`.
- 2026-07-18 (167): Cabinet redesign + one-time webinar payment + provider abstraction. `web_cabinet/cabinet.html` rebuilt to the author mockup (header «Искусство жить / Школа Сергея Замкового», «Гармонизатор» block with tariff/dates/subscription/cancel, «Другие тарифы» with ✓-bullets, «Дополнительно» = one-time webinar + book link + monthly-intensive combobox m1..m7); self-contained CSS, Hanken Grotesk; «Другие тарифы» and webinar block hidden for «Мастер», demo treated as free for webinar; 8 locales («искусство жить»→«the art of life»). One-time webinar (ONE_TIME): migrations `20260718170000_payment_contracts_one_time.sql` (`product_kind`, `product_ref`, tier widened to webinar/book, periodicity MONTHLY|ONE_TIME) + `20260718173000_payment_offers_one_time.sql` (seed webinar/book offerId = `LAVATOP_WEBINAR_ID`/`LAVATOP_BOOK_ID`); `lava.ts` `createLavaOneTimeInvoice` + `resolveLavaOfferIdByName` + `resolveLavaPrice(periodicity)`; `checkout` `kind:"webinar"` (webinarId from `ctx` or nearest upcoming; `alreadyRegistered` short-circuit); webhook `payment.success` one_time → upsert `webinar_registrations` (no membership change); `overview-data.ts` adds `webinar:{webinarId,purchase}` and `upgradeTiers[].purchase:{mode,price,url}` (provider abstraction `checkout`|`link` for future RU acquiring). App: `WebinarScreen` re-checks `isRegistered` on foreground; `MembershipEventsBridge` shows «Вы записаны на вебинар» modal (`gate.webinarPaid.*`, 8 locales, sync-meta updated) on detecting registration after a `ctx=webinar:<id>` cabinet visit (10s retry). `i18n-sync check` green; tsc clean (app + `_legacy_web`). Docs `account_web/*`.
- 2026-07-18 (167): Home warm-gate for paid cold-start. Existing paid user on a new device signed in, route gate sent them straight to Home with no phone cache, and `useDayContent` called `completeHomeBootstrap()` as soon as the base forecast was ready (`isDayContentReadyForHome` for paid = `isBaseForecastValid` only) — the «Готовим ваш день» overlay hid instantly, the card showed `recommendation.loading` (`model: unknown`), and LLM texts loaded in the background and didn't always apply until navigating away and back. Fix in `modules/home/useDayContent.ts`: added `holdWarmForTexts()`/`releaseWarmIfHeld()` — the paid server-fetch path no longer completes the bootstrap overlay when the response lacks slogan + short recommendation; the overlay stays until the secondary `enrichWithMorningContent` effect applies the texts (`releaseWarmIfHeld`), or until `HOME_WARM_TEXTS_TIMEOUT_MS = 90_000` (same fallback as onboarding warmup). Cache paths (paid cache is always complete — `saveDayContentCache` for paid only writes on `isDayContentComplete`) and the free path (`isFreeDayContentRenderable` requires texts → throws otherwise) unchanged. `forceRefresh`/`localeChange` (non-blocking refresh) unaffected — `holdWarmForTexts` doesn't set `blocking=true`, only holds an already-active overlay. tsc clean. Docs `daily_forecast/*`.
- 2026-07-18 (166): Wizard steps 3-7 — new RU copy + image swaps. Each author line break is now a separate paragraph rendered by its own `<WizardBody>` (same `scrollContent.gap: 16` as step 1): `wizard.step{3..7}.{title,body1..bodyN}` (steps 3-6 → 3 paragraphs, step 7 → 4). Old single `wizard.stepN.body` removed from 8 locales; 16 new keys translated via `i18n-sync fill --all` (premium model); `i18n-sync check` green; tsc clean. Step 3 image → `psycho_2_600.jpg`, step 5 → `breath_2_600.jpg`. Docs `onboarding/*`.
- 2026-07-18 (165): Onboarding day warmup fix — prefetch after step 2 now uses `fetchDailyForecast({ forceRefresh: true, timeoutMs: 90_000 })` so the server runs `ensureMorningRecommendation` (slogan + short + long in one LLM response), writes `saveDayContentCache` with Home-matching keys, skips «Готовим ваш день» when texts are already ready, otherwise waits up to 90s then opens Home anyway. Step-2 geo deny stays on the step with GeoGate-like actions (grant / settings / close app). Cron idle window stays 3 days. Docs `onboarding/*`.
- 2026-07-18 (164): Wizard birth-place suggestions open **above** the field (not below) so the list stays usable over the keyboard; search status hints are out-of-flow so «Далее» no longer jumps. Onboarding steps 2–7 get `paddingTop: 12` under the progress bar; sign-in/OTP unchanged. Docs `onboarding/history.md`.
- 2026-07-18 (163): Wizard step 2 polish — removed the 72px `placeSpacer` above «Далее» (city suggestions already absolute-overlay the button); date/time masks insert `-`/`: ` immediately after the 2nd digit (`07-`, `07-11-`, `12:`) with delete-safe trailing separators; map button label `wizard.placeMap.open` → «Проверить на карте» (all 8 locales). Docs `onboarding/history.md`.
- 2026-07-18 (162): Wizard OTP confirm — no auto keyboard-lift. `footerInContent` only on welcome (`sub === "welcome"`); confirm keeps content in place (image fully visible), keyboard covers the bottom, manual scroll. Welcome + birth step 2 still auto-lift. `WizardShell` disables KAV `behavior` when not in lift mode; `key={sub}` resets scroll offset. Docs `onboarding/*`.
- 2026-07-18 (161): Wizard keyboard — revision 3. Reverted revision 2's "footer pinned outside scroll" (it made the CTA overlap the inputs). Restored `footerInContent`: CTA + legal sit under the fields in one page flow. Stability without the old jitter/two-stage: one `scrollToEnd({ animated: false })` only when the keyboard opens; `scrollEnabled={false}` while open (blocks iOS auto-scroll on field focus change); no `KeyboardAwareScrollView` (dependency removed). iOS: `KeyboardAvoidingView behavior="padding"`; Android: `adjustResize`. Same behavior on wizard steps 1–2. Docs `onboarding/*`. tsc clean.
- 2026-07-18 (160): Wizard keyboard — revision 2 (superseded by 161). Attempted fixed footer outside scroll + KAV only; caused CTA to detach and overlap fields. Replaced by revision 3.
- 2026-07-18 (159): Onboarding module docs + timezone audit + notifications pre-permission revert + wizard images. (1) New module doc `docs/02_modules/onboarding/` (`spec.md`, `dependencies.md`, `history.md`) — routes, `WizardShell`, steps, masked inputs, geo, warmup, legal, geo-gate; registered in `MAP.md`. (2) Timezone audit: confirmed `localChartDateTime` (`modules/astro-core/ephemeris.ts`) already converts local→UTC via `luxon.DateTime.fromISO(date+time,{zone})` + `.toUTC()`, resolving the **historical** IANA offset (DST/political changes); IANA zone flows from Open-Meteo `GeoPlace.timezone` → `BirthData.location.timezone` → `POST /api/astro/natal`; no separate UTC column stored (canonical inputs only). Documented in `astro/spec.md` §3/§5 + `astro/history.md`. No code change. (3) Reverted the notifications pre-permission `Alert` (`reminderPrePermission*` removed from `modules/home/i18n/home.ts` RU/EN + overlays de/fr/it/es/pt/nl; `OpportunityWindows.saveReminder` back to direct `requestPermissionsAsync`) — standard iOS system dialog is fine. (4) Wired new JPGs `assets/onboarding/*_600.jpg` into `INTRO_STEPS` (`app/onboarding.tsx`) + `EMAIL_ART` (`app/sign-in.tsx`). `i18n-sync check` green; tsc clean. Docs `onboarding/*`, `astro/*`.
- 2026-07-17 (158): Wizard UI/UX polish + copy overhaul. `WizardShell` forces a pure-white light-theme surface and gains `footerInContent` (CTA + legal footer render inside the scroll content so the keyboard lifts them by its own height — steps 1 & 2); legal footer now only on step 1. Step 2: masked `DD-MM-YYYY` date and `ЧЧ:ММ` time inputs with auto-advancing separators (helpers `formatDateMask`/`formatTimeMask`/`ddmmyyyyToIso`/`isoToDdmmyyyy` in `app/onboarding.tsx`), relabelled fields, `BirthPlacePicker` (`modules/onboarding/ui/BirthPlacePicker.tsx`) shows the city dropdown as an absolute overlay over the «Далее» button. `LegalDocumentModal` rewritten so the body scrolls with a fixed «Закрыть» button. New RU copy for step-2 body, steps 3-7 bodies (consolidated to a single `wizard.stepN.body` key), geo-gate title/message, and a notifications pre-permission alert (`reminderPrePermission*` in `modules/home/i18n/home.ts` + `OpportunityWindows.saveReminder` shows it before the system request — iOS system dialog text itself is not editable). 7 target locales translated via `i18n-sync fill --all` (premium model); `i18n-sync check` green. Docs `i18n/*`.
- 2026-07-17 (157): OTP email greeting — fresh name via side-channel. `signInWithOtp` doesn't update `user_metadata` for existing users, so the hook saw a stale DB name instead of the one just typed on wizard step 1. New table `public.signin_name_hints(email PK, name, updated_at)` + anon RPC `set_signin_name_hint` (security definer, validated; migration `20260717130000`, applied to remote). `modules/auth/sign-in-email.ts` upserts the typed name by email before `signInWithOtp`; `send-auth-email` reads it by `user.email` via REST + `SUPABASE_SERVICE_ROLE_KEY` (bypasses RLS), fallback `user_metadata.full_name` → generic `greeting`. `services/supabase-types.ts` adds the RPC type. Edge function redeployed (v25). Docs `i18n/*`.
- 2026-07-17 (156): OTP email — HTML redesign + named greeting. HTML body rebuilt as structured inline-CSS (max-width 600, system font-stack, sage-green letter-spaced code, left-accent guide block, tight signature); dropped the mockup's Tailwind Play CDN `<script>` + Google Fonts `<link>` (email clients strip `<script>`/`<link>` → unstyled). Added per-locale `greetingName` with `{name}` placeholder (locale-appropriate punctuation: RU «Здравствуйте, {name}!», FR «Bonjour {name} !», ES «¡Hola, {name}!»…); `renderEmail` reads `user_metadata.full_name` (wizard step 1), falls back to generic `greeting` when absent. 8 JSONs + `templates.ts` + `.sync-meta.json` updated; `i18n-sync check` green. Docs `i18n/*`.
- 2026-07-17 (155): OTP email — removed `{app}`/`{cta}` runtime placeholders; app name and «Что делать?» button baked directly into each locale's template (email is fully one language, no mismatch risk). CTA verified to match `recommendation.discussButton` in `modules/home/i18n/home.ts` for all 8 locales. `index.ts` simplified (only `{code}` substituted); 8 JSONs + `templates.ts` regenerated; `.sync-meta.json` updated; `send-auth-email` redeployed (v24). Docs `i18n/*`.
- 2026-07-17 (154): OTP email overhaul: `From`/signature name → RU «Сергей Замковой» / others «Sergei Zamkovoi» (was localized app name); footer line removed; body adds a 5-step quick user guide + personal closing; app name and «Что делать?» button injected via `{app}`/`{cta}` placeholders. 8 locales (RU hand-authored, 7 via `i18n-sync fill` premium model); `templates.ts` regenerated; `send-auth-email` redeployed (v23). Docs `i18n/*`.
- 2026-07-17 (153): Home geo-gate: `modules/home/ui/GeoGate.tsx` wraps the Home tab (`app/(tabs)/index.tsx`); when foreground-location permission is missing/revoked it shows a card with «Allow location» / «Open settings» (when `canAskAgain === false`) / «Close app» (Android `BackHandler.exitApp()`; iOS signs out as escape hatch). New i18n keys `home.geoGate.*` (8 locales, `i18n-sync` green). Edge function `send-auth-email` redeployed to Supabase (v22, localized `From` name). Docs `i18n/*`.
- 2026-07-17 (152): i18n native brand + device locale: `localeStore.deviceLocale()` uses `expo-localization` `getLocales()` (ordered preferred-languages, EN fallback); localized app display name + iOS permission reason strings for 8 locales via `app.config.ts` `expo.locales` (`plugins/appLocalesData.js`) → iOS `.lproj/InfoPlist.strings` + `CFBundleLocalizations`, Android `values-<lang>/strings.xml`; OTP email subject/body + `From` name use the locale's app name (`send-auth-email`). `services/supabase-types.ts` adds `claim_push_token` RPC (fixes tsc). Docs `i18n/*`.
- 2026-07-18 (151): Lava multi-language: `payment_offers` table (tier,locale→offerId, fallback en); prices fetched from Lava `/api/v2/products` (10min cache) into `overview.upgradeTiers[].price`; `cabinet.html` no longer hardcodes prices; `lava_integration.md` doc (Lava contract, mapping, currency, webhooks, policies, env, doc links). RU stays on Lava (en product, RUB) until Russian acquiring lands.
- 2026-07-15 (150): Lava.top payments: `payment_contracts` migration; `/api/account/checkout|subscription|webhooks/lava` (A3 upgrade policy, cancel keeps access till period end, 48h renewal grace); app geo-currency (`resolveBillingCurrency`) + `ctx`/`currency` in cabinet URL; `cabinet.html` subscription block + cancel + real checkout + prices (8 locales, courses placeholder); Vercel `LAVATOP_*` env + prod deploy. Docs `account_web/*`.
- 2026-07-15 (149): External payments follow-up: practice catalog browsable for all tiers (gate only on «Начать практику»/«Открыть», by kind); upsell panel + webinar banner font → sectionTitle kegel regular + chevron rotate; OpportunityWindows exact aspect shows (транзит)/(натал) labels (8 locales, incl. help popup, fixed transit-planet source); «Личный кабинет» button added to Profile access card; Supabase migrations applied (trial 1 day, OTT/app_config/realtime); Vercel env `ACCOUNT_CABINET_SECRET`/`ACCOUNT_CABINET_ALLOWED_ORIGIN` + prod deploy. Docs `subscription/*`, `home/*`, `practices/*`.
- 2026-07-14 (148): External payments (Consumption-Only): email-OTP auth (Apple/Google removed), 3-step onboarding with city autocomplete (`/api/geo/search`), tiers Навигатор/Наставник/Мастер + 1-day master trial, `AccountGateDialog`/`AccountUpsellPanel` compliance gates + `app_config` kill-switch, OTT cabinet (`/api/account/*`, `web_cabinet/cabinet.html`), Realtime tier sync (`MembershipEventsBridge`), edge `send-auth-email` (8 locales, Yandex SMTP). Migrations `20260714210000`, `20260714220000`. Docs `account_web/*` (new), `subscription/*`, `profile/*`, `i18n/*`, MAP.
- 2026-07-14 (147): Support screenshots: upload ArrayBuffer via expo-file-system (fixes 0-byte Storage objects from RN Blob). Docs `admin_panel/*`.
- 2026-07-14 (146): Admin support attachments: click «Файл N» downloads immediately (no lightbox). Docs `admin_panel/*`.
- 2026-07-14 (145): Support copy (title/hint/sent); admin attachments via authenticated blob download (fixes blank tab). Docs `admin_panel/*`, i18n catalog.
- 2026-07-14 (144): Support: up to 3 screenshots; admin icons/preview, bulk delete, unprocessed badge. Migration `20260714160000`. Docs `admin_panel/*`.
- 2026-07-14 (143): Push inbox hang + cold-start back stack; OpportunityWindows chart uses resolved forecast location; push title «Уведомление». Docs `notifications/*`, `daily_forecast/*`, `i18n`.
- 2026-07-14 (142): Admin: delete notification no longer forces logout; POST delete + safer `adminFetch`/`AdminChrome` auth. Docs `admin_panel/*`, `notifications/*`.
- 2026-07-14 (141): Admin send: harden Expo fetch against `TypeError: terminated`; push tap opens in-app reader with full text + links. Docs `notifications/*`.
- 2026-07-14 (140): Push: `claim_push_token` reassigns device token to current user; `resolveNotificationCopy` is the single locale entry for remote/inbox. Docs `notifications/*`.
- 2026-07-14 (139): Push: always sync UI locale → `users.locale`; Expo `interruptionLevel: active` for audible iOS alerts. Docs `notifications/*`.
- 2026-07-14 (138): Home LatestPostBanner + UpcomingWebinarBanner enforce exact UI-locale titles (same as Videos feed). Docs `author_presence/*`, `webinars/*`.
- 2026-07-14 (137): Videos/webinars: exact UI-locale titles only (no en→ru in feed); admin webinars list shows recording title when created. Docs `author_presence/*`, `webinars/*`, `i18n/*`.
- 2026-07-14 (136): Notifications: Expo sound+priority, DELETE, multi-locale admin send (preferred→en→ru); posts/webinars same content fallback; feed RPC (+fix kind/webinar_id). Docs `notifications/*`, `author_presence/*`, `webinars/*`, `admin_panel/*`, `i18n/*`.
- 2026-07-14 (135): Webinar banner bullet; Home date month casing via Intl (no capitalize); announce date sans year; Videos tab hides announce strip. Docs `webinars/*`, `author_presence/*`.
- 2026-07-14 (134): Webinar banner date/title polish + local timezone hint; spinner-only post/webinar loading; comment composer vertical align; admin announce questions chronological. Docs `webinars/*`, `author_presence/*`.
- 2026-07-14 (133): Webinar admin announce/recording editors match Video PostEditor locale UI (dots, labels, delete translation, Delete). Docs `webinars/*`, `admin_panel/*`.
- 2026-07-14 (132): Webinar announce questions reuse video CommentsSection (docked keyboard + copy props); comment API translates multi-locale webinars; admin delete-locale link matches Video. Docs `webinars/*`, `author_presence/*`.
- 2026-07-14 (131): Unify webinar recording cards with videos and fix free day-content midnight load. Docs `webinars/*`, `daily_forecast/*`, `admin_panel/*`, `author_presence/*`.
- 2026-07-13 (130): Webinars dual model — announce i18n/cover + join window +1h; recording as `posts.kind=webinar_recording` for registrants; admin tabs. Docs `webinars/*`, `author_presence/*`, `admin_panel/*`.
- 2026-07-13 (129): Free locale switch no longer waits on LLM when long_explanation is stripped as legacy; free Home uses location fallback so recommendations stay visible after update/GPS miss. Docs `daily_forecast/*`, `profile/*`.
- 2026-07-13 (128): Admin videos list infinite scroll (20/page, cursor API). Docs `admin_panel/*`, `author_presence/*`.
- 2026-07-13 (127): Videos tab infinite scroll (page size 8, cursor RPC + locale filter). Migration `20260713161954`. Docs `author_presence/*`.
- 2026-07-13 (126): Relative timestamps use abbreviated units (мин/h/дн) — one dominant unit, no declension. Docs `i18n/*`.
- 2026-07-13 (125): Locale-aware relative timestamps via `formatRelativeTime` (Hermes-safe); comments + notifications. Docs `i18n/*`, `author_presence/*`.
- 2026-07-13 (124): Optimistic new comment appends at bottom of list (no top→bottom jump). Docs `author_presence/*`.
- 2026-07-13 (123): Admin globe from title_i18n; comment optimistic UI + after() translate; free locale switch avoids full-day forceRefresh / prefers direct Supabase read. Docs `author_presence/*`, `daily_forecast/*`, `profile/*`.
- 2026-07-13 (122): Admin PostEditor: translate copies source cover to empty locales; clear-locale / clear-cover; shared File uploaded once. Docs `author_presence/*`, `admin_panel/*`.
- 2026-07-13 (121): Unify VideoCard preview/spacing; comment translate runs in `after()` so send stays instant. Docs `author_presence/*`.
- 2026-07-13 (120): Comment translate uses `AI_MODEL_STANDARD`; post/story admin translate stays one-shot `AI_MODEL_PREMIUM`. Deploy `POST /api/comments`.
- 2026-07-13 (119): Videos polish #2: admin create→list + tab/cover locale match; home VideoCard under OpportunityWindows with view dismiss; comment i18n API; feed preview+Open. Docs `author_presence/*`.
- 2026-07-13 (118): «Публикации» → «Видео»: locale-strict feed, cover compress, comment keyboard dock, countLabel, admin any-locale + fill-missing translate, Unauthorized refresh. Docs `author_presence/*`, `admin_panel/*`.
- 2026-07-13 (117): Locale switch: Profile publishes warmed day texts before `setLocale`; Home consumes bridge (+ async cache) so Navigator shows slogan/recommendation immediately; paid refresh no longer overwrites complete cache with stripped Loading state.
- 2026-07-13 (116): Paid morning DE→EN: monologue language miss falls back to `translateMorningTextFields` (like free text_i18n) before failing; Profile shows localized rebuild error instead of technical mismatch string. Deploy required for API.
- 2026-07-13 (115): Paid DE→EN locale: Profile/Home share `resolveDayContentAccessKeys` (no `trial` vs `premium` miss); no soft-commit; no second monologue after warm; UI hides wrong-language day texts; free cron pretranslates only active user locales.
- 2026-07-13 (114): Cyrillic language guard uses explicit `А-Яа-яЁё` ranges (Hermes does not reliably match `\p{Script=Cyrillic}`), so EN Home no longer accepts Russian slogan/recommendation as valid.
- 2026-07-13 (113): Locale warm uses compact `BlockingStatusToast` («Идёт перевод…»); rebuild confirm stays AppDialog. Home rejects Cyrillic day texts for non-RU locales (strip + rehydrate) so EN UI no longer paints RU slogan/recommendation.
- 2026-07-13 (112): DE→EN locale switch: server rejects Cyrillic morning texts before `scenario_cache` save (+ language retry); bad RU-under-en cache invalidated; Profile soft-commits UI locale if ensure still fails (no stuck technical mismatch dialog).
- 2026-07-13 (111): Locale change always shows Profile overlay «Идёт перевод…» until complete day cache is peekable; Home locale subscribe uses accessMode/tier overrides + relaxed peek (fixes paid incomplete slogan/recommendation after switch).
- 2026-07-13 (110): Profile language combo shows picked value immediately; paid locale ensure rejects Cyrillic slips into non-RU caches and always keeps validated monologue texts (fixes DE→EN showing Russian slogan/recommendation).
- 2026-07-13 (109): Locale switch: viewport-centered Modal dialog (stable confirm→spinner); commit locale only after day texts are in `dayContentCache`; Home applies warmed cache on locale change (fixes paid empty slogan/recommendation). Docs `profile/*`.
- 2026-07-13 (108): Free day cold-start no longer blocks on LLM (`after()` + cron warm via `/api/ai/global-content/warm`); profile language switch probes day texts and confirms rebuild with spinner. Docs `daily_forecast/*`, `profile/*`, `i18n/*`.
- 2026-07-13 (107): Locale change no longer remounts tab navigator (`key={locale}` removed) — profile language combo keeps scroll position. Docs `i18n/*`, `profile/*`.
- 2026-07-13 (106): Shared `ComboBox` (`modules/ui/ComboBox.tsx`): practice card duration/chakra/pulse + profile language; outside-tap dismiss; options panel under the trigger row. Docs `practices/*`, `profile/*`.
- 2026-07-12 (105): Practice stats polish: callout flush to scrub line, default first bar, Y-axis alignment, gray callout fill.
- 2026-07-12 (104): Practice stats: centered date ticks, Apple Health-style scrub callout, updated unit hints (RU+7 locales). Docs `profile/*`.
- 2026-07-12 (103): Profile reports: fixed donut focus refresh loop that stuck «Загружаем отчёты»; practice stats chart got Y-axis + single-line `ДД.ММ` tick markers. Docs `charts/*`, `profile/*`.
- 2026-07-12 (102): Profile «Статистика практик»: календарное окно минут, даты `ДД.ММ`, читаемые оси 7/30/90 (90д — недельные средние). Docs `profile/*`, `practices/*`.
- 2026-07-12 (101): Health FINAL: active energy labeled as kcal/kilocalories (Apple+Google always kcal; no cal/kcal heuristic). Docs `assistant/*`.
- 2026-07-12 (100): Apple Health: always `requestAuthorization` before query + auth canary in export; summarizing refreshes stale matrix-filtered day chakra; `родник` no longer maps to sphere 4 (`родн` false positive). Docs `communicator/*`, `assistant/*`.
- 2026-07-12 (99): Summarizing FINAL: no more invented steps (prompt example «8200» + yoga-only hole); strip invented Health sentences; Apple HealthKit samples-first + sequential queries. Docs `communicator/*`, `assistant/*`.
- 2026-07-12 (98): Summarizing Health: убран soft-wait 5s; единый старт native Apple/Google в Communicator при открытии summarizing; `health_debug.collectionTrace` в export. Docs `communicator/*`, `assistant/*`.
- 2026-07-12 (97): Day target = astro primary (fixes dialog «2nd chakra» vs Home Moon/1st); summarizing Health soft-wait 5s + `health_debug` in FINAL export. Docs `assistant/*`, `communicator/*`, `open_questions.md`.
- 2026-07-12 (96): Summarizing no longer blocks POSTs on Health `whenReady` (fixes multi-minute spinner); Home passes `timeZone`; EN leaks `day`/`usual` stripped; planning FINAL no longer doubles glued `1.` action lists. Docs `assistant/*`, `communicator/*`.
- 2026-07-12 (95): Planning FINAL no longer doubles same-order cake refinements (`кекс`/`съесть` identity + collapse + sequential numbering); Health preload/`whenReady` only for summarizing (planning «Что делать?» no longer blocks on HealthKit). Docs `assistant/*`, `communicator/*`.
- 2026-07-12 (94): Day tab no longer paints SecureStore on focus (spinner until `/api/day`); breath selector widens pool when day chakra matches only one practice so recent-stack rotates; Communicator `await whenReady()` before every dialog POST; summarizing FINAL must cite Health figures or omit health entirely. Docs `practices/*`, `assistant/*`, `communicator/*`, `daily_forecast/*`.
- 2026-07-12 (93): markers `SIMULATE_EVENT`→`SUMMARIZE_EVENT` + client strip on `complete.fullText`; `nativeHealth` always queries after auth attempt + Luxon day bounds; practice launch: no double overlay dismiss, 1s min handoff, breath → `/day`; Day tab drops stale cache/in-memory after practice/`isDayPlanCurrent`. Docs `assistant/*`, `communicator/*`, `practices/*`, `daily_forecast/*`.
- 2026-07-11 (92): `services/nativeHealth.ts`, `CoherenceBreathScreen.tsx`, `modules/breath/i18n/coherence.ts`, `_legacy_web/app/api/day/route.ts`, docs `practices/*`, `communicator/*`, `assistant/*` — summarizing снова получает реальные Health-метрики (query после soft backoff; нули по-прежнему omit); дыхание «без пульсометра» без фонарика/камеры и с упрощённым results; Day pending-карточка закрывается по `practice_slug` без `started_at >= created_at`.
- 2026-07-11 (91): `_legacy_web/app/api/_utils/planningDonePhrases.ts`, `dialogTurnGuards.ts`, `dialogHealthPrompt.ts` (new), `route.ts`, `services/nativeHealth.ts`, docs `assistant/*`, `communicator/*` — dialog QA: empty-plan «Ничего планировать не хочу» больше не пишется в `planned_events` (и planning финалится → practice card); Health `steps/sleep=0` не попадает в summarizing FINAL; Apple/Google не query-ят при denied/backoff.
- 2026-07-10 (90): `modules/ui/{SurfaceHelpModal,ModalHeaderCloseButton}.ts`, `surfaceCard.ts`, `OpportunityWindows.tsx` — типовая help-модалка surface-card: заголовок + × в одной строке, «Закрыть» внизу; единый прототип для всех «?»-подсказок.
- 2026-07-10 (87): `modules/home/{ui/OpportunityWindows,i18n/home}.ts`, `services/opportunityWindowsExplanation.ts`, typed home overlays, docs `daily_forecast/*`, `i18n/*` — «Окна возможностей»: plural title, `?` на строке заголовка; paid intro/aspect/help без склонений планет; локализация деталей и help для всех 8 языков (убран EN-хардкод).
- 2026-07-10 (85): `modules/stories/ui/StoryViewerModal.tsx`, docs `author_presence/*` — adjacent video-stories больше не переиспользуют один и тот же `VideoPlayer` через `replaceAsync()`; player пересоздаётся на каждый `displayMediaUri`, чтобы убрать поломку звука при возврате со второго видео к первому.
- 2026-07-10 (84): `modules/stories/ui/StoryViewerModal.tsx`, `_legacy_web/app/api/admin/stories/{route,cleanup/route,cleanupExpiredStories.ts}`, `supabase/migrations/20260709215553_schedule_cleanup_expired_stories.sql`, docs `author_presence/*`, `admin_panel/*`, `infra/*` — stories viewer теперь remount-ит `VideoView` на каждом новом видео, чтобы убрать короткое дребезжание/мигание старого кадра; expired stories cleanup вынесен в общий helper, запускается при `GET /api/admin/stories` и закреплён hourly `pg_cron` invoke edge-функции.
- 2026-07-10 (83): `modules/stories/ui/StoryViewerModal.tsx`, docs `author_presence/*` — авто-переход `video -> video` в stories viewer теперь ждёт readiness следующего ролика и жёстко возвращает poster/overlay-state перед commit, что убирает вспышку и редкий freeze первого кадра на соседних видео.
- 2026-07-09 (82): `modules/stories/{core/storyVideoCache,core/storiesClient,ui/StoriesRing,ui/StoryViewerModal}.tsx`, docs `author_presence/*` — stories-video перешли с hidden-player handoff на локальный `file://` mp4-cache через `expo-file-system`; первый показ нового непросмотренного видео теперь открывается после докачки локального файла, а viewer предпочитает cached URI.
- 2026-07-09 (81): `modules/stories/ui/{StoriesRing,StoryViewerModal}.tsx`, docs `author_presence/*` — modal упрощена до одного активного player; handoff из кольца оставлен только для первого нового видео, чтобы убрать конфликт соседних video-story.
- 2026-07-09 (80): `modules/stories/ui/{StoriesRing,StoryViewerModal}.tsx`, docs `author_presence/*` — первый новый video-story теперь prebuffer-ится скрытым `VideoView` в кольце и передаётся в modal тем же `VideoPlayer` (handoff), без повторного cold-load URL.
- 2026-07-09 (79): `modules/stories/{core/storiesClient,ui/StoryViewerModal,ui/StoriesRing}.tsx`, `_legacy_web/app/api/admin/stories/mediaPipeline.ts`, docs — убран HTTP Range warm (зелёные кадры); фикс утечки звука video→photo; ffmpeg closed-GOP + cover из encoded mp4.
- 2026-07-09 (78): `modules/stories/{core/storyVideoWarm,core/storiesClient,ui/StoriesRing,ui/StoryViewerModal}.tsx`, docs `author_presence/*` — cold-start нового video: HTTP warm mp4 + readiness hold (1.5–12s) вместо бесполезной 60s паузы; stall recovery mid-playback.
- 2026-07-09 (77): `modules/stories/ui/StoryViewerModal.tsx`, `_legacy_web/app/api/admin/stories/mediaPipeline.ts`, docs `author_presence/*` — откат регрессии video-stories: dual-slot + poster поверх VideoView до first frame; cover из encoded mp4; убран 1.5s reveal-fallback, который открывал чёрный кадр.
- 2026-07-09 (76): `modules/stories/{core/storiesClient,ui/StoriesRing,ui/StoryViewerModal}`, `_legacy_web/app/admin/_lib/storyUpload.ts`, `_legacy_web/app/api/admin/stories/mediaPipeline.ts`, docs `author_presence/*` — убран зелёный артефакт: удалён `storyVideoWarm.ts`; viewer переписан без warm-player; chunked upload убран, лимит 45 МБ / 45 с; 60-секундная задержка перед показом новых сторис в кольце.
- 2026-07-09 (75): `modules/stories/{core/storiesClient,core/storyVideoWarm,ui/StoriesRing,ui/StoryViewerModal}.tsx`, docs `author_presence/*` — ring обновляется быстрее после upload в админке; video-stories играют с первого открытия.
- 2026-07-09 (74): `modules/dev/patchDevLoadingView.ts`, `app/_layout.tsx`, `modules/{auth/AuthProvider,stories/core/storiesClient,stories/ui/StoriesRing}.tsx` — dev-only: скрыт синий HMR-баннер «Refreshing…»; фоновый poll сторис и auth не дергают UI без реальных изменений feed.
- 2026-07-09 (72): `modules/stories/ui/StoryViewerModal.tsx`, docs `author_presence/*` — fix чёрного экрана video-stories: два `VideoView` для preload-слотов, poster поверх видео до первого кадра.
- 2026-07-09 (71): `supabase/migrations/20260709120000_story_media_storage_limits.sql`, `_legacy_web/app/admin/{stories/page,_lib/{adminApi,storyUpload}}.ts`, `_legacy_web/app/api/admin/stories/{process/route,upload-chunk/route,uploadSession}.ts`, docs — stories video >50 MiB: chunked upload через Vercel (Free Supabase не даёт global limit >50 MiB без Pro).
- 2026-07-09 (69): `_legacy_web/app/admin/stories/page.tsx`, `_legacy_web/app/admin/posts/{page,_components/PostEditor}.tsx`, `_legacy_web/app/api/admin/{translate,stories/[id],stories/process,stories/storyPayload,posts/postPayload,posts/[id]}/route.ts`, `supabase/migrations/20260709100000_posts_i18n.sql`, docs `author_presence/*` — редактирование сторис (карандаш + модальная форма, замена медиа, переводы-аккордеон, автоперевод); LLM-перевод сторис и публикаций через `POST /api/admin/translate` (AI_MODEL_PREMIUM); языковые вкладки в PostEditor (RU+EN/DE/FR/IT/ES/PT/NL); кнопка «Опубликовать» вместо «Создать»; миграция `posts_i18n` применена на remote.
- 2026-07-09 (68): `modules/stories/ui/StoryViewerModal.tsx`, `_legacy_web/app/api/admin/stories/mediaPipeline.ts`, docs `author_presence/*` — убран visual back-jump на старте video-stories: для заранее прогретых next-video viewer скрывает poster сразу, а новые `cover.jpg` теперь извлекаются с самого начала ролика, а не с более позднего таймкода.
- 2026-07-09 (67): `package.json`, `modules/stories/{ui/StoryViewerModal,core/storiesClient}.ts`, docs `author_presence/*` — video-stories viewer переведён на `expo-video`: poster-first rendering до `onFirstFrameRender`, ускоренный startup buffer и отдельный preloaded player для ближайшего следующего видео, чтобы переход photo → video начинался без долгого статичного первого кадра.
- 2026-07-09 (66): `_legacy_web/app/api/admin/stories/mediaPipeline.ts`, docs `author_presence/*`, `admin_panel/*`, `infra/*` — исправлен `ffprobe ENOENT` при загрузке video-stories; binary paths теперь валидируются от package dir, а video pipeline нормализует ролики под stories (`1080x1920`, `30 fps`, `H.264 High`, `AAC 128k`, `+faststart`, fullscreen poster).
- 2026-07-09 (65): `package.json`, `modules/stories/{core/storyMediaPreload,ui/StoriesRing,ui/StoryViewerModal}.tsx`, docs `author_presence/*` — stories image-layer переведён на `expo-image` (`memory-disk` cache + `loadAsync` decode), modal больше не открывается после таймаута вслепую до готовности первого кадра.
- 2026-07-09 (64): `modules/stories/{core/storyMediaPreload,core/storiesClient,ui/StoryImageWarmer,ui/StoriesRing,ui/StoryViewerModal}.tsx`, docs `author_presence/*` — убран ошибочный fullscreen-показ квадратной `thumbnail_url`; stories viewer теперь открывается только после скрытого decode первого full-кадра, скрыто греет neighborhood full-кадров и держит прогресс синхронным с реально показанной фотографией.
- 2026-07-09 (63): `modules/stories/{core/storyMediaPreload,core/storiesClient,ui/StoryViewerModal,ui/StoriesRing}.tsx` — blur-up placeholder (thumbnail→full), prefetch window 5, progress synced to displayIndex, no black screen/spinner on open.
- 2026-07-09 (62): `modules/stories/ui/StoryViewerModal.tsx` — нет чёрного экрана на открытии (optimistic activeVisualReady для warm images); нет задержки на warm-переходах (immediate commit + JS-thread setValue); transparent background при cold-load.
- 2026-07-09 (61): `modules/stories/ui/{StoryViewerModal,StoryCaption}.tsx` — убраны полупрозрачные панели; прогресс-бар теперь всегда в DOM (не мигает); сегмент переключается мгновенно через `progressIndex = pendingIndex ?? displayIndex`; `progress.setValue(0)` при старте перехода в `goTo`.
- 2026-07-09 (60): `modules/stories/ui/{StoryViewerModal,StoriesRing}.tsx`, docs `author_presence/*` — исправлены мелькание при смене сторис, сброс прогресс-бара при ре-рендере родителя и ранний показ подписи следующей сторис; transition overlay теперь единственный путь для warm/cold переходов, все callbacks стабилизированы через refs.
- 2026-07-08 (59): `modules/stories/ui/{StoriesRing,StoryViewerModal}.tsx`, `services/supabase.ts`, `modules/auth/bootstrapRecoverSession.ts`, docs `author_presence/*`, `profile/*` — stories viewer больше не стартует с чёрного кадра и не зацикливается на первой сторис после локальной пометки просмотра; auth session storage переведён на двухслотовую SecureStore-запись, а invalid-refresh recover теперь повторно перепроверяет диск перед локальным sign-out.
- 2026-07-08 (58): `modules/stories/{core/storyMediaPreload,storiesClient,ui/StoriesRing,ui/StoryViewerModal,ui/StoryCaption}.tsx` — stories viewer polish: mark viewed on open, persist ring state across tab refocus, prefetch media (24h TTL), sync caption+image transitions, linkified captions via `Linking.openURL`.
- 2026-07-08 (57): `supabase/migrations/20260708200500_stories_media_pipeline_feed_cleanup.sql` (applied remote), `modules/stories/{core/storiesClient,ui/StoriesRing,ui/StoryViewerModal}.tsx`, `_legacy_web/app/admin/stories/page.tsx`, docs `author_presence/history.md` — stories: применена миграция `get_story_feed` + `thumbnail_url`; клиентский fallback при отсутствии RPC; убрано ложное кольцо без сторис; легаси-сторис без thumbnail открываются по `image_url`; админское удаление показывает ошибку в карточке вместо Next runtime overlay.
- 2026-07-08 (57): `modules/auth/{bootstrapRecoverSession,AuthProvider}.ts`, `services/supabase.ts`, `modules/auth/bootstrapRecoverSession.test.ts`, docs `profile/*` — исправлена гонка ротации refresh token на cold start: recover перечитывает SecureStore и не затирает живую сессию; read-back verify записи сессии; пропуск recover при пустом диске.
- 2026-07-08 (56): `modules/auth/{authNetworkErrors,bootstrapRecoverSession}.ts`, `services/{supabase,supabase-auth-console-filter}.ts`, docs `profile/*` — cold start с отозванным refresh token: тихая очистка SecureStore (`clearPersistedAuthSession`), без шести повторов `setSession` и без красного LogBox; запись сессии в SecureStore без «окна пустоты» при ротации токена. `vitest` + `tsc --noEmit` зелёные.
- 2026-07-08 (55): `_legacy_web/app/admin/stories/page.tsx`, `_legacy_web/app/api/admin/stories/{process,cleanup}/route.ts`, `_legacy_web/app/api/admin/stories/{mediaPipeline,storyPayload}.ts`, `_legacy_web/app/api/admin/uploads/route.ts`, `modules/stories/{core/storiesClient,ui/StoriesRing,ui/StoryViewerModal,ui/StorySessionBootstrap}.tsx`, `app/(tabs)/index.tsx`, `app/_layout.tsx`, `supabase/migrations/20260708200500_stories_media_pipeline_feed_cleanup.sql`, `supabase/functions/cleanup-expired-stories/index.ts`, `services/supabase-types.ts`, `modules/i18n/catalog/*`, docs `author_presence/*`, `admin_panel/*`, `infra/*` — Stories polished end-to-end: админская публикация переведена на server-side image/video pipeline (`sharp`/`ffmpeg`, `thumbnail_url`, poster frame, raw guards), клиентский контракт — на `get_story_feed` (все активные + `is_viewed`), кольцо встроено в header avatar с сегментами и session thumb preload, viewer открывается с первого unseen и поддерживает swipe + 7s photos, а expired stories чистятся и вручную, и отдельным cron edge function. `npx tsc --noEmit` в root и `_legacy_web` зелёный; `node scripts/i18n-sync.mjs fill --all && check` зелёный.
- 2026-07-08 (54): `_legacy_web/app/admin/{users/[id],users/stats,payments,payments/stats}/**`, `_legacy_web/app/admin/_components/AdminChrome.tsx`, `_legacy_web/app/admin/_lib/adminDates.ts`, `_legacy_web/app/api/admin/{payments/*,users/stats}/route.ts`, `supabase/migrations/20260708190000_payments_edited_at.sql`, `services/supabase-types.ts`, docs `admin_panel/*` — полировка админки после продуктового фидбека: отдельный раздел «Платежи» и две страницы статистики, модальное добавление/редактирование платежей с `edited_at` и синком latest-payment → профиль пользователя, унифицированный формат дат `ДД.ММ.ГГГГ[ ЧЧ:ММ]`, обновлённая карточка пользователя (`Язык`, без `Триал до`, уточнённый онбординг). `tsc --noEmit` в `_legacy_web` зелёный.
- 2026-07-08 (53): `modules/support/*` (new: `SupportModal`, `sendSupportMessage`), `modules/metrics/core/appOpen.ts` (new), `modules/notifications/ui/PushRegistrationBridge.tsx` (app_open на логине и возврате из фона), `app/(tabs)/profile.tsx` (карточка «Поддержка»), `_legacy_web/app/admin/{feedback,users,users/[id],prompts,prompts/[key]}/page.tsx` (new, вместо заглушек), `_legacy_web/app/admin/users/_components/TierBadge.tsx`, `_legacy_web/app/admin/_components/DashboardMetrics.tsx` (new), `_legacy_web/app/api/admin/{feedback/*,users/*,metrics,prompts/*}` (new), `_legacy_web/app/api/ai/prompt-studio/` + `_legacy_web/middleware.ts` (удалены — Prompt Studio выведен из эксплуатации), `supabase/migrations/2026070816-18*.sql` (new, применены: `support_messages`; `payments` + RPC `admin_search_users`; RPC `admin_dashboard_metrics`/`admin_llm_metrics`), `services/supabase-types.ts` (реген ×3), `modules/i18n/catalog/*` (`support.*`, 8 локалей), `_legacy_web/app/api/_utils/vimeo.test.ts` (фикс: тест чистил не все имена env vimeo-токена и ходил в сеть), docs `admin_panel`/MAP — **админ-панель, этапы 5–8 (финал)**: Поддержка (форма в Профиле → `support_messages` под RLS → `/admin/feedback` с отметкой «обработано»); Пользователи (поиск по имени/email через security-definer RPC, карточка с платежами и последней активностью, ручное назначение тарифа с записью в леджер `payments`); Дашборд (метрики целиком в БД: тиры/регистрации/DAU-WAU-MAU/платежи/LLM avg+p95 latency, error rate, prompt-токены; клиентское событие `app_open` с 30-мин троттлингом); Промпты (версии с наследованием метаданных, инвариант «одна активная на ключ», generic playground через боевой Gemini-пайплайн). E2E-смоуки этапов 5/6/8 + ручная проверка метрик зелёные; tsc root+web чисты; vitest 621/629 + 359/359 (vimeo-тест починен).
- 2026-07-08 (52): `modules/notifications/{index,core/pushRegistration,core/notificationsClient,ui/PushRegistrationBridge,ui/MyNotificationsScreen}` (new), `app/my-notifications.tsx` (new), `app/_layout.tsx` (мост + роут), `app/(tabs)/profile.tsx` (карточка «Мои уведомления» с бейджем непрочитанных), `_legacy_web/app/admin/notifications/page.tsx` (new, вместо заглушки), `_legacy_web/app/api/admin/notifications/{route,expoPush,segment}.ts` (new), `supabase/migrations/20260708150000_notifications.sql` (new, применена: `notifications` + `notification_deliveries`; `push_tokens` переиспользована из init), `services/supabase-types.ts` (реген), `modules/i18n/catalog/*` (`notifications.*`, 8 локалей), docs `notifications` (new триада)/`admin_panel`, MAP — **админ-панель, этап 4 «Уведомления»**: двойная доставка (Expo push пачками по 100 + гарантированная строка deliveries → экран «Мои уведомления» работает и без push-разрешений); сегменты `all`/`tier:*`/`webinar:*` (записавшиеся этапа 3); клиент регистрирует Expo-токен после логина (permissions → `getExpoPushTokenAsync` → upsert), тап по push открывает `data.url`; `DeviceNotRegistered` деактивирует токен; история рассылок со счётчиками. E2E-смоук зелёный (send→delivery под RLS→mark read→history→каскадная зачистка), tsc root+web чисты, vitest 620/629 (1 pre-existing `vimeo.test.ts`).
- 2026-07-08 (51): `modules/webinars/{index,core/webinarsClient,ui/WebinarScreen,ui/UpcomingWebinarBanner,ui/WebinarsStrip}` (new), `app/webinar/[id].tsx` (new), `app/_layout.tsx`, `app/(tabs)/index.tsx` (баннер вебинара над баннером публикации), `modules/posts/ui/PostsFeedScreen.tsx` (WebinarsStrip в шапке), `_legacy_web/app/admin/webinars/{page,new/page,[id]/page,_components/WebinarEditor}.tsx` (new, вместо заглушки), `_legacy_web/app/api/admin/webinars/*` (new), `_legacy_web/app/api/admin/_utils/authEmails.ts` (new — email из auth.users), `supabase/migrations/20260708140000_webinars.sql` (new, применена), `services/supabase-types.ts` (реген), `modules/i18n/catalog/*` (`webinars.*`, 8 локалей), docs `webinars`/`admin_panel`, MAP — **админ-панель, этап 3 «Вебинары»**: таблицы `webinars` (анонс, трансляция, запись) + `webinar_registrations` (RLS self; будущий сегмент рассылки этапа 4); вопросы = полиморфные `comments('webinar')`, лайк = голос (сортировка по голосам в админке); админ-CRUD с датой в поясе админа, вопросами, списком записавшихся (имя+email+тариф) и прикреплением записи; клиент: экран вебинара («Пойду», «Подключиться», вопросы через общий CommentsSection, запись — гейт Master `webinar_community` + UpgradeDialog), баннер ближайшего вебинара на главной, блок «Вебинары» во вкладке «Публикации». E2E-смоук зелёный (create→register→question→vote→admin detail→recording→delete), tsc root+web чисты.
- 2026-07-08 (50): `modules/posts/{index,core/postsClient,core/linkify(+test),ui/PostsFeedScreen,ui/PostScreen,ui/CommentsSection,ui/LinkifiedBody,ui/LatestPostBanner}` (new), `app/(tabs)/{posts.tsx(new),_layout.tsx,index.tsx}`, `app/post/[id].tsx` (new), `app/_layout.tsx`, `_legacy_web/app/admin/posts/{page,new/page,[id]/page,_components/PostEditor}.tsx` (new, вместо заглушки), `_legacy_web/app/api/admin/{posts/*,comments/[id]/route.ts,_utils/storageCleanup.ts}` (new), `_legacy_web/app/api/admin/uploads/route.ts` (параметр `bucket`), `supabase/migrations/2026070813{0000_posts_comments,1000_target_comments_nullable_name}.sql` (new, применены), `services/supabase-types.ts` (реген), `modules/i18n/catalog/*` (`posts.*`, `tabs.posts`, 8 локалей), docs `author_presence`/`admin_panel` — **админ-панель, этап 2 «Публикации»**: таблицы `posts`/`comments` (полиморфная, под вебинары этапа 3)/`comment_likes` + бакет `post-covers` + RPC `get_posts_feed`/`get_target_comments` (security definer; `display_name` nullable — фолбэк локализуется на клиенте); админ-CRUD с редактором и модерацией комментариев (скрыть/удалить); клиент: вкладка «Публикации», экран поста с кликабельными URL и комментариями (лайки, удаление своих), живой `LatestPostBanner` на главной вместо захардкоженной заглушки. E2E-смоук на dev-сервере (create→comment→like→RPC→hide→delete: комменты и обложка зачищаются), tsc root+web чисты, vitest 626/629 passed (1 pre-existing `vimeo.test.ts`); `yogaTitlePrefix` de вновь восстановлен («Übung») после i18n-fill.
- 2026-07-08 (49): `modules/stories/{index,core/storiesClient,ui/StoriesRing,ui/StoryViewerModal}` (new), `app/(tabs)/index.tsx` (кольцо сторис после HomeHeader), `_legacy_web/app/admin/stories/page.tsx` (CRUD вместо заглушки), `_legacy_web/app/api/admin/stories/{route,[id]/route,storyPayload,storyPayload.test}.ts` (new), `_legacy_web/app/api/admin/uploads/route.ts` (new), `_legacy_web/app/admin/page.tsx`, `modules/i18n/catalog/*` (ключи `stories.*`, 8 локалей), `supabase/migrations/20260708120000_stories_storage.sql` (new, применена: бакет `story-media` public read + policies, `stories.kind` += `'video'`), `services/supabase-types.ts` (регенерация), `docs/02_modules/{author_presence,admin_panel,daily_forecast}/*`, `MAP.md` — **админ-панель, этап 1 «Stories»**: владелец публикует фото/видео из `/admin/stories` (signed upload URL → файл напрямую в Storage, мимо лимита Vercel; `expires_at = publish_at + 24h` по умолчанию, планирование, «бессрочная», toggle, удаление с зачисткой бакета), пользователи видят кольцо сторис на главной и полноэкранный вьюер (RPC `get_user_stories`, просмотры в `user_story_views`, видео через `expo-av`). E2E прогнано на dev-сервере (create→RPC→PATCH→DELETE), tsc чист, vitest 616/617 (1 pre-existing `vimeo.test.ts`). Попутно: восстановлен `yogaTitlePrefix` в `practices/de.json` («Übung»), затёртый авто-переводом i18n-fill.
- 2026-07-08 (48): `_legacy_web/app/admin/*` (new: layout, AdminChrome, login, дашборд-хаб, заглушки 7 разделов), `_legacy_web/app/api/admin/me/route.ts` (new), `requireAdmin` в `_legacy_web/app/api/_utils/supabase.ts`, `_legacy_web/public/admin-manifest.json` (new), `modules/access/core/{paidAccess,paidAccess.test}.ts` (new), `modules/access/core/access.tsx`, `modules/home/useDayContent.ts`, `services/globalContentClient.ts`, `modules/communicator/ui/Communicator.tsx`, `_legacy_web/app/api/_utils/userModelTier.ts`, `_legacy_web/app/api/ai/global-content/route.ts`, greeting/dialog/recommendation-text selects, `supabase/functions/precompute-daily-forecasts/index.ts`, `scripts/sync-vercel-server-modules.mjs`, `supabase/migrations/20260708010000_admin_panel_tier_foundation.sql` (new, применена), `services/supabase-types.ts` (регенерация), `docs/02_modules/admin_panel/*` (new), `docs/02_modules/subscription/*`, `MAP.md`, `open_questions.md` — **админ-панель, этап 0 «Фундамент»** по утверждённому плану. (1) Встроенная админ-PWA: shell `/admin` (тёмная тема Tailwind v4, сайдбар/нижняя навигация, PWA-манифест `scope:/admin`), вход email/password + гейт `GET /api/admin/me`; серверный `requireAdmin()` (JWT + `user_roles.role='admin'` через service client) — обязателен для всех будущих `api/admin/*`. (2) Тарифы: constraint `users.membership_tier` расширен до `free/oracle/practitioner/master` (данные `premium`→`oracle`), добавлен `membership_expires_at` (истечение ручного гранта; истёкший = free). (3) Пять дублей условия «premium ИЛИ trial» заменены единым `modules/access/core/paidAccess.ts` (клиент+сервер; vendored для Vercel; зеркало в Edge precompute) — закрыты 2 open questions по subscription. (4) Починена история миграций Supabase: 20260705203000/20260706130000/20260707010000 помечены applied (были применены в обход `db push`), удалённый дубль 20260706222434 → reverted; после этого `db push` наката 20260708010000 чисто. tsc (root + _legacy_web) чист; vitest 613/614 (1 pre-existing fail `vimeo.test.ts` — сетевой артефакт, не связан).
- 2026-07-07 (47): `_legacy_web/app/api/_utils/{globalTransitMath,mathLevelI18n,mathLevelI18nTargets,mathLevelBuilder.test,globalTransitMath.test}.ts`, `supabase/functions/_shared/{dailyForecast,mathLevelI18n,mathLevelI18nTargets}.ts`, `services/globalContentClient.ts`, `_legacy_web/app/api/_utils/devDayContentReset.ts`, `_legacy_web/app/api/ai/dev-day-reset/route.ts`, `services/devDayContentResetClient.ts`, `app/(tabs)/index.tsx`, `docs/02_modules/daily_forecast/*` — правки «Математика дня» и кнопки «Обновить». Free: удалены блоки «Почему выбрана…», «Топ-3 лепестка», «Вес каждого аспекта»; «Активные аспекты дня» → «Ключевые аспекты дня» (в каждую строку добавлен «вес» = вклад аспекта); в «Полном рейтинге» «тон» → «гармония» с числом в шкале -1…+1 (новое поле `harmoniousness` в `planet_scores`, хелпер `globalHarmoniousnessFor`; для старых cached-строк пересчитывается из `aspects`). Paid: «Importance»→«Значимость», «Activation»→«Активация» во всём блоке 3 и блоке 4; формула `Значимость(P)=Активация(P)×(0.5+0.5×S_eff(P))`; построчные «Сатурн: Значимость=0.530×…=0.398»; «Победитель (Значимость=…)». Локализации для всех 8 локалей (RU/EN/DE/FR/IT/ES/PT/NL) в node + edge-зеркалах i18n. Dev-reset: новый `resetScope:"both"` — «Обновить» (test-mode) чистит и shared `global_daily_content`, и персональные `scenario_cache`/`user_daily_forecasts` одним вызовом, затем `refresh({forceRefresh})` регенерирует рекомендацию активного тарифа на текущую дату; чинит застарелый paid-кэш при смене промпта. tsc + vitest 356/356 + i18n-sync green.
- 2026-07-07 (46): `supabase/migrations/20260707010000_morning_recommendation_prompt_v6_unified.sql` (new), `_legacy_web/app/api/_utils/{morningRecommendation,ensureGlobalDailyContent}.ts`, `_legacy_web/app/api/ai/monologue/route.ts`, `_legacy_web/config/contentLengths.ts`, `supabase/functions/_shared/{contentLengths,data/chakra_states_baseline.json}`, `supabase/functions/precompute-{global-recommendations,daily-forecasts}/index.ts`, `docs/02_modules/{assistant,daily_forecast}/*` — единый промпт v6 «Рекомендаций на день» на главной для paid и free. Раньше `monologue_morning_recommendation` (paid) и `global_morning_recommendation` (free) были двумя разными шаблонами v5 с разным набором переменных; теперь это **один шаблон v6** для обоих `prompt_key`, а различие тарифов задаётся переменной `{{personalization_mode}}` (paid — персональный с наталом, free — безличный без натала). Переменные унифицированы: `{{primary_chakra}}` (вместо пары номер+label), `{{primary_harmoniousness}}`/`{{secondary_harmoniousness}}`/`{{tertiary_harmoniousness}}` (число в шкале **-1.0…+1.0**, без предпереведённого «tone»), `{{primary_main_transit}}`/`{{primary_main_aspect}}` (для free — пустые), `{{primary_harmonic_states}}`/`{{primary_dissonant_states}}` (палитра из `chakra_states_baseline.json` — **теперь подаётся и в free**, раньше free её не получал и модель галлюцинировала состояния), `{{user_phrases}}`, `{{petals_relation}}`. SHORT_TEXT — три полноценных абзаца в юнгианском архетипическом регистре; LONG_EXPLANATION — прагматичная астропсихология, шесть блоков §1-§6; SLOGAN — 5-8 слов, напутствие от архетипа главной планеты; §6 «Заключение» — без приглашения к математическому уровню, вместо него общая рекомендация как прожить день. Убраны примеры-фразы (клоны текста) и английские термины (tone/gravity/top_petals); упоминания «второй/третьей планеты» заменены на явные `{{secondary_planet}}`/`{{tertiary_planet}}`. Целевые длины: `SHORT_TEXT_TARGET_CHARS` 500→**700**, `LONG_EXPLANATION_TARGET_CHARS` 1500→**2000** (slogan 50 без изменений); MIN/MAX обновлены соответственно. Free-билдер `ensureGlobalDailyContent.ts` теперь собирает тот же набор переменных, считает гармоничность из баланса транзит-транзитных аспектов (`globalHarmoniousnessFor`, шкала -1…+1) и передаёт baseline states + `author_voice_block` (RU, «вы») + `FREE_PERSONALIZATION_MODE`. Paid-билдеры `morningRecommendation.ts` и `monologue/route.ts` переведены на новые имена переменных + `buildPersonalizationModePaid`. Edge-cron `precompute-global-recommendations` упрощён до **structural-only** (пишет детерминированную строку с `llm_model = null`, LLM-тексты генерирует Node `/api/ai/global-content` при первом заходе) — иначе крон с прежними переменными записал бы мусор под новый шаблон. Edge-cron `precompute-daily-forecasts` (paid) переведён на новые переменные + baseline states (JSON скопирован в `_shared/data/`) + `buildPersonalizationModePaid`. Миграция обнуляет `llm_model` в `global_daily_content` и удаляет `scenario_cache` для `morning_recommendation` → форсирует регенерацию текстов под v6 при первом заходе. **Откат:** `update prompts set is_active=false where prompt_key in ('monologue_morning_recommendation','global_morning_recommendation'); update prompts set is_active=true where prompt_key='monologue_morning_recommendation' and version=5; update prompts set is_active=true where prompt_key='global_morning_recommendation' and version=5;` (v5 сохранена в БД).
- 2026-07-06 (45): `supabase/functions/_shared/llm.ts` (new), `supabase/functions/precompute-{global-recommendations,daily-forecasts}/index.ts`, `docs/02_modules/daily_forecast/*` — root cause of the cron never pre-warming `global_daily_content` (the table held only the route's fallback row for days): the Edge cron's `generateGeminiJson` called ONLY the Gemini API with the model from `AI_MODEL_PREMIUM/STANDARD`, and on a **non-retryable primary failure** (404 model-not-found when `AI_MODEL_*` is a `deepseek-*` id, or missing key) it threw **before** reaching `AI_MODEL_FALLBACK` → all 3 dates errored every hour (HTTP 200 with `generatedCount:0`). New shared `_shared/llm.ts` ports the Vercel server's DeepSeek routing (`isDeepSeekModelId` → DeepSeek OpenAI-compatible API via `DEEPSEEK_API_KEY`/`DEEPSEEK_BASE_URL`, `response_format: json_object`, `thinking: disabled`) AND makes the primary→fallback chain fall back on **ANY** primary failure (not just retryable), mirroring `_legacy_web/app/api/_utils/{gemini,deepseekOpenAi}.ts`. Both `precompute-global-recommendations` and `precompute-daily-forecasts` switched to the shared router. **Infra (CLI):** the immediate blocker was a missing Supabase Edge secret — `resolveFallbackGeminiModelIdFromEnv()` threw `Missing AI_MODEL_FALLBACK` before any LLM call; pushed `AI_MODEL_FALLBACK=gemini-2.5-flash` + `DEEPSEEK_API_KEY` + `DEEPSEEK_BASE_URL` (from root `.env.local`) and redeployed both Edge Functions (v8). Verified: `global_daily_content` 2026-07-05 now written with `llm_model=gemini-3-flash-preview`, slogan 80 / short_text 483 / long_explanation 2303 chars + `text_i18n.en`; today's date fills on the next hourly pass (the cron regenerates dates sequentially and one date ≈ one Edge timeout window).
- 2026-07-06 (44): `_legacy_web/app/api/ai/global-content/route.ts`, `services/globalContentClient.ts`, `services/globalContentClient.test.ts`, `docs/02_modules/daily_forecast/{spec,history}.md` — free Home перестал зависать на детерминированном fallback-тексте («Сегодня полезно удерживать внимание на теле и дыхании…», `model: unknown · free`) весь день. Корень: 2026-07-05 «background refresh» был `void … .catch()` (fire-and-forget), а **Vercel serverless убивает контекст после ответа** → structural-fallback-строка от `writeStructuralGlobalRow` не замещалась настоящим LLM-текстом роутом, только кроном; при промахе крона (нет Supabase-секретов/промпта/деплоя Edge) fallback висел сутки. Фикс: роут заменяет fire-and-forget на **блокирующий** `await ensureGlobalDailyContentRow` (Vercel держит функцию живой) + `export const maxDuration = 120`; клиент `GLOBAL_CONTENT_TIMEOUT_MS` 25s → 105s (как monologue) + `attempts: 1` (чтобы ретрай не умножал ~113s × 3). Cache-hit от крона остаётся мгновенным. Тесты fake-timers переведены на 105s.
- 2026-07-06 (43): `modules/remote-play/{core/remotePlayService.ts,ui/TVRemoteScreen.tsx,i18n/remotePlay.ts}`, `docs/remote-play/wordpress-snippet.html`, `docs/02_modules/practices/*`, `docs/remote-play/README.md` — доработка TV Remote (3 нюанса, завершение после сбоя модели). Snippet: tab close → `status="stopped"` + `sessionStorage` restore; `linkDevice` закрывает stale-сессии. Phone: stopped→replay+reconnect; null→connection lost; confirm на «Отключить ТВ»; запись `practice_sessions` при `elapsed >= duration-10s` (auto на tab close near end). tsc + vitest чисты. **Обновить WordPress snippet на zamkovoi.yoga/tv/.**
- 2026-07-06 (42): `modules/remote-play/ui/TVRemoteScreen.tsx`, `modules/remote-play/i18n/remotePlay.ts`, `docs/02_modules/practices/*`, `docs/remote-play/README.md` — три нюанса TV Remote. (1) Потеря связи с ТВ (вкладка закрыта/ТВ выключен → `status="closed"`): вместо застывших disabled-кнопок панель «Связь с ТВ потеряна» + «Подключить ТВ заново» → `/connect-tv` с params (после ввода нового кода — «Начать практику»). (2) Порог завершения `durationSec - 10 сек` (было 95%); `maybeRecordTvSession` идемпотентна через `recordedRef` и вызывается и из `confirmFinish`, и автоматически при потере связи — досмотр засчитается даже без нажатия «Завершить»; ранняя остановка не пишется. (3) «Отключить ТВ» теперь через `PracticeStopConfirmDialog` (`pendingAction="disconnect"` → stop + disconnect + /connect-tv); «Стоп»/✕ → `pendingAction="stop"` → stop + back (TV остаётся paired). Новые строки `connectionLostTitle`/`connectionLostHint`/`reconnectButton`. tsc + vitest + i18n-sync check чисты.
- 2026-07-06 (41): `modules/practices/ui/{useAsanaRemotePlayLauncher.ts,PracticeCatalogScreen.tsx}`, `modules/communicator/ui/Communicator.tsx`, `modules/remote-play/ui/{ConnectTVScreen.tsx,TVRemoteScreen.tsx}`, `docs/02_modules/practices/*`, `docs/remote-play/README.md`, `docs/04_workspace/open_questions.md` — запись `practice_sessions` для ТВ-просмотра. Параметры `practiceId`/`slug`/`chakraIds`/`launchSource` проброшены через весь ТВ-путь (хук + каталог → connect-tv|tv-remote → startPractice → tv-remote → disconnect). `TVRemoteScreen.confirmFinish` пишет строку **только если `elapsedSec >= durationSec*0.95`** (досмотр до конца); `context.playback_mode="tv"`. Таймер `elapsedSec` переписан — аккумулирует только пока `status==="playing"`, замораживается на паузе, сбрасывается в replay (прежний wall-time расчёт прыгал на resume). tsc + vitest + i18n-sync check чисты.
- 2026-07-06 (40): `modules/remote-play/ui/TVRemoteScreen.tsx`, `modules/remote-play/i18n/remotePlay.ts`, `docs/02_modules/practices/*`, `docs/remote-play/README.md`, `docs/04_workspace/open_questions.md` — подтверждение завершения практики на пульте ТВ. ✕ и «Стоп» теперь открывают общий `PracticeStopConfirmDialog` (строки `getCoherenceBreathStrings`, те же что у асаны/дыхания/медитации). «Продолжить» — закрывает диалог, «Завершить» — `remotePlay.stop()` + `router.back()` (видео на ТВ стоп, pairing сохранён, возврат в Каталог/День). Добавлены `finishing`/`showStopConfirm` состояния и `finishingButton` («Завершаем…»). Запись `practice_sessions` для ТВ не ведётся — вынесено в open_questions. tsc + vitest + i18n-sync check чисты.
- 2026-07-06 (39): `modules/remote-play/i18n/remotePlay.ts`, `modules/remote-play/ui/TVRemoteScreen.tsx`, `docs/02_modules/practices/*`, `docs/remote-play/README.md` — подсказка пульта ТВ: ссылка `https://zamkovoi.yoga` → `https://zamkovoi.yoga/tv/` (ru/en + fallback), теперь совпадает с экраном `/connect-tv`. tsc + i18n-sync check чисты.
- 2026-07-06 (38): `modules/remote-play/core/remotePlayService.ts`, `modules/remote-play/i18n/remotePlay.ts`, `modules/remote-play/ui/TVRemoteScreen.tsx`, `docs/02_modules/practices/*`, `docs/remote-play/README.md`, `docs/00_index/CHANGELOG.md` — фикс залоченного пульта ТВ + локализация статусов. (1) `stopRemotePlayback` перестал стирать `vimeo_id` (только `status: "stopped"`) — раньше после стопа `resume()` не монтировал плеер на ТВ (требовалось `vimeo_id`) → пульт залипал. (2) Конечный автомат `TVRemoteScreen`: главная кнопка динамическая — stopped→«Запустить заново» (`playVimeo` + сброс таймера), paused→«Продолжить», иначе→«Пауза»; «Стоп» disabled при stopped. (3) Локализация статусов: `TvRemoteStrings.status` мапит `playing/paused/stopped/waiting/closed` → «запущено/пауза/остановлено/ожидание/закрыто» (ru/en); добавлены `replayButton`/`replayFailed`. tsc + vitest + i18n-sync check чисты.
- 2026-07-06 (37): `app/_layout.tsx`, `modules/remote-play/ui/{TVRemoteScreen.tsx,ConnectTVScreen.tsx}`, `modules/practices/ui/{useAsanaRemotePlayLauncher.ts,PracticeCatalogScreen.tsx}`, `docs/02_modules/practices/*`, `docs/remote-play/README.md`, `docs/00_index/CHANGELOG.md` — фикс пустого экрана `/tv-remote` + замыкание цикла disconnect. (1) `TVRemoteScreen` переписан с `ModalScreenLayout` на шелл `StackScreenLayout` + `StackScrollView` + `SurfaceCardView` + `FloatingCloseButton` (как `app/asana-practice.tsx`): на устройстве модальный шелл не рендерил `<Text>` (структура и ✕ рисовались, текст — нет, даже raw красный `<Text>`; строки/тема проверены логами — корректны). Роут `tv-remote` возвращён к обычному push (`fullScreenModal` убран). (2) Параметры практики (`vimeoId`/`title`/`durationSec`/`audiotrack`) теперь прокидываются через весь ТВ-путь: каталог/хук → `/connect-tv`|`/tv-remote` → `ConnectTVScreen.startPractice` → `/tv-remote` → `TVRemoteScreen.disconnect` → `/connect-tv`. После «Отключить ТВ» и повторного подключения снова появляется «Начать практику» (а не «Закрыть»). tsc чисто.
- 2026-07-06 (36): `app/_layout.tsx`, `app/asana-practice.tsx`, `modules/practices/i18n/asanaScreen.ts`, `modules/practices/ui/{useAsanaRemotePlayLauncher.ts,PracticeCatalogScreen.tsx}`, `modules/remote-play/ui/ConnectTVScreen.tsx`, `modules/remote-play/i18n/remotePlay.ts`, `docs/remote-play/wordpress-snippet.html`, `docs/02_modules/practices/*`, `docs/00_index/CHANGELOG.md` — правки полевого фидбека по (35). (1) ТВ-страница: добавлена локализованная строка `remotePlay` для 8 локалей, eyebrow теперь `${appName} ${remotePlay}` (раньше «Remote Play» был латиницей → на русском оставался английский хвост); локализован и `unavailable`. (2) `app/asana-practice.tsx`: убран подзаголовок «Выберите, где смотреть…» (`ScreenHeader subtitle`, `AsanaScreenStrings.subtitle`). (3) `TVRemoteScreen` рендерился пустым каркасом — роут `tv-remote` переведён в `presentation: "fullScreenModal"` (ModalScreenLayout рассчитан на модал). (4) `ConnectTVScreen`: после подключения при наличии параметров практики кнопка «Закрыть» → «Начать практику» → `playVimeo(vimeoId, audiotrack)` + `router.replace("/tv-remote")`; хук `useAsanaRemotePlayLauncher` и `onRemotePlay` каталога валидируют `vimeoId` до навигации и передают `vimeoId/title/durationSec/audiotrack` в `/connect-tv`. **Деплой-шаг:** обновить WordPress Custom HTML на `zamkovoi.yoga/tv` свежим сниппетом. tsc чисто; 600/600 тестов; i18n-sync green.
- 2026-07-06 (35): `app/asana-practice.tsx`, `modules/practices/ui/PracticeCard.tsx`, `modules/practices/ui/PracticeCatalogScreen.tsx`, `modules/practices/ui/useAsanaRemotePlayLauncher.ts` (new), `modules/communicator/ui/Communicator.tsx`, `modules/remote-play/{RemotePlayProvider.tsx,core/remotePlayService.ts,ui/ConnectTVScreen.tsx,ui/TVRemoteScreen.tsx,i18n/remotePlay.ts}`, `modules/ui/AppDialog.tsx`, `services/supabase-types.ts`, `supabase/migrations/20260705203000_remote_play_drop_locale.sql` (new), `docs/remote-play/{README.md,wordpress-snippet.html}`, `docs/02_modules/practices/*`, `docs/00_index/MAP.md` — переработка UX асан/ТВ по полевому фидбеку. (1) ТВ-страница берёт язык из браузера (`navigator.language`), fallback `en` (был `ru`); колонка `tv_sessions.locale` удалена миграцией — приложение больше не пишет `locale` в сессию (`linkDevice`/`playVimeoOnRemote`/провайдер очищены от `locale`). `audiotrack` остаётся. (2) Удалена панель «TV connected · code» + «Сменить ТВ» из каталога. (3) Карточка асаны всегда несёт две кнопки «Открыть на телефоне» + «Открыть на ТВ» (каталог и коммуникатор); ТВ-путь в хуке `useAsanaRemotePlayLauncher`. (4) `app/asana-practice.tsx` стал телефонным плеером — сегмент «Телефон/ТВ», `TvPanel`, SecureStore-mode и `useRemotePlay()` убраны (чинит «странное окно с ТВ-кодами»). (5) `ConnectTVScreen` не автосабмитится на 4 символах — только по кнопке; после успеха кнопка → «Закрыть». (6) `TVRemoteScreen` переделан: сообщение «Откройте … https://zamkovoi.yoga» + управление + «Отключить ТВ» (`disconnect()` + `/connect-tv`). (7) `RemotePlayProvider.disconnect()`. (8) `AppDialog.actionsRow` `space-between` → `center` — кнопки стоп-диалога центрированы (медитация/дыхание/асаны). `onRemotePlay` каталога/коммуникатора передаёт `audiotrack`. **Деплой-шаги:** `supabase db push` (drop locale) + обновить WordPress Custom HTML на `zamkovoi.yoga/tv` свежим сниппетом. tsc чисто; 600/600 тестов; i18n-sync green.
- 2026-07-05 (34): `modules/remote-play/RemotePlayProvider.tsx`, `modules/remote-play/core/remotePlayService.ts`, `services/supabase-types.ts`, `supabase/migrations/20260705193000_remote_play_locale.sql` (new), `docs/remote-play/{README.md,wordpress-snippet.html}`, `docs/02_modules/practices/*`, `docs/00_index/MAP.md` — ТВ-страница Remote Play теперь говорит на языке приложения. Провайдер читает `useAppLocale().locale` и пробрасывает её в `linkDevice`/`playVimeoOnRemote` (публичный API не изменился). Миграция добавляет nullable `locale text` в `tv_sessions`. Сниппет получил компактную инлайн-таблицу `STRINGS` для 8 локалей (только ресурсы, без дублирования страницы): язык = `row.locale` (после pairing) → `navigator.language`-детект (до pairing) → `ru`; `locale` в fingerprint → переключение на язык приложения сразу при подключении пары. Локализованное имя приложения (`appName`: Гармонизатор / Harmonizer / Harmonisator / Harmoniseur / Armonizzatore / Armonizador / Harmonizador / Harmoniseerder) в eyebrow `${appName} Remote Play`. `audiotrack` (33) остаётся для iframe URL, `locale` — для UI-текста. **Откатано в (35)** — колонка `locale` удалена, страница берёт язык из браузера (fallback `en`). **Деплой-шаг:** обновить WordPress Custom HTML на `zamkovoi.yoga/tv` свежим сниппетом. tsc чисто; тесты 23/23.
- 2026-07-05 (33): `modules/practices/core/{vimeo,vimeo.test}.ts`, `modules/practices/index.ts`, `app/asana-practice.tsx`, `modules/remote-play/{core/remotePlayService.ts,RemotePlayProvider.tsx}`, `services/supabase-types.ts`, `supabase/migrations/20260705190000_remote_play_audiotrack.sql` (new), `docs/remote-play/{README.md,wordpress-snippet.html}`, `docs/02_modules/practices/*`, `docs/00_index/MAP.md` — языковая настройка практики асаны теперь следует локали приложения. Раньше телефон всегда играл русский (`vimeoEmbedHtml(vimeoId)` без `audiotrack`), а ТВ держал `VIMEO_AUDIO_TRACK = "ru"` хардкодом и `tv_sessions` не хранил дорожку. Новый `vimeoAudiotrackForLocale(locale)` (`ru → "ru"`, остальное → `"en"`) питает и телефонный `vimeoEmbedHtml`, и `playVimeo(vimeoId, audiotrack)` для ТВ. ТВ: миграция добавляет nullable `audiotrack text` в `tv_sessions`; `playVimeoOnRemote`/`useRemotePlay().playVimeo` пробрасывают дорожку; WordPress-сниппет читает `row.audiotrack` (fallback `ru`) и перемонтирует плеер при смене дорожки (audiotrack в fingerprint). **Деплой-шаги:** `supabase db push` + обновить WordPress Custom HTML на `zamkovoi.yoga` свежим сниппетом (пока не обновлён — ТВ остаётся на русском). tsc чисто; тесты 23/23.
- 2026-07-05 (32): `app/asana-practice.tsx`, `modules/practices/i18n/asanaScreen.ts`, `docs/02_modules/practices/*` — полировка экрана асаны. Убран блок метаданных под плеером (PracticeID/Vimeo ID/Длительность/Чакры/Дата/Источник) и кнопка «Назад к каталогу»; под видео — одна кнопка «Завершить практику». Тап по ней (и по ✕) открывает единый с дыханием/медитацией `PracticeStopConfirmDialog` (`getCoherenceBreathStrings(locale)`); по подтверждению — запись сессии и переход `assistant|day → /day`, иначе `router.back()` (каталог). Прежнее «Практика сохранена» на кнопке убрано. Удалены неиспользуемые строки/хелперы из `asanaScreen.ts`. tsc чисто; practices-тесты 21/21.
- 2026-07-05 (31): `app/asana-practice.tsx`, `docs/02_modules/practices/history.md` — регрессия (30) на устройстве: WebView открывал Safari на `https://zamkovoi.yoga` вместо рендера плеера, потому что `originWhitelist={["https://*/*"]}` (с path-сегментом) не попадал под top-level baseURL `https://zamkovoi.yoga` (без path) → `react-native-webview` вызывал `Linking.openURL`. Убрал переопределение `originWhitelist` (дефолт `['http://*','https://*']` подходит). tsc чисто; vimeo-тесты 8/8.
- 2026-07-05 (30): `modules/practices/core/{vimeo,vimeo.test}.ts`, `modules/practices/index.ts`, `app/asana-practice.tsx`, `docs/02_modules/practices/*` — фикс `PrivacyError` Vimeo на телефоне. Прямой `WebView source={{ uri: vimeoEmbedUrl(id) }}` отдаёт «Because of its privacy settings, this video cannot be played here»: видео доменно-привязаны к `zamkovoi.yoga`/`zamkovoi.ru`, а WebView грузил `player.vimeo.com` без `Referer` с разрешённого домена. Фикс без деплоя: новый `vimeoEmbedHtml(vimeoId)` — HTML-документ с тем же iframe, грузится через `source={{ html, baseUrl: VIMEO_EMBED_BASE_URL }}`, где `VIMEO_EMBED_BASE_URL = "https://zamkovoi.yoga/"`; iOS `WKWebView baseURL` и Android `loadDataWithBaseURL` делают документ исходящим с `zamkovoi.yoga` → `Referer` iframe'а проходит privacy-чек. tsc чисто; vimeo-тесты 8/8.
- 2026-07-05 (29): `app/asana-practice.tsx`, `modules/practices/core/{vimeo,vimeo.test}.ts` (new), `modules/practices/index.ts`, `modules/practices/i18n/asanaScreen.ts`, `docs/02_modules/practices/*`, `docs/00_index/MAP.md`, `docs/04_workspace/open_questions.md` — асаны теперь запускаются и на телефоне, и на ТВ. `app/asana-practice.tsx` перестал быть заглушкой: сегмент «Телефон / ТВ» (UI-state, persists в SecureStore). Телефон — Vimeo-плеер на `react-native-webview` (пакет уже был в зависимостях; устаревший текст «dev-client не содержит native WebView» удалён), embed URL строит новый `vimeoEmbedUrl(vimeoId)` = `https://player.vimeo.com/video/<id>?audiotrack=ru` (без других query-параметров). ТВ — переиспользование `modules/remote-play`: «Запустить на ТВ» → `useRemotePlay().playVimeo(vimeoId)` → `/tv-remote`; «Подключить ТВ» → `/connect-tv`. В `context` сессии добавлено `playback_mode`. MAP пополнился модулем `remote-play`. tsc чисто; practices-тесты 18/18; i18n-sync green.
- 2026-07-05 (28): `_legacy_web/app/api/profile/practice-by-chakra/practiceByChakraWindow.ts`, `docs/02_modules/profile/history.md` — тип-only фикс 2 pre-existing tsc-ошибок (TS2345). Параметр `now` расширен с выведенного `DateTime<true>` до явного `DateTime<boolean>`, чтобы функция принимала `DateTime.fromISO(...)` (возвращает `DateTime<true> | DateTime<false>`). Поведение не изменилось (invalid-DateTime уже обрабатывался через `toISO() ?? new Date().toISOString()`).
- 2026-07-05 (27): `_legacy_web/app/api/_utils/{whisperPrompts,authorVoice}.ts` + их тесты, `docs/02_modules/i18n/{spec,history}.md` — серверные layer-C / STT fallback'и приведены к i18n-инварианту (русский — конечный fallback). Раньше для неизвестной/отсутствующей локали код fallback'ал на English / авто-детект, что противоречило `resolveResponseLocale` (оканчивается на `ru`) и ломало 3 теста (`whisperPrompts.test.ts`, `authorVoice.test.ts`). (1) `getDomainPrompt(language?)`: известная → свой промпт (`ru`/`en`); поддерживаемая европейская без dedicated-промпта (`de/fr/it/es/pt/nl`) → мультиязычный `AUTO_DETECT_DOMAIN_PROMPT`; **неизвестная/отсутствующая → RU-промпт**. (2) `normalizeWhisperLanguage(language?)`: сигнатура `string | undefined` → `string`, неизвестная/отсутствующая → `"ru"` (ранее `undefined` → авто-детект Whisper). (3) `getAuthorVoice(language?)`: `ru`→RU-профиль, `en`→EN, `de/fr/it/es/pt/nl`→EN-каденс (нативного профиля нет), **неизвестная/отсутствующая → RU** (ранее → EN). Тесты расширены кросс-локальными регрессиями (de/fr/it/es/pt/nl + zh-CN/пустая/undefined): 19/19 green; полный набор 80 файлов / 586 тестов green.
- 2026-07-05 (26): `scripts/generate-binaural-loops.mjs` (new), `assets/audio/mandala-sound/binaural/beat-*.wav` (12 new), `modules/mandala-sound/core/{binaural,binaural.test}.ts` (new), `modules/mandala-sound/core/{engine,types,assets}.ts`, `modules/mandala-sound/index.ts`, `docs/02_modules/audio/*` — binaural-слой переведён с 4 дискретных полос на мультиполосный кроссфейд из 12 loop'ов, чтобы бит следовал за плавной сигмоидой `targetHz` вместо 4 ступенчатых переключений. (1) **Генератор:** `scripts/generate-binaural-loops.mjs` синтезирует 12 WAV (22050 Гц, Int16, stereo, 4.000 c) с частотами биения `12, 11, 10, 9, 8, 7, 6, 5, 4, 3, 2.5, 2 Гц`, несущая 150 Гц, `L = 150 − b/2`, `R = 150 + b/2`. Все частоты кратны 0.25 Гц → `4f` целое → луп бесшовный (верифицировано). (2) **Кроссфейд:** чистая функция `binauralCrossfadeGains(targetHz, beats)` выбирает два соседних loop'а, между которыми лежит `targetHz`, и интерполирует громкости (сумма активных gain'ов = 1, только 1–2 активных loop'а); движок применяет `gains[i]·binauralGain` на каждый 250-мс тик. (3) **Рефакторинг ассетов/движка:** `MandalaSoundAssetPreset.binaural` → `readonly MandalaSoundBinauralLoop[]` (отсортированы по убыванию `beatHz`); `engine.binaural` → массив `BinauralHandle`; `setBinauralVolumes(band)` заменён на индексный кроссфейд. (4) **Удалены устаревшие ассеты:** `binaural/{alpha,beta,theta,delta}.wav` и папка `events/` (git сохраняет историю). Итог: бит скользит с шагом ~1 Гц + кроссфейд вместо 4 ступеней, рассогласование визуал/звук на переходах устранено. Не реализована только медленная модуляция несущей 140–180 Гц (4-сек луп не кодирует 45-сек период) — несущая фиксирована 150 Гц (рекомендованная PDF база). Полноценный непрерывный per-ear осциллятор заблокирован конфликтом `react-native-audio-api` с `react-native-worklets` 0.5.1 на Expo SDK 54 (Issue #739). Тесты: `binaural.test.ts` (6 случаев); 30/30 mandala-sound green; tsc clean.
- 2026-07-05 (25): `modules/mandala-sound/core/{sync,engine,types,assets}.ts`, `modules/mandala-sound/ui/MandalaSoundProvider.tsx`, `modules/mandala-sound/core/sync.test.ts`, `modules/mandala-sound/index.ts`, `docs/02_modules/audio/*` — звуковые «якоря» сокращены до двух намеренных переходных гонгов; слой случайных Punctual Events удалён. (1) **Удалены случайные события:** убран блок вероятностных one-shot events в `engine.ts` (колокольчики/шелест/хруст, ~8–12 раз за 20-минутную сессию — те самые «сложно идентифицируемые, слегка напоминающие гонг» звуки раз в несколько минут), `EVENT_COOLDOWN_MS`/`lastEventAtMs` удалены, поле `events` убрано из `MandalaSoundAssetPreset` и `MANDALA_SOUND_ASSETS` (WAV-файлы остались на диске, в бандл не включаются). (2) **Гонг теперь срабатывает на двух порогах частоты, а не на смене полосы:** `detectGongTransition(previousBand, currentBand)` → `detectGongCrossing(previousHz, currentHz)`; первый гонг — точно в момент пересечения `SCHUMANN_RESONANCE_HZ = 7.83 Гц` (резонанс Шумана, альфа→тета → средний гонг), второй — на пересечении `4 Гц` (тета→дельта → большой гонг). `buildMandalaSoundFrame` принимает `previousTargetHz` вместо `previousBand`; `MandalaSoundProvider` держит `previousTargetHzRef` и обновляет его каждый 250-мс тик. Длительность гонга не изменилась (2.6 c). (3) **Итоговое поведение:** 20-минутная сессия — ровно 2 гонга (Шуман + 4 Гц); 5-минутная — 1 (Шуман); 1–3-минутная — 0 (не опускается ниже 7.83 Гц). Тесты: `sync.test.ts` — gong-кейсы переписаны под `detectGongCrossing` + 2 интеграционных (ровно 2 гонга на 20 мин с проверкой порядка, 0 на 3 мин); 24/24 green; tsc clean. Не внедрено (отдельная задача, см. open_questions): непрерывный per-ear binaural-осциллятор — блокируется конфликтом `react-native-audio-api` с `react-native-worklets` 0.5.1 на Expo SDK 54 (Issue #739).
- 2026-07-05 (24): `modules/mandala-sound/core/timeline.ts`, `modules/mandala-sound/core/sync.ts`, `modules/mandala-sound/core/timeline.test.ts`, `modules/mandala-sound/core/sync.test.ts`, `modules/mandala-sound/index.ts`, `docs/02_modules/audio/*`, `docs/04_reference/audio/ibaa_layered_audio.md`, `docs/04_workspace/open_questions.md` — частотный таймлайн звукового модуля и мерцания мандалы перестроен на сигмоидальную адаптивную модель из исследования «Алгоритм светозвуковой стимуляции мозга». Одна функция `getMandalaSoundTargetHz` питает и `flickerHz` (мерцание облачка мандалы), и binaural-банду, и `binauralDeltaHz`, поэтому дыхательные и медитативные сессии получили новую прогрессию одновременно. (1) **Сигмоид PDF:** `f(t) = f_end + (f_start − f_end) / (1 + exp(k·(t − t_mid)))`, `f_start = 12 Гц` (альфа, безопасна по фотосенситивности), `t_mid = 0.45·T`, `k = 7/T`; фазы Catch → Glide → Hold. (2) **`f_end(T)` кусочно-линейный** (`getMandalaSoundEndHz`): 1 мин → 11 Гц, 3 → 8, 5 → 6, 8 → 4.5, 10 → 3.5, 15 → 2.75, 20 → 2 Гц, пол 2 Гц — соответствует таблице PDF. (3) **Safety cap:** `MANDALA_SOUND_MAX_TARGET_HZ` 16 → 13 Гц (исключает бета-диапазон 15–25 Гц, провоцирующий фотопароксизмальные реакции у женщин 35–60); `MANDALA_SOUND_START_HZ = 12`. (4) **Затухание мерцания:** `flickerIntensity *= (1 − 0.4·progress)` — адаптация зрительной системы к темноте к концу сессии. (5) **Gong на первом кадре:** `detectGongTransition(null, band)` теперь `null` (старт в альфа, а не бета; первый кадр — установка полосы, а не переход). (6) **Решено открытое «20 мин → 2 Гц слишком низко, ~5 Гц»** в пользу PDF: 2 Гц обосновано временем входа в транс 4–8 мин. Пиковая скорость сброса: ~2.33 Гц/мин (3 мин) → ~0.88 Гц/мин (20 мин). Не внедрено (вынесено в open_questions): непрерывный per-ear binaural-осциллятор с несущей 150 Гц (требует ухода от expo-av volume-control движка — крупная переработка) и фазовая интеграция мерцания `φ = ∫2π·f dt` (при текущих скоростях скачок фазы <0.02 рад, пренебрежимо). Тесты: `timeline.test.ts` переписан (17 случаев — таблица f_end, f(0)≈12, f(T) по PDF, монотонность, safety cap, band-прогрессия, пиковая скорость); `sync.test.ts` +1 регресс на первый кадр; 22/22 green; tsc clean.
- 2026-07-05 (23): `services/aiClient.ts`, `services/dayContentIntegrity.ts`, `modules/home/useDayContent.ts`, `_legacy_web/app/api/_utils/ensureGlobalDailyContent.ts`, `_legacy_web/app/api/ai/global-content/route.ts`, `modules/home/i18n/home.ts`, `modules/i18n/typed/catalog/home/{de,fr,it,es,pt,nl}.json`, `modules/i18n/typed/.sync-meta.json`, `docs/02_modules/daily_forecast/*` — Home now reliably loads for **both** free and all paid tiers when the cron pre-warm missed or the LLM is unavailable. Model-agnostic — reads `AI_MODEL_*` from `.env.local` via `getModelByHint`/`generateGeminiJson` (primary → `AI_MODEL_FALLBACK` retry chain); no model hardcode. (1) **Paid hang fixed:** `callMonologue` had no client timeout → cold-miss morning-recommendation hydration could wait forever, freezing the «Загружаю текст рекомендации на этот день» state; added `MONOLOGUE_TIMEOUT_MS = 105s` (headroom over the ~90s worst-case server retry/fallback chain + DB) → on timeout the card falls back to deterministic text; server may still write cache for next open. (2) **Free hard error fixed:** `ensureGlobalDailyContentRow` no longer throws on LLM failure — structural forecast + `math_level` are deterministic, so it always upserts the row (fallback texts + `llm_model=null`, self-heals). (3) **Free cold-miss no longer times out:** new `writeStructuralGlobalRow` writes a fast deterministic row (no LLM, ~1–2s) and the route returns it immediately, then triggers **background** (throttled, fire-and-forget) LLM-text refresh — the cold free path answers well within the 25s client timeout instead of blocking on the server LLM chain; a usable existing row is served immediately + background refresh on staleness; past-day row is last resort; 503 only when nothing usable is producible. (4) New `isFreeDayContentRenderable` render gate (long text optional) + strict `isDayContentCacheable` (free placeholder not cached → self-heals on reopen). (5) Copy: `recommendation.loading` → «Загружаю текст рекомендации на этот день» across RU/EN + de/fr/it/es/pt/nl. i18n check green; tsc clean (client + `_legacy_web`, pre-existing Luxon test errors unrelated); dayContentIntegrity/globalContentClient/ensureGlobalDailyContent tests green.
- 2026-07-05 (22): `modules/ui/PracticeOverlayPanel.tsx`, `modules/mandala/experiments/SacredSymbolStreamScreen.tsx`, `modules/breath/i18n/coherence.ts`, `modules/i18n/typed/catalog/breath/{de,fr,it,es,pt,nl}.json`, `_legacy_web/modules/i18n/typed/catalog/breath/{de,fr,it,es,pt,nl}.json`, `docs/02_modules/practices/*` — five meditation-panel parity fixes from the field test against the breath panel. (1) **Translucency**: the shared panel card used `theme.colors.surface` (`rgba(30,32,38,0.92)` — near-opaque); on the meditation screen the canvas behind is black + a dark mandala, so 0.92 read as a solid black card, while the breath panel sits over the lighter debug footer and looked semi-transparent. Fixed at the **base element**: `PracticeOverlayPanel` now uses a dedicated `PANEL_CARD_BACKGROUND = "rgba(20,22,28,0.72)"` — both panels are now equally translucent (the mandala / footer softly shows through). (2) **Auto-show timing**: the meditation overlay was `initialVisible: true` (panel appeared the instant the screen opened). Switched to `initialVisible: false` + a one-shot `OVERLAY_HINT_DELAY_MS = 5_000` timer that calls `showOverlay()` — the meditation screen now opens with no controls, the placard slides up 5 s in, then auto-hides after the same `OVERLAY_AUTOHIDE_MS = 4_000` as breath. (3) **Smoother slide**: `PANEL_SLIDE_DURATION_MS` 220 → 320 and the easing swapped from `Easing.out/in(Easing.cubic)` to `Easing.bezier(0.22,1,0.36,1)` (show) / `Easing.bezier(0.5,0,0.75,0)` (hide) — the entry/exit now reads as a gentle glide instead of a snap (applies to both panels, keeping them identical). (4) **Close-button alignment**: the meditation `FloatingCloseButton` used the default `top: insets.top + 18` (≈77 px on a notched phone), while the breath screen pins its close button to a fixed `top: 54, right: 18` via `styles.topCloseButton`. The meditation `topClose` style now sets the same `top: 54, right: 18`, so the '×' sits at the same spot in both practices. (5) **Stop-confirm text unified**: the meditation `PracticeStopConfirmDialog` now renders the **breath** stop-confirm strings (`getCoherenceBreathStrings(locale)` — title, message, continue, finish) instead of its own `SymbolStreamStrings` copy, so the dialog is literally the same text in both practices. The shared message was also reworded per request in all 8 locales: RU "Практика будет остановлена, а результаты по прошедшему отрезку не будут засчитаны." (was "...по текущему отрезку не будут рассчитаны.") / EN "The session will be stopped and results for the elapsed segment will not be counted." / DE "...bisherigen Abschnitts werden nicht angerechnet." / FR "...du segment écoulé ne seront pas comptabilisés." / IT "...del segmento trascorso non saranno conteggiati." / ES "...del segmento transcurrido no se contabilizarán." / PT "...do trecho decorrido não serão contabilizados." / NL "...van het verstreken segment worden niet meegeteld." i18n sync check green (en). tsc clean; module tests 26/26 (8 skipped).
- 2026-07-05 (21): `modules/ui/PracticeOverlayPanel.tsx`, `modules/mandala/experiments/SacredSymbolStreamScreen.tsx` — fix two meditation-panel regressions found in field test. (1) **Panel rendered light/white on a light-mode phone**: `app/_layout.tsx` derives the UI theme from the system color scheme, and the breath screen forces dark via `<ThemeProvider value={defaultTheme}>` at the screen level, but the meditation screen did not — so `PracticeOverlayPanel`'s `useTheme()` returned the light palette. Fixed at the **base element**: `PracticeOverlayPanel` now uses `defaultTheme` directly (always dark, self-sufficient, independent of the surrounding ThemeProvider) — per the user's instruction to fix the base element rather than per-screen. The meditation screen is also wrapped in `<ThemeProvider value={defaultTheme}>` so the `FloatingCloseButton` and the "Завершить" `AppButton` (rendered outside the panel) are dark too, matching breath. (2) **Tap on the meditation screen did nothing (panel + close button didn't appear)**: the GL canvas (`BinduSuccessionFlowCanvas`) captures touches, so wrapping it in a `<Pressable onPress={handleScreenTap}>` never fired. Replaced the wrapping `Pressable` with a transparent tap-backdrop `Pressable` (`absoluteFillObject`, `backgroundColor: transparent`, `zIndex: 10`) rendered OVER the canvas and BELOW the panel (zIndex 40) / close button (zIndex 30) — the same pattern `BreathPracticeShell` uses (`tapBackdrop`). Taps on empty screen now reach the backdrop and toggle the overlay; taps on the panel card / close button still hit those (they sit above). tsc clean; module tests 39/39.
- 2026-07-05 (20): `modules/ui/PracticeOverlayPanel.tsx` (new), `modules/breath/ui/BreathOverlayControlPanel.tsx`, `modules/mandala/experiments/SacredSymbolStreamScreen.tsx`, `modules/mandala/i18n/symbolStream.ts`, `docs/02_modules/practices/*` — unified meditation ("Вспышка") control panel with the breath practice panel via a shared shell. NEW `PracticeOverlayPanel` — common shell owning the slide-up/down animation (Reanimated `withTiming`, `PANEL_HIDDEN_TRANSLATE_Y=260`, `PANEL_SLIDE_DURATION_MS=220`), themed card, centered title block, time row (totalLabel / `-m:ss`), 4px progress track, and a `controls: ReactNode` slot. `BreathOverlayControlPanel` rewritten as a thin wrapper delegating to `PracticeOverlayPanel` with the close-X + beats pill + chevrons as the slot — breath visuals/props and `CoherenceBreathScreen` caller unchanged. `SacredSymbolStreamScreen` rewritten: (1) removed the top header (eyebrow/title/description) — fully black + canvas; (2) replaced the custom `Animated.Value` panel with `<PracticeOverlayPanel>` (title "Вспышка" + a full-width "Завершить" `AppButton` slot — no "Сколько осталось" label, no speed control); (3) removed the test-mode block (density chips, new-line/next-mandala/pause, `densityBias`/`sceneOffset`/`isPaused`/`sessionSeed` state) — canvas pinned to fixed defaults; (4) removed the mood/rating dialog; (5) keeps `FloatingCloseButton` (shared) + `useImmersiveOverlayAutohide({ autoHideMs: 4000, initialVisible: true })` — identical auto-show/auto-hide timing to breath; (6) **session-completion mark moved to practice end**: `recordPracticeSession({ selfRating: null, completionPct: 100 })` now fires in the elapsed-tick effect the moment `elapsed >= durationMs` (guarded by `completionSavedRef`), not in a mood dialog — fixes the bug where closing via X without rating left no completion row; (7) **fade-to-black + return-to-origin** at completion: 600ms black-curtain 0→1 then `exitAfterPractice` (`assistant`/`day` → `router.replace('/day')`, else `router.back()` to catalog card or Day tab). Added `minutesShort` to `SymbolStreamStrings` (ru "минут" / en "min"); updated `stopConfirmMessage` to drop the rating reference. i18n sync check green (en). tsc clean; module tests 39/39. The 3 failing `_legacy_web` authorVoice/whisperPrompts locale-fallback tests are pre-existing on main and unrelated.
- 2026-07-04 (19): `modules/biofeedback/core/metrics.ts`, `modules/breath/ui/CoherenceBreathScreen.tsx`, `services/breathPracticeInterpretation.ts`, `_legacy_web/app/api/communicator/v2/practice-interpretation/route.ts`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/{biofeedback,practices,communicator}/*` — stress-index rescale + remove mood-picker screen + fade-in results from black + LLM interpretation refactor. (1) **Stress scale**: field test `1783174144609` (BLE 20 min, push-ups 7 min before end, algoVer 1.2.27) showed the stress-index line hugging ~85–95 % for most of the session because the user's RMSSD was genuinely low (~8 ms) → high Baevsky raw I (~420–585) → `100·(1−e^(−I/220))` saturates near 100. The physiology is correct (low HRV = high stress), but the visual scale was alarming. `mapBaevskyStressToPercent` remapped from `100·(1−e^(−I/220))` (0–100) to `10 + 80·(1−e^(−I/275))` (10–90): 95 %→~83 %, 90 %→~75 %, 20 %→~23 %, 10 %→~16 %. Typical sessions now live in a 20–80 % band, extremes reach 10/90. Divisor 220→275, floor 10, span 80. Raw I and direction unchanged. (2) **Mood-picker screen removed**: the "стало лучше/хуже" smiley screen between practice and results is gone — `ResultsView` no longer has a `stage="mood"` state; the session is auto-recorded on results mount with `selfRating: null` instead of on mood tap. Removed `ResultsMood` type, `moodRef`, `handleMoodSelect`, the mood JSX, and the `resultsMoodQuestion/moodRow/moodCell/moodEmoji/moodCaption/resultsCentered` styles. (3) **Fade-in results from black**: the black curtain (zIndex 80, was sensor→running only) now also fires on `phase==="results"` — snaps to opacity 1, then `withTiming(0, 600 ms)` after a 60 ms mount delay, so results appear fading in from darkness (the practice ends, screen goes black, results emerge). (4) **LLM interpretation — no subjective feedback**: removed `subjectiveMood` from `BreathPracticeInterpretationRequest` and the server prompt's "Subjective feeling after practice" line; the client no longer sends a mood. (5) **LLM interpretation — only positive/neutral metrics**: `buildInterpretationOutcomePayload` now filters `seriesInsights` per metric direction — coherence/RMSSD/RSA included only if Δ(start→end) ≥ −0.1 (not declined), stress included only if Δ ≤ 0.1 (not risen), pulse always (baseline context). A metric that worsened is omitted from `seriesInsights`, and the server prompt now explicitly forbids the model from mentioning or reading metrics absent from `seriesInsights` elsewhere in the payload. (6) **"New technique" phrasing**: the server prompt now instructs "NEVER claim or imply that the user is a beginner, that this is a new technique for them, or that they are first learning the practice" — the model had invented "не редкость при первых попытках освоить новую технику" on its own from a falling RMSSD; with RMSSD now also omitted when it declines, the trigger is gone too. Tone set for a woman 35–60, encouraging. (7) **RMSSD/RSA spike at ~4 min in the same export**: diagnosed as a Polar H10 single-packet HR-field glitch (HR field jumped 81→61→46→81 over ~1 s at t=273) creating a brief artifactual RR swing inside one breath cycle → RSA~24 bpm + RMSSD spike for ~10 s, self-corrected. Not an algorithm bug; left untouched to avoid regressing the 1.2.26 trend-aware dense-HRV gate. Tests: 100/100 green, tsc clean. algoVer → 1.2.28.
- 2026-07-04 (18): `modules/breath/ui/CoherenceBreathScreen.tsx`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/{biofeedback,practices}/*` — fix phone sleeping during practice (keep-awake). Field test `1783165022235` (BLE 5 min, sensor removed 110→150 s, algoVer 1.2.26) showed the phone entering sleep mode ~41 s after the practice started and again at ~113 s (two pulseLog timestamp gaps of +8.3 s and +10.0 s with no logged beats in between), which extended the sensor-off gap from the intended 40 s to ~108 s (sensor returned at 150 s but wearable didn't reconnect until 221 s because the JS/VC loop was suspended while asleep). Root cause: the keep-awake activation used a manual `activateKeepAwakeAsync(tag)` inside `useEffect` with an async `deactivateKeepAwake(tag)` cleanup. Under React 18 Strict Mode (dev-client), effects run mount→cleanup→mount; the async `deactivateKeepAwake` from the first cleanup could resolve AFTER the second mount's `activateKeepAwakeAsync`, removing the tag and leaving the screen unlocked for the iOS 30 s idle timer. Fix: replaced the manual effect with a conditional `<PracticeKeepAwake>` component that calls the `useKeepAwake(tag)` hook (expo's tag-lifecycle-safe hook) and is mounted only while `phase ∈ {warmup, qualityCheck, running}`; unmount on phase exit releases the tag. Added an `AppState` listener that re-runs `activateKeepAwakeAsync` on `active` (iOS can reset the idle timer after backgrounding) as a belt-and-suspenders guard. The other two field tests in this round confirm 1.2.26 algorithm work holds: `1783164624005` (camera 5 min, 3×3 s + 1×20 s lifts) — all advanced metrics correctly withheld (camera mode computes no RMSSD/coherence/RSA/stress per spec); pulse graphs handle lifts with gaps + flat interpolation on the guidance chart; only nonLive periods are the lifts (4.8 s at the 130 s lift, 30.5 s at the 200 s lift) with no spurious breaks. `1783165720980` (BLE 10 min, ~1 min push-ups at 100 s) — the 1.2.26 trend-aware dense-HRV gate produces continuous RMSSD/stress/RSA through the 70→131 bpm ramp (only 0.9 s nonLive at startup, no metric gaps during push-ups); small coherence gaps during the exercise are honest (coherence FFT window lacks stable breathing coverage while HR is swinging); RMSSD trough + stress peak during exercise recovery is physiologically correct. Tests: 100/100 green, tsc clean. algoVer → 1.2.27.
- 2026-07-04 (17): `modules/biofeedback/wearables/{WearablePickerDialog,useWearableScanner,useRememberedWearableProbe}.ts*`, `modules/breath/ui/CoherenceBreathScreen.tsx`, `modules/breath/core/breath-results-series.ts`, `docs/02_modules/{biofeedback,practices}/*` — trend-aware dense-HRV gate + RSA outlier cap raised + equal-width picker buttons + BLE reconnect reliability. Field tests `1783158512492` (BLE 10 min, push-ups mid) and `1783157506376` (BLE 5 min, sensor removed 100→140 s) showed: (1) large RMSSD + stress-index gaps during the push-up HR ramp even though the Polar H10 RR stream was continuous (no nonLive). Root cause: the 1.2.22 sequential-deviation gate (`max(110 ms, median·0.22)` from a 12-RR context median) rejected almost the entire HR-ramp RR run — when HR climbs ~70→130 the RR falls 838→460 ms but the context median lags at the pre-ramp ~816 ms, so every new RR deviates >184 ms and is dropped; with <8 accepted RR in many 30 s windows, `DENSE_RMSSD_MIN_INTERVALS` gaps RMSSD/stress (simulation on the export: 63 windows with <8 RR). Fix: a trend-aware escape hatch in `buildDenseHrvSeriesFromBeats` — a rejected RR is admitted when the last 4 accepted RR form a monotonic ramp (each consecutive step ≥ `DENSE_HRV_TREND_MIN_STEP_MS = 8` in the same direction) AND the candidate continues that ramp; this distinguishes a real HR ramp from motion noise (a lone 576/499 among stable 730 has no preceding monotonic run, so it is still rejected). Simulation: 0 windows with <8 RR, RMSSD/stress now compute through the ramp, wild motion outliers still rejected. (2) A genuine 21 bpm RSA point during the push-up breath swing was deleted by `RSA_RESULTS_OUTLIER_BPM = 20`, punching a small RSA gap right where HR rose. Raised to 28 bpm: a real HR swing of ~70→100 inside one breath legitimately reaches ~20–25 bpm RSA amplitude, while off-body/synthetic garbage reads near 0 or >50, so 28 keeps real exercise peaks and still rejects artifacts. (3) **BLE reconnect loop** (user-reported): after removing and re-wearing the Polar H10, "Найти пульсометр" repeatedly reported "не найден" and, after finding it and tapping "Подключить", the catalog probe flipped back to "не найден" so the sensor dropdown reset to camera — even though the OS / Polar app showed the strap paired. Root cause: the scanner and the catalog probe relied solely on BLE advertisements, but a Polar H10 that is OS-paired/remembered (or held by another Central) does not advertise within the 4–12 s window. Fix: `useWearableScanner.startScan` and `useRememberedWearableProbe` now also consult `manager.devices([HR_SERVICE_UUID])` (OS-known peripherals) and seed the candidate list / mark the remembered device available when the matching peripheral is still connectable — so a remembered, non-advertising but available strap is found immediately instead of looping. Stale remembered entries with `isConnectable === false` are skipped. (4) **Picker button widths** (user-reported regression): in the "пульсометр не найден" dialog "Повторить поиск" wrapped onto 3 lines while "Закрыть" sat on one line at a much wider width. The `flex: 1, flexBasis: 0` style on `AppButton` was not producing equal widths because intrinsic content size could override it. Fix: each button is now wrapped in a `<View style={{flex:1, flexBasis:0, minWidth:0}}>` and the `AppButton` inside uses `width: "100%"`, guaranteeing identical widths on any device regardless of label length. Solo close button keeps `alignSelf: stretch`. Tests: 100/100 green, tsc clean. algoVer → 1.2.26.
- 2026-07-04 (16): `modules/biofeedback/wearables/{wearableRrQuality,wearableRrQuality.test,BleHeartRateSource}.ts*`, `modules/breath/ui/CoherenceBreathScreen.tsx`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/{biofeedback,practices}/*` — frozen-RR HR-field guard + interpretation loading spinner. Field test `1783123388556` (BLE 10 min, bhastrika ~1 min mid-session, algoVer 1.2.24) showed a mid-practice pulse/metric gray band (~287–291 s, ~300–302 s) that broke every metric graph. Root cause: `isFrozenRrRun` (tolerance 8 ms) tripped on a **real low-HRV on-body run** — during bhastrika the Polar H10 RR was genuinely near-constant (~991 ms, jitter <8 ms) while the strap was on the chest, and the previous "real RR vary ≥10 ms beat-to-beat" assumption does not hold for a high-precision strap in low-HRV states. The off-body signature is different: frozen RR **paired with a wild/disagreeing HR field** (field 1783096820335: 659 ms RR + HR swinging 115/120/75/85/140/150/169/91). Fix: `isFrozenRrRun` gains an optional `recentHeartRateBpm` argument and an HR-field guard — a frozen-looking RR run is treated as real (returns false) when the HR field is stable (span ≤ 6 bpm over the run) AND consistent with the RR-derived bpm (|hr − 60_000/rrMean| ≤ 12 bpm); otherwise it is off-body frozen as before. `BleHeartRateSource` now keeps a rolling `recentHeartRateBpm` history (paired with `recentRrMs`) and passes it to the detector. This keeps off-body detection (wild/disagreeing HR) while no longer tearing gray bands during bhastrika/meditation. Separately (UI, test 2/3 request): the results modal's "Готовлю интерпретацию…" text (`resultsInterpretationLoading`) is replaced by a centered `ActivityIndicator` (same spinner used before the practice starts), so the loading state shows a spinner instead of text. Tests: `wearableRrQuality.test.ts` (+3 HR-field guard cases). 97+3=100/100 green, tsc clean. algoVer → 1.2.25.
- 2026-07-04 (15): `modules/breath/ui/CoherenceBreathScreen.tsx`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/{biofeedback,practices}/*` — remove over-aggressive coherent-bridge budget gate. Field test `1783121037481` (camera 5 min, algoVer 1.2.23) showed many short measured-pulse breaks (0.5–1.1 s) scattered across the whole session plus unbridged 4–10 s gaps at the 100/130/160 s lifts — a regression vs the prior camera test. The 1.2.23 `bridgeWithinCoherentBudget` gate in `cameraReadyForLog` required `event.bridgingShortGap` to be within `OPTICAL_OPEN_BRIDGE_MAX_MS` (12 s) of the last **coherent** beat (`getLastStableBeatTs()`). On marginal PPG `looksCoherent` rarely fires, so `lastStableBeatTs` stalls and the gate cuts bridge-liveness even for 0.5 s gaps where the engine IS bridging (`bridgingShortGap=true`, `interpolationHoldActive=true`) → `measured=0` → gray micro-breaks. The gate was redundant: protection against the 1.2.22 70 s bridge is already provided by (a) the 1.2.23 bridge-anchor freeze (bogus short RR can no longer shrink `lastStableMedianRrMs` toward 97 bpm) and (b) the engine's own `OPTICAL_OPEN_BRIDGE_MAX_MS` cap on `openGap`, plus `trustedFreshBeat` now requiring `looksCoherent`. Fix: drop `bridgeWithinCoherentBudget` (and the now-unused `OPTICAL_OPEN_BRIDGE_MAX_MS` import + `coherentBeatAgeMs`/`lastStableBeatTs` locals) from `cameraReadyForLog`; bridge-liveness reverts to the engine's bridging flag, so a 0.5–10 s marginal-PPG gap stays live (interpolated) while a genuine 20 s+ loss still grays out. Tests: 97/97 green. algoVer → 1.2.24.
- 2026-07-04 (14): `modules/biofeedback/engines/pulse-bpm-engine.ts`, `modules/biofeedback/bus/biofeedback-pipeline.ts`, `modules/breath/ui/CoherenceBreathScreen.tsx`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/{biofeedback,practices}/*` — optical bridge anchor freeze + coherent-bridge budget. Field test `1783118620906` (camera 5 min, algoVer 1.2.22) showed a ~70→97 bpm spike/plateau on both pulse graphs (t≈30–35 s) followed by a ~70 s gray band (t≈35–106 s) with guidance stuck at 97.8 bpm — regression vs the prior camera test. Root cause: during an open bridge on marginal PPG (amplitude ~0.005), bogus short RR from peak-detector noise passed the sequential filter and (a) shrank `lastStableMedianRrMs` so the bridge anchor climbed to 97 bpm even though anchoring was enabled, and (b) reset `lastFreshBeat` via the `bridgingShortGap` trusted-beat gate, keeping bridge "live" for 70 s with flickering measured=0/live=1 stripes. Fixes: (1) `PulseBpmEngine` captures `bridgeAnchorRrMs` at bridge onset and holds candidate BPM at `60_000/bridgeAnchorRrMs` for the whole episode; `lastStableMedianRrMs` no longer updates during `bridging || reacquiring`. (2) `trustedFreshBeat` requires `looksCoherent` only (not bridging). (3) `cameraReadyForLog` caps bridge liveness at `OPTICAL_OPEN_BRIDGE_MAX_MS` (12 s) since the last coherent beat (`pipeline.getLastStableBeatTs()`), so garbage beats cannot extend bridge forever. Pulse at session start (t=0) was actually live ~70 bpm in the export — the perceived "no pulse" was the dominant spike+gray artifact dominating the chart. Tests: `pulse-bpm-engine.reacquire.test.ts` (+1 bridge-anchor freeze). algoVer → 1.2.23.
- 2026-07-04 (13): `modules/biofeedback/engines/pulse-bpm-engine.ts`, `modules/biofeedback/constants.ts`, `modules/breath/ui/CoherenceBreathScreen.tsx`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/{biofeedback,practices}/*` — wearable BPM responsiveness + dense-HRV motion-RR gate. Field test `1783116290771` (BLE 10 min, push-ups at 100 s, algoVer 1.2.21) showed two distinct issues. (1) **Pulse plateau at 131 bpm**: the real HR peak during push-ups was ~135 bpm (RR 445 ms from the Polar), but `measuredPulseRateBpm` rendered a flat 131 plateau for ~7 s — there is no hard cap, the cause is over-smoothing on the trusted path: `pushTrusted` took the median of a 10 s RR window (`PULSE_WINDOW_MS`) and `updateDisplayBpm` then took a 2.5 s median of candidate BPMs (the pool). The double median flattened the brief exercise peak and lagged the decline. Fix: trusted sources (wearable/simulated/emulated) now use a shorter `PULSE_WINDOW_TRUSTED_MS = 6 s` window, and `updateDisplayBpm` gains a `directCandidate` mode (used from `pushTrusted`) that sets `displayBpm = candidateBpm` directly when reliable (bypassing the 2.5 s pool median) — the 6 s median is already robust to a single missed beat, so the second smoothing layer is unnecessary for trusted RR. The optical path is unchanged (still 10 s + pool + slew). Effect: the exercise peak shows at its true ~133–135 bpm and the decline tracks without a plateau; no cap needed (the user's "raise to 145" was the perceived ceiling, not a real limit). (2) **RMSSD spike to 56 ms + stress-index V-dip to ~0** during the push-up burst, coinciding with the first RSA gap. Root cause: the Polar H10 emits motion-corrupted RR during movement (R-peak detection errors) — the series jumps `576↔717↔499↔691 ms` (ΔRR ±148–192 ms) while the real rhythm is ~730 ms. These values pass the dense-HRV hard range (300–1500 ms) and are NOT caught by Hampel (they arrive in bursts, not as isolated outliers, so the running MAD stays large and the median shifts with them). They inject huge ΔRR → RMSSD spikes to 56 ms, and inflate MxDMn in the Baevsky window → SI → 0 → stress % plunges. Fix: a sequential-deviation gate in `buildDenseHrvSeriesFromBeats` (same rule as `PulseBpmEngine`'s wearable filter: reject RR deviating more than `max(110 ms, median·0.22)` from the running context median of the last 12 accepted RR) applied before Hampel. Simulation on the field export drops the RMSSD peak from 56 → ~25 ms (physiologically plausible mild elevation across the rest→exercise transition) and rejects only 13/770 RR — the motion noise — while resting RSA variability (±100 ms ≈ 14%) is preserved. The stress V-dip resolves with it (MxDMn returns to normal range). The RSA chart's two gaps during the push-up burst are honest and retained: per-second RSA there is 27–40 bpm (the real HR swing 60→135 over a breath), which exceeds `RSA_RESULTS_OUTLIER_BPM = 20`, so the outlier filter gaps it — RSA is not meaningful during exercise and faking a 40 bpm "RSA" point would mislead. Confirmed no other (bug-caused) RSA gap sources: the only gates are `metricInsideGap` (nonLive) and the 20-cap, and there is no nonLive interval during the on-body push-up period. Tests: existing engine/pipeline/series suites still green (97/97). algoVer → 1.2.22.
- 2026-07-03 (12): `modules/biofeedback/wearables/{wearableRrQuality,BleHeartRateSource,wearableRrQuality.test}.ts*`, `modules/breath/ui/CoherenceBreathScreen.tsx`, `modules/practices/ui/PracticeCard.tsx`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/{biofeedback,practices}/*` — off-body frozen-RR detection (BLE) + results-chart header window + coherence auto-scale + pulse-source persistence + seriesInsights aligned to chart headers. Field tests `1783096820335` (BLE, sensor removed 100→140 s) and `1783098346952` (BLE 10 min, push-ups) showed a Polar H10 lifted off the chest keeps streaming HR/RR with no real cardiac waveform: RR collapse to a near-constant run (field: exact `659 ms` → bogus `91 bpm`) and HR jumps wildly (`115/120/140/150/169/91`). The range filter (350–1500 ms) passes 659 ms, so frozen beats were ingested as real — producing the post-gap ~91 bpm "step"/spike on the measurement graph, the RSA fragment + vertical drop after the gap, and the RMSSD chart starting at the Y-axis max (77.3 ms) then declining plus a late needle spike (all from ΔRR across frozen beats). The Polar H10 doesn't set the HRP sensor-contact bit, so contact can't gate it. Fix: pure `isFrozenRrRun(recentRrMs, minCount=6, toleranceMs=8)` — real RR vary ≥10 ms beat-to-beat, so 6 consecutive RR within ±8 ms is physiologically impossible (off-body signature). `BleHeartRateSource.ingestRrIntervals` accumulates a rolling RR history; on a frozen run it stops ingesting, latches `signalLost` (→ emulated synthetic pacing, no fake beats), clears history; real variability returns → latch clears + timeline resets. This removes the 91 spike and the downstream RMSSD/RSA gap artifacts at the source. Separately: `summarizeSeries` header `start→end` now uses the first/last **60 s** of the measured series (was first/last third of points — ~3–4 min on a 10-min session, didn't represent start/end) — this applies to **all** charts (pulse, coherence, RSA, RMSSD, stress); coherence chart Y auto-scales with 25% headroom (floor 0%) so 0–30% sessions fill the panel (header/headline % unchanged); `PracticeCard` no longer overwrites `preferredSensorMode` to `fingerCamera` on a transient probe failure (the cause of the catalog resetting to phone pulse after a BLE practice) — preference is the user's intent, only the in-memory selection falls back to camera while the strap is genuinely unavailable and restores to `ble` when the probe succeeds. **seriesInsights → LLM:** `buildInterpretationOutcomePayload` now runs `summarizeSeries` on the same display-prepared point arrays (`prepareSeriesForDisplay`, with `trimZeroEdges` for coherence) that feed the chart headers, so the `startMean`/`endMean`/`endMinusStart` sent to the Gemini interpretation exactly match the `start → end` values the user sees on every results chart — previously it ran on the raw `resultsGraphs` (leading coherence zeros dragged `startMean` to 0 even though the chart starts at the first real measurement). The server prompt already instructs the model to compare start/mid/end from `seriesInsights`, so the interpretation now reflects real start-vs-end change per metric. Tests: `wearableRrQuality.test.ts` (+4). algoVer → 1.2.21.
- 2026-07-03 (11): `modules/biofeedback/bus/biofeedback-pipeline{,.test}.ts`, `modules/biofeedback/constants.ts`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/biofeedback/*` — finger-off bridge cap. Field test `1783095121380` (algoVer 1.2.19, same 3×3 s + 1×20 s lift protocol) showed a residual plateau artifact: the 20 s finger lift drew a perfectly flat ~67.1 bpm "live" plateau for ~12 s on the measurement graph before the gray bar — the 1.2.19 bridge anchoring made the held baseline visually flat, and the 12 s budget meant a deliberate 20 s finger-off was masked as 12 s of fake "live" pulse (the user flagged it as a synthetic plateau that should be a gap). The 3 s lifts in the same session re-locked in 3–6 s, so the 12 s budget was only ever consumed by the long deliberate lift. Root cause: the bridge budget was finger-state-agnostic — it could not tell a deliberate finger-off (user expects gray) from a marginal-PPG re-lock with the finger on (interpolation is the point). Fix: new `OPTICAL_FINGER_OFF_BRIDGE_CAP_MS = 4 s` and pure helper `applyFingerOffBridgeCap` applied in `pushOpticalSample` after the bridge override — when `!contactPresent` and the gap since the last real beat exceeds the cap, the bridge is cut to gray (`bridgingShortGap=false, reacquiring=true, bpm=0`); while the finger is present the full 12 s re-lock budget is preserved. Effect: 3 s lifts stay bridged, a 20 s lift shows a ~4 s bridge plateau then gray (not 12 s live), marginal-PPG re-lock with finger on stays bridged up to 12 s. The post-gap first point (~72.4 bpm, real HR after the 20 s off, settling to 70) is honest — the visual "spike" is the jump from the stale bridge baseline to the real post-gap HR. Tests: `applyFingerOffBridgeCap` (4 cases). algoVer → 1.2.20.
- 2026-07-03 (10): `modules/biofeedback/engines/pulse-bpm-engine{,.field.test,.reacquire.test}.ts`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/biofeedback/*` — bridge-candidate anchoring + 12 s bridge budget. Field test `1783093877906` (algoVer 1.2.18, same 3×3 s + 1×20 s lift protocol) showed two residual artifacts: (1) a sharp post-gap edge spike to ~84.7 bpm right after the 130 s lift (pre-gap ~71.6) plus a second smaller spike nearby — both RR-miscalculation artifacts at gap boundaries; (2) a wide gray band after the 160 s lift where interpolation did NOT fire (unlike 100/130 s). Root cause (1): `bridgingShortGap` stays true while the gap is in the 10 s window, but `candidateBpm` was the mixed recon median (synthetic baseline + real post-gap beats); the first 1–2 s of post-gap beats carry artifactualy short RR (bandpass ring on the `dropSamplesSince` discontinuity + motion from finger return) and as they accumulated they pulled the mixed median to ~84 — the very "edge spike at gray-band boundary" the bridge is supposed to prevent; the RR-drift gate is disabled during bridging so it could not catch it. Root cause (2): the 160 s lift's re-lock took ~15 s on marginal post-return PPG (the 1.2.18 relax-sweep re-opened the detector but the pulse oscillations were genuinely below the bandpassed noise floor), exceeding the 8 s bridge budget and leaving ~7.6 s gray. Fixes: (1) during `bridging` the `candidateBpm` is anchored to `60_000/stableRr` (the baseline) instead of the mixed median — the bridge now actually holds the last stable rhythm while the gap is in the window; once the gap ages out, real (now settled) beats drive BPM again. (2) `OPTICAL_INTERP_MAX_MS` and `OPTICAL_OPEN_BRIDGE_MAX_MS` raised 8 s → 12 s (covers a 3 s lift + up to ~9 s re-lock; the 160 s case → ~3 s gray tail instead of 7.6 s); genuine 20 s losses still exceed the budget and fall through to reacquire → emulated. Tests: new "anchors BPM to baseline during a bridged gap" regression; 20 s-loss / hard-reacquire tests adjusted to the new budget. algoVer → 1.2.19.
- 2026-07-03 (9): `modules/biofeedback/signal/peak-detector{,.test}.ts`, `modules/biofeedback/bus/biofeedback-pipeline.ts`, `modules/biofeedback/constants.ts`, `modules/breath/ui/CoherenceBreathScreen.tsx`, `modules/breath/core/breath-session-signal-policy.ts`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/{biofeedback,practices}/*` — peak-detector re-acquire sweep + start-of-running emulated grace. Field test `1783091581263` (algoVer 1.2.17) still showed a ~24 s start-of-session gray band: the peak detector lost lock in settle's tail (last beat ~11 s before `running`) and didn't re-lock until t≈13 s on marginal PPG, while the emulated fallback waited the full 20 s beat-staleness so the practice sat on a gray non-pacing graph for ~9 s. Two paired fixes. (1) `detectBeats` gains a `relaxThresholds` param: when the pipeline has seen no accepted beat for `OPTICAL_REACQUIRE_RELAX_MS = 2.5 s` while the finger is present, the height/prominence thresholds drop their 35th-percentile-of-local-maxima term (which floats with a sparse noise floor and can block marginal pulse peaks) and halve the robustScale fractions — re-opening the detector to sub-threshold pulse oscillations. Downstream RR/coherence filtering still rejects noise, so the sweep cannot seed bogus pacing. (2) `CoherenceBreathScreen` adds `runningLiveBeatSeenRef` (false until a trusted beat arrives IN `running`): while false, the emulated-fallback uses `BREATH_CAMERA_EMULATED_START_GRACE_MS = 4 s` instead of the full 20 s, so a marginal-PPG start switches to synthetic pacing within ~4 s of running instead of ~9 s of gray. Once a trusted beat arrives, the 20 s threshold resumes (mid-session brief losses stay on bridge/hold). Tests: new `peak-detector.test.ts` (2 cases — relax lowers below_height/below_prominence rejections on a marginal+spikes signal; relax is a no-op on a clean strong pulse). algoVer → 1.2.18.
- 2026-07-03 (8): `modules/biofeedback/bus/biofeedback-pipeline{,.test}.ts`, `modules/breath/ui/CoherenceBreathScreen.tsx`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/{biofeedback,practices}/*` — plausible-baseline bridge + sanitized camera-hold/emulated seeds. Field test `1783091581263` (algoVer 1.2.16) still showed many spontaneous short gray bands (t≈17.7, 31, 47.6, 56.6, 241–249 s) not from finger lifts, plus a bogus guidance-graph "floor" at ~44.9 bpm at t=0. Root cause: on marginal PPG the coherence gate (`jitter ≤ 0.2×RR`) almost never passes, so `lastStableRrMs`/`lastStableBeatTs` stay 0 for the whole session and neither the engine bridge nor the 1.2.16 safety net (which anchored on the coherent baseline) could fire — every 1–3 s flicker painted gray; the camera-hold guidance also fell through to the noisy `snapshot.pulseRateBpm ≈ 44.9` because the baseline term was 0. Fixes: (1) new pipeline field `lastPlausibleBpm` = the last slew-limited `displayBpm` in 50–110 (noise ~46 bpm excluded by the range), preserved across finger-off hard-resets, exposed via `getLastPlausibleBpm()`; (2) `computePipelineBridgeOverride` now uses a `bridgeRrMs`/`bridgeAnchorTs` pair that prefers the coherent baseline but falls back to the plausible bpm, drops the `gapSinceTrusted > gapThresh` gate in favor of a `bpm <= 0` discriminator (engine dropped out), and clamps the bridge RR to 500–1200 ms (50–120 bpm); (3) camera-hold guidance value, planner seed at `running`, and `switchToEmulatedPulse` camera seed all use `getLastPlausibleBpm()` as the second-tier fallback (above the safe 65 default) so start-of-session pacing reflects the user's real ~80 bpm instead of noise/65. The ~24 s start gray band is honest — the peak detector lost lock ~11 s before `running` (marginal PPG / adaptive-prominence stuck, tracked in open_questions) and the 8 s bridge budget cannot cover a 24 s loss; the emulated ramp that follows now seeds at the plausible rate. algoVer → 1.2.17.
- 2026-07-03 (7): `modules/biofeedback/bus/biofeedback-pipeline{,.test}.ts`, `modules/biofeedback/engines/pulse-bpm-engine.ts`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/biofeedback/*` — pipeline-level short-gap bridge safety net. Field test `1783089776509` (algoVer 1.2.15) showed two short spontaneous gray bands (t≈14–16 s, t≈72–73 s) not from finger lifts: on marginal PPG the RR filter over-rejected erratic residual beats, the engine open-tail bridge didn't fire, `cameraReadyForLog` fell to false, `measuredPulseRateBpm = 0` → gray, although a coherent baseline was known. New pure helper `computePipelineBridgeOverride` (tested in 6 cases) returns a bridge snapshot when the engine didn't bridge and isn't reacquiring but the gap since the last trusted beat is ≤ 8 s and a baseline exists; applied in `pushOptical`. Long gaps and live frames untouched; wearable/emulated excluded. The start 85.3→80 ramp is left as honest warmup HR/bandpass settling (smooth ~5 bpm over ~5 s, not a spike). algoVer → 1.2.16.
- 2026-07-03 (6): `modules/biofeedback/engines/pulse-bpm-engine{,.field.test}.ts`, `modules/biofeedback/bus/biofeedback-pipeline.ts`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/biofeedback/*` — empty-window bridge rescue on the coherent baseline. Field test `1783088279299` (algoVer 1.2.14) showed a ~22 s start-of-session gray band: on marginal PPG the RR filter over-rejected the erratic residual beats, `PulseBpmEngine.pushOptical` got an empty `filteredBeatTimestampsMs` window, `lastBeat == null` skipped the open-tail bridge, and even the first 1.8 s of the loss painted gray — although a coherent baseline RR (~76 bpm) was already known. `BiofeedbackPipeline` now records `lastStableBeatTs` (preserved across finger-off hard-resets) and passes it plus `lastStableRrMs` to the engine; `pushOptical`'s `stableRr` fallback chain is `engine → realMedian → pipeline baseline`, and `reconstructOpticalBeatWindow` synthesizes a bridge on that baseline from the trusted-beat anchor when `acceptedBeats` is empty but the gap is within the 8 s budget. Short losses now stay live/smooth regardless of RR-filter rejection; only >8 s losses fall to reacquire/emulated. algoVer → 1.2.15.
- 2026-07-03 (5): `modules/breath/ui/CoherenceBreathScreen.tsx`, `modules/biofeedback/bus/biofeedback-pipeline.ts`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/{biofeedback,practices}/*` — camera start + long-lift seed now use the coherent baseline, not warmup noise. Field test `1783086748164` (algoVer 1.2.13, coherence-gating already in place) showed two residual polish issues once noise no longer paced breathing: (a) on a marginal-PPG start the practice held `snapshot.pulseRateBpm ≈ 54.7` (a warmup noise value, real pulse ~80) for the first ~16 s — it seeded the planner AND the logged guidance graph's "floor" — until the 20 s emulated fallback fired; (b) the 20 s finger lift fell back to emulated with `seedBpm = 65` (default) instead of the user's real ~80 because the contact-detector `shouldHardReset → softReset()` on long finger-off wiped `lastStableRrMs`. Fixes: planner seed at `running` and the camera-hold guidance value both prefer `pipeline.getLastStableRrMs()` (coherent baseline, only updated on low-jitter windows) over the noisy `snapshot.pulseRateBpm`, clamped to the camera-seed range 50–90; `lastStableRrMs` is now preserved across the finger-off hard-reset (saved/restored around `softReset()`), so emulated synthetic pacing after a long lift matches the user's true rate instead of falling to 65. The measurement-graph gray band at start is honest (no live beat yet); no edge spikes remain (slew-limit held the 75.8–86.5 range through all lifts). algoVer → 1.2.14.
- 2026-07-03 (4): `modules/breath/ui/CoherenceBreathScreen.tsx`, `modules/biofeedback/bus/biofeedback-pipeline.ts`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/{biofeedback,practices}/*` — camera noise-tracking no longer paces breathing. Field test `1783085782371` (algoVer 1.2.12) showed the peak detector flickering `tracking` on marginal-PPG noise for ~2.5 min (amplitude 0.006–0.037 but `signalQuality` 0.73–0.87 and `fingerPresenceConfidence` 0.74–0.81 — both "good"), producing a bogus climbing BPM 44→77 that paced breathing and plotted as live measurement, while the real pulse was ~88–94. The (3) emulated-fallback fix never fired because each noise `tracking` flicker reset the staleness clock before 20 s. Fix: `trustedFreshBeat` now requires `looksCoherent || bridgingShortGap` (bare `tracking` no longer resets the loss clock) → incoherent noise accumulates staleness → emulated synthetic sine wave within 20 s, measured chart grays out noise honestly; `lastStableRrMs` only updates on a coherent window (was any `trackingNow`) so the emulated seed is a real baseline; camera seed sanitized to 65 bpm default + clamp 50–90 when no coherent baseline exists. Pipeline `lockState` keeps the loose anti-freeze gate; only the breath-screen trust gate and baseline-RR recording now require coherence. Recovery from emulated likewise requires a coherent beat (no bounce-back onto noise). algoVer → 1.2.13.
- 2026-07-03 (3): `modules/breath/ui/CoherenceBreathScreen.tsx`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/practices/*` — camera emulated-fallback now fires on pure beat staleness (≥ 20 s with no fresh real beat), not only when the finger is detected as absent. Field test `1783084493476` (algoVer 1.2.11) collapsed: finger stayed detected (high presence confidence) but the peak detector produced no beats for 4.5 min (marginal PPG amplitude 0.007–0.013), and the old `(!fingerOk || fingerAbsent ≥ 3 s)` guard blocked the synthetic sine-wave fallback forever — the practice ran on a stale held ~76 bpm instead of switching to emulated. The camera-time staleness path now triggers emulated on its own (wall-clock path keeps the finger-absence guard for bg/fg safety); brief hiccups are still absorbed upstream by the engine's ≤ 8 s bridge + reacquire hold, so 20 s staleness is unambiguously a real loss. This is the "палец есть, пульс не считывается" → synthetic sine wave case from the spec. algoVer → 1.2.12.
- 2026-07-03 (2): `modules/biofeedback/engines/pulse-bpm-engine{,.field.test,.reacquire.test}.ts`, `modules/biofeedback/bus/biofeedback-pipeline{,.test}.ts`, `modules/breath/core/{breath-results-series,coherence-session-analysis,coherence-constants}.ts`, `modules/breath/ui/CoherenceBreathScreen.tsx`, `docs/02_modules/biofeedback/*` — optical look-ahead reconstruction: `PulseBpmEngine` rebuilds the camera beat window with synthetic beats over any gap ≤ 8 s (closed or open-tail, new `bridgingShortGap` flag threaded into breath liveness/pulseLog so bridged gaps draw no gray band and no edge spikes); sequential RR-filter context resets across long gaps (post-gap RR no longer validated against the pre-gap median — this deadlock caused the 10–15 s reacquisition); finger-return regain flush reverted to full `optical.reset()` (partial drop left a time hole the zero-phase bandpass rang on for ~12 s); camera displayBpm slew-limited to 6 bpm/s. Verified on field export `1783079328709` replay: gray band only for the 20 s lift, zero >8 bpm spikes. algoVer → 1.2.11.
- 2026-07-03 (1): `modules/biofeedback/engines/pulse-bpm-engine.ts`, `modules/biofeedback/engines/pulse-bpm-engine.reacquire.test.ts`, `modules/breath/ui/CoherenceBreathScreen.tsx`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/{biofeedback,practices}/*` — short camera gaps now stay on source-level interpolation instead of falling into a full post-gap reacquire, and long camera fallback recovery no longer starves itself with 2.5 s probe bursts. Field test `1783032655745` (algoVer 1.2.9) showed the core remaining mismatch: two of the three 3 s finger lifts still produced ~5–8 s of `holding + 0 bpm` after the finger was already back because `PulseBpmEngine` treated any beat gap `> 2.5 s` as a hard reacquire and threw away the pre-gap RR context, even when the gap was only ~3.5–4.0 s (just a few missed beats at ~66 bpm). The same export also showed the 20 s lift never recovering: after fallback to `emulated`, camera recovery probes only unpaused optical for 2.5 s every 6 s, so the PPG path kept getting reset before it could accumulate a stable real beat stream. Fixes: (1) finger-camera post-gap reacquire is now **dynamic** — only genuinely long gaps enter the 5-RR reacquire gate; gaps that are still plausibly interpolable from the last stable RR (~up to 4–5 missed beats, clamped to ~2.8–5.0 s) keep the previous BPM context and resume without a fake second gray band; (2) camera emulated fallback now retries more aggressively and lets a recovery probe stay alive continuously while the optical path sees real finger/signal signs, up to a capped continuous search window, instead of chopping recovery into short blind bursts. This matches the intended product rule: brief pulse losses are bridged by delayed interpolation, and only truly long losses fall through to synthetic pacing. algoVer → 1.2.10.
- 2026-07-02 (15): `modules/breath/core/breath-session-signal-policy.ts`, `modules/breath/ui/CoherenceBreathScreen.tsx`, `modules/breath/core/breath-results-series{,.test}.ts`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/{biofeedback,practices}/*` — short camera holds are live again. Field test `1783030499402` (algoVer 1.2.8) showed that even after the low-fps bandpass fix, 3 s finger lifts still rendered as ~10 s gray bands because the camera pipeline and the breath runtime used different liveness semantics: `pulseLockState` could be `tracking`/`holding` with a recent beat for several seconds, but `liveMeasurementNow` still demanded a much fresher beat and zeroed `measuredPulseRateBpm`, so the measured chart/log fell into a fake dead zone long before the runtime truly gave up. Raised `BREATH_CAMERA_LIVE_BEAT_MAX_AGE_MS` `2.5 s → 5 s` and, for camera mode, treat recent `holding`/`tracking` with `event.bpm > 0` and `lockState !== "searching"` as live measured pulse instead of forcing `0`. This restores the intended “interpolate 2–3 beats / ride through a short finger lift” behavior and dramatically narrows or removes gray bands on brief removals; genuinely stale camera data (`>5 s`) still becomes non-live and then emulated. Export/log consistency was also tightened: while `useEmulatedPulseMode` is active, `pulseLog` now records `pulseSource="emulated"` and `pulseLockState="searching"` instead of leaking transient internal probe states as if they were real optical tracking. algoVer → 1.2.9.
- 2026-07-02 (14): `modules/biofeedback/signal/ppg-bandpass.ts`, `modules/biofeedback/bus/snapshot-adapter.ts`, `modules/breath/ui/CoherenceBreathScreen.tsx`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/{biofeedback,practices}/*` — optical low-fps recovery + honest fallback status. Field test `1783029200609` (algoVer 1.2.7) showed two residual issues: after each finger return the camera briefly fell to `5–6 fps`, but `bandpassPpgForPeakDetection()` bypassed filtering entirely below 8 Hz, so the peak detector saw raw exposure-settling drift (`amplitude 0.17–0.26`) instead of a pulse wave and stayed in `holding` for ~10 s; and long-gap camera recovery could briefly report `tracking` / `75 bpm` with no real optical beat yet because the probe window accepted stale pre-probe state. Added matched Butterworth SOS buckets for `6` and `8 Hz`, lowered the bandpass bypass floor `8 → 5 Hz`, and kept the pick-floor at 6 Hz so short post-gap low-fps stalls stay filtered instead of raw. Camera emulated-fallback recovery now requires a **fresh real beat observed after the current probe window started** before switching back from emulated, and snapshot-adapter suppresses synthetic `tracking`/`hasFreshBeat` while `pulseSource=emulated`, so the UI no longer claims real tracking during fallback. algoVer → 1.2.8.
- 2026-07-02 (13): `modules/biofeedback/engines/pulse-bpm-engine.ts`, `modules/biofeedback/engines/pulse-bpm-engine.reacquire.test.ts`, `modules/breath/core/coherence-constants.ts`, `docs/02_modules/biofeedback/*` — post-gap leading-spike fix + longer live-readout hold. Field test `1783027581696` (algoVer 1.2.6) showed the measured-pulse chart starting at a single high point then dropping sharply after the 1st and 4th gray bands (e.g. 73 → 70 → 67 after the 100 s lift): the first 1-2 post-gap RR are artifactualy short (~822 ms vs true ~857 ms) because the zero-phase bandpass rings on the `dropSamplesSince` discontinuity (clean pre-gap history → DC step → new samples) and the peak detector catches the first peak slightly early, so the median of only 3 post-gap RR came out high. Raised `POST_GAP_MIN_RR` 3 → 5 so two artifact intervals are outvoted by three true ones and the first published BPM is clean; the displayBpm pool then ramps the held value into the new one over ~1.8 s (no spike). Also raised the non-reacquiring lost-signal `displayBpm → 0` timeout 4.5 s → 8 s so the live readout holds the last known BPM longer instead of flashing 0 during brief mid-practice dropouts (the chart's gray band is driven by `liveMeasurementActive`, not by this value, so gaps on the graph are unaffected). The 20 s-lift emulated→live plateau from the prior round did not recur (the `setPulseSource` beat-clear fix held). algoVer → 1.2.7.
- 2026-07-02 (12): `modules/biofeedback/bus/biofeedback-pipeline.ts`, `docs/02_modules/biofeedback/*` — `setPulseSource` now clears `mergedBeats`/`canonicalBeats`/`beatEligible` and resets `livePulse` on a source switch (was only resetting the BPM engine). Fixes the emulated→live handoff plateau after a long finger lift: emulated synthetic beats (~75 bpm) stayed in the sliding window for ~10 s after the camera resumed, pinning `displayBpm` at the emulated value (the flat ~75 bpm shelf after the 20 s gray band) until they aged out; the new source now starts clean and the reacquire gate reintroduces real beats. algoVer → 1.2.6.
- 2026-07-02 (11): `modules/biofeedback/sensors/FingerPpgCameraSource.tsx`, `docs/02_modules/biofeedback/*` — reverted `TARGET_PPG_FPS` 10 → 15; `TARGET = 10` broke detection (`useCameraFormat({fps:10})` picked a weaker format, camera fell to ~6 fps, FS_10 mismatched 6 fps, ~75% holding). `TARGET = 15` selects the better format; camera self-limits to ~10 fps via torch auto-exposure, matched FS_10. Heat is driven by real ~10 fps capture, not nominal 15. algoVer → 1.2.5.
- 2026-07-02 (10): `modules/biofeedback/sensors/FingerPpgCameraSource.tsx`, `docs/02_modules/biofeedback/*` — lowered `TARGET_PPG_FPS` 15 → 10 to cut heat on long sessions; raw optical diagnostics proved the camera delivers only ~10 fps under torch (iOS auto-exposure caps the frame rate), so `TARGET = 15` wasted ISP capture load. Matched FS_10 bandpass + 9 Hz floor keep the filter correct; `cadenceScore` was already 0 at ~10 fps so SQ unchanged. algoVer → 1.2.4.
- 2026-07-02 (9): `modules/biofeedback/signal/optical-pipeline.ts`, `modules/biofeedback/bus/biofeedback-pipeline.ts`, `modules/biofeedback/signal/optical-pipeline.test.ts`, `docs/02_modules/biofeedback/*` — optical recovery switched from full-buffer `reset()` to `dropSamplesSince(regainTs − absentForMs)`: only the no-finger "poison" samples are dropped, clean pre-lift history is kept, so the bandpass resumes in ~2 s instead of ~12–16 s after a 3 s finger lift (the camera delivers only ~10 fps under torch, so a full window refill cost 12 s). `mergedBeats` no longer cleared. algoVer → 1.2.3.
- 2026-07-02 (8): `modules/biofeedback/engines/pulse-bpm-engine.ts`, `modules/biofeedback/bus/biofeedback-pipeline.ts`, `modules/breath/core/coherence-session-analysis.ts`, `modules/breath/ui/CoherenceBreathScreen.tsx`, `docs/02_modules/biofeedback/*` — post-gap RR-drift reacquire gate (holds displayBpm when window median RR deviates >25% from the last stable baseline, killing the post-finger-lift 53 bpm needle spike; `lastStableMedianRrMs` only updates within ±25% band so a bogus rhythm can't become "stable"); added raw optical diagnostics (`opticalRedMean`/`LumaMean`/`SaturationRatio`/`Amplitude`/`fingerPresenceConfidence` etc.) to `pulseLog` to diagnose why finger-presence stays high during a direct-torch lift. algoVer → 1.2.2.
- 2026-07-02 (7): `modules/biofeedback/sensors/FingerPpgCameraSource.tsx`, `docs/02_modules/biofeedback/*` — reverted `TARGET_PPG_FPS` 12 → 15 Hz; 12 Hz still dropped short 4–9 s beats (peak detector marginal at 144 samples/window) despite matched filter + debounce + buffer flush; 15 Hz reinstated as verified reliability/heat compromise.
- 2026-07-02 (6): `modules/biofeedback/constants.ts`, `modules/biofeedback/bus/biofeedback-pipeline.ts`, `docs/02_modules/biofeedback/*` — flush optical ring buffer on finger return after ≥1.5 s absence so the bandpass starts on clean samples instead of 12 s of finger-off garbage; 3 s lifts now re-lock in ~2 s instead of ~12–15 s.
- 2026-07-02 (5): `modules/biofeedback/constants.ts`, `modules/biofeedback/bus/biofeedback-pipeline.ts`, `docs/02_modules/biofeedback/*` — optical re-acquisition: debounced `contactLost` (`CONTACT_LOST_GRACE_MS = 4 000`) so a 3 s finger lift no longer collapses calibration into a 10 s warmup; brief removals keep the FSM in `ready` and re-lock in ~2–3 s instead of ~15 s.
- 2026-07-02 (4): `modules/biofeedback/signal/ppg-bandpass.ts`, `modules/biofeedback/sensors/FingerPpgCameraSource.tsx`, `modules/breath/core/{coherence-session-analysis,tachogram-4hz,breath-results-series}.ts`, `modules/breath/ui/CoherenceBreathScreen.tsx`, `docs/02_modules/{biofeedback,practices}/*` — 2nd field-verify round: fixed optical near-total loss of lock (added matched 10/12 Hz Butterworth SOS; camera long-run 10→12 Hz — 10 Hz had no matched filter and mangled the waveform); killed RMSSD 90–135 ms spikes (Hampel-clean each dense window + exclude in-gap RR + replace flagged RR with local median); RSA startup skip + linear-detrend + gap-gating so the inflated leading point and drift bursts are gone; fused one strap-removal's two gray bands via `mergeWearableReconnectIslands` (15 s). Metric start offsets (coherence ~60 s, stress ~15–30 s) confirmed inherent; LLM terseness confirmed data-driven. New `modules/biofeedback/signal/ppg-bandpass.test.ts`; verified in `modules/breath/core/__field_verify.test.ts`.
- 2026-07-02 (3): `modules/biofeedback/wearables/{wearableRrQuality,BleHeartRateSource}.tsx`, `modules/breath/core/{breath-results-series,coherence-session-analysis,coherence-constants}.ts`, `modules/breath/ui/CoherenceBreathScreen.tsx`, `modules/biofeedback/sensors/FingerPpgCameraSource.tsx`, `docs/02_modules/biofeedback/*` — field-data pass: killed spurious BLE signal-loss bands on a stably-worn Polar (350 ms RR floor, keep-good-drop-bad-interval packet policy, no `signalLost` on transient); unified wearable measured/guidance pulse to one RR-derived value (no plateaus/divergence) with short-blip hold + 8 s wearable gap debounce; bridged single Polar RR-buffer voids (`bridgeArtifactRrGaps`, ≤12 s) so RSA/coherence stay continuous; dense per-second RMSSD/stress from the beat stream gated to the same gaps as the pulse chart; optical `running` fps 15→10 Hz to cut heat. Verified in `modules/breath/core/__field_verify.test.ts`.
- 2026-07-02 (2): `modules/breath/core/breath-results-series.ts`, `modules/breath/ui/CoherenceBreathScreen.tsx`, `docs/02_modules/practices/*` — fixed camera `CoherenceEngine.finalize() without active session` crash at 5:00; anchored pulse-chart gap band to the last live sample so the measured line no longer ends before the gray zone; guidance chart now draws continuously across the hold band; documented that coherence/RMSSD/stress warm-up is inherent to each metric's window, not a legacy optical artifact.
- 2026-07-02: `modules/biofeedback/bus/biofeedback-pipeline.ts`, `modules/breath/core/{coherence-session-analysis,breath-results-series}.ts`, `modules/breath/ui/CoherenceBreathScreen.tsx`, `docs/02_modules/{biofeedback,practices}/*` — camera guidance-only overheating fix (trust/coherence work moved off the per-frame path, coherence session not started in camera mode), honest metric charts (no bridging across gaps, no flat tail, fixed-width Y gutter so gap bands align), dense per-second RSA amplitude, guidance step-limiter rewrite (removes phantom plateau), and correct post-gap measured-pulse artifact folding.
- 2026-06-29: `modules/breath/core/coherence-session-analysis.ts`, `modules/breath/ui/CoherenceBreathScreen.tsx`, `docs/02_modules/{biofeedback,i18n}/history.md` — export `pulseLog` now records explicit `pulseSource`/`emulated`/BLE runtime context for loss-analysis, and the camera reminder string was updated to “рисунок дыхания точно соответствовал вашему пульсу”.
- 2026-06-29: `modules/biofeedback/engines/coherence-engine.ts`, `modules/breath/ui/CoherenceBreathScreen.tsx`, `docs/02_modules/biofeedback/history.md` — debug JSON export now uses the final screen-level `analysis` override, so camera guidance-only exports match what the user actually saw instead of leaking raw cached coherence-engine output.
- 2026-06-29: `modules/breath/ui/CoherenceBreathScreen.tsx`, `docs/02_modules/practices/history.md` — removed misleading BLE running banners for initial connect and transient HR-only downgrades, and tightened camera loss detection around the age of the last real beat so reminder/fallback trigger when live pulse actually stops.
- 2026-06-29: `modules/breath/ui/CoherenceBreathScreen.tsx`, `docs/02_modules/practices/history.md` — camera guidance-only loss detection now also watches the age of the last real beat, fixing cases where finger removal kept producing frames but failed to trigger the soft reminder or `emulated` fallback.
- 2026-06-29: `modules/biofeedback/wearables/BleHeartRateSource.tsx`, `docs/02_modules/biofeedback/history.md` — fixed a BLE regression where HRV calibration was reinitialized on every packet, causing result-screen/debug-export `RMSSD` and stress to disappear even on otherwise good wearable sessions.
- 2026-06-29: `modules/breath/ui/CoherenceBreathScreen.tsx`, `modules/breath/i18n/coherence.ts`, `modules/i18n/typed/catalog/breath/*.json` — simplified production camera breathing UX to one soft reminder shown at most once per session after sustained signal loss, instead of multiple technical PPG-status banners.
- 2026-06-29: `modules/breath/i18n/coherence.ts`, `modules/i18n/typed/catalog/breath/*.json`, `docs/02_modules/i18n/history.md` — cleaned up camera-mode runtime copy so pulse-loss overlays no longer mention paused biometrics/metrics now that production camera breathing is guidance-only.
- 2026-06-29: `biofeedback/spec.md`, `biofeedback/history.md`, `practices/spec.md`, `practices/history.md` — documented the cleaned BLE reconnect/runtime state machine plus the new automatic `emulated -> live` return path for restored camera/BLE pulse, so the practice can keep running through long signal loss without staying synthetic forever.
---

## id: 00_index/CHANGELOG

title: Documentation Changelog
version: 3.18
updated: 2026-07-02
depends_on: [docs/_proposal]
code_refs: [docs/_proposal.md]

## Changelog

- 2026-07-12: `daily_forecast` / `i18n` — Day tab help «?» on «Психо-практики» and «Йога-практики» (`SurfaceCardTitleRow` + `SurfaceHelpModal`); copy in `modules/day/i18n/day.ts` + typed overlays. Synced `daily_forecast/history.md`, `i18n/history.md`.
- 2026-07-12: `assistant` — planning day-focus sync fix (QA `текст-4808…`): strip gathering scaffold («Хорошо, собираю план.») from Day-tab/`CORRECT_RECOMMENDATION` text; greeted-flow hidden finalize-repair when day recommendation is missing so FINAL is not just «Внимание на N чакру.» + list. Synced `assistant/{spec,history}.md`.
- 2026-07-01 (8): `biofeedback` / `practices` — camera (finger PPG) post-gap reacquire gate in `PulseBpmEngine` (holds BPM + new `pulseBpm.reacquiring` flag until clean post-gap RR accumulate, wearable RR untouched); breath result charts stop re-sanitizing guidance (removes the phantom horizontal plateau), drop the false post-gap ~58 BPM measured point via `dropPostGapMeasuredArtifacts`, hold guidance flat during emulated gaps (removes the phantom downward dip), and fix the coherence `perSecond` off-by-one coverage lookup. Added `pulse-bpm-engine.reacquire.test.ts`; synced `biofeedback/{spec,history}.md`, `practices/history.md`.
- 2026-07-01 (7): `biofeedback` / `practices` — Polar recovery now prefers RR-derived BPM over raw HR when they disagree, rejects impossible long-RR reconnect packets without falling through to `ready`, keeps synthetic fallback beats out of final metric beat-series after BLE recovery, prevents camera guidance BPM from freezing into false plateaus during large real pulse changes, holds camera guidance/planner steady during non-live interpolation windows, debounces camera hard-loss recovery and result pulse gaps so a brief false tracking blip does not split a long finger removal, filters short PPG recovery spike-runs in pulse charts, and renders all breath result charts on the fixed practice-duration axis. Synced `biofeedback/{spec,history}.md`, `practices/{spec,history}.md`.
- 2026-07-01 (6): `biofeedback` / `practices` — align pulse-chart highlight bands on a fixed session timeline, retry BLE reconnect after failed connect, show interpretation when partial metric series exist, equal-width wearable not-found buttons.
- 2026-07-01 (5): `practices` / `biofeedback` — fix missing breath pulse chart lines after decimation (segment split only at zero samples, not sampling interval); wearable search shows Close-only while scanning.
- 2026-07-01 (4): `biofeedback` / `practices` — measured vs guidance breath pulse charts diverge correctly (measured breaks/shades non-live gaps; guidance keeps hold/emulated pacing with spike filtering), BLE guidedOnly stale RR zeroes measured pulse, export adds `pulseLockTransitions`, BLE prep fades via the shared black curtain, and wearable not-found dialog uses primary retry in one row.
- 2026-07-01 (3): `biofeedback` / `practices` — breath chart rendering now uses vertical steps at pulse-loss plateaus, filters isolated RMSSD/stress/RSA spikes during strap-off garbage beats, skips RSA gap-bridging that caused false flatlines, removes BLE prep→practice UI flash, and fixes interpretation timeout leaving the loading state stuck forever.
- 2026-07-01: `biofeedback` / `practices` / `communicator` / `i18n` — breath results charts now distinguish live measurement, short camera interpolation, and synthetic fallback: camera emulated pacing waits ~20 s, BLE measured pulse zeroes when RR is stale, RSA/RMSSD/stress/coherence bridge non-live gaps, BLE prep shows «Подготовка» until the first live pulse, interpretation requests time out with a visible error instead of hanging, and `pulseLog` exports `liveMeasurementActive` / `interpolationHoldActive`. Synced `biofeedback/history.md`, `practices/history.md`.
- 2026-06-30: `biofeedback` / `practices` / `communicator` — camera guidance fallback stopped reconfiguring the whole PPG capture session on every synthetic window and now pauses only the optical pipeline, reducing the risk of torch flicker during fallback/recovery. BLE startup again waits for a real ready state before the practice begins, off-body H10 packets can now downgrade runtime to `signalLost` via `sensorContactDetected`, remembered wearables are re-validated by a short live probe instead of BLE cache alone, metric charts no longer draw fake left-edge plateaus, and practice-interpretation client errors now explicitly flag “HTML instead of JSON” as a missing/outdated backend route symptom. Synced `biofeedback/spec.md`, `biofeedback/history.md`, `practices/spec.md`, `practices/history.md`.
- 2026-06-30: `biofeedback` / `practices` — camera `emulated -> live` returns no longer leave post-fallback pulse charts frozen because the pipeline now resets time-based throttles on source switches between camera CMTime and wall-clock synthetic beats. Breath results charts were also padded to the full session duration so sparse BLE RMSSD/stress/RSA tails no longer end at `4:52` just because the last live sample arrived a few seconds early. Synced `biofeedback/history.md`, `practices/history.md`.
- 2026-06-30: `biofeedback` / `practices` / `i18n` — breath results no longer hide partial BLE metric charts just because the final aggregate session failed the trust/coverage gate, and pulse charts now split into explicit measured vs guidance series when fallback pacing diverges from the live sensor. Debug `pulseLog` was extended with `measuredPulseRateBpm` / `guidancePulseRateBpm`, and all 8 locales gained labels for the two pulse-series states. Synced `biofeedback/spec.md`, `biofeedback/history.md`, `practices/spec.md`, `practices/history.md`, `i18n/history.md`.
- 2026-06-30: `biofeedback` / `practices` — camera guidance-only long-loss now enters emulated pacing as soon as the hard signal-loss condition is reached instead of effectively waiting for a second full timeout, and breath JSON export now falls back to the already rendered pulse chart if the live `pulseLog` was cleared before export. Synced `biofeedback/history.md`, `practices/history.md`.
- 2026-06-30: `biofeedback` / `practices` / `communicator` / `i18n` — breath results now render post-practice charts (camera: pulse only; BLE: pulse plus any reliable coherence/RSA/RMSSD/stress series), inline interpretation is shown only when real biometrics exist, and BLE HRV accumulation no longer freezes after the first long gap because wearable beat merge/resume handling was repaired. Synced `biofeedback/{spec,history}.md`, `practices/{spec,history}.md`, `communicator/{spec,history}.md`, `i18n/history.md`.
- 2026-06-30: `biofeedback` / `practices` — BLE sleep/resume follow-up: wearable sessions no longer reuse camera `fingerDetected` to decide background-resume auto-abort, which could surface the “Практика приостановлена” path after lock/sleep without a real crash. Breath debug export also gained wearable HR/RR freshness fields in `pulseLog` so future BLE detach tests can distinguish real reconnect/fallback from a strap that kept streaming valid RR. Synced `biofeedback/{spec,history}.md`, `practices/{spec,history}.md`.
- 2026-06-29: `biofeedback` / `practices` / `communicator` / `i18n` — breath results now stay local: `Обсудить` was replaced with inline `Интерпретация` via `POST /api/communicator/v2/practice-interpretation`, while camera/BLE debug export now records per-session runtime events and last-fresh-beat age so long-loss fallback and first-run sleep issues can be diagnosed from JSON. Synced `biofeedback/{spec,history}.md`, `practices/{spec,dependencies,history}.md`, `communicator/{spec,dependencies,history}.md`, `i18n/{spec,history}.md`, `MAP.md`, `open_questions.md`.
- 2026-06-29: `biofeedback` / `practices` / `i18n` — breath camera mode now runs as pulse-guidance only: `CoherenceBreathScreen` pauses advanced HRV/coherence/RSA capture for `fingerCamera`, keeps those metrics for BLE RR devices, and shows localized guidance that Bluetooth sensors are required for detailed biometrics. Synced `biofeedback/{spec,history}.md`, `practices/{spec,history}.md`, `i18n/history.md`.
- 2026-06-29: `biofeedback` / `practices` — BLE chest strap now reconnects after packet stalls (`signalLost`), practice cards probe remembered devices before showing them as selected, and wearable search dialogs include troubleshooting tips. Finger camera remounts on resume and parity bench keeps capture during in-app navigation. Synced `biofeedback/{spec,history}.md`, `practices/history.md`.
- 2026-06-28: doc-sync pre-push — finger signal-trust / metric-beats push: deduped `practices/spec.md` `CoherenceBreathScreen` bullet; added `BiofeedbackPipeline` introspection API to `biofeedback/spec.md` §2; synced reverse breath↔biofeedback type coupling and parity route in `biofeedback/dependencies.md`.
- 2026-06-29: `biofeedback` / `practices` — refined finger grace-window (evaluate from session start when clean, after last early gap, or from minute two if failures persisted); parity coherence/RSA now computed on trimmed `metricBeats` like RMSSD/stress; hybrid breath windows use the same grace; finger pipeline feeds full HRV beats to `CoherenceEngine`. Synced `biofeedback/{spec,history}.md`, `practices/history.md`.
- 2026-06-29: `biofeedback` / `practices` — parity bench now starts debug coherence only after the first real sensor timestamp (fixing blank camera `coherence/RSA`), shows `—` instead of false `0` when the post-trim wearable HRV window is still too short, and whole-session finger trust now forgives early first-minute gap/jitter damage when a long clean tail follows. Synced `biofeedback/{spec,history}.md`, `practices/{spec,history}.md`.
- 2026-06-28: `biofeedback` — parity bench source panels now also surface live `coherence` average and `RSA` from a debug `CoherenceEngine` session, while still withholding those fields whenever session trust falls below `full_biometrics`. Synced `biofeedback/{spec,history}.md`.
- 2026-06-28: `biofeedback` — finger metric smoothing now applies a second conservative median-of-3 RR pass, and session-level signal trust no longer resets to “clean” after a repeated finger re-attach. This makes Polar parity on synchronized tail windows tighter for clean/short-gap cases and prevents long-gap sessions from falsely re-entering `full_biometrics`. Synced `biofeedback/{spec,history}.md`.
- 2026-06-28: `biofeedback` / `practices` — late finger-camera degradation no longer has to erase HRV completely: if the final session trust falls to `pulse_only`, the app now preserves `RMSSD` / stress from the latest reliable 180/120/90-second tail window when available, while still withholding coherence/RSA. Synced `biofeedback/{spec,history}.md`, `practices/{spec,history}.md`.
- 2026-06-28: `biofeedback` / `practices` — coherence/RSA now also get a late-session tail fallback, but through a stricter path than HRV: only continuous 180/150/120-second finger tail windows with no local gap events and a passing coherence-analysis gate may surface recovered coherence/RSA. Synced `biofeedback/{spec,history}.md`, `practices/{spec,history}.md`.
- 2026-06-28: `biofeedback` / `practices` — finger-camera sessions now compute an explicit `SignalTrustLevel` from gap events plus post-filter RR jitter. Breath results use it to separate `full_biometrics`, `guided_limited`, and `pulse_only`, hiding coherence/RSA or all final biometrics when the camera signal degrades instead of showing misleading numbers. Synced `biofeedback/{spec,history}.md`, `practices/{spec,history}.md`.
- 2026-06-28: `biofeedback` — finger-camera beat merge now both repairs obvious missed optical beats and damps alternating short/long timestamp jitter before coherence/HRV/stress, so PPG timing noise less aggressively inflates RMSSD versus Polar. Synced `biofeedback/{spec,history}.md`.
- 2026-06-28: `biofeedback` — parity bench metrics no longer compare second-zero wearable accumulation against post-`ready` finger accumulation: the chest-strap debug panel trims the initial 20 s (`warmup + settle`) before showing RMSSD / stress. Synced `biofeedback/{spec,history}.md`.
- 2026-06-28: `biofeedback` — finger-only metric path now applies median-of-3 RR smoothing before RMSSD / Baevsky stress, and the parity bench/export were switched to the same `metricBeats` series so visual comparison matches the numbers being shown. Synced `biofeedback/{spec,history}.md`.
- 2026-06-28: `biofeedback` — Polar RR reconstruction now extends from the last committed beat instead of re-anchoring every notify to packet arrival time, and a new debug parity bench (`biofeedback-parity`) compares live `BPM` / `RR` from finger camera vs BLE chest strap. Synced `biofeedback/{spec,history}.md`.
- 2026-06-28: `biofeedback` — BLE multi-RR packets commit only new beat timestamps after the last merged beat, fixing Polar strap RR sawtooth and coherence withhold on full sessions. Synced `biofeedback/{spec,history}.md`.
- 2026-06-28: `biofeedback` — BLE RR packets now reconstruct beat timestamps per HRS (backward from packet time); wearable path skips PPG PulseBpm filter when appending to coherence/HRV so chest-strap metrics match finger pipeline output. Synced `biofeedback/{spec,history}.md`.
- 2026-06-27: doc-sync pre-push — `WearablePickerDialog` export and practices↔biofeedback dependency: `biofeedback/spec.md` (§2 wearables barrel), `biofeedback/dependencies.md`, `practices/dependencies.md`, `MAP.md` code_refs.
- 2026-06-27: `biofeedback` / `practices` — Polar H10 QA: fixed BLE RR timeline reset (metrics no longer withheld by 5–10 s artifact gaps), improved first-scan discovery (`180D` filter, duplicate advertisements, 12 s picker window), and collapsed `PracticeCard` selectors on return from breath screen. Synced `biofeedback/{spec,history}.md`, `practices/{spec,history}.md`.
- 2026-06-27: doc-sync pre-push — BLE breath sensor path: synced stale sections in `biofeedback/{spec,dependencies}.md`, `practices/{spec,dependencies}.md`, `communicator/{spec,history}.md`, `infra/spec.md` (wearables code_refs, launch-param contract, communicator round-trip).
- 2026-06-27: `practices` / `biofeedback` — breathing card now uses `Пульс с телефона` / `Пульсометр Bluetooth` / `Без пульсометра`, reuses one shared BLE picker dialog before start and after runtime BLE failures, starts Bluetooth sessions straight in the running screen (without the old prep overlay), and hardens both iOS BLE scan startup and `BleHeartRateSource` effect stability. Synced `practices/{spec,history}.md` and `biofeedback/{spec,history}.md`.
- 2026-06-27: `practices` / `biofeedback` / `infra` / `i18n` — breathing gained BLE chest-strap groundwork: richer breath launch contract (`sensorMode` + remembered device metadata), generic Heart Rate Service RR ingestion with trusted Polar path, Expo BLE plugin/permissions, and synced docs/open question for trusted-device thresholds.
- 2026-06-26: doc-sync pre-push — paired `daily_forecast/dependencies.md` and `assistant/dependencies.md` with `loadCachedMorningRecommendation` / `buildClientForecastPayload` on `POST /api/astro/daily-forecast` (spec/history already current).
- 2026-06-27: `daily_forecast` — paid Home loads cron morning texts in first `daily-forecast` response (`loadCachedMorningRecommendation`); client skips fallback flash, shows loading only on cache miss. Synced `daily_forecast/{spec,history}.md`.
- 2026-06-27: `infra` / `daily_forecast` — `precompute-daily-forecasts` bundling: cron deps moved to `supabase/functions/_shared/` (`contentLengths`, `mathLevelI18n*`, `contentLocales`; activation from `dailyForecast.ts`); deploy + paid morning pre-warm restored.
- 2026-06-27: `infra` / `daily_forecast` — Supabase Edge deploy fix: `_shared/dailyForecast.ts` uses esm.sh for `astronomia` imports so `precompute-global-recommendations` bundles and cron pre-warm works again. Synced `daily_forecast/history.md`, `infra/history.md`.
- 2026-06-26: doc-sync pre-push — `global_morning_recommendation` v5 + `isCurrentGlobalLongExplanation` validators: synced `assistant/{spec,history,dependencies}.md`, `chakra/{spec,history,dependencies}.md`, `daily_forecast/dependencies.md`, `astro/interpretation_layers.md` (literal §1–§6 long text, chakra ban in global long_explanation, legacy row flush).
- 2026-06-26: `daily_forecast` — free Home detailed recommendation stays transit-only but now follows the paid-style six-section explainer shape (`§1…§6`) without chakra language; stale `global_daily_content` rows are regenerated on structure/lexicon mismatch, and direct DB fallback drops legacy long text. Synced `daily_forecast/{spec,history}.md`.
- 2026-06-26: doc-sync pre-push — Home explainer stack: `SlideUpModalLayer`, `HomeExplainerLevel`, `stackLayer` presentation, chart orchestration in `ModalLongExplanation`, export `chartAspectsFromMathLevel`. Synced stale `nestedOverlay` refs in `daily_forecast/dependencies.md`, `astro/interpretation_layers.md` (spec/history already current).
- 2026-06-26: doc-sync pre-push — completed `recommendationText.ts` / morning prompt v5 alignment in `assistant/{spec,dependencies}.md`, `chakra/{spec,dependencies,history}.md`, `daily_forecast/dependencies.md`, `astro/interpretation_layers.md` (tone-key + §6 normalization, `sanitizeRecommendationDisplay`, math modal nested overlay).
- 2026-06-26: `daily_forecast` / `assistant` / `i18n` — Home QA: recommendation card shows deterministic fallback (not «Обновляем…») while LLM layer hydrates; `recommendationText.ts` strips English tone keys and §6 «С МОСТИКОМ» from visible copy; math modal opens as nested overlay inside «Подробнее» (fixes dead button); morning prompt v5. Synced `daily_forecast/{spec,history}.md`, `assistant/history.md`.
- 2026-06-26: doc-sync pre-push — aligned `assistant/spec.md` v4 morning prompt contract (numeric chakra labels, not long_explanation ban); documented server `chakraText.ts` in `chakra/{spec,dependencies,history}.md` and paired `assistant/dependencies.md`, `daily_forecast/dependencies.md`.
- 2026-06-26: `assistant` / `daily_forecast` / `infra` — LLM fallback policy split by workload: interactive recommendation-generation now goes straight from the requested tier model to `AI_MODEL_FALLBACK`, while cron/precompute for personal morning cache and free global content retries the exact primary model 3 times with 60-second pauses before one fallback attempt. Synced `assistant/{spec,history}.md`, `daily_forecast/history.md`, `infra/{spec,history}.md`.
- 2026-06-26: `daily_forecast` / `i18n` / `assistant` / `astro` — astrology Home follow-up: free-tier upsell copy updated, recommendation texts normalize chakra names to numeric labels, free math/chart labels are fully locale-aware (including direct DB fallback after `global-content` timeout), and the `Подробнее → Расчёты и формулы` transition no longer animates the previous modal downward first. Synced `daily_forecast/{spec,history}.md`, `i18n/{spec,history}.md`, `assistant/history.md`, `astro/interpretation_layers.md`.
- 2026-06-25: doc-sync pre-push — `communicator/spec.md`, `daily_forecast/{spec,dependencies,history}.md`, `communicator/history.md`, `assistant/spec.md`, `MAP.md`: tariff-scoped Home «Обновить» via `POST /api/ai/dev-day-reset`; morning prompts v4 refs in assistant spec.
- 2026-06-25: `daily_forecast` / `assistant` — Home test «Обновить» uses tariff-scoped dev reset (`global` vs `personal`); modal subtitles use standard `screenHint` typography; morning prompts v4 fix primary-chakra practice hint and ban chakras in long_explanation.
- 2026-06-25: `daily_forecast` / `assistant` / `astro` — free Home astrology flow now uses a deeper premium-backed global prompt, requires a complete precomputed `mathLevel`, opens a transit-only chart for free users, and unifies explainer modal closing to `Закрыть → Home`; aspect colors were normalized to green (harmonic) / blue (challenging). Synced `daily_forecast/{spec,dependencies,history}.md`, `assistant/{spec,dependencies,history}.md`, `astro/{history,interpretation_layers}.md`.
- 2026-06-24: `daily_forecast` — Day tab header shows the localized current date in `overdue_summary` and empty states instead of the static tab label. Synced `daily_forecast/history.md`.
- 2026-06-24: `ui` / `daily_forecast` / `profile` / `practices` — tab screens: убрана двойная нижняя inset-логика (`SafeAreaView` bottom edge + `useBottomTabBarHeight` padding), из-за которой над tab bar оставалась серая полоска; `TabScreenLayout` теперь inset-ит только top/left/right, а `useTabContentBottomPadding` добавляет лишь небольшой gap. Synced `daily_forecast/history.md`, `profile/history.md`, `ui_kit.md`.
- 2026-06-24: `ui` / `practices` / `profile` / `daily_forecast` / `biofeedback` / `calibration` — second-wave UI ownership: added shared non-tab shells (`Stack/Modal/Form/Hero`), `AssistantModalShell`, section/header primitives, fullscreen modal scaffold, and initial immersive chrome primitives (`FloatingCloseButton`, `useImmersiveOverlayAutohide`, `PracticeStopConfirmDialog`). Synced `docs/03_rules/ui_kit.md` plus affected module `history.md` files.
- 2026-06-24: `practices` — yoga catalog now seeds the Practices tab from a per-locale local snapshot and keeps cached cards visible during slow/error Supabase revalidation; catalog errors are serialized into readable text instead of `[object Object]`. Synced `practices/{spec,history}.md`.
- 2026-06-24: `practices` — yoga titles now go through one shared locale-aware formatter (`resolveYogaPracticeTitle`) in both catalog and asana screen, and the Practices list bottom padding now follows the real tab-bar height to keep cards out from under the lower navigation. Synced `practices/{spec,history}.md`.
- 2026-06-24: `daily_forecast` / `profile` / `practices` — scrollable tab screens now share `useTabContentBottomPadding(...)`: Home, Day, Profile, and Practices keep their last cards and scroll indicators above the real tab-bar height instead of relying on fixed bottom padding. Synced `daily_forecast/history.md`, `profile/history.md`.
- 2026-06-24: `ui` / `daily_forecast` / `profile` / `practices` — introduced shared tab-screen primitives (`TabScreenLayout`, `TabScrollView`, `ScreenHeader`, `SurfaceCardView`, `StateCard`) and migrated Home/Day/Profile/Practices to the common shell path; filled `docs/03_rules/ui_kit.md` so future tab UI tweaks go through `modules/ui/` first.
- 2026-06-23: `daily_forecast` / `charts` / `i18n` — `ChakraFlower` center caption now uses home **`planetLabels[planetOfTheDay]`** instead of charts **`strengthLabel`**; enlarged center ring (68/82 design px). Synced `daily_forecast/{spec,dependencies}.md`, `charts/{spec,dependencies,history}.md`, `i18n/{spec,dependencies,history}.md`, `chakra/dependencies.md`.
- 2026-06-23: `i18n` — Day tab: `actionsTitle` / `yogaTitle` renamed to Psycho-practices and Yoga-practices (8 locales). Synced `i18n/history.md`.
- 2026-06-23: `daily_forecast` — уточнён free-path transport (отдельный **8s** direct fallback после **25s** route timeout), paid-home reuse complete server LLM fields без monologue rerun, partial-success alert после natal save, Profile locale без blocking Home reload. Synced `daily_forecast/{spec,dependencies,history}.md`.
- 2026-06-23: `daily_forecast` / `assistant` — personal daily cron now treats users inactive after 3 days instead of 14 and pre-warms `scenario_cache` for `morning_recommendation` (locale-aware slogan/short/long/math). Home paid-path no longer forces `morning_recommendation` refresh on every ordinary load, so warmed cache is actually reused. Synced `daily_forecast/{spec,dependencies,history}.md`, `assistant/dependencies.md`, `MAP.md`.
- 2026-06-23: `daily_forecast` — free-path timeout fix: `fetchGlobalContent` now really falls back to direct `global_daily_content` read after `global-content` HTTP timeout (matching the documented contract), and Home no longer re-labels a dev-forced `Free` session back to premium after a successful global fetch. Added regression test `services/globalContentClient.test.ts`; synced `daily_forecast/history.md`.
- 2026-06-23: `astro` / `daily_forecast` — natal save-path on Home now times out after 30s instead of hanging indefinitely, and warm natal cache is keyed against current `users.birth_*` fingerprint so Home no longer mixes a new day forecast with a stale local natal card after birth-date changes/reopen. Synced `astro/{spec,history}.md`.
- 2026-06-22: `daily_forecast` — смена языка на Home больше не делает hard reload дня: locale-refresh сохраняет текущий structural forecast при timeout/сети и откатывается к локализованным fallback-текстам вместо красной ошибки. Synced `daily_forecast/{spec,history}.md`.
- 2026-06-22: `daily_forecast` / `i18n` — doc-sync: `ChakraFlower` canvas 214×214 in spec; dependencies — `accessMode`, `captionFree`/`captionPersonal`; home typed overlays `planetLabels` + caption keys. Synced `daily_forecast/{spec,dependencies}.md`, `i18n/{dependencies,history}.md`.
- 2026-06-22: doc-sync pre-push — synced `profile/spec.md`, `communicator/{spec,history}.md`, `practices/{spec,history}.md` with push bundle (report retry UI, practice handoff cover + 200 ms dismiss, mic tap-or-hold, `practice-by-chakra` calendar window already documented).
- 2026-06-22: `profile` / `services` — отчёты профиля: retry снова работает для `AppUserError` network; inline-ошибки локализованы (не `[profile-reports] transient network`); кнопка «Повторить».
- 2026-06-22: `ui` — единый токен `SURFACE_CARD` (padding 18, gap 14) для карточек Day/Profile; интервал между действиями на Day ещё −20%.
- 2026-06-22: `communicator` / `practices` — assistant practice launch: handoff cover hides chat before Modal dismiss; Day tab uses same delayed dismiss as Home; caption subtitles → `screenHint` on Home/Profile cards.
- 2026-06-21: `profile` / `charts` — practice-by-chakra now uses the user's local calendar-day window instead of sliding `N*24h`, and `calcBalance` now uses a stricter normalized-distance curve so visibly uneven donuts no longer read like near-perfect `96–97%`. Synced `profile/{spec,history}.md`, `charts/{spec,history}.md`.
- 2026-06-21: `charts` / `profile` / `daily_forecast` — donut bugfix doc-sync: single-segment full ring no longer disappears; all report donuts use viewport reveal; period reload can clear visualization via `hideVisualization`. Synced `charts/{spec,history}.md`, `profile/{spec,history}.md`, `daily_forecast/history.md`.
- 2026-06-21: `profile` / `charts` — doc-sync push: `PracticeByChakraReportCard` stable donut + frozen `animationKey` during period reload; `DonutVisibilityProvider` optional `scrollRef`. Synced `profile/{spec,history}.md`, `charts/spec.md`.
- 2026-06-22: `daily_forecast` / `i18n` — home `ChakraFlower`: локализованные `planetLabels` в overlays de/fr/it/es/pt/nl; canvas 214px, легенда вправо. (Ранее: «Архетипы дня», подзаголовки free/personal, центр силы для free/paid.)
- 2026-06-21: `daily_forecast` / `charts` / `i18n` — `ChakraFlower`: prop `natalProfile`, центр с `S_initial` + `strengthLabel`; цвета `CHAKRA_SEGMENT_COLORS`; `getChartStrings` расширен `strengthLabel`. Synced `daily_forecast/{spec,dependencies,history}.md`, `charts/{spec,dependencies,history}.md`, `i18n/{spec,dependencies}.md`, `chakra/dependencies.md`, `MAP.md`.
- 2026-06-21: `charts` — `revealMode` immediate/inViewport, interval-based animation, remove static complete fallback; fix period-switch key during loading. Synced `charts/{spec,history}.md`.
- 2026-06-21: `charts` — donut reveal on viewport entry (layout settle, min visible px), tab-focus re-animate via `revealSession`; stable animation key during chakra period reload (7d fix). Synced `charts/{spec,history}.md`.
- 2026-06-21: `charts` — doc-sync: `useDonutVisibilityTrigger` options object + progress-aware poll (250 ms / 1.8 s stuck / 5 s ultimate), `useDonutAnimation.complete()` + 2.4 s reveal fallback, center typography and `textMuted` balance stroke. Synced `charts/{spec,dependencies}.md`.
- 2026-06-21: `charts` — donut viewport animation: RAF via `useDonutAnimation` (replaced reanimated), expanded `useDonutScrollProps` / visibility poll; profile tab mirrors Day focus refresh. Synced `charts/{spec,dependencies}.md`, `profile/dependencies.md`.
- 2026-06-21: `i18n` / `charts` — doc-sync for push bundle: typed-module gate entry `charts` (`balanceLabel` overlays), consumer contract in `i18n/dependencies.md`; `MAP.md` charts entry points include `app/(tabs)/profile.tsx` and `modules/charts/i18n/charts.ts`.
- 2026-06-20: `assistant` / `communicator` / `practices` — doc-sync: planning identity dedupe (`dedupePlanningMarkersByIdentity`, cross-conversation match, `1ora`/study stems) and assistant overlay dismiss min-delay 700 ms + `useFocusEffect`. Synced `assistant/spec.md`, `communicator/spec.md`, `practices/spec.md`.
- 2026-06-20: `charts` (new) — unified balance donut (`modules/charts/`): MAD balance calc, animated segments + balance arc, legend on the right; adopted on Day tab life spheres and all three profile donut reports. Synced `charts/{spec,dependencies,history}.md`, `daily_forecast/history.md`, `profile/{spec,history}.md`, `MAP.md`.
- 2026-06-20: `assistant` / `communicator` / `practices` — doc-sync for push bundle: planning desc-blob filter (`filterPlanningDescBlobMarkers`), `applyPracticeCardOverridesToPayload` launch sync, assistant overlay dismiss via mount signal + Home `Modal.visible`/`animationType`. Synced `assistant/{spec,dependencies}.md`, `communicator/{spec,dependencies}.md`, `practices/{spec,dependencies}.md`.
- 2026-06-19: `assistant` — stronger cross-message planned-action dedupe (`samePlannedEventIdentity` study/lang stems + `1ora`; batch dedupe + cross-conversation identity upsert in add-flow). Synced `assistant/history.md`.
- 2026-06-19: `communicator` / `practices` — assistant practice launch: close Home dialog only after practice route is ready + min delay (no Home flash). Synced `communicator/history.md`, `practices/history.md`.
- 2026-06-19: `assistant` — incremental planning blob markers filtered when shorter siblings exist; `practice_picked.launch.params` now match card overrides (`durationMin`/`chakraIndex`). Synced `assistant/history.md`, `practices/history.md`.
- 2026-06-19: `communicator` / `practices` — assistant practice launch from Home no longer flashes the Home tab: overlay dismiss waits for the practice route mount signal (`assistantPracticeOverlayDismiss.ts`), and Home modal closes with `animationType="none"`. Synced `communicator/{spec,history}.md`, `practices/history.md`.
- 2026-06-18: `assistant` — doc-sync for planning closure refactor: `planningDonePhrases.ts` shared util, `filterPersistablePlanningMarkers` / `collectPlanningBranchDialogHistory` contract points, inference timeline input fix. Synced `assistant/{spec,dependencies}.md`, `MAP.md`.
- 2026-06-18: `daily_forecast` / `profile` — Day tab persisted cache (`services/dayPlanCache.ts`), instant restore on focus, bearer via `getSupabaseAccessSession` + `rememberSupabaseSession` in `AuthProvider`. Synced `daily_forecast/{spec,dependencies}.md`, `profile/{spec,dependencies,history}.md`, `MAP.md`.
- 2026-06-19: `assistant` — IT planning QA (`текст-471D-939E-C4-0`): closure replies to “anything else to add?” no longer persist as planned actions; context-first `planningDonePhrases.ts` + closure-echo filter in `filterPersistablePlanningMarkers`, history-aware inference skip, and no pending closure text on finalize. Synced `assistant/{spec,history}.md`.
- 2026-06-19: `assistant` / `i18n` — follow-up to the IT regression bundle: locale-sensitive summary/practice guards were generalized to all 8 supported locales (RU/EN/DE/FR/IT/ES/PT/NL), scaffold clarifiers no longer ask for “one or two words” in non-RU locales, and the always-on i18n rule now explicitly requires cross-locale verification for locale-sensitive bug fixes. Synced `assistant/{spec,history}.md`, `dialog_scaffold/*`, `.cursor/rules/i18n.mdc`.
- 2026-06-18: `assistant` — IT add-flow now closes on `Niente più` and similar close-answers; add persistence no longer duplicates reworded actions when the model bumps `display_order` or renames the verb (`Corsa di 3 km` → `Correre 3 km`), and add-flow finalize deletes stale rows from the same conversation. Synced `assistant/{spec,history}.md`.
- 2026-06-18: `assistant` — Day-tab add-flow finalization is now context-aware: replies like `No, non aggiungi più niente` / `Sì, possiamo finire` close the plan even as answers to the assistant’s own close-question; if the model still stays in gathering mode, the server does one hidden finalize retry, and add-flow persistence now updates the same conversation row by `display_order` instead of duplicating reworded actions. Synced `assistant/{spec,history}.md`.
- 2026-06-18: `assistant` — add-flow now treats Italian replies like `Non voglio affrontare niente` as a real “done adding” signal, so Day-tab add dialogs close and persist per-action recommendations; the LLM add-opening is also forced to acknowledge that the day already has planned actions before asking what else to add. Synced `assistant/{spec,history}.md`.
- 2026-06-18: `assistant` — Day-tab add-flow finals now survive cross-locale done-phrases and English `Recommendation:` leaks inside non-EN dialogs, so add-flow closes with `shouldClose` and persists per-action `recommendation_text`; summary health prompt now formats sleep as hours+minutes. Synced `assistant/{spec,history}.md`.
- 2026-06-18: `assistant` — summarizing mixed-event replies now use one hidden buffered LLM repair retry (`summaryTurnRepair.ts`) instead of server-side visible rewrites; kept cross-locale thin-answer detection. Synced `assistant/{spec,history}.md`.
- 2026-06-18: `practices` — lighter yoga catalog Supabase query (no heavy `params`), 30s deferred timeout, locale-native empty states. `daily_forecast` — Day tab refresh no longer blocked while assistant is open on first load; `/api/day` client timeout 45s.
- 2026-06-18: `assistant` — doc-sync: shared `_utils/sphereHint.ts` (Day-tab `sphereHint` + dialog add-flow `addFlowSphereBalanceLens`); removed stray PLANNING § line. Synced `assistant/{spec,dependencies}.md`, `MAP.md`.
- 2026-06-18: `assistant/spec.md` — doc-sync hygiene: removed stray commit-message line accidentally left in PLANNING § after Day-tab add-flow opening update.
- 2026-06-18: `assistant` / `i18n` / `communicator` — cross-locale planning-marker merge by `display_order` when inference locale ≠ response locale; `userSignalsPlanningDone` word-boundary matching; `getTranscribeLocale()` always returns active locale; exported `resolveVoiceTurnLocales`. Synced `assistant/{spec,history}.md`, `i18n/spec.md`, `communicator/spec.md`.
- 2026-06-18: `assistant` — Day-tab add-flow opening is LLM-driven again with shared sphere-balance lens (`sphereHint.ts`) instead of fixed `planningAddOpening`. Synced `assistant/{spec,history}.md`.
- 2026-06-18: `assistant` — Day-tab add-flow opening: `existingActionCount` now reads `triggerMeta.dayActions` (not overdue due-events); deterministic layer-C `planningAddOpening` for the first turn when the day already has actions. Synced `assistant/{spec,history}.md`, `dialog_scaffold/*`.
- 2026-06-18: `i18n` / `communicator` / `assistant` — inverted voice locale routing by product request: outside `EXPO_PUBLIC_I18N_TEST_MODE` replies stay on the selected/profile locale, while test mode becomes the speech-driven QA path (Whisper auto-detect → per-turn reply locale override). Synced `i18n/{spec,dependencies,history}.md`, `communicator/{spec,dependencies,history}.md`, `assistant/{spec,dependencies,history}.md`.
- 2026-06-17: `communicator` — `buildClientTurnHistory` now forwards assistant `branches`/`dialog_branches` in `turnHistory` meta (alongside `practicePicked`) so server planning-backstop can rebuild multi-message plans under lean storage. Synced `communicator/{spec,dependencies,history}.md`, `assistant/dependencies.md`.
- 2026-06-18: `assistant` — planning persistence now rebuilds a canonical action list from the whole planning branch (`turnHistory` + current turn), not only from the last user message or final invisible markers; the server upserts that accumulated list on every planning turn, fills deterministic spheres for fallback actions, and excludes `dismissed` rows so cancelled actions do not resurrect in Day. Synced `assistant/{spec,history}.md`.
- 2026-06-18: `assistant` — planning persistence no longer relies solely on invisible model markers: `route.ts` falls back to inferring actions from the current user message, and main-final orphan cleanup now runs only for explicit marker-backed finals, so a dropped item in the model wrap-up no longer erases already gathered Day actions. Synced `assistant/{spec,history}.md`.
- 2026-06-17: `assistant` — cross-locale salvage of planning markers from visible finalize: `extractPlanningMarkersFromVisibleFinalize` parses locale-native scaffold labels (8 locales, `1.`/`1)`, FR colon spacing, optional markdown-bold); `route.ts` reuses `resolvedPlanningMarkers` for visible-final assembly. Synced `assistant/{spec,history}.md`.
- 2026-06-18: `daily_forecast` / `i18n` — free-tier Home timeout for non-RU locales: `global-content` no longer blocks on on-demand `text_i18n` pretranslate; client SDK fallback for any locale + 25s timeout. Synced `daily_forecast/{spec,history}.md`, `i18n/{spec,history}.md`.
- 2026-06-17: `assistant` — Day-tab add-flow no longer auto-closes on the first added action; final visible numbered list now backfills missing `planned_events.recommendation_text`, fixing action recommendations on `/api/day`. Synced `assistant/{spec,history}.md`.
- 2026-06-16: `assistant` — planning finalize post-assembly day-focus backstop: when `[CORRECT_RECOMMENDATION]` is missing and `recommendationCorrected` stays empty after `buildPlanningFinalVisibleText`, `route.ts` re-salvages day focus from assembled `cleanText`, persists via `persistDayFocus`, and rebuilds visible-final; deterministic action labels use locale-native `recommendationLabel` (FR tests). Synced `assistant/{spec,history}.md`.
- 2026-06-16: doc-sync pre-push — i18n QA pass: Whisper optional language + 8 aliases; `/api/day` locale-native sphere/hint/practice titles; practiceCardSummary generic fallbacks (8 locales); practiceSelection localized names; communicator transcriptionReviewHint + empty-reply strings. Synced `assistant/spec.md`, `practices/{spec,history,dependencies}.md`, `communicator/spec.md`, `daily_forecast/{spec,history}.md`, `i18n/spec.md`, `infra/{spec,history}.md`, `open_questions.md`.
- 2026-06-16: communicator/i18n locale routing — dialog POST carries `inputLocale` + `responseLocale`; test mode keeps STT on RU and replies on profile locale (`inputLanguageDecouplingInstruction`); production voice may auto-detect spoken language and override dialog turn locales only; function-valued communicator strings and deterministic Day/practice copy localized for FR/DE. Synced `i18n/{spec,dependencies,history}.md`, `communicator/{spec,dependencies,history}.md`, `assistant/{spec,history}.md`, `open_questions.md`.
- 2026-06-16: i18n test mode — dialog POST carries `inputLocale` (`getTranscribeLocale()`); server prompt decouples input language from reply locale when they differ (fixes RU speech + FR UI → RU reply). Client passes explicit `responseLocale`/`inputLocale` from `Communicator`.
- 2026-06-16: doc-sync pre-push — practices catalog i18n gaps: typed extractor (quoted/hyphenated keys, multiline values), `practiceCountOne`/`practiceCountWithTotal`/`catalogFooterTemplate`, `chakraTagLabel` overlay for de–nl; breath/practices overlay refill. `i18n/spec.md` §4.3, `practices/{spec,dependencies,history}.md`, `chakra/{spec,history}.md`.
- 2026-06-16: i18n — fix home recommendation buttons on DE/FR (typed extractor + `recommendation.*` overlay paths); localize natal/transit chart (`astroChartModal`); refilled catalog + home typed overlays.
- 2026-06-16: doc-sync pre-push — startup overlay → i18n catalog (`startup.step.*`); typed overlay flat-path merge + `rebuild-typed-overlays` gate; practices duration units + yoga jsonb locale lookup. `i18n/{spec,dependencies,history}.md`, `practices/{spec,dependencies,history}.md`.
- 2026-06-16: doc-sync pre-push — i18n locale plumbing: `i18n/spec.md` (`inlineBaseLocale`, `intlLocaleTag`, `mathLevelI18nTargets`, §8); `i18n/dependencies.md` (`UpgradeDialog`, tab remount, life-spheres); `subscription/` (UpgradeDialog → catalog); `chakra/spec.md`, `profile/dependencies.md`; `MAP.md` (subscription ↔ i18n).
- 2026-06-16: i18n — **locale plumbing fix:** load all 8 JSON catalogs in `t.ts`; pass full `appLocale` to typed string getters (was `en ? en : ru`); fix nested chakra overlay flatten; life-sphere titles for 8 locales; `UpgradeDialog` catalog strings + new assistant body; `mathLevelI18n` for de/fr/it/es/pt/nl.
- 2026-06-15: doc-sync pre-push — layer C dialog scaffold (8 locales): `i18n/spec.md` §3/§6/§8, `assistant/spec.md` §2/§3/§4, `dependencies.md` обоих модулей, `history.md`, `open_questions.md`, `i18n_architecture.md` — выровнены с `dialogScaffold/`, `contentLocales.ts`, `i18n-sync.mjs`.
- 2026-06-15: i18n — **Layer C RU-first sync:** dialog scaffold joins `i18n-sync.mjs` + pre-push hook (`dialog_scaffold/ru.json` → auto-fill 7 targets).
- 2026-06-15: i18n — **Phase 3 layer C:** dialog deterministic scaffold localized for all 8 locales (`dialog_scaffold` catalogs).
- 2026-06-15: i18n + daily_forecast — **8 locales enabled** (catalog + typed overlays filled); home locale switch refreshes slogan/recommendation/math via locale-scoped cache + `outputLanguagePrompt` on monologue + `outputLocale` cache invalidation; `homeTextsLoading` UX.
- 2026-06-14: doc-sync pre-push — locale-aware home day content: `daily_forecast/` (`stripHomeLlmTexts`, `localeChange`, `subscribeAppLocale`, scopeKey+locale, monologue force-refresh, global-content SDK fallback ru-only); `i18n/` (on-demand `text_i18n` precompute, `i18n-sync` DeepSeek fallback).
- 2026-06-14: doc-sync pre-push — i18n Phase 2 follow-up: `chakra/` (multilingual `i18n.ts`, `getPlanetChakraMap`), `daily_forecast/` (`responseLocale`, `text_i18n`, locale-aware home), `practices/` (`loadYogaPractices(locale)`, `getPracticeCatalogStrings`), `profile/` (report locale, life-sphere labels, Luxon axis); `i18n/spec.md` §6 выровнен с `resolveContentLocale` (8 locales layer B).
- 2026-06-14: i18n — **Phase 2 completion: users.locale write-back, 8-locale layer B, typed sync gate, global text_i18n.** Клиент: `syncUserLocaleToServer` при смене языка. Сервер: `contentLocales.ts` (`resolveContentLocale` / `resolveDialogScaffoldLocale`); free-tier `global_daily_content.text_i18n` + pretranslate на upsert/cron. Гейт: typed overlays (`modules/i18n/typed/`, `fill --all` для de–nl). Spec/deps/history §5.
- 2026-06-14: i18n — **UI sweep + layer B locale plumbing.** Клиент: sentence case в отчётах Profile, единый источник подписей состояний (`chakraShortLabelDisplay`), free-tier banner и math modal в `home.ts`, кнопка «What to do?», «Add action», PeriodSelector в Practice statistics. Сервер: `responseLocale` в monologue/global-content, кэш morning_recommendation по локали, EN-перевод free-tier текстов, math markdown RU/EN. Spec §8 — инвентарь слоёв A/B/C и чеклист нового языка.
- 2026-06-14: i18n — **новый модуль документации + завершение конечной части Phase 2**. Создан триадный модуль `docs/02_modules/i18n/` (`spec`/`dependencies`/`history`), зарегистрирован в `MAP.md` (Engines & Services), добавлено always-on правило `.cursor/rules/i18n.mdc` (мультиязычность = инвариант системы: где задача касается UI-строк/языка ответа/дат/множественного числа/нового экрана — реализовывать через `@/modules/i18n`, без хардкода RU). Конечная проводка: Home (`index.tsx`), Профиль (chrome перенесён в JSON-каталог + локаль проброшена в report-карточки) и точки входа ассистента День/Дыхание теперь следуют **общему стору локали**, а не `profile.locale`/хардкоду `"ru"` — переключатель языка в Профиле переключает RU/EN по всему приложению. Зафиксирована двухуровневая стратегия строк (JSON-каталог для новых строк; типизированные `get*Strings` остаются) и точный незакрытый хвост (`practices/core/catalog.ts` названия дыхательных практик; стартовый оверлей; dev-диагностика). Документировано добавление нового языка (spec §6) и гейт синхронизации.
- 2026-06-14: i18n — **Phase 2 framework + plumbing** (вариант A). Новый клиентский слой `modules/i18n/`: `localeStore.ts` (единый источник истины активной локали, persist через expo-secure-store/web-localStorage, sync-геттеры `getResponseLocale`/`getTranscribeLocale` для сервисов, `APP_LOCALE_OPTIONS` с флагом `enabled` — RU/EN включены, 6 языков ждут наполнения, `I18N_TEST_MODE` из `EXPO_PUBLIC_I18N_TEST_MODE`), `t.ts` (`t`/`tCount` с множественным числом через `Intl.PluralRules` — язык-полно: RU one/few/many; JSON-каталоги `catalog/{ru,en}.json`, fallback requested→en→ru→key), хуки `useAppLocale`/`useTranslate`. Профиль получил **переключатель языка** (пишет в стор, persist; в тест-режиме — подсказка «говорю по-русски, отвечает на выбранном»). Проводка: dialog POST теперь несёт `responseLocale` (`getResponseLocale()`); серверный приоритет — ENV-override → body → `users.locale` → `ru` (`resolveResponseLocale(userLocale, requestedLocale)`); транскрипция через `getTranscribeLocale()` (в тест-режиме остаётся `ru`). Экраны `Day`/`Breath`/Profile-reports сняты с хардкода `locale="ru"` на общую локаль; ярлыки табов — через каталог. **Гейт синхронизации переводов** `scripts/i18n-sync.mjs` (`check`/`fill`, diff-based, мета `catalog/.sync-meta.json`) + pre-push обёртка `scripts/i18n-sync.sh` (ставится `docs-sync/install.sh`, не блокирует push). Дизайн/слои A/B/C/фазы — `docs/04_workspace/i18n_architecture.md`; `scripts/README.md`.
- 2026-06-14: `assistant` / i18n — **Phase 1 response-locale foundation**. Новый `_utils/dialogLocale.ts` (`resolveResponseLocale` + `localeToLanguageName`) разделяет язык ввода (STT) и язык ответа ассистента. ENV-флаг `DIALOG_RESPONSE_LOCALE` форсит язык ответа (тест-режим RU in → EN out); при незаданном флаге поведение идентично прежнему (нулевая регрессия). Проброшено в `dialog/route.ts` (контекст промпта, life-spheres baseline, sanitize, post-processing) и `greeting/route.ts`. Полностью поддержаны RU/EN; DE/FR/IT/ES/PT/NL — после локализации детерминированных visible-text builders. Дизайн/фазы — `docs/04_workspace/i18n_architecture.md`.
- 2026-06-14: `assistant` — **отмена запланированного действия через диалог** (light, не афишируется в UI). Новый невидимый маркер `[CANCEL_EVENT: ref="…"]`: парсится в `_utils/markers.ts` (`cancelEvents`) и вырезается из видимого текста; новый `dismissPlannedEvents` в `dialogBrainPersistence.ts` fuzzy-сопоставляет ref с открытыми (`status='planned'`) событиями дня и ставит `status='dismissed'` (колонка уже есть, миграция не нужна) — подытоженные события не трогаются, `/api/day` сразу перестаёт их показывать; `route.ts` обрабатывает маркер в начале planning-ветки (и в add-flow, и в обычном планировании). Promt `buildPlanningPrompt`: эмитить маркер только по явной просьбе пользователя убрать/отменить действие.
- 2026-06-14: `assistant` — QA fixes (выгрузки `текст-4E95…`, `текст-415D…`, `текст-40B6…`). (1) **Единая LLM-модель в диалоге**: весь диалог «Ассистента» (все ходы `dialog/route.ts` + opening из `greeting/route.ts`) переведён с `premium` на **`standard`** (новая константа `DIALOG_MODEL_TIER`) — единообразие держит prefix-cache DeepSeek v4 тёплым. Главная (астро-интерпретация, день-рекомендация, текст «Подробнее» в `recommendation-text/route.ts`, `ai/monologue`, `morningRecommendation`, `ensureGlobalDailyContent`) НЕ тронута — у неё свой tier. Исправлена опечатка `AI_MODEL_PREMIUM=ddeepseek-v4-flash` в корневом `.env.local`. (2) **Сферы жизни в «Дне» (баг 4-й сферы)**: причина — (a) salvage `extractPlanningMarkersFromVisibleFinalize` хардкодил `cells=[{4,0.5},{1,0.5}]` (sphere 4 = «друзья/семья») для ЛЮБОГО действия; (b) planning-промпт давал пример `spheres="1:0.6;4:0.4"`, который Flash дословно копировал. Фикс: salvage теперь инферит сферы из текста (keyword RU/EN, fallback sphere 1, **никогда** не sphere 4 по умолчанию); planning-промпт получил блок LIFE SPHERES + domain-карту (дом/уборка→1, отдых/прогулка/велосипед→2, работа/деньги→3, люди/отношения→4, творчество→5, обучение→6, вера→7) + явный анти-копи и формат-only пример. (3) **Обрезка названия действия** («убрать терр»): salvage `desc.slice(0,40)` (резал по середине слова) → `clampPlanningDesc` (по границе слова, ≤60, без многоточия). (4) **Заглавная буква рекомендации**: `polishPlanningRecommendation` теперь капитализирует первую букву (через `capitalizeFirstLetter`) — текст рекомендации к действию в «Дне» больше не со строчной. (5) **Add-flow первое сообщение**: `PlanningTurnInput.existingActionCount` (=число уже запланированных действий дня); если день уже спланирован, opening «Добавить» признаёт это и спрашивает, что добавить (варьируя формулировку), а не планирует с нуля. (6) **Summarizing**: новое CRITICAL-правило «уточняющий вопрос XOR закрытие в одном ходе» (нельзя в одном сообщении и уточнять текущее событие, и спрашивать про следующее → конец смешению ответов); «DEFAULT TO CLOSING» (не допытываться, если состояние уже понятно; не расщеплять близкие чакры); bridge к следующему событию — короткая связка без пересказа/оценки закрытого; запрос формулировать «как говорят носители» («удалось ли почитать книгу», не «устроить чтение книги»). (7) Преамбула: инструкция писать «как носитель языка» сделана языко-независимой (по `languageName`).
- 2026-06-14: doc-sync pre-push — `assistant/spec.md` §SUMMARIZING выровнен с кодом (расширенный `userSaysEventDidNotHappen`, закрытие без `summaryAskedCount<1`, prompt «ничего особенного»); §PRACTICE — `userDeclinesPractice` без `\b` для кириллицы; таблица `dialogBranchPrompts.ts` — `capitalizeFirstLetter`/`polishPlanningMarker`.
- 2026-06-14: `assistant` — QA fixes (выгрузки `текст-484D…`, `текст-4338…`, `текст-4D75…`, `текст-46F2…`). (1) **Несостоявшиеся события** в summarizing: `userSaysEventDidNotHappen` расширен с «голого нет» до выверенного набора формулировок («не почитал», «не пошёл», «не успел», «не получилось», «не до того», «не было времени», «забыл», «перенесли», EN-эквиваленты) — но без «не было» в одиночку, чтобы не ловить «ничего особенного не было». Условие закрытия в `route.ts` больше не требует `summaryAskedCount<1`: при сигнале «не было» событие закрывается сразу, без уточняющего вопроса и без записи в матрицу. (2) **Дубли действий**: `persistPlanningFinalize` на основном финале (не add-flow) удаляет «осиротевшие» `planned`-строки этой беседы, которые модель переформулировала между инкрементальными сохранениями (fuzzy-identity их не сопоставил) — «Работа над результатами» + «Поработать для результатов» больше не двоятся в «Дне». (3) **Закрытие после отказа от практики**: `userDeclinesPractice` в `route.ts` больше не использует `\b` (не работает с кириллицей) — «Нет, ничего не надо сохранять» теперь закрывает диалог сразу; bare-«нет» считается отказом только если в реплике нет запроса практики (тип/длительность). (4) **Заглавная буква действия**: `polishPlanningMarker` приводит первую букву `desc` к верхнему регистру (через `capitalizeFirstLetter`) → действия из «Добавить» больше не отображаются со строчной. (5) Prompt summarizing: не допытываться о состояниях, если событие «ничего особенного / обычные ритуалы».
- 2026-06-13: doc-sync pre-push — `assistant/spec.md` §PLANNING (gathering invite cap ~2×, shared-occasion event segmentation); `communicator/spec.md` (`onMessage` Day pre-warm); `daily_forecast/spec.md` (Home `router.push("/day")` до dismiss + planning-final prefetch).
- 2026-06-14: `assistant`/`communicator` — QA planning из Home. Подтверждено: persistence действий + день-рекомендация + порядок чинятся деплоем (пост-деплой выгрузка корректна). Planning-промпт: мягкий **cap на «есть ли ещё что добавить?»** (максимум ~дважды, потом предлагать собрать план, без бесконечного чек-листа) + правило сегментации «один совместный выход = одно событие» (встретиться с кем-то, чтобы сделать что-то вместе). Home: закрытие диалога открывает «День» **мгновенно** (`router.push("/day")` до dismiss модалки, без ожидания `loadDayPlan` в `.finally`), а контент «Дня» **прогревается** во время финала planning через новый `onMessage`-хендлер (`loadDayPlan`→`storePrefetchedDayPlan`).
- 2026-06-14: `assistant` — planning robustness fix (QA `текст-44D5-9EB7-27-0`): planned actions now persist **incrementally** (`persistPlanningFinalize` runs on every turn a `[PLANNED_EVENT]` appears, idempotent; prompt emits a light `[PLANNED_EVENT]` the moment an action is named), so the plan survives an interrupted dialog like summarizing does. Robust `finalizeIntent` (done-signal / visible numbered finalize / `[CORRECT_RECOMMENDATION]` / add-flow) drives `persistDayFocus` + FSM advance + close. New `extractDayFocusFromVisibleFinalize` salvages the day recommendation from visible text when the marker is missing (so the Day-tab header updates). New `planningFinalizedThisTurn` gate prevents incremental markers from turning a gathering ack into a premature finalize. Broadened `userSignalsPlanningDone` / practice-decline detectors. Fixes: actions not saved, missing day recommendation, dialog not closing (mic staying active) after practice decline.
- 2026-06-13: doc-sync pre-push — `assistant/spec.md` aligned with FSM code (planning short_text=visible, add-flow/decline-planning, summary reflection without «Пo событиям», practice empty-text fallback, GET /api/day expires stale pending offers); `communicator/spec.md` (InteractionManager dismiss, health bootstrap, practiceToSummary duration); `daily_forecast/spec.md` + `astro/caching_strategy.md` (preserve dialog recommendation on forecast recompute; Day tab scroll stability).
- 2026-06-13: `assistant` — more polish: planning-final now explicitly forbids copying the example phrases verbatim / starting every time with «Сегодня» (flash was tiling one example 4/7 → down to 1/7). Saturn (5th chakra) tonal register «творческого выражения» → «творческого самовыражения». Summary-final step-citation example reworded to «вы прошли всего 1240 шагов». Summary-final practice nudge when there were ZERO practices now invites specifically toward the app's yoga/meditation practices (psychological transformation + body vitality), not generic exercise. `chakraExpertLens` depth lens added to the SUMMARIZING final too (uses the summarized day's chakra). `Why: {explain}` (chooseTargetChakra) stripped of numeric matrix internals (shares/thresholds) — now a clean semantic rationale; SYSTEM line split into focus-chakra + rationale + planet-sets-tone (planet may differ from focus chakra, kept intentionally).
- 2026-06-13: `assistant` — planning prompt polish: day-recommendation example reworded toward self-development framing («направьте внимание на N чакру», «наибольшим потенциалом обладает», «открывает наибольшие возможности для вашего развития», «действуйте в потоке N чакры»); added `targetChakraGenitive` to `BrainPromptContext`. `chakraExpertLens` «пусть он звучит так» → «пусть он будет написан так»; tonal registers «Хорошо звучат фразы/темы про…» → «Уместны слова и темы…» (×7). SYSTEM harmonic/dissonant state lines reworded to "use to recommend" / "to gently recommend to avoid". USER event-segmentation rule rewritten via explicit [PLANNED_EVENT] cards, and the FINALIZE-NOW directive rephrased to make clear that markers are what persists the plan to the Day tab.
- 2026-06-13: `assistant` — planning day-recommendation no longer defaults to auditory metaphors for the chakra («звучит/слышна/тишина»): the prompt example that seeded «Сегодня сильнее обычного звучит N чакра…» was replaced with non-auditory invitations plus an explicit anti-cliché note (vary the image; sound/voice imagery fits mainly the throat 5th chakra). Tonal registers (shared preamble) left unchanged.
- 2026-06-13: `assistant` — summarizing "did it happen?" detection reworked to be LLM-first: the model decides non-occurrence from meaning (any language) and closes via `[SUMMARIZE_EVENT … outcome_cells=""]` without a clarifying question; the deterministic `userSaysEventDidNotHappen` is demoted to a minimal language-stable backstop (bare «нет/no/не» only, fired only before the first clarifying question) — the brittle verb/ending whitelists were removed so the EN version needs no separate tuning.
- 2026-06-13: `assistant` — summarizing no longer asks a "what did it stir in you?" clarifying question about an action the user said they did NOT do: `userSaysEventDidNotHappen` now also catches generic negated past-tense verbs («не читал», «не ходил», «не звонил»…, Cyrillic-safe boundary), excluding inner-state verbs (понял/хотел/думал…); the prompt also explicitly forbids state questions for non-events (warm one-line sympathy, close without cells). Planning day-recommendation rephrased to name the chakra by number as a living invitation («Сегодня стоит направить внимание на седьмую чакру…») with a motivational/emotional hook (each day = a unique chance to grow), and shortened to ~150–230 chars. Verified `chakraExpertLens` is wired into the planning final (intentionally subtle, never named).
- 2026-06-12: `assistant`/`communicator` — day-recommendation is now fully LLM-authored: the model names the day's chakra by number itself and writes ONE compact paragraph (~180–300 chars); the visible planning-final intro and the saved Day-tab `short_text` are now the SAME text (`extractPlanningIntro` prefers `short_text` verbatim), ending the length/wording mismatch. Code `prependChakraAttention` is now a safety-net only (fires when the model forgot a chakra mention). Planning gathering only suggests substantial, summarizable actions (no micro-gestures like «записать одну мысль»). Home `Что делать?` opens the dialog instead of jumping to the Day tab when the day has no planned actions (a leftover practice card no longer short-circuits). Practice launch from the communicator dismisses the modal only after the navigation transition settles, removing the Day-tab flash before the practice screen.
- 2026-06-12: `assistant` — fix planning branch silently skipped → no `planned_events` saved: `coerceFsmBeforeTurn` no longer treats the summarizing FINAL message (which mentions past practices and ends with a question) as a practice offer, and the user's first planning reply containing an incidental word like «потом» no longer false-matches a practice decline. Day recommendation (Day-tab header + planning final) now gets a numeric chakra prefix («Внимание на седьмую чакру.») via `prependChakraAttention`. Deterministic `replaceSpontaneousEnglishRu` safety net strips stray English words (task/quietly/mindfully/flow…) from Russian replies. Summarizing clarifying question must not paraphrase the user and is right-sized to the action; failed/недоступные events («не удалось…») are detected and closed without a clarifying question. On a fresh day where planning follows, the summarizing opening signals a look-back in one sentence (no dates). Chakra-expert depth lens stays planning-final only.
- 2026-06-12: `assistant` — planning-final recommendations refined: each recommendation is right-sized to its action's scope (all-day activity → background orientation; brief one-off → one precise small shift), kept concretely doable and logically consistent with the action (no "pick a few pages" of an unopened book), and gently invites the user out of their habitual pattern toward the day's chakra tone; the overall day-focus paragraph gains an unnamed "depth lens" from thinkers associated with the day's target chakra (`chakraExpertLens`, never named/quoted). Per-planet tonal register unchanged.
- 2026-06-12: `assistant` / `communicator` — overdue `Подытожить`→plan QA: planning now persists `planned_events` to the current local day (`context.localDate`), not the overdue summary `workingLocalDate` (fixes new actions silently filed under yesterday and disappearing from the Day tab); summary-final capitalization restored after day-word stripping; prompt bans spontaneous English words in Russian, requires at least one concrete Health number, and asks domain-aware right-sized summary clarifying questions (no fixed «тело/настроение/мысли/отношения» checklist); client `practiceToSummary` bakes the editable duration override into the pending Day-tab practice card (shows chosen 20 min instead of catalog 10 min).
- 2026-06-12: `assistant` / `communicator` — Home overdue summary now passes `workingLocalDate`, Communicator bootstraps native health for that flow (not only Day-tab `daySummaryRequested`), server honors client `workingLocalDate` without the flag and anchors chakra context to the overdue date; `formatHealthForPrompt` lists yoga when practice count > 0 even for 1–2 min sessions.
- 2026-06-12: `assistant` / `communicator` / `daily_forecast` / `practices` — QA round: summary-final day-word sanitizer rewritten with Unicode letter look-arounds (JS `\b` never matched Cyrillic, so «вчера/сегодня/вчерашний» leaked every time); the «По событиям» per-event recap is removed and the model now writes one cohesive psychological reflection; summary→planning hand-off drops the model's own date-tainted question and appends a deterministic «...на текущий день?» bridge; declining to plan now closes the dialog gracefully (motivational note, no practice branch); practice turns can no longer emit empty text (deterministic catalog-aware clarification fallback) — fixes the "Assistant reply was empty after hydration" / Sentry `Model returned empty text after sanitization` crash on 30-min breathing (requires Vercel deploy); daily-forecast recompute preserves a dialog-written day recommendation (birth-date edit no longer wipes the Day header); Day route auto-expires pending practice offers from past local days; Day tab no longer snaps to top on every refresh (scroll-to-top now only on focus return).
- 2026-06-11: `assistant` / `communicator` / `daily_forecast` / `practices` — follow-up QA: summary-final strips leading separators and relative day words; planning-final no longer includes “add more?” questions after markers; practice bubble/card texts are distinct with correct RU chakra case; Home prefetches Day plan before/while navigating from assistant.
- 2026-06-11: `assistant` / `communicator` — follow-up QA Home→Day flow: past-day summarized actions are deleted immediately after matrix persistence while current-day summaries stay visible; summary questions avoid quoted action titles; final summary event blocks are rebuilt from server artifacts; `[PRACTICE_DECLINED]` no longer flashes in partial stream.
- 2026-06-11: `assistant` / `communicator` / `practices` — QA-fix полного Day dialog: planning/practice финалы теперь буферизуются до server-side сборки без visible flicker; sleep quality provider codes нормализуются в русские подсказки; per-action рекомендации сохраняются как вежливые предложения с пунктуацией; practice offer снова спрашивает тип/длительность/отказ; запуск практики сначала навигирует на экран, затем закрывает overlay.
- 2026-06-11: `assistant` / `daily_forecast` — QA-fix Day testing: summary no longer persists an event when the visible turn is still a clarifying question, strict `SUMMARIZE_EVENT.ref` prevents next-event bleed, Day `sphereHint` now appears only for actionable missing-sphere recommendations, and background Day refresh errors no longer replace an already rendered plan with a red retry block.
- 2026-06-11: `assistant` / `communicator` / `daily_forecast` — QA-fix полного Day dialog: overdue `Подытожить` теперь продолжает planning/practice как Home `Что делать?`; summary guard буферизует заменяемые ходы и задаёт вариативные уточнения; planning-final собирается из маркеров в порядке focus/actions/practice; practice reply и card text разведены; запуск практики закрывает модальный коммуникатор до навигации.
- 2026-06-11: `daily_forecast` / `profile` — fix повторного `need_location` на cold start в Expo: async relaxed day-cache read, локальный кэш coords профиля, GPS coords используются даже при сбое persist в Supabase.
- 2026-06-11: `assistant` — dialog time-of-day windows (night greeting 22:00–03:00, day-part rhetoric ban 00:00–03:00); fix `phaseTimeFor` early-morning mislabel; tonal register restored in `sharedPreamble`; planning day-focus synced with Day tab via `injectPlanningDayFocus`.
- 2026-06-11: `communicator` — `summarizingHealthContext` + `Communicator` background health snapshots on every summarizing POST; instant Day/Home summary open.
- 2026-06-11: `infra` — docs-sync workflow rebases onto `main` before push to avoid non-fast-forward after parallel commits.
- 2026-06-11: `assistant` / `communicator` — Day summary: мгновенный старт диалога; health/yoga в фоне на весь summarizing (snapshot на каждый POST); prefetch вкладки при summarized-event; FSM подставляет уточняющий вопрос при thin-ответе вместо ложного закрытия маркером.
- 2026-06-10: `assistant` / `communicator` — восстановлена v7-логика practice-ветки: ответ на practice-offer переводит FSM в `practice` без повторного planning-final, медитации не попадают в `planned_events`, pick-turn показывает только card blurb + карточку, post-practice/«Спасибо» — короткий wind-down; mic/text блокируются при `shouldClose`.
- 2026-06-10: `daily_forecast` — home cold start без coords: GPS last-known + 12s timeout, relaxed day-cache, `locationIssue` + Settings CTA, offline stale fallback вместо пустого экрана.
- 2026-06-10: pre-push doc-sync — выравнивание baseline-данных ассистента (`life_spheres_baseline`, `dto.ts`, `dialogTonalRegisters.ts`, `topPetals.ts`, `chakra_states_baseline.json`); `planetChakra.ts` читает `chakraName` через `chakraLabelRu`. Обновлены **`docs/02_modules/assistant/{spec,history}.md`**, **`docs/02_modules/calibration/history.md`**, **`docs/02_modules/daily_forecast/{spec,dependencies,history}.md`**, **`docs/02_modules/chakra/{spec,dependencies,history}.md`**.
- 2026-06-10: `assistant` / `communicator` / `daily_forecast` — Day tab was rebuilt around three explicit UI modes: `empty_today`, `overdue_summary`, and `current_day`. `/api/day` now returns `mode + sections[]` instead of one flat day payload, overdue past days are rendered as separate sections with a single `Подытожить` CTA, and Home routes to `/day` only for a real current-day plan without an overdue summary tail.
- 2026-06-10: `assistant` / `communicator` — summary persistence was simplified back to one table: the same `planned_events` row now becomes `status='summarized'`, keeps `summarized_at` / `outcome_text` / `outcome_cells`, remains visible on the Day tab until the local day ends, and still appears in JSON debug export with real matrix cells. Old `summarized` rows from previous local dates are auto-deleted on the next day, so no extra summary table or migration is needed.
- 2026-06-09: pre-push doc-sync — `assistant/dependencies.md`, `communicator/spec.md`, `communicator/dependencies.md`: reconcile-plans как no-op, FSM-синхронная персистенция вместо delayed `queued`/`queued_summaries`, `resolvePracticeCard`, SSE `orchestrator_decision` опционален; снят resolved open question про v7-imports.
- 2026-06-09: `assistant` — пост-FSM зачистка: DeepSeek prefix-cache (стабильный `systemInstruction` = `sharedPreamble`, походовые инструкции в финальный `userInstruction`); удалён мёртвый v7-код (`_archive_v7/`, `summaryRepair`, `dueSummaryState`, `planningReconciliation`, `dialogDebugExport` + тесты); `reconcile-plans` route → совместимый no-op; SSE `orchestrator_decision` больше не шлётся; добавлен `dialogFsm.test.ts`; `tsc`+`vitest`(302) зелёные; v7 prompts-reference вынесен в `docs/05_archive/migrated/assistant/`; legacy-описание удалено из активного `spec.md`. БД не менялась (вестигиальны лишь jsonb-ключи и always-null time-колонки `planned_events`).
- 2026-06-09: `assistant` / `communicator` — «мозг» daily dialog переписан на явный FSM (`dialogFsm.ts` + `dialogBranchPrompts.ts` английское ядро + `dialogBrainPersistence.ts` синхронная запись + `dialogPracticeCard.ts`); `route.ts` — тонкий диспетчер (один LLM-вызов/ход, один маркер). Прежний v7-«мозг» (`dialogArcOrchestrator.ts`, `dialogOpeningHints.ts`, мега-промпт `dialog_system_v3`) перенесён в `_legacy_web/app/api/_archive_v7/` и больше не выполняется. URL/SSE-контракт и клиент не менялись; debounce `reconcileDialogPlans` теперь no-op (персистенция синхронна).
- 2026-06-09: pre-push doc-sync — `assistant/prompts-reference.md` §1–§3.1: зеркало `dialog_system_v3` v7, обновлённые `opening`/`inquiry`/`final_recommendation`, numbered chakra labels и planning/summary guardrails; `assistant/spec.md` §4 — v7 как активный prompt.
- 2026-06-09: `infra` / `communicator` — меньше шума в Sentry при штатной перегрузке LLM: warning вместо error, фильтр `failed to pipe response`, graceful SSE `error` event; клиент abort стрима при unmount Communicator.
- 2026-06-09: `assistant` — planning-артефакты daily dialog очищены до sphere-only: `PLANNED_EVENT` теперь предпочитает `spheres="..."`, `planned_events.cells` и `GET /api/day` держат только сферы жизни без chakra-tail, а `daily_matrices` больше не строятся из будущих planned rows. Это убирает лишние planning-токены и не даёт матричной логике принимать решения по ещё не прожитым событиям.
- 2026-06-09: `assistant` / `communicator` — summary больше не закрывается без lived-state по состоявшемуся событию: ассистент обязан потратить один уточняющий ход на состояние для матрицы, а события без `outcome_cells` (не состоялись или так и не дали state) теперь закрываются без вклада в `daily_matrices`. Home daily dialog теперь стартует fresh, не ремонтируется от `forecast.computedAt`, умеет передавать `dayHealthContext`, пишет `latencyMs` в dialog meta/export, а JSON-export явно показывает `summary_events_applied_to_matrix` и `summary_events_closed_without_matrix`.
- 2026-06-09: `assistant` / `communicator` — home planning-flow больше не теряет ветку `planning` после summary-части, planning не собирает лишние время/психологические детали, получил 7-дневный planning-lens по слабо представленным сферам, а planning-final теперь отделён от practice-final. На клиенте Home перестал перетирать запуск практики переходом на `/day` и начал сохранять ассистентскую карточку как pending practice для вкладки `День`.
- 2026-06-09: `assistant` / `practices` — reconcile planning-candidates теперь видит rows, вставленные в этом же проходе, поэтому одинаковый queued event больше не может дважды пройти как `create_new`; при отсутствии model `recommendation` сервер даёт fallback-текст по `day_target_chakra`, а завершение ассистентской медитации возвращает пользователя на `/day`.
- 2026-06-09: `assistant` / `communicator` — Day add-flow now starts from a fresh empty chat, and planning final-turns no longer lose new actions if the model forgets `PLANNED_EVENT`: backend falls back to inferred planning artifacts for delayed persistence into `planned_events`.
- 2026-06-08: `assistant` — follow-up Day dialog fix: явные режимы `dayTabMode = add/plan` больше не сваливаются в summarizing из-за due-событий, summary идёт по одному событию с максимум двумя вопросами на эпизод, а финал summary жёстко закрывается без автоматической practice-card и с просьбой называть конкретные health-цифры.
- 2026-06-08: doc-sync pre-push — `assistant/spec.md`, `communicator/spec.md`, `prompts-reference.md` §3.1 и `MAP.md`: сняты устаревшие 36-часовые auto-expiry/delete для `planned_events`, зафиксированы day-summary guardrails, `canPersistPlanningMarkers` и точки входа вкладки «День».
- 2026-06-08: `assistant` / `communicator` / `daily_forecast` / `practices` — нефинальные ходы daily dialog переведены в дружеский ясный режим без “псевдо-психологии”, day-summary закреплён на одном событии за ход без промежуточного feedback, chakra labels в видимом RU-тексте заменены на нумерованные, а финал без карточки практики получил кнопку `Выйти из диалога` и force reconcile queued summary/planning artifacts перед закрытием.
- 2026-06-08: `assistant` / `communicator` / `practices` — summary-ветка вкладки «День» закреплена для тестирования: `dialog_system_v3` v6 снова активирован в `public.prompts`, `getActivePrompt()` получил fallback на последнюю версию prompt-а, due-summary на Day tab теперь идёт строго по одному событию за ход, а yoga baseline считается по 7 последним дням с мягким ориентиром `~30 мин/день`.
- 2026-06-08: `assistant` / `communicator` / `infra` — follow-up fix для вкладки «День»: миграция `20260608153000_allow_day_entry_source.sql` расширяет `conversations.entry_source` значением `day`, чтобы modal daily dialog перестал падать с `23514 conversations_entry_source_check` при открытии summary/planning из Day tab.
- 2026-06-08: `communicator` / `infra` — добавлен native health provider для summary-ветки: Apple HealthKit на iOS, Google Health / Android Health Connect на Android, read-only permissions для шагов/калорий/тренировок/сна, мягкий backoff повторного запроса прав и Expo prebuild-конфиг для dev-client.
- 2026-06-08: `assistant` / `communicator` / `practices` — summary-ветка вкладки «День» передаёт временный `dayHealthContext`: йога за день сравнивается с обычной практикой пользователя, Apple/Google Health явно помечены как недоступные до native provider, а финальный prompt prioritizes психологическую обратную связь по целевой чакре и использует health-данные только если они реально переданы.
- 2026-06-08: `assistant` / `communicator` / `practices` — QA-фикс диалога и вкладки «День»: planning больше не сохраняет промежуточные inferred events и не путает выбор практики с действием дня, `default` practice-marker уступает уверенной истории пользователя, добавленные действия сохраняют порядок, Home «Что делать?» ведёт на `/day` при уже собранном плане, а `PracticeCard` сохраняет выбранную длительность.
- 2026-06-08: `assistant` — неподытоженные `planned_events` больше не удаляются по 36-часовому окну и не отсекаются из summarizing по нижней границе due-window; ignored due-rows тоже остаются до явного удаления или summary. Planning marker `desc` сужен до короткого названия действия для вкладки «День».
- 2026-06-08: `profile` — «Сферы жизни» и «Проживаемые состояния» вынесены в самостоятельные donut-карточки; цвета сфер синхронизированы с цветами состояний, нулевые пункты серые, «Практики по чакрам» сортирует легенду 1–7 с русскими числовыми названиями чакр, а «Толщина линии жизни» считает 7-дневные блоки.
- 2026-06-07: `assistant` / `communicator` / `practices` / `profile` / `subscription` — добавлена первая вертикаль вкладки «День»: feature gate `day_planning`, endpoint `/api/day`, миграция `20260607183000_day_tab_foundation.sql`, видимые действия с рекомендациями, одна pending-практика дня, day-summary режим ассистента, radial-диаграмма сфер дня и профильные radial-проекции матрицы.
- 2026-06-07: `communicator` + `practices` — ассистентская `PRACTICE_PICK`-карточка теперь автоматически сохраняется как pending-практика вкладки «День» через `Communicator.onPracticeOffered(...)` и `POST /api/day`.
- 2026-06-07: `assistant` — doc-sync push refinement daily dialog: `buildPostRecommendationTimingGuard` для post-recommendation, planning gating только по inferred artifacts, prompt-лимит due/open plans до 3; `prompts-reference.md` §3–§3.1 и `communicator/history.md` (due window до 3).
- 2026-06-04: `assistant` — follow-up QA по `текст-4E29-8568-EF-0.txt`: daily dialog теперь держит до `3` текущих due-событий в summarizing, single-due summary repair не путает согласование новой практики с итогом старого события, single-event финал звучит более директивно, а post-recommendation больше не должен преждевременно спрашивать, как прошла практика.
- 2026-06-03: `assistant` + `communicator` — follow-up QA по `текст-4106-A447-F2-0.txt`: multi-due summarizing больше не repair-ится только в single-event кейсе, unanswered due remainder после partial summary не теряется сразу, planning inference перестал канонизировать выбор практики и supportive detail-фразы как `planned_events`, финальный multi-event текст жёстче держит структуру `общий фокус дня -> по одному абзацу на событие -> практика`, а communicator смягчил post-practice fallback после карточки и дольше ждёт hydration реального short reply.
- 2026-06-03: `assistant` + `communicator` — daily dialog получил safe core refinement: due-события теперь идут по циклу `prompted -> one reminder -> delete ignored`, planning сужен до `1-3` важных событий, summarizing явно собирает «как событие проживалось», а multi-event финал допускает короткие per-event рекомендации без ломки single-event natural flow.
- 2026-06-03: `communicator` — voice-failure UX: `AbortError`/`Aborted` больше не показываются как сырой generic error без retry; failed transcription bubbles помечаются отдельно и не попадают в `turnHistory` как будто это реальная реплика пользователя.
- 2026-06-02: `assistant` + `profile` — ветка `planning` в daily dialog больше не считается закрытой одними существующими open/due-событиями из summarizing, а `life-matrix` / `Толщина линии жизни` теперь открываются по продуктовым условиям (`>= 5` summarized events + `>= 5` календарных дней с первого summary) вместо `active_days_count >= 5`; `calendarTrend` пересчитан по календарным 5-дневным окнам.
- 2026-06-02: `astro` — `fetchActiveNatalProfileCached(userId, options?)` с `onBackgroundRefresh`; кэш «нет карты» и сетевые сбои чтения натала больше не маскируют отсутствие `birth_date` на Home (`spec.md`, `dependencies.md`, `history.md`; уточнение в `profile/spec.md`).
- 2026-06-02: `assistant` — в QA с `TEST_MODE_FORCE_PHASE` `dueEvents` внутри dialog-context теперь режутся по representative часу forced-phase, чтобы untimed/daypart-планы не становились `summarizing` раньше симулируемого времени; `plannedEventInference.ts` дополнительно перестал сохранять meta-фразы про «интересный день», игнорирует голые time-clarification реплики и разделяет независимые coordinated-actions вроде `фильм` + `ужин`.
- 2026-06-02: `profile` / Home — баннер «Нужна дата рождения» только если в `users` нет `birth_date`; сбой загрузки `user_natal_charts` больше не блокирует главный экран.
- 2026-05-28: pre-push doc-sync — `assistant/spec.md` ( `normalizeTurnHistory`, post-practice scan по любому assistant-turn с карточкой, `conversation_summaries` upsert `onConflict` ); `open_questions.md` — контекст summary `outcome_cells` приведён к двухшаговой цепочке reconcile.
- 2026-05-27: `assistant` — delayed `reconcile-plans` для summary-ветки больше не классифицирует `outcome_cells` тем же смешанным prompt-ом, что и planning anti-duplicate: `planningReconciliation.ts` теперь сначала нормализует outcome summary-эпизода, а затем отдельным classifier prompt-ом вычисляет клетки матрицы из compact outcome + baseline-ов. Это снижает semantic bleed между основным доменом события и побочными эффектами.
- 2026-05-27: `assistant` — `plannedEventInference.ts` перестал терять несколько планов из одной длинной спонтанной реплики: short daypart/time-фрагменты теперь остаются рядом с соседним событием, а разные планы больше не схлопываются только из-за одинакового synthetic default time. В том же проходе replace-flow после карточки снова остаётся `practice_repick`, даже если между карточкой и запросом был короткий `post_recommendation`.
- 2026-05-27: `assistant` + `communicator` — QA на `текст-4D64-AFFA-FE-0.txt`: `conversation_summaries` теперь upsert-ится по `conversation_id` вместо падения с `23505` после `complete`, а `normalizeTurnHistory()` перестал выкидывать `practicePicked` / `turn_mode`, поэтому после карточки снова корректно работают `post_recommendation` и `practice_repick`.
- 2026-05-27: `assistant` — `prompts-reference.md` v1.16: зеркало v6, §3.1 server-side validation/context, обновлены `{{due_events}}` / `{{historical_context}}` и описание эскалации под текущий `route.ts` / `markers.ts`.
- 2026-05-26: pre-push doc-sync — `assistant/dependencies.md`, `communicator/dependencies.md`, `MAP.md`, `assistant/prompts-reference.md` §3: delayed `reconcile-plans` (planning + summary), flush на unmount/launch practice, формат `dueEventWhenLabel` для `{{due_events}}` / `{{historical_context}}`.
- 2026-05-26: `assistant` + `communicator` — practice validation перестала путать clock-time с длительностью практики (`четыре часа дня` больше не превращаются в `240 мин дыхания`), `historical_context` теперь помечает synthetic planning-time как approximate вместо точного пользовательского часа, а delayed reconcile дополнительно флашится при закрытии чата/запуске практики, чтобы queued plans и summaries не зависали в `trigger_meta`.
- 2026-05-26: `assistant` + `communicator` — delayed reconcile шаг daily dialog теперь общий для planning и summarizing: assistant-turn складывает и `PLANNED_EVENT`, и `SUMMARIZE_EVENT` в `conversations.trigger_meta`, low-cost `reconcile-plans` одним LLM JSON-pass и канонизирует `planned_events`, и вычисляет `outcome_cells` для `daily_matrices`, а клиент дебаунсит reconcile и по `queued_summaries`.
- 2026-05-26: `assistant` + `communicator` — planning persistence в daily dialog стал delayed: assistant-turn теперь складывает candidates в `conversations.trigger_meta`, клиент запускает debounce `reconcile-plans`, а сервер канонизирует `planned_events` отдельным low-cost LLM-step (`AI_MODEL_LOW` / fallback `AI_MODEL_STANDARD`). В том же проходе `route.ts` получил provider-aware explicit cache для стабильного префикса structured dialog requests.
- 2026-05-26: `assistant` + `communicator` — daily dialog перевёл отказ от практики из отдельной terminal-ветки в общий финальный контракт: оркестратор теперь выдаёт `final_without_practice`, сервер отдельно различает “финал вообще” и “финал с карточкой”, а клиентская hydration умеет восстанавливать такой no-card финал при lean storage без ожидания `practicePicked`.
- 2026-05-26: `assistant` — `practice_declined` больше не теряет summarizing для единственного due-события: backend repair-path восстанавливает `[SUMMARIZE_EVENT]`, а closing-instruction этого режима теперь допускает invisible summary marker без рекомендации практики.
- 2026-05-26: `assistant` + `communicator` — клиентский `turnHistory` снова несёт минимальный `meta.practicePicked`, поэтому сервер различает обычный `post_recommendation` после карточки и явный запрос `другая практика`; для замены добавлен отдельный короткий режим `practice_repick` с новым `[PRACTICE_PICK]`, а duration-parser перестал терять explicit формат `йоги минут 40`.
- 2026-05-26: `assistant` — после QA на `текст_2_5.txt` диалог перестал пропускать отказ вида `что касается практики, нет сейчас времени этим заниматься`, `historical_context` начал получать компактный список уже открытых планов пользователя, а planning inference больше не сохраняет общие workload-реплики вроде `нужно поработать, много дел` как отдельные события.
- 2026-05-26: `assistant` — daily dialog теперь принудительно держит одинаковые `тип + минуты` между видимым финальным текстом и `practice_picked.overrides`, а relative phrases в forced-phase QA (`через полчаса`, `через час`) считаются от реального текущего времени, не превращаясь в мгновенно просроченные `planned_events`.
- 2026-05-26: `assistant` — после QA на `текст_2_4.txt` planning inference перестал поднимать причинные пояснения вроде `потому что нужно спортом заниматься` как отдельные планы, а `planned_events.description` очищается от вводных `думаю, что...` / `надеюсь, что...`.
- 2026-05-26: `assistant` — planning dedupe стал user-wide на горизонте `today/tomorrow`: backend использует единый matcher для augment/filter/persist, обновляет existing `planned_event` при `same`-совпадении и `skip`-ает конфликтующее второе explicit-time событие как `ambiguous_time_conflict`; debug export теперь показывает и `updated` rows.
- 2026-05-26: `assistant` — short/minimal practice requests больше не перетягивают duration из соседнего жизненного контекста (`через 15 минут... короткая медитация` -> `1 мин meditation`), а planning dedupe лучше схлопывает общие уточнения вроде `в магазин я пойду` с уже найденным shopping-событием.
- 2026-05-26: `assistant` + `practices` — mixed practice phrases больше не переключаются на первый встретившийся тип, `короткая/minimal` практика без числа минут теперь мапится на нижнюю границу каталога (1 / 5 / 20), а `route.ts` повторяет standard-ход, если пользователь уже явно подытожил единственное due-событие, но модель не вернула `[SUMMARIZE_EVENT]`.
- 2026-05-26: `assistant` — planning inference перестал превращать past-tense summary-фразы в новые `planned_events`, для time-less leisure-планов добавлен implicit evening default, а `inquiry` теперь явно трактует `йога > 20 минут` как асаны без лишнего переподтверждения.
- 2026-05-26: `assistant` — planning parser больше не читает `завтра` внутри `позавтракать`, получает implicit morning/evening defaults для `завтрак`/`театр`, а уточнение уже открытого события в той же беседе теперь обновляет existing `planned_event` вместо insert-дубля.
- 2026-05-25: `assistant` — test mode больше не ускоряет 36-часовое окно `planned_events`: anti-replan и reopen TTL по-прежнему сжаты, но due/summarizing и cleanup идут по реальному `expected_at`; параллельно planning inference перестал включать хвост запроса на практику в `description` события.
- 2026-05-25: `assistant` — при эскалации `standard -> premium` backend больше не теряет `[SUMMARIZE_EVENT]` / `[PLANNED_EVENT]` маркеры до персистенции; due-events в prompt форматируются в локальном времени пользователя, а forced phase синхронизируется с разбором planning-time фраз.
- 2026-05-25: `assistant` — inquiry больше не растягивает planning-кейс, если timed-событие уже названо и практика задана валидно: `route.ts` умеет сразу эскалировать в финальную рекомендацию из history, `dialogArcOrchestrator.ts` запрещает добирать сферу/подтекст события и точную минуту внутри допустимого диапазона.
- 2026-05-25: pre-push doc-sync — SSE split `complete` / `turn_artifacts`, `DialogTurnArtifactsEvent` + `onTurnArtifacts`; lean-storage hydration при пустом server `content`; `communicator/spec.md`, `communicator/dependencies.md`, `assistant/dependencies.md`.
- 2026-05-25: pre-push doc-sync — `communicator/spec.md` + `dependencies.md` (`dialogExportMerge`, расширенный `DialogCompleteEvent`); `assistant/spec.md` + `dependencies.md` (`complete.messageId`, `matrixCells`).
- 2026-05-25: `communicator` + `assistant` — restore early SSE `complete` for practice card; `turn_artifacts` after persist for planning meta; lean-storage session hydration from server meta.
- 2026-05-25: `communicator` + `assistant` — `messages.meta.planning_persistence` согласован с `dialog_state_after`: SSE `complete` после persist с `planningPersistence`, dev-export merge по role-index + reconcile fallback.
- 2026-05-25: `assistant/spec.md` — детали `augmentPlannedMarkers`, `loadOpenPlannedEventsForConversation`, `hasRequiredBranchArtifacts` (open planned rows), `timeParser` «через N минут»; `MAP.md` — code ref `plannedEventInference.ts`.
- 2026-05-25: `assistant` — server-side inference planned events from user history when planning branch active; `plannedEventInference.ts`, `route.ts`.
- 2026-05-25: pre-push doc-sync — GET dialog session sync (resume/TTL/`debugExport`), `Communicator` mount без cache `conversationId`, `clearHomeDailyDialogCache` на dev-сбросе дня; `assistant` + `communicator` spec/history/dependencies.
- 2026-05-25: pre-push doc-sync — `assistant/spec.md` (`practicePicked` только на `isFinalMode` в SSE `complete` и `messages.meta`), `communicator/spec.md` (mount: `fetchDialogSession` + cache, server reset, приоритет восстановления ленты).
- 2026-05-25: pre-push doc-sync — `communicator/dependencies.md` (turnHistory, dialogSessionCache), `assistant/spec.md` (conversation_summaries tag, buildLifeMatrixReportSnapshot), `communicator/spec.md` (buildClientTurnHistory).
- 2026-05-25: `assistant` + `communicator` + `profile` — daily dialog перестал хранить тексты на сервере: клиент держит current-day local session cache, backend пишет только служебные `messages.meta`, live `planned_events`, `daily_matrices` и новый snapshot-слой `profile_report_snapshots`; профильные отчёты читаются из snapshot с fallback rebuild из day-rollups.
- 2026-05-23: `assistant` + `communicator` — dev-export и `dialog_state_after` расширены для QA planning/summarizing: per-turn `planning_persistence`, snapshot планов на старт/конец диалога.
- 2026-05-23: `assistant` — финальные premium-ответы daily dialog больше не должны задавать пользователю дополнительных вопросов после выбора практики; `final_recommendation`, `final_recommendation_with_validation_warning` и `forced_final` явно завершает ход, если под ним уже показывается карточка практики.
- 2026-05-23: `assistant` — после повторных QA-диалогов `markers.ts` дополнительно усилен: длительность теперь привязывается к упоминанию именно выбранного типа практики, refusal распознаёт `не до практик` / `нет времени выполнять асаны, пранаяму` / `не нужно предлагать практику`, а parser понимает `2,5`, `2.5` и `3/4 часа`.
- 2026-05-23: `assistant` + `practices` — `validateHistoryHasDurationAndType` перестал путать время события с длительностью практики и теперь выбирает минутность рядом с упоминанием practice-type; это исправляет кейсы вроде «через полчаса вебинар, предложи 3 минуты медитации» и подтверждение «ладно, 5 минут медитации». Одновременно `PracticeCard` / shared `assistantSelectableDurations` / `catalog.ts` выровнены на диапазон медитации **1–5 минут**.
- 2026-05-23: `assistant` + `communicator` — `practice_declined` стал терминальным: при явном отказе от практики диалог закрывается одним ответом с рекомендацией на день без практики и с пожеланием по времени суток; export теперь может читать конкретную беседу по `conversationId`, чтобы завершённые диалоги не терялись в сокращённый local snapshot; `timeParser` понимает bare-hour формулировки вроде `10`.
- 2026-05-23: `assistant` + `communicator` — после первых QA-диалогов закрыт ранний уход в финальную практику без planning/summarizing артефактов: `inquiry` теперь удерживает ветку до `PLANNED_EVENT` / `SUMMARIZE_EVENT`, server-side ready escalation блокируется без этих маркеров, fallback времени без явного указания смещён на дневное окно (`16:00`), дальние планы за пределами `сегодня/завтра` сохраняются как `skipped_planned_events`, а export показывает статус `conversation.server_sync`.
- 2026-05-23: `assistant` + `communicator` — исправлен opening-hint для комбинированной ветки `summarizing + planning` (сначала подытоживание, потом планы), а debug/export диалога стал каноническим: `Communicator` предпочитает server session sync, JSON включает `message_id`, `related_event_ids`, `matrix_cells`, а `dialog_state_after` расширен `context_snapshot` и `conversation_summary_for_exported_conversation`.
- 2026-05-22: pre-push doc-sync — `daily_forecast`: `computeDiurnalWindowTimes` / `dayFractionFromIso`, единая дискретизация окон восхода/кульминации и графика home; **`docs/02_modules/daily_forecast/{spec,dependencies,history}.md`**.
- 2026-05-22: `assistant` — найден и закрыт crash после финального текста без карточки: сервер нормализует legacy `time_resolution = "daypart"` в `daypart_default` перед insert в `planned_events`; `monitoring.ts` сериализует object-errors в JSON вместо `"[object Object]"`.
- 2026-05-22: `assistant` + `communicator` — opening первого хода теперь без чакр/астро-лексики и с явной привязкой `summarizing` к событию; `Communicator` получил многократную session hydration финального `complete` и более подробный dialog export (`conversation`, `practice_picked`, `branches_active`, `phase_time`, `target_chakra`).
- 2026-05-22: pre-push doc-sync — `assistant/spec.md` (retry `[PRACTICE_PICK]` `maxOutputTokens` 320, логи селектора), `communicator` (`dialogTurnHydration` в spec/dependencies).
- 2026-05-22: `assistant` + `communicator` — `fast_track_final` только для practice-only первой реплики; success-path hydration `complete`/`practicePicked` из session sync; диагностика empty final practice pick и retry `[PRACTICE_PICK]` увеличен до 320 токенов.
- 2026-05-21: `communicator` — карточка практики: немедленный commit при `complete.practicePicked`; `syncDisplayText` перед deferred reveal (fix stripTarget vs revealGoal).
- 2026-05-21: pre-push doc-sync — `OpportunityWindows`: штрих «сейчас» — `Svg` `Line` (`strokeDasharray`), цвет `theme.colors.textPrimary`; кривая по чакре `graphPlanet`; **`docs/02_modules/daily_forecast/{spec,dependencies,history}.md`**.
- 2026-05-21: pre-push doc-sync — `OpportunityWindows`: SVG-полилиния, цвет по `graphPlanet`, подпись `graphTrack` при расхождении с планетой дня; **`docs/02_modules/daily_forecast/{spec,dependencies,history}.md`**.
- 2026-05-21: pre-push doc-sync — `daily_forecast`: `planetDiurnalCurve`, экспорты суточной криваты, `useDayContent.userLocation`, график `OpportunityWindows` на реальной высоте планеты; **`docs/02_modules/daily_forecast/{spec,dependencies,history}.md`**, **`docs/00_index/MAP.md`**.
- 2026-05-21: pre-push doc-sync — `astro`/`daily_forecast`: `J2000Century` для `solar.apparentLongitude`, регрессии `solar-ephemeris` и `windows-of-opportunity`; home UI (`ChakraFlower`, `OpportunityWindows`). **`docs/02_modules/{astro,daily_forecast}/{spec,history}.md`**.
- 2026-05-21: pre-push doc-sync — `assistant`: `prompts-reference.md` §2 `opening` + `{{opening_day_question}}`; `spec.md` — server-side ready escalation, `dialogOpeningHints`, `forcePlanningOnOpening`.
- 2026-05-21: `assistant` — server-side ready escalation при confident inquiry; opening hints по ветке/фазе; planning на opening несмотря на anti-replan.
- 2026-05-21: pre-push doc-sync — `communicator`: при salvage через `fetchDialogSession` в `complete` копируются `practicePicked` и метаданные оркестратора из `meta`; `assistant/spec.md` — `{{catalog_reconciliation}}` в списке плейсхолдеров `expandOrchestratorInstruction`.
- 2026-05-21: `assistant` — оркестратор: `catalog_reconciliation` при конфликте тип/минуты; `resolvePracticePublic` без маркера при `confident`; refusal-check только если тип/минуты не названы.
- 2026-05-21: `assistant` — `validateHistoryHasDurationAndType`: `confident` только при согласованных тип/минуты (v6).
- 2026-05-21: pre-push doc-sync — `promptLocalHour` в `buildDialogSystemInstruction`; `prompts-reference.md` §3, `assistant/spec.md` (testMode, debug export).
- 2026-05-21: `assistant` — `TEST_MODE_FORCE_PHASE` подменяет `local_hour`/`time_of_day` в промпте (9/14/19), не только `phaseTime`.
- 2026-05-21: pre-push doc-sync — daily dialog v6 (inactive): `practice_declined`, отказ на сессию, debug export; `prompts-reference.md` §1–3, `assistant/spec.md`, `communicator/spec.md`, `MAP.md`, `assistant/history.md`.
- 2026-05-21: `communicator` — pre-push doc-sync: salvage частичного SSE, XHR onerror на iOS, transcribe 12s + `useNetworkRetry`, recovery сессии при ошибке стрима, UX `timeout`; **`docs/02_modules/communicator/{spec,dependencies,history}.md`**.
- 2026-05-21: `assistant` — v6 (inactive): согласование тип/длительность с фиксацией после «да»; отказ от практики на сессию; оркестратор `practice_declined`; test mode: сжатый TTL только при GET resume, не между репликами.
- 2026-05-21: `communicator` — голос: удержание URI до ответа, transcribe 3×10s, Retry без перезаписи; `voiceTurnPipeline.ts`, `Communicator.tsx`.
- 2026-05-21: client — localized network error UX (ru/en), hidden retries, Retry in Communicator; `userFacingErrors`, `modules/ui/i18n/userErrors.ts`.
- 2026-05-21: pre-push doc-sync — dialog test mode (`testMode.ts`): `assistant` spec/history/dependencies, `infra` instrumentation, `MAP.md`; справочник `docs/04_reference/test_mode.md`.
- 2026-05-20: pre-push doc-sync — profile: публичный контракт §2 (`loadLifeMatrixReport` без `days`, экспорты `*ReportCard` / `useLifeMatrixReport`), `dependencies` (поток `planned_events` → `calendarTrend`), точки входа в MAP. **`docs/02_modules/profile/{spec,dependencies}.md`**, **`docs/00_index/MAP.md`**.
- 2026-05-21: profile UX blocks — 4 карточки, селекторы только у stats/chakra-pie, life-matrix all-time + calendar trend по 5 активным дням, единые empty states. **`docs/02_modules/profile/{spec,history}.md`**, **`_legacy_web/app/api/profile/life-matrix/route.ts`**, **`lifeMatrix.ts`**.
- 2026-05-20: pre-push doc-sync — profile: публичный контракт `modules/profile` (`periodPresets`, `PeriodSelector`, i18n, `ProfileReports.locale`), decision log, точки входа в MAP (push: независимые периоды отчётов, дефолт 7д). **`docs/02_modules/profile/{spec,history}.md`**, **`docs/00_index/MAP.md`**.
- 2026-05-21: profile reports UX patch — `PeriodSelector`, `periodPresets.ts`, `profile/i18n`, локализованные заголовки, независимые периоды, легенда 7 чакр, layout heatmap; диагностика `practice_sessions` и dev-перегрева. **`docs/02_modules/profile/{spec,history}.md`**, **`docs/04_workspace/open_questions.md`**.
- 2026-05-19: pre-push doc-sync — `route.ts` импортирует шаги длительности карточки из `@shared/assistantSelectableDurations` (`_legacy_web/shared_core/assistantSelectableDurations.ts`, копия клиентского модуля). Обновлены **`docs/02_modules/{assistant,practices}/{dependencies,spec,history}.md`**, **`docs/00_index/MAP.md`**.
- 2026-05-18: pre-push doc-sync — profile reports: легенда чакр на сервере через `planetChakraLegend.ts` / `planet_chakra_map.json` вместо импорта `modules/home/planetChakra`. Обновлены **`docs/02_modules/profile/{spec,dependencies,history}.md`**, **`docs/02_modules/assistant/dependencies.md`**, **`docs/00_index/MAP.md`**.
- 2026-05-18: pre-push doc-sync — HARMONIZER v2 push: активация `dialog_system_v3` v5 (`20260519030000_dialog_system_v3_v5_activate.sql`), life-matrix foundation и profile reports уже в коде; синхронизированы **`docs/02_modules/assistant/{prompts-reference,spec,history,dependencies}.md`**, **`docs/02_modules/communicator/{spec,history}.md`**, **`docs/00_index/MAP.md`**.
- 2026-05-17: deploy-readiness fix for `assistant` — миграция **`20260516150000_dialog_system_v3_v5.sql`** больше только подготавливает запись `dialog_system_v3` v5 и оставляет её `is_active=false`; активной должна оставаться v4 до ручной вычитки владельцем. Уточнены **`docs/02_modules/assistant/{spec,history}.md`**.
- 2026-05-16: HARMONIZER v2 doc-sync — `assistant` переведён в документации на двухфазный daily dialog v5 (`summarizing/planning`, `planned_events`, `daily_matrices`, `day_target_chakra`, новые markers и time parser); `daily_forecast` зафиксировал поля `user_daily_forecasts.day_target_*`; `profile` — новые server-backed reports (`life matrix`, `range trend`, `practice-by-chakra`); `subscription` — расширенный смысл gate `stats`. В `open_questions.md` добавлены хвосты про locale-aware chakra baseline и отсутствие отдельного cleanup job для `planned_events`.
- 2026-05-15: `assistant` + `communicator` — `[PRACTICE_PICK]` расширен полем `card_blurb`; финальные и `fast_track_final` инструкции daily dialog v3 требуют model-generated текст карточки в том же premium-вызове, `route.ts` подставляет `{{chakra_label_accusative}}`, валидирует `card_blurb` и использует его только когда итоговая практика совпала с маркером; клиент `Communicator` предпочитает `practicePicked.card_blurb` с fallback на `reason`. Обновлены **`docs/02_modules/{assistant,communicator}/{spec,history}.md`**, **`docs/02_modules/assistant/prompts-reference.md`**.
- 2026-05-15: pre-push doc-sync — diff миграции **`20260515030000_dialog_system_v3_pack_b.sql`**: в **`docs/00_index/MAP.md`** добавлена точка входа для строки модуля **`assistant`**; в **`docs/02_modules/infra/{dependencies,history}.md`** уточнено, что `dialog_system_v3` в **`public.prompts`** эволюционирует цепочкой миграций (ссылка на **`assistant`**). **`CHANGELOG.md`**.
- 2026-05-15: `assistant` — пакет `B` для daily dialog v3: `fast_track_final` срабатывает и на первой содержательной реплике пользователя после `opening`, добавлен dev-лог **`[FAST_TRACK_DIAG]`**, `inquiry` получил подстановку `{{practice_refusal_check}}` после двух подряд незакрытых вопросов о практике. Новая миграция **`supabase/migrations/20260515030000_dialog_system_v3_pack_b.sql`** активирует `dialog_system_v3` v4 (антизатягивание после готовности, явный отказ от практики, запрет расписывать технику). Обновлены **`docs/02_modules/assistant/{prompts-reference,spec,history}.md`**.
- 2026-05-14: pre-push doc-sync — diff `practiceSelection.ts` / `route.ts` / `assistantSelectableDurations.ts` / `PracticeCard.tsx`: в **`assistant/spec.md`** уточнены **`.trim()`** у `id` маркера, поле **`markerCatalogPracticeKind`**, цепочка минут до клипа (каталог / 10 / 3), разделение серверного и клиентского лога **`[PRACTICE_CARD_MISMATCH]`** (`**source: "practice_card_client_sync"**`); **`practices/spec.md`** — клиентский лог карточки; **`MAP.md`** — точка входа **`assistantSelectableDurations.ts`**; **`assistant`/`practices` `dependencies.md`** — явный импорт **`route.ts`** ↔ **`assistantSelectableDurations.ts`**; **`{assistant,practices}/history.md`**. **`CHANGELOG.md`**.
- 2026-05-15: `assistant` + `practices` — карточка практики в диалоге: **`resolvePracticePublic`** (`route.ts`) — `overrides.durationMin` из **`validateHistoryHasDurationAndType`** при **`confident`** (маркер только если история без уверенной длительности), клип к 5–20 / 1–10 мин через **`modules/practices/core/assistantSelectableDurations.ts`**, лог **`[PRACTICE_CARD_MISMATCH]`**; **`choosePractice`** — при **`confident`** и расхождении **`kind`** маркера с историей сброс id маркера + **`historyKindConflictResolved`**. **`PracticeCard`** — клип **`overrideDurationMinutes`**. Тесты **`practiceSelection.test.ts`**. **`docs/02_modules/{assistant,practices}/{spec,history}.md`**, **`CHANGELOG.md`**.
- 2026-05-15: `assistant` + `practices` — **`choosePractice`** / **`selectPracticeCandidate`**: явный `[PRACTICE_PICK]` не обходит окно недавних предложений/сессий; недавние ключи нормализуются к **`id`** каталога (`resolvePracticeKeyToCatalogId`); селектор исключает только по **`id`**; классификация дыхания по `practice_slug` в сессиях — по slug строк `breath` из загруженного каталога (удалён хардкод списка slug); при расхождении эвристики типа и `kind` маркера окно недавних по типу маркера. Тесты **`practiceSelection.test.ts`**, **`selector.test.ts`**. **`docs/02_modules/{assistant,practices}/{spec,history}.md`**.
- 2026-05-14: `profile` / auth cold start — при `INITIAL_SESSION === null` или safety-таймауте без события SDK восстановление сессии из SecureStore и **`auth.setSession`** с повтором (`modules/auth/bootstrapRecoverSession.ts`, `services/supabase.ts`, `modules/auth/AuthProvider.tsx`); safety 35 с; исправление ложного `/sign-in` после транзиентного `Network request failed` на refresh. **`docs/02_modules/profile/{spec,history}.md`**, **`docs/00_index/MAP.md`**.
- 2026-05-14: `daily_forecast` + `profile` — уточнён публичный контракт в **`spec.md`**: экспорт **`DayContentRefreshOptions`**, детали **`NatalBirthDataModal`** (`initialDate`/`initialTime`, **`NATAL_BRIDGE_DEFAULT_LOCATION`**), на профиле после сохранения — **`Alert`** + **`router.push("/calibration")`** / **`router.replace("/")`**.
- 2026-05-14: `daily_forecast` — после **`refresh({ forceRefresh })` / `blockingReload`** персонального дня **`useDayContent`** снова вызывает **`morning_recommendation`** с **`forceRefresh`** даже при уже заполненных текстовых полях в forecast (исправление «старые рекомендации после смены натала»). Код: **`modules/home/useDayContent.ts`**. **`docs/02_modules/daily_forecast/{spec,history}.md`**.
- 2026-05-14: `profile` + `daily_forecast` — вкладка **`app/(tabs)/profile.tsx`**: «Обновить профиль» открывает **`NatalBirthDataModal`** и **`createNatalProfile`** (как главный), после сохранения — **`markHomeDayContentBlockingReload`** / **`consumeHomeDayContentBlockingReload`** и **`useDayContent.refresh({ blockingReload })`** на фокусе Home (`useFocusEffect`). Хук **`modules/home/useDayContent.ts`**: опция **`blockingReload`**. **`modules/home/ui/NatalBirthDataModal.tsx`**, **`services/homeDayContentReloadRequest.ts`**. Документация: **`docs/02_modules/{profile,daily_forecast}/{spec,dependencies,history}.md`**.
- 2026-05-14: `assistant` — режим оркестратора **`fast_track_final`** (первое сообщение пользователя уже с длительностью и типом): сразу **premium** + `streamGeminiText`, без standard-хода и без `[READY_FOR_RECOMMENDATION]`; `decideTurnMode(..., pendingUserContent?)`. Код: **`dialogArcOrchestrator.ts`**, **`communicator/v2/dialog/route.ts`**; подпись фазы в **`modules/communicator/i18n/communicator.ts`**. Документация: **`docs/02_modules/assistant/{prompts-reference,spec,history}.md`**, **`docs/02_modules/communicator/history.md`**.
- 2026-05-14: `assistant` — **`practiceSelection.ts` / `choosePractice`**: явный `[PRACTICE_PICK]` (не `default`) сначала резолвится по **полному** каталогу практик, чтобы карточка совпадала с выбором премиум-модели; эвристика `practiceKind` из реплик пользователя больше **не** отфильтровывает id из маркера (раньше это давало «Вспышку» при тексте про дыхание). Лог **`marker_id_not_in_catalog`** при промахе id. Тест **`practiceSelection.test.ts`**. **`docs/02_modules/assistant/{spec,history}.md`**, **`docs/02_modules/practices/{spec,history}.md`**.
- 2026-05-14: `profile` / auth dev UX — **`supabase-auth-console-filter.ts`**, **`authNetworkErrors.ts`**: LogBox больше не краснеет на ожидаемом abort refresh (15 с) и на **`AuthRetryableFetchError`** от auth-js; уточнён детектор abort (Hermes / стек auth-js) + **`LogBox.ignoreLogs`**. **`docs/02_modules/profile/{spec,history}.md`**.
- 2026-05-13: pre-push doc-sync — в **`docs/00_index/MAP.md`** в точках входа модуля **`assistant`** добавлены **`dialogTonalRegisters.ts`** и миграции **`20260513223000_dialog_system_v3_polish_pack_a.sql`** / **`20260513224500_dialog_system_v3_polish_pack_a_fix.sql`** (соответствие diff кода).
- 2026-05-13: `assistant` — **`20260513224500_dialog_system_v3_polish_pack_a_fix.sql`**: `dialog_system_v3` v3, секция отказа от практики перенесена в шаблоне; **`prompts-reference.md` v1.3**, **`spec.md`**, **`history.md`**.
- 2026-05-13: `assistant` — пакет «А» для daily dialog v3: миграция **`supabase/migrations/20260513223000_dialog_system_v3_polish_pack_a.sql`** (новый активный `dialog_system_v3` v2), **`dialogTonalRegisters.ts`**, правки **`dialogArcOrchestrator.ts`** / **`communicator/v2/dialog/route.ts`** (`{{tonal_register}}`, подстановка плейсхолдеров в финальные инструкции, `[DIALOG_V3_DEBUG_PROMPT]`). Документация: **`docs/02_modules/assistant/{prompts-reference.md,spec,history}.md`**.
- 2026-05-13: pre-push diff — **`markers.ts`** (`inferDurationSecFromText`, `inferKindFromText`, экспорт **`stripDialogScaffoldMarkdown`**), **`practiceCardSummary.ts`**, **`dialog/route.ts`** (yoga без **`overrides`** в payload), клиент **`Communicator.tsx`** / **`UserBubble`** / **`types.ts`** / **`communicator.ts`**. Документация: уточнены **`docs/02_modules/assistant/{spec,history}.md`**, **`docs/02_modules/communicator/{spec,history}.md`**, **`CHANGELOG.md`**.
- 2026-05-13: `communicator` + сервер `assistant` — клиент **`dialogTextCleanup.ts`** (`stripDialogScaffoldMarkdown`), **`Communicator` / `UserBubble` / `AssistantBubble`** (голосовой якорь **`scrollToIndex`**, **`FlashList`**, восстановление пустого SSE через **`fetchDialogSession`**); **`markers.ts`** / **`practiceCardSummary.ts`** / **`dialog/route.ts`** + тесты. Документация: **`docs/02_modules/communicator/{spec,dependencies,history}.md`**.
- 2026-05-13: `assistant` — **`prompts-reference.md`**: исправлен YAML-frontmatter (случайная поломка `## ]`); таблица переменных §3 выровнена по ширине колонок; **version 1.1**.
- 2026-05-13: `assistant` — справочник дословных промптов и инструкций v3: **`docs/02_modules/assistant/prompts-reference.md`** (шаблон `dialog_system_v3`, `ORCHESTRATOR_INSTRUCTIONS`, переменные `renderPrompt`); cross-link в **`spec.md`**; путь в **`MAP.md`**; конвейер **`scripts/docs-sync/sync.sh`** + правило в **`documentation_update_rules.md`**.
- 2026-05-13: `communicator` + `assistant` — голос: **`scrollToIndex`** с задержкой под layout **`FlashList`**, **`communicatorListDataRef`**, повторы по **`Promise`** (до 8, без `scrollToEnd` при ошибке); **`stripDialogScaffoldMarkdown`** / **`markers`** — удаляется **целиком** содержимое `**…**` (не список слов). Ранее в этот день: якорь по **`voiceAnchorTick`**, йога без **`overrides`**, «три четверти часа» в **`markers`**. Документация: **`docs/02_modules/{communicator,assistant}/{spec,history,dependencies}.md`**, **`CHANGELOG.md`**.
- 2026-05-13: `communicator` — голосовой плейсхолдер без видимого текста (только спиннер); якорный скролл к пузырю пользователя (~24% сверху); без авто-якоря к стриму ассистента после голоса; обрезка устаревших хвостов в `practicePicked.reason` при показе карточки. Код: **`Communicator.tsx`**, **`UserBubble.tsx`**. **`docs/02_modules/communicator/{spec,history}.md`**.
- 2026-05-13: `communicator` — при голосовой отправке сразу показывается пузырь пользователя со спиннером (`meta.voiceTranscribing`, `voiceUserBubblePending`); при отбрасывании расшифровки плейсхолдер удаляется. Код: **`Communicator.tsx`**, **`UserBubble.tsx`**, **`communicator.ts`**, **`types.ts`**. Обновлены **`docs/02_modules/communicator/{spec,history}.md`**.
- 2026-05-13: `communicator` — при пустом SSE-агрегате одна попытка восстановить ответ через `fetchDialogSession` (~700 ms); заглушка «не получил полный текст» больше не ждёт отложенного коммита до 60 с; клиентская очистка `stripDialogScaffoldMarkdown` (`dialogTextCleanup.ts`) для `---` и `**…**`. `assistant` — та же очистка в `sanitizeAssistantText` (`markers.ts`); карточка практики без хвоста «Рядом с вашим запросом» (`practiceCardSummary.ts`). Код: **`Communicator.tsx`**, **`AssistantBubble.tsx`**, **`markers.ts`**, **`practiceCardSummary.ts`**. Документация: **`docs/02_modules/{communicator,assistant}/{spec,history}.md`**.
- 2026-05-13: `assistant` — в **`markers.ts`** для **`isBreathVersusOtherParallelOffer`** убрана зависимость от JS-`\b` на кириллице (фразы «подышать или через тело» снова не затирают ранее выбранную йогу в **`practiceKind`**). Тест **`markers.test.ts`**. Обновлены **`docs/02_modules/assistant/{spec,history}.md`**.
- 2026-05-13: `communicator` — убраны подписи фазы оркестратора в пузыре ассистента (`AssistantBubble`); после транскрипции отсекаются известные «субтитровые» галлюцинации Whisper (`transcriptionGuard.ts`). `assistant` — краткий текст карточки практики на сервере (`practiceCardSummary.ts`, поле `reason` в `practice_picked`). Код: **`AssistantBubble.tsx`**, **`Communicator.tsx`**, **`practiceCardSummary.ts`**, **`dialog/route.ts`**. Обновлены **`docs/02_modules/{communicator,assistant}/{spec,history,dependencies}.md`** (communicator dependencies).
- 2026-05-14: `profile` / routing — атомарное завершение auth bootstrap (`session` + `initializing`), debounce первого `INITIAL_SESSION` с пустой сессией, задержка редиректа на `/sign-in` при кратковременном `session === null` после входа. Код: **`modules/auth/AuthProvider.tsx`**, **`app/_layout.tsx`**. Обновлены **`docs/02_modules/profile/{spec,history}.md`**.
- 2026-05-14: `communicator` — `FlashList` для истории, построчный стрим с Reanimated (`StreamingAssistantLines`), якорный `scrollToIndex` при старте ответа, без автоскролла в конец пока `streamBusy`; удалён `useStreamReveal`. Код: **`Communicator.tsx`**, **`StreamingAssistantLines.tsx`**, **`package.json`**. Обновлены **`docs/02_modules/communicator/{spec,history,dependencies}.md`**.
- 2026-05-13: `communicator` — SSE диалога на native через `XMLHttpRequest` (инкрементальный поток вместо буферизующего `fetch`); UI стрима и `useCommunicatorStream`/`Communicator` доработаны для побуквенного отображения. Обновлены **`docs/02_modules/communicator/{spec,history,dependencies}.md`**.
- 2026-05-12: Cold start `AuthProvider` упрощён — удалён параллельный `getSession()` / `resolveInitialSession()`; единственный источник сессии — `onAuthStateChange`; в `services/supabase.ts` добавлен 15 с abort для auth token refresh. Обновлены **`docs/02_modules/profile/{spec,history}.md`**.
- 2026-05-12: `loadOpportunityWindowsExplanation` разделён на free/paid пути — free получает упрощённый текст без транзитной планеты и точного аспекта. Обновлены **`docs/02_modules/daily_forecast/{spec,history}.md`**.
- 2026-05-12: doc-sync дополнение: `daily_forecast/spec.md` дополнен описанием `opportunityWindowsExplanation.ts` и трёхуровневой валидации (`isBaseForecastValid`/`isDayContentReadyForHome`/`isDayContentComplete`), `daily_forecast/history.md` — запись о help-модале; `profile/{spec,history}.md` — таймаут `fetchProfile` 10 с; `astro/{spec,history}.md` — bump frontmatter.
- 2026-05-12: ускорен startup главной: `daily-forecast/route.ts` больше не блокирует первый рендер синхронным `morning_recommendation`, `useDayContent` догружает вторичные AI-тексты в фоне, `OpportunityWindows` получил lazy help-modal, а global free precompute (`precompute-global-recommendations`) переведён на rolling window `yesterday/today/tomorrow` с hourly cron. Обновлены **`docs/02_modules/{daily_forecast,infra}/{spec,history}.md`**, **`supabase/README.md`**, **`DEPLOY.md`**.
- 2026-05-12: для ускорения повторных запусков `HomeScreen` переключён на `fetchActiveNatalProfileCached(profile.id)` с локальным кэшем `expo-secure-store` / `localStorage` в `services/natalProfileClient.ts`; сетевое чтение активного натала ограничено `10s`, а `services/globalContentClient.ts` получил общий таймаут `15s` и для fallback-чтения `global_daily_content`. Обновлены **`docs/02_modules/{astro,daily_forecast}/{spec,history}.md`**.
- 2026-05-12: Консолидация вывода длительности/типа практики: `inferPreferredDurationSec` / `inferPreferredPracticeKind` удалены из `practiceSelection.ts`, `validateHistoryHasDurationAndType` (`markers.ts`) расширен полями `durationSec` и `practiceKind` (тип `PracticeKindInferred`). Обновлены **`docs/02_modules/assistant/{spec,history}.md`**.
- 2026-05-12: `choosePractice` расширен полями `chakraId` / `preferredDurationMin`; `inferPreferredDurationSec` обрабатывает полную историю диалога; `overrides` в ответе практики теперь безусловно присутствует с fallback-цепочкой (marker → inferred → planet_of_the_day/null); удалены DEBUG BOLT блоки из `Communicator.tsx` (tech debt закрыт). Обновлены **`docs/02_modules/{assistant,communicator}/{spec,history}.md`**, **`docs/04_workspace/open_questions.md`**.
- 2026-05-12: `maxOutputTokens` для standard/premium вызовов в `dialog/route.ts` увеличен с 1500 до 2500; в `Communicator.tsx` добавлены временные DEBUG BOLT диагностические блоки (tech debt, зафиксирован в `open_questions.md`). Обновлены **`docs/02_modules/assistant/history.md`**, **`docs/04_workspace/open_questions.md`**.
- 2026-05-12: Маркер `[PRACTICE_PICK]` перемещён на первую строку финальной рекомендации в инструкциях оркестратора; добавлен retry-вызов Gemini для восстановления маркера при его отсутствии в premium-ответе. Обновлены **`docs/02_modules/assistant/{spec,history}.md`**.
- 2026-05-12: В **`dialog/route.ts`** добавлена защита от пустого ответа premium-модели после санитизации (лог `[PREMIUM_EMPTY_RESPONSE]`, ошибка вместо пустого пузыря); в **`Communicator.tsx`** на восстановлении сессии фильтруются assistant-сообщения с пустым `content`; `practicePicked` в SSE `complete` прокидывает `overrides` и `markerIdResolved` через `PracticeCard`. Обновлены **`docs/02_modules/{assistant/spec,communicator/spec,communicator/history}.md`**.
- 2026-05-12: `[PRACTICE_PICK]` маркер расширен полями `duration_min` и `chakra`; overrides прокидываются через `meta.practicePicked.overrides` в SSE и в `PracticeCard`; в `selector.ts` добавлен `markerIdResolved` флаг для видимости невалидных model-generated id.
- 2026-05-11: daily dialog переведён на v3: в **`_legacy_web/app/api/_utils/gemini.ts`** добавлены structured Gemini request и best-effort explicit context caching, удалены soft-cap артефакты (**`softCap.ts`**, **`information_axes.json`**, env `DIALOG_SOFT_CAP_*`), добавлены **`dialogArcOrchestrator.ts`**, **`dialogConfig.ts`**, миграция **`20260511161000_dialog_system_v3.sql`** и общий `PracticeCard` для communicator/catalog. Обновлены **`docs/02_modules/{assistant,communicator,practices,infra}/{spec,dependencies,history}.md`**, **`docs/00_index/MAP.md`**, **`docs/04_workspace/open_questions.md`**.
- 2026-05-11: удалены **`explicitSignals.ts`** и **`dialog_signals.json`**; в **`dialog/route.ts`** слот **`explicit_signals_json`** для оркестратора — литерал **`"[]"`** до **`dialog_v3_unified_prompt`**; сужены маркеры в **`insightDetection.ts`**. Обновлены **`docs/02_modules/assistant/{spec,history,dependencies}.md`**, **`docs/00_index/MAP.md`**, **`CHANGELOG.md`**.
- 2026-05-11: удалён файл миграции **`supabase/migrations/20260509120000_dialog_quality_v4.sql`** из репозитория (откат в БД — **`20260511140000_revert_dialog_quality_v4.sql`**). Обновлены **`docs/02_modules/assistant/{spec,history}.md`**, **`docs/00_index/MAP.md`**, **`CHANGELOG.md`**.
- 2026-05-09: pre-push diff (**`gemini.ts`**, **`authorVoice.ts`**, клиент): в **`docs/02_modules/assistant/spec.md`** уточнены цепочка моделей (primary→env fallback→`fallbackModels`), условия повтора (в т.ч. **404**/NOT_FOUND), лог **`[GEMINI FALLBACK]`**, разделение финального сообщения пользователю vs проброс ошибки; в профиле автора — поля **`assistant_should_NOT_say`** в **`few_shot_examples`**; в **`code_refs`** — **`gemini.ts`**. Запись в **`docs/02_modules/assistant/history.md`**.
- 2026-05-09: pre-push diff: диалог v2 — интеграция **`explicitSignals`** / **`softCap`**, метрики ходов, расширение **`OrchestratorDecision`** и рендера **`responder_main`**; миграция **`20260509120000_dialog_quality_v4.sql`**. Обновлены **`docs/02_modules/assistant/spec.md`**, **`history.md`**, **`dependencies.md`**, **`docs/00_index/MAP.md`**; из **`docs/04_workspace/open_questions.md`** удалён устаревший пункт про отсутствие вызова **`detectExplicitSignals`** из маршрута.
- 2026-05-09: **`authorVoice.ts`** и тесты приведены к **`author_voice.json` v2** (`usage_note`, два массива зачинов, без `structural_patterns` / `somatic_language`); пункт рассинхрона убран из **`docs/04_workspace/open_questions.md`**.
- 2026-05-09: **`gemini.ts`**: env **`AI_MODEL_STANDARD_FALLBACK`** / **`AI_MODEL_PREMIUM_FALLBACK`**, автоповтор при перегрузке Gemini, человекочитаемая ошибка после исчерпания попыток; **`Communicator`** / **`DailyRecommendationCard`**: отладочная подпись модели только в **`__DEV__`**, нормализация текста ошибки перегрузки в **`Alert`**. Обновлены **`docs/02_modules/{assistant,communicator,daily_forecast,infra}`**, **`.env.example`**, **`DEPLOY.md`**.
- 2026-05-09: pre-push diff: новые **`_legacy_web/app/api/_utils/explicitSignals.ts`** и **`softCap.ts`** — обновлены **`docs/02_modules/assistant/spec.md`** (архитектура, env soft cap), **`history.md`**, **`docs/00_index/MAP.md`**; в **`docs/04_workspace/open_questions.md`** уточнён пункт про **`dialog_signals.json`** (есть импорт в утилите, нет вызова из маршрута диалога).
- 2026-05-09: pre-push diff: обновления `_legacy_web/data/author_voice.json`, `chakra_states_baseline.json`, новый `dialog_signals.json`, косметика `gemini.ts` — в **`docs/04_workspace/open_questions.md`** (раздел `assistant`) зафиксировано отсутствие потребителей у `dialog_signals.json` (отдельно от интеграции **`detectExplicitSignals`** в маршрут v4).
- 2026-05-08: docs-sync перенесён на GitHub Actions; локальный post-commit оставлен как fallback.
- 2026-05-08: docs-sync переведён с pre-push на post-commit hook.
- 2026-05-08: pre-push docs-sync: cursor-agent аутентифицируется через CURSOR_API_KEY из .env.local; устраняет EPERM при push изнутри Cursor IDE.
- 2026-05-08: откат docs-sync к синхронному режиму через изолированный HOME (схема с очередью отменена как требующая ручного запуска).
- 2026-05-08: экран **`PracticeCatalogScreen`**: гонки **`onLateYogaPractices`** / микрозадачи (**`await Promise.resolve()`**), срез медитация+дыхание в ref и отложенная йога (колбэк при **`loading`** больше не отбрасывается). **`docs/02_modules/practices/history.md`**, **`spec.md`**.
- 2026-05-08: каталог асан на экране «Практики»: для ветки с **`onLateYogaPractices`** в **`modules/practices/core/catalog.ts`** добавлены таймаут 12 с, обработка **`reject`** и повторный колбэк после таймаута при позднем ответе Supabase (раньше возможен «вечный» спиннер). Обновлены **`docs/02_modules/practices/spec.md`**, **`history.md`**.
- 2026-05-08: pre-push docs-sync: в **`scripts/docs-sync/sync.sh`** для **`cursor-agent`** используется изолированный **`HOME`** под **`.git/cursor-agent-home`** (и однократное копирование **`cli-config.json`**), чтобы не конфликтовать с IDE по **`~/.cursor/cli-config.json.tmp`** (EPERM при push из Cursor).
- 2026-05-08: исправление обрезанных ответов Gemini: в **`_legacy_web/app/api/_utils/gemini.ts`** для **`gemini-2.5*`** в `generationConfig` — **`thinkingConfig: { thinkingBudget: 0 }`** (thinking не съедает **`maxOutputTokens`** у видимого текста); **`gemini-3*`** без принудительного `thinkingConfig` (глубина по умолчанию API). Ранее в этот же день: merge SSE/`complete`, fallback пустого ответа, пол **`maxOutputTokens`** в **`communicator/v2/dialog`**. Файлы: `gemini.ts`, `Communicator.tsx`, `communicator.ts` (i18n), `dialog/route.ts`, `communicator`/`assistant` `history.md`, `communicator/spec.md`.
- 2026-05-07: миграция модулей `webinars` и `author_presence` (формальные заглушки, завершение Волны 3). Источники: grep по коду, `modules/access/core/features.ts`, `supabase/migrations/20260423080000_init.sql`, архивные брифы тарифов. Созданы/обновлены: `docs/02_modules/webinars/spec.md`, `dependencies.md`, `history.md`; `docs/02_modules/author_presence/spec.md`, `dependencies.md`, `history.md`, `integrations.md` (ссылка на триаду). Уточнён `docs/00_index/MAP.md` (точки входа для частичного задела). Обновлены `docs/04_workspace/open_questions.md` (разделы `webinars`, `author_presence`).
- 2026-05-07: миграция модуля `communicator` (волна 3). Источники: код `modules/communicator/**/*`, `services/communicator-client.ts`, `services/communicatorConfig.ts`, `app/(tabs)/index.tsx`, `app/calibration.tsx`, контекст архива `docs/05_archive/migrated/assistant/MODULE_4_AIAssistant_TZ.md`, `docs/05_archive/migrated/practices/assistant_practice_recommendation_brief.md`. Созданы/обновлены: `docs/02_modules/communicator/spec.md`, `dependencies.md`, `history.md`. Перенесены в архив: `docs/05_archive/migrated/communicator/Коммуникатор.txt` (бывший `docs/tmp_docs/Коммуникатор.txt`). Уточнён `docs/00_index/MAP.md` (`communicator`, `practices` — парная колонка «От него зависят»; `code_refs`). Обновлены `docs/04_workspace/open_questions.md` (раздел `communicator`).
- 2026-05-07: миграция модуля `assistant` (волна 3). Источники: код `_legacy_web/app/api/ai/monologue/route.ts`, `_legacy_web/app/api/communicator/v2/dialog/route.ts`, `_legacy_web/app/api/_utils/{orchestrator,insightDetection,scenarios,prompts,userModelTier,authorVoice,topPetals,mathLevelBuilder,scenarioCache}.ts`, `_legacy_web/app/api/communicator/v2/dialog/practiceSelection.ts`, `_legacy_web/config/contentLengths.ts`, `services/aiClient.ts`, `services/communicator-client.ts`, `supabase/migrations/20260501173500_scenarios_architecture.sql`, `supabase/seed.sql`, миграции фаз/монологов; исторический контекст в `docs/tmp_docs/29042026/` (до архивации). Созданы/обновлены: `docs/02_modules/assistant/spec.md`, `dependencies.md`, `history.md`. Перенесены в архив: `docs/05_archive/migrated/assistant/PATCH_8_author_voice_system_prompt.md`, `PATCH_9_phase_prompts_library.md`, `PATCH_10_insight_engine.md`, `PATCH_12_scenarios_architecture.md`, `PATCH_13_monologue_prompts.md`, `MODULE_4_AIAssistant_TZ.md`. Уточнён `docs/00_index/MAP.md` (`assistant`, `daily_forecast` code_refs; у `assistant` колонка «От него зависят»: `daily_forecast`). Обновлены `docs/04_workspace/open_questions.md` (раздел `assistant`).
- 2026-05-07: `docs/04_workspace/open_questions.md` (§ `practices`): уточнён пункт про `**user_practice_preferences**` / триггер `practice_sessions_update_prefs` и отсутствие `practice_id` у breath — добавлены продуктовое намерение (round-robin асан 15 дней, breath ~7 дней / с ростом каталога, медитация одна), варианты исправления и отдельный пункт про **15-дневное окно для асан** как намеренное правило со ссылкой на `_legacy_web/shared_core/selector.ts`. Версия файла **1.9**.
- 2026-05-07: уборка заглушек модуля `**practices`** после миграции: удалены `docs/02_modules/practices/analytics.md`, `asanas.md`, `breathing.md`, `components.md`, `meditation.md`, `report.md` (TODO-скелеты 2026-05-06; канон — триада `spec.md` / `dependencies.md` / `history.md` по `docs/03_rules/migration_protocol.md`). Ссылка в `docs/00_index/GLOSSARY.md` (термин MeditationSession) перенаправлена на `docs/02_modules/practices/spec.md`. Перекрёстные упоминания удалённых имён файлов вычищены в `docs/_proposal.md` и `docs/03_rules/change_protocol.md`.
- 2026-05-07: миграция модуля `**practices**` (волна 3). Источники: код `modules/practices/*`, `modules/breath/*`, `modules/mandala/experiments/SacredSymbolStreamScreen.tsx`, `app/(tabs)/practices.tsx`, `app/breath-coherence.tsx`, `app/sacred-symbol-stream.tsx`, `app/asana-practice.tsx`, `app/(tabs)/index.tsx`, `services/practiceSessions.ts`, `_legacy_web/app/api/communicator/v2/dialog/practiceSelection.ts`, `docs/_proposal.md`. Созданы/обновлены: `docs/02_modules/practices/spec.md`, `docs/02_modules/practices/dependencies.md`, `docs/02_modules/practices/history.md`. Перенесены в архив: `docs/05_archive/migrated/practices/practices_module_brief.md`, `assistant_practice_recommendation_brief.md`, `access_tiers_navigation_brief.md`, `vimeo_asanas_import_brief.md` (бывшие `docs/tmp_docs/02052026/*.md`). Уточнён `docs/00_index/MAP.md` (`code_refs` для `practices`). Обновлён `docs/04_workspace/open_questions.md` (раздел `practices`).
- 2026-05-07: продуктово зафиксировано: `**modules/breath/**` — внутренняя реализация дыхательного подсценария в составе `**practices**`, не отдельный модуль в `MAP.md`. В `docs/02_modules/audio/dependencies.md` §1 пункт «breath» объединён с `**practices**`; в `MAP.md` у `audio` в «Зависит от» снова `**practices**`, удалено примечание про граф breath; обновлены `audio/spec.md`, парность в `practices/dependencies.md` §2.
- 2026-05-07: уточнён граф `audio` ↔ `bindu` ↔ `practices` по коду: такт и таймлайн (`timeline.ts`, `MandalaSoundProvider`) — в `mandala-sound`; вход фаз — `modules/breath` (`PlannedCycle`), не `modules/practices`; контракт яркости/триггеров — `modules/mandala/core`. Обновлены `docs/02_modules/audio/*`, `bindu/dependencies.md`, `practices/dependencies.md` (парность §2), `MAP.md` (колонка «Зависит от» у `audio`, примечание про `breath`), `docs/04_workspace/open_questions.md` (пакетные границы mandala ↔ mandala-sound).
- 2026-05-07: ревизия консистентности графа зависимостей перед волной 3 (мигрированные модули): выровнен `MAP.md` под `docs/02_modules/*/dependencies.md` и `spec.md` (колонки «Зависит от» / «От него зависят», `code_refs` для `astro`, `daily_forecast`, `biofeedback`, `calibration`, `subscription`, `profile`, `practices`); дополнен `docs/02_modules/practices/dependencies.md` парными связями с `audio`, `subscription`, `daily_forecast`, `biofeedback`, `bindu`, `infra`. В `docs/04_workspace/open_questions.md` добавлен пункт про `recentPlanetsOfDay` / клиентский стек планеты дня.
- 2026-05-07: миграция модуля `daily_forecast`. Источники: код `modules/home/useDayContent.ts`, `app/(tabs)/index.tsx`, `services/dailyForecastClient.ts`, `services/dayContentCache.ts`, `services/globalContentClient.ts`, `modules/daily-engine/*`, `_legacy_web/app/api/astro/daily-forecast/route.ts`, `supabase/functions/daily-forecast/index.ts`, `supabase/functions/precompute-daily-forecasts/index.ts`, `supabase/functions/_shared/daily-engine-parity.test.ts`, `docs/_proposal.md`, архив `docs/05_archive/migrated/astrology/MODULE_2_DailyEngine_TZ.md` (контекст намерений, не цитировался в spec). Перенесены в архив: `docs/05_archive/migrated/daily_forecast/PATCH_3_forecast_cache_timezone.md`, `docs/05_archive/migrated/daily_forecast/PATCH_4_engine_parity.md` (бывшие `docs/tmp_docs/29042026/PATCH_3_forecast_cache_timezone.md`, `PATCH_4_engine_parity.md`). Удалён дубликат брифа: `docs/tmp_docs/02052026/spiral_day1_day2_plan.md` (канон — `docs/05_archive/old_briefs/spiral_day1_day2_plan.md`). Уточнён `MAP.md` (`code_refs`, зависимость от `calibration`). Обновлены `docs/04_workspace/open_questions.md` (доп. пункт про покрытие parity-теста).
- 2026-05-07: миграция модуля `profile`. Источники: код `modules/auth/*`, `app/(tabs)/profile.tsx`, `app/(tabs)/index.tsx`, `app/onboarding.tsx`, `app/_layout.tsx`, `services/natalProfileClient.ts`, `services/practiceSessions.ts`, `modules/location/acquireAndPersistUserCoordinates.ts`, `_legacy_web/app/api/astro/natal/route.ts`, `docs/_proposal.md`, `docs/tmp_docs/29042026/MASTER_README.md` (контекст расхождений). Отдельных файлов в `docs/modules/` под profile не найдено — перенос в архив не потребовался. Уточнён `MAP.md` (`code_refs` для `profile`); в `docs/02_modules/practices/dependencies.md` добавлена парная связь «`profile` → `practiceSessions`»; в `docs/04_workspace/open_questions.md` добавлен раздел `subscription` (два пункта). Чистка `docs/planning/`: `spiral_day1_day2_plan.md` → `docs/05_archive/old_briefs/spiral_day1_day2_plan.md` (статус-блок в шапке).
- 2026-05-07: миграция модуля `subscription`. Источники: код `modules/access/*`, потребители (`app/(tabs)/*`, `app/asana-practice.tsx`, `modules/practices/ui/PracticeCatalogScreen.tsx`, `modules/home/useDayContent.ts`, `services/globalContentClient.ts`, `services/dayContentCache.ts`, `_legacy_web/app/api/_utils/userModelTier.ts`, communicator v2 routes, `global-content`), `docs/_proposal.md`, `docs/04_reference/product/tier_model.md`. Перенесены в архив: `docs/05_archive/old_briefs/access_tiers_navigation_brief.md` (бывший `docs/planning/access_tiers_navigation_brief.md`), `docs/05_archive/migrated/subscription/PATCH_14_free_tier.md` (бывший `docs/tmp_docs/29042026/PATCH_14_free_tier.md`). Уточнён `MAP.md`: расширены `code_refs` для `subscription`.
- 2026-05-07: миграция модуля `biofeedback`. Источники: код `modules/biofeedback/*`, `modules/breath/ui/CoherenceBreathScreen.tsx`, интеграции `mandala-sound` / `BreathBinduMandala`, `docs/_proposal.md`. Перенесены в архив: `docs/05_archive/migrated/biofeedback/biofeedback-architecture.md`, `docs/05_archive/migrated/biofeedback/biofeedback-parity-contract.md` (бывшие `docs/biofeedback-architecture.md`, `docs/biofeedback-parity-contract.md`). Уточнён `MAP.md`: у `biofeedback` в колонке «Зависит от» убрана ошибочная зависимость от `audio` (направление потока данных — от biofeedback к audio).
- 2026-05-07: наполнены `docs/02_modules/astro/caching_strategy.md`, `docs/02_modules/astro/interpretation_layers.md`; дополнён `docs/02_modules/astro/history.md` (мотивация серверного натала); в `docs/02_modules/daily_forecast/spec.md` добавлена отсылка к стратегии кэша.
- 2026-05-07: миграция модуля `astro`. Источники: `docs/_proposal.md` (раздел astro), `docs/05_archive/migrated/astrology/MODULE_1_AstroCore_TZ.md` (контекст и расхождения с кодом), код `modules/astro-core/*`, `_legacy_web/app/api/astro/natal/route.ts`, `_legacy_web/app/api/_utils/astro-db.ts`, `services/natalProfileClient.ts`, потребители (`daily-engine`, `calibration`, communicator routes). Активный архивный ТЗ уже в `docs/05_archive/migrated/astrology/`; дополнительных переносов файлов не потребовалось. Уточнены двусторонние зависимости с `calibration` в `docs/02_modules/calibration/dependencies.md`.
- 2026-05-06: миграция модуля `calibration`. Источники: `docs/modules/calibration_and_orchestrator.md`, `docs/tmp_docs/29042026/MODULE_3_Calibration_TZ.md`, `docs/tmp_docs/29042026/PATCH_1_M3_averaging_ratio.md`, `docs/tmp_docs/29042026/PATCH_6_proposal_pending_fix.md`, `docs/tmp_docs/29042026/chakra_states_baseline.json.txt`. Перенесены в архив: `docs/05_archive/migrated/calibration/calibration_and_orchestrator.md`, `docs/05_archive/migrated/calibration/MODULE_3_Calibration_TZ.md`, `docs/05_archive/migrated/calibration/PATCH_1_M3_averaging_ratio.md`, `docs/05_archive/migrated/calibration/PATCH_6_proposal_pending_fix.md`, `docs/05_archive/research_assets/calibration/chakra_states_baseline.json.txt`.
- 2026-05-06: миграция модуля `infra`. Источники: `docs/tmp_docs/29042026/PATCH_5_RLS_tightening.md`, `docs/tmp_docs/29042026/PATCH_11_whisper_quality.md`, `docs/tech_stack.md` (сверка по PWA/API), `docs/remote-play/README.md` (секция Remote Play для `pwa.md`), `docs/android-adaptation-notes.md` (только перекрёстные пометки в `history.md`). Перенесены в архив: `docs/05_archive/migrated/infra/PATCH_5_RLS_tightening.md`, `docs/05_archive/migrated/infra/PATCH_11_whisper_quality.md`.
- 2026-05-06: миграция модуля `bindu`. Источники: `docs/modules/bindu_succession_lab.md`, `docs/modules/bindu_succession_visual_spec_v2.md`, `docs/modules/mandala.md`, `docs/meditation_video_generator_spec.md`, `docs/modules/visual_module_map.md`. Перенесены в архив: `docs/05_archive/migrated/bindu/bindu_succession_lab.md`, `docs/05_archive/migrated/bindu/bindu_succession_visual_spec_v2.md`, `docs/05_archive/migrated/bindu/mandala.md`, `docs/05_archive/migrated/bindu/meditation_video_generator_spec.md`, `docs/05_archive/migrated/bindu/visual_module_map.md`.
- 2026-05-06: создан `03_rules/migration_protocol.md` как стандарт разовой миграции модуля по референсу `audio`.
- 2026-05-06: миграция модуля `audio`. Источники: `docs/modules/mandala_sound.md`, `docs/tmp_docs/Мандала/Звук.txt`. Перенесены в архив: `docs/05_archive/migrated/audio/mandala_sound.md`, `docs/05_archive/migrated/audio/Звук.txt`.
- 2026-05-06: создан скелет новой документации по `_proposal.md`. Старая документация продолжает функционировать параллельно до завершения миграции.
- 2026-05-06: в новой структуре создано `75` файлов и `26` папок. Старая структура `docs/` оставлена нетронутой и продолжает существовать параллельно.
- 2026-05-06: скорректирован `00_index/MAP.md`: зависимости приведены к двусторонней согласованности, запись `error_tracking` убрана как отдельная строка, а `report` удалён из зависимых модулей `practices`.

