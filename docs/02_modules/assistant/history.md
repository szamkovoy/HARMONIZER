---
id: 02_modules/assistant/history
title: Assistant History
version: 2.61
updated: 2026-06-09
depends_on: [01_foundation/product_model, 02_modules/astro/spec, 02_modules/practices/spec, 02_modules/subscription/spec]
code_refs: [_legacy_web/app/api/communicator/v2/dialog/route.ts, _legacy_web/app/api/communicator/v2/dialog/practiceCardSummary.ts, _legacy_web/app/api/_utils/markers.ts, _legacy_web/app/api/_utils/gemini.ts, supabase/migrations/20260501173500_scenarios_architecture.sql, supabase/migrations/20260501185700_monologue_prompts_v2.sql, supabase/migrations/20260511140000_revert_dialog_quality_v4.sql]
---

## Decision Log

- **2026-06-09 (7):** Pre-push doc-sync: удалены legacy helper-модули daily dialog (`dialogArcOrchestrator.ts`, `dialogOpeningHints.ts`, `dialogDebugExport.ts`, `dueSummaryState.ts`, `planningReconciliation.ts`, `summaryRepair.ts` + tests); активный `prompts-reference.md` перенесён в `docs/05_archive/migrated/assistant/dialog_v7_prompts-reference.md`. `MAP.md` и `spec.md` §3 больше не ссылаются на удалённые файлы; §2 и архивный prompts-reference сохраняют описание v7-контракта до появления FSM brain в коде.
- **2026-06-09 (6):** `topPetals.ts` и `globalTransitMath.ts` переведены на `chakraLabelRu` из `modules/chakra/labels.ts`; `getActivePrompt()` в `prompts.ts` реализует fallback на последнюю версию prompt-а при отсутствии active-флага (ранее задокументировано, теперь в коде).
- **2026-06-09 (5):** Pre-push doc-sync: активирован `dialog_system_v3` v7 (`20260608203500_activate_dialog_system_v3_v7.sql`); `prompts-reference.md` §1–§3.1 синхронизирован с v7 migration, обновлёнными `ORCHESTRATOR_INSTRUCTIONS` (`opening`/`inquiry`/`final_recommendation`) и server-side guardrails `buildDialogSystemInstruction`; `spec.md` §4 зафиксировал v7 как текущий runtime prompt.
- **2026-06-09 (4):** Planning persistence очищен от chakra-tail. `markers.ts`, `planningReconciliation.ts`, `lifeMatrix.ts` и `route.ts` перевели `PLANNED_EVENT` на sphere-only контракт: новый preferred marker — `spheres="1:0.6;4:0.4"` (legacy `cells="sphere:chakra:weight"` всё ещё читается), а в `planned_events.cells` теперь канонизируется только распределение по сферам жизни без chakra/weight-логики матрицы. `GET /api/day` и debug export начали отдавать для planning rows те же sphere-only данные, а `upsertDailyMatrixForDate(...)` больше не строит `daily_matrices.source = "plan"` из будущих событий: матрица дня снова зависит только от summary/outcome-cells, что убирает лишние токены и снижает риск ложных решений по ещё не прожитым действиям.
- **2026-06-09:** Dialog SSE при ошибке responder: `route.ts` шлёт событие `error` с user-facing текстом и закрывает стрим через `controller.close()` вместо `controller.error`; `reportRouteError` логирует штатную перегрузку LLM как Sentry `warning` (`expected_llm_unavailable`), `sentry.server.config.ts` фильтрует `failed to pipe response`.
- **2026-06-09 (3):** QA по выгрузке `текст-4E28-AF69-8E-0.txt` привёл к ещё одному tightening summary/runtime-контуров. `summaryRepair.ts` и `route.ts` больше не считают summary завершённым только по факту `«съездил / получилось / не успел»`: если событие состоялось, но в ответе не хватает lived-state, ассистент должен потратить единственный уточняющий ход именно на это, с коротким объяснением про матрицу состояний. Одновременно `planningReconciliation.ts` получил явный safe-path для событий без вкладов в матрицу: если outcome classifier вернул пустые `outcome_cells` (событие не состоялось или state так и не был собран), row всё равно закрывается и удаляется из `planned_events`, но не merge-ится в `daily_matrices`. В том же проходе Home summary получил тот же `dayHealthContext`, что и Day-tab summary, server `complete/messages.meta` начали писать `latencyMs`, per-turn `matrix_cells` в dialog meta/debug перестали дублировать planning-cells и теперь отражают именно summary/matrix-вклад, а `GET /dialog?debugExport=1` стал всегда отдавать `dialogStateAfter`, чтобы client export видел `summarized` rows и их `outcome_cells` даже вне глобального server debug mode.
- **2026-06-09 (2):** Follow-up QA по уже обновлённому planning-flow закрыл ещё два persistence-хвоста. Во-первых, `planningReconciliation.ts` теперь сразу добавляет freshly inserted `planned_events` в `mutableOpenPlans` того же reconcile-прохода; раньше второй такой же кандидат из той же очереди мог снова пройти как `create_new`, потому что duplicate-check не видел запись, вставленную несколькими строками выше. Во-вторых, queued planning-candidate больше не остаётся без текста рекомендации только из-за пропущенного model-marker: если `recommendation` отсутствует, reconcile now synthesizes короткий fallback по `user_daily_forecasts.day_target_chakra`, чтобы во вкладке «День» не показывался placeholder вместо совета по действию.
- **2026-06-09:** Follow-up QA по home planning-flow закрыл уже не частные, а системные причины нового набора регрессий. Во-первых, после summary-части ветка `planning` могла тихо отпасть из-за anti-replan логики, даже если пользователь уже начал называть новые действия; `route.ts` теперь повторно удерживает planning по current-turn inferred events и по последнему assistant-turn с planning-branch meta. Во-вторых, server-side guardrails и `dialogArcOrchestrator.ts` жёстко запретили в planning вытягивать технические детали события и психологические состояния, если для planning они не нужны: время и подробности уровня `что именно с машиной` больше не собираются без прямого сигнала пользователя. В-третьих, planning получил отдельный 7-дневный `planningSphereLens` из `daily_matrices`: prompt видит наименее представленные сферы жизни последних активных дней и может осмысленно, а не случайно, предлагать расширение дня. Наконец, planning-финал и practice-финал разведены по содержанию: planning должен нести общий фокус дня через `[CORRECT_RECOMMENDATION]` и per-event советы, а practice-ветка после уже готового planning-итога говорит только о самой практике.
- **2026-06-09:** Follow-up QA по add-flow вкладки «День» закрыл два связанных бага. Во-первых, planning-ветка могла честно дойти до `final_recommendation`, но не вернуть ни одного `PLANNED_EVENT`, если сервер эскалировал в финал по `inferredPlanningArtifactsFromHistory`; из-за этого новые действия не попадали в `planned_events`. `route.ts` теперь применяет safe fallback и отправляет inferred planning-artifacts в delayed persistence, если финал уже случился, а invisible markers так и не пришли. Во-вторых, Day add-flow теперь требует fresh chat start на клиенте: старый `entrySource: "day"`-чат не должен подмешиваться в новую ветку добавления.
- **2026-06-08 (8):** Follow-up QA по выгрузкам `текст-4920-9ABF-1D-0.txt` и `текст-45AE-AF1A-56-0.txt` довёл Day dialog до более жёсткого server-side поведения. `route.ts` теперь различает явный `triggerMeta.dayTabMode = "add" | "plan"` и не даёт backend-ветвлению вернуть пользователя в summarizing только из-за старых due-событий. Для `daySummaryRequested` добавлен deterministic auto-advance: один due-event в prompt, максимум два вопроса на событие, автоматический synthetic `[SUMMARIZE_EVENT]` по явному ответу пользователя или после второго расплывчатого ответа, затем переход к следующему событию/финалу без forced practice-card. Финальный summarizing guardrail дополнительно требует упомянуть каждое подытоженное действие и называть конкретные health-цифры, если они реально пришли в `dayHealthContext`.
- **2026-06-08 (7):** Daily dialog получил ещё один prompt/runtime polish-pass под QA summary-ветки. Новый `dialog_system_v3` v7 фиксирует friendly non-final contract: вне финалов ассистент не играет психолога, приветствия `доброе утро/день/вечер` требуют `!`, а тональность целевой чакры обязана сохраняться во всех трёх ветках, не только в recommendation. Одновременно server-side guardrails усилили summarizing: Day tab теперь напоминает модели работать только с текущим `due_event`, а промежуточные ходы по событию запрещают давать feedback до общего финального резюме дня.
- **2026-06-08 (6):** После `db push` обнаружился второй runtime-blocker: в `public.prompts` не осталось активной строки `dialog_system_v3`, и daily dialog падал с `Active prompt not found`. Runtime теперь защищён с двух сторон: v6 prompt снова активирован в БД, а `getActivePrompt()` при пустом active-флаге аварийно берёт последнюю версию prompt-а и пишет предупреждение вместо 500.
- **2026-06-08 (5):** Daily dialog для вкладки «День» упирался не в prompt/runtime, а в БД: `conversations.entry_source` всё ещё был ограничен старым CHECK без значения `day`. Миграция `20260608153000_allow_day_entry_source.sql` расширяет `conversations_entry_source_check`, чтобы `entrySource: "day"` мог создавать conversation и summary/planning-ветки вкладки «День» реально запускались.
- **2026-06-08 (4):** `dayHealthContext` больше не ограничен йогой: клиентский native provider подставляет реальные внешние health-метрики при доступных правах (steps, active calories, workout/exercise minutes, sleep duration) и статус `available` / `permission_denied` / `unavailable`. Серверный prompt остаётся защитным: использует только переданные значения и не выдумывает health-цифры при пустом контексте.
- **2026-06-08 (3):** Summary-ветка вкладки «День» получила временный `dayHealthContext`: route форматирует йогу за день, сравнение с обычной практикой и статус Apple/Google Health для LLM. Финальный summarizing guardrail теперь требует сначала психологическое резюме по целевой чакре/состояниям, затем 1-2 наблюдения о йоге/здоровье только по реально переданным данным; при `unavailable` provider модель не должна выдумывать шаги, сон, калории или тренировки.
- **2026-06-08 (2):** QA по выгрузке `текст-4D6D-9987-D4-0.txt` развёл planning/practice жёстче. `dialogArcOrchestrator.ts` больше не задаёт вопрос о практике в opening и запрещает в planning уточнять состояния/подтекст/примерные части дня; `route.ts` перестал мержить rule-based inferred events в persisted planning markers и больше не сохраняет `PLANNED_EVENT` с промежуточных `inquiry`-ходов. `planningReconciliation.ts` сохраняет порядок действий по `display_order`, добавленные позже действия ставит после уже существующих и не стирает старую рекомендацию пустым update. `practiceSelection.ts` больше не трактует `PRACTICE_PICK id="default"` как дыхание, если история уверенно говорит про асаны/медитацию/дыхание.
- **2026-06-08:** Auto-expiry для неподытоженных `planned_events` отключён: `expireStalePlannedEvents()` больше не удаляет старые rows, `loadDuePlannedEvents()` больше не режет due-события нижним 36-часовым окном, а `dueSummaryState` больше не удаляет ignored due-rows после reminder. Planning guardrails дополнительно требуют короткий `desc` для списка действий и перенос деталей в `recommendation`.
- **2026-06-07:** Добавлена foundation-вертикаль вкладки «День»: миграция `20260607183000_day_tab_foundation.sql` расширяет `planned_events` полями `display_order`, `recommendation_text`, `explicit_time_text` и создаёт `day_practice_offers` для одной pending-практики на день. `PLANNED_EVENT` теперь может нести `recommendation` и `display_order`, delayed reconcile сохраняет их в `planned_events`, а `triggerMeta.daySummaryRequested` заставляет daily dialog подытоживать все неподытоженные действия рабочего дня без planning-ветки.
- **2026-06-07:** Doc-sync по push daily dialog refinement: `route.ts` получил runtime-guard `buildPostRecommendationTimingGuard(...)` для `post_recommendation` (не спрашивать про итог практики, пока не прошла выбранная длительность и пользователь сам не сообщил о завершении); `hasRequiredBranchArtifacts` / bypass planning теперь опираются только на current-turn inferred planning-artifacts, а не на carry-over open rows; `formatDueEvents` / `formatOpenPlansHistoricalContext` режут prompt до 3 событий на секцию. Обновлены `spec.md`, `prompts-reference.md` §3–§3.1.
- **2026-06-04:** Follow-up QA по `текст-4E29-8568-EF-0.txt` сузил ещё три хвоста daily dialog. Во-первых, `dueSummaryState.ts` поднял окно текущего summarizing с `2` до `3` due-событий, чтобы opening не терял третий уже наступивший эпизод. Во-вторых, `summaryRepair.ts` перестал считать обычное согласование новой практики (`«да, давай 5 минут медитации»`) ответом на remaining single due-event: single-due fallback теперь блокируется на practice-selection формулировках, иначе summary чужого события мог ошибочно приклеиться к последнему оставшемуся due-row. В-третьих, `dialogArcOrchestrator.ts` + server-side guardrails ужесточили post-recommendation и single-event final: после карточки ассистент больше не должен сам спрашивать `как прошла практика?`, пока пользователь не сообщил о завершении, а один конкретный дневной эпизод описывается как прямой совет к действию, а не как расплывчатое «предсказание дня». Заодно `plannedEventInference.ts` перестал тянуть в planning-memory past-tense summary-сегменты вроде `«почитал, поужинал, хороший вечер был»`.
- **2026-06-03:** Follow-up QA по выгрузке `текст-4106-A447-F2-0.txt` уточнил safe refinement. `summaryRepair.ts` перестал repair-ить только single-due сценарий: теперь backend может распознать, что user-text уже является итогом конкретного due-события, и повторить standard-pass ради invisible `[SUMMARIZE_EVENT]` даже при нескольких due-эпизодах. Параллельно `dueSummaryState.ts` больше не удаляет unanswered remainder сразу после partial summary: оставшийся due-event остаётся активным ещё на один ход, чтобы диалог успел вернуться к нему вместо молчаливой потери. В том же проходе `plannedEventInference.ts` жёстче отделил practice-выбор и supportive details от planning-memory (практика `час дыхания, три минуты` и фразы вида `нужно будет почувствовать его интересы...` больше не должны канонизироваться как `planned_events`), `markers.ts` получил RU-эвристику против STT-конфликта `час X, три минуты`, а `dialogArcOrchestrator.ts`/server guardrails сузили multi-event финал до структуры `общий фокус дня -> один абзац на событие -> практика`, чтобы не разрывать одно событие на две туманные части и не добавлять свободные «теневые» риски вне связи с реальным контекстом пользователя.
- **2026-06-03:** Реализован safe daily dialog core refinement без смены продуктовой логики выбора чакры дня. `route.ts` теперь держит `trigger_meta.due_summary_state` для текущих prompted due-events и проводит цикл `спросили -> один reminder -> delete`, физически удаляя проигнорированные `planned_events` через `deletePlannedEventsByIds(...)`, а не через `dismissed`. В том же проходе prompt/orchestrator были сужены к `1-3` важным событиям, summarizing стал явно собирать не только outcome, но и **как** событие было прожито, opening рекомендует разводить жизненный вопрос и вопрос о практике на два коротких абзаца, а final-инструкции допускают короткие per-event блоки для multi-event days.
- **2026-06-02:** QA по диалогу `746f1124-97d9-4b23-a4cb-83fe98dbc4ec` показал сбой ветвления: `route.ts` считал ветку `planning` уже «собранной», если на горизонте today/tomorrow есть любые открытые `planned_events`, даже если это те же самые due-события, пришедшие в summarizing. Из-за этого сервер мог эскалировать в `final_recommendation` после одного `[SUMMARIZE_EVENT]` и выбора практики, не спросив о новых планах. Теперь carry-over planning больше не закрывает ветку сам по себе: planning должен подтверждаться новым planning-артефактом текущего диалога, а существующие open rows остаются только памятью для dedupe/update.
- **2026-06-02:** QA по диалогу `feff4790-5ea1-49be-8024-57efa69de167` показал, что при `TEST_MODE_FORCE_PHASE=morning` untimed/fallback-план мог стать `due` раньше симулируемого времени, если реальный локальный час устройства уже ушёл дальше representative-часа forced phase. `dialogDailyContext.ts` / `lifeMatrixPersistence.ts` теперь считают `dueEvents` внутри dialog-context по forced-phase cutoff (`9 / 14 / 19`), сохраняя при этом реальное 36-часовое expiry-окно `planned_events`. В том же проходе `plannedEventInference.ts` перестал поднимать meta-фразы вроде `«предстоит интересный день»`, игнорирует голые time-clarification реплики вроде `«планирую на 8»` и умеет расклеивать независимые coordinated-actions (`фильм` + `ужин`) без разрыва поддерживающих пар вроде `погулять и подышать свежим воздухом`.
- **2026-05-27:** Delayed summary reconcile в `planningReconciliation.ts` перестал вычислять `outcome_cells` тем же смешанным low-cost prompt-ом, что и planning anti-duplicate. Теперь summary-кандидаты проходят двухшаговую цепочку внутри того же idle/end reconcile-pass: сначала low-cost normalization удерживает primary domain события в компактном `normalized_outcome`, затем отдельный classifier prompt классифицирует `outcome_cells` по `life_spheres_baseline` + `chakra_baselines`, уже без planning-очереди и без шумного сырого диалогового контекста. Это уменьшает semantic bleed, где побочный эффект (`лучше спалось`, `задумался о смысле`) мог увести театральный/контрактный эпизод в нецелевую сферу.
- **2026-05-27:** После QA на `текст-4C53-BA7A-F5-0.txt` planning inference в `plannedEventInference.ts` перестал терять несколько планов из одной длинной спонтанной реплики. Разбиение пользовательского текста теперь удерживает короткие daypart/time-хвосты (`вечером`, `примерно в 11:30`) рядом с соседним событием, а dedupe больше не схлопывает разные планы только из-за одинакового synthetic default time. В том же проходе post-practice replace-flow стал устойчивым к промежуточному `post_recommendation`: запрос `другая практика` теперь остаётся `practice_repick`, даже если перед ним уже было короткое `Спасибо!` / `Хорошего дня`.
- **2026-05-27:** QA по `текст-4D64-AFFA-FE-0.txt` вскрыл server-side разрыв после `complete`: `upsertConversationSummary()` в `lifeMatrixPersistence.ts` вызывался без `onConflict`, поэтому на втором и последующих assistant-turn срабатывал `23505` по `conversation_summaries.conversation_id`. Клиент уже видел видимый ответ, но сервер падал до `persistAssistantMessage()` / `turn_artifacts`, из-за чего новые assistant-ходы не появлялись в БД, а queued planning/summarizing оставались в `trigger_meta`. Фикс: upsert теперь конфликтует по `conversation_id`.
- **2026-05-27:** `prompts-reference.md` v1.16 синхронизирован с QA-фиксами и runtime: §1 снова дословно зеркалит v6 (6–20 дыхание, «больше 20» асаны); добавлен §3.1 про backend-каталог **1–5 / 5–20 / 20–70**, `validateHistoryHasDurationAndType`, approximate/synthetic planning-time в `{{due_events}}` / `{{historical_context}}`, premium bridge-lock и branch gating; §2 выровнен с `dialogArcOrchestrator.ts` (убрана устаревшая строка про multi-type из `inquiry`).
- **2026-05-26:** После QA на `текст-4AAB-A7D7-93-0.txt` практика и planning-context были разведены жёстче. `validateHistoryHasDurationAndType` больше не даёт поздним clock-time фразам вроде `«четыре часа дня»` перезаписывать уже подтверждённые `15 минут дыхания`, а assistant-side catalog range для асан был опущен до реалистичного потолка вместо прежних 480 минут. Параллельно `historical_context` перестал показывать synthetic fallback/daypart время как будто пользователь сам назвал точный час: non-explicit планы теперь помечаются как approximate/synthetic, чтобы модель не делала ложные выводы о жёстких пересечениях.
- **2026-05-26:** Delayed reconcile для daily dialog расширен с planning-only до общего artifact-pass. `route.ts` больше не делает inline merge/delete для `[SUMMARIZE_EVENT]`: summary-кандидаты, как и planning-кандидаты, сначала копятся в `conversations.trigger_meta.pending_planning_reconciliation`, а low-cost LLM-step одним JSON-ответом одновременно решает anti-duplicate действия по `planned_events` и вычисляет `outcome_cells` для записи в `daily_matrices`.
- **2026-05-26:** Planning persistence для daily dialog стал delayed. `route.ts` больше не пишет новые `planned_events` прямо в assistant-turn: planning-candidates сначала складываются в `conversations.trigger_meta.pending_planning_reconciliation`, а затем отдельный helper `planningReconciliation.ts` после idle-окна канонизирует их в `planned_events` через low-cost JSON-step (`AI_MODEL_LOW`, fallback на `AI_MODEL_STANDARD`). Это уменьшает число дублей между перефразировками и разносит live UX диалога и запись в долговременную planning-memory. В том же проходе route начал использовать explicit provider-cache для стабильного префикса structured-dialog request, если текущая модель поддерживает cached content; при `deepseek-*` cache-path автоматически пропускается.
- **2026-05-26:** Финал daily dialog больше не моделируется как отдельная special-case ветка `practice_declined`. `dialogArcOrchestrator.ts` перевёл явный отказ от практики в общий режим `final_without_practice`, то есть в тот же финальный контракт, что и у обычного closing-turn, только без карточки. `route.ts` вслед за этим развёл два server helper-а: «ход финальный вообще» и «ход несёт карточку практики», поэтому SSE/meta/debug-export больше не пытаются заполнять `practicePicked` там, где финал сознательно заканчивается без практики. Для старых persisted meta сохранена обратная совместимость: legacy `practice_declined` нормализуется в `final_without_practice` при чтении истории.
- **2026-05-26:** После QA на `текст_4_1.txt` выяснилось, что `practice_declined` мог потерять summary-marker: пользователь уже подытоживал единственное due-событие, но сервер закрывал ход без `[SUMMARIZE_EVENT]`, и `planned_event` оставался открытым. `route.ts` получил отдельный retry-guard для missing-summary и на `practice_declined`, а инструкция `PRACTICE_DECLINED_SESSION_INSTRUCTION` теперь прямо разрешает invisible summary marker до видимого закрывающего ответа.
- **2026-05-26:** После QA на `текст_2_6.txt` post-practice хвост daily dialog снова стал stateful. Клиентский `turnHistory` теперь несёт минимальный `meta.practicePicked`, поэтому сервер видит уже показанную карточку даже при lean storage и корректно уходит в `post_recommendation` на простое `спасибо`. Для явной замены (`другая практика`, `не подходит`, `замени`) `dialogArcOrchestrator.ts` получил отдельный режим `practice_repick`: короткий premium-ход с новым `[PRACTICE_PICK]`, без повторного длинного финала. В том же проходе `markers.ts` научился читать explicit duration в обратном порядке (`йоги минут 40`), чтобы слово `небольшую` больше не схлопывало такие запросы до минимальных 20 минут.
- **2026-05-26:** После QA на `текст_2_5.txt` daily dialog получил более жёсткий anti-noise контур. `markers.ts` теперь распознаёт и фразы вида `«что касается практики, нет сейчас времени этим заниматься»` как session-wide отказ от практики, поэтому оркестратор уходит в `practice_declined` и не возвращается к гипотетической вечерней практике. Параллельно `route.ts` начал подмешивать в `{{historical_context}}` компактную сводку уже открытых планов пользователя (due / later today / tomorrow), а `dialogArcOrchestrator.ts` прямо запрещает продолжать широкие planning-вопросы, если этот каркас дня уже собран. В том же проходе `plannedEventInference.ts` перестал превращать реплики уровня `«нужно поработать, много дел, не знаю как пойдёт»` в отдельные `planned_events`.
- **2026-05-26:** После QA на `текст_2_4.txt` backend выровнял два remaining mismatch-а daily dialog. Во-первых, финальный premium-ход при `validation.confident` теперь получает server-side lock на точные `тип + минуты`, чтобы видимый текст рекомендации не спорил с `practice_picked.overrides` (`1 мин` в карточке больше не превращается в `5 минут` в мостике к практике). Во-вторых, `timeParser` научился в forced-phase test mode считать relative phrases (`через полчаса`, `через час`) от реального `context.nowLocal`, а не от искусственно сдвинутого `effectiveDialogNowLocal`, так что такие планы не рождаются сразу как почти просроченные due-events.
- **2026-05-26:** После QA на `текст_2_4.txt` planning inference перестал превращать причинные пояснения вроде `«потому что нужно ведь и спортом заниматься»` в отдельный `planned_event`, если конкретный эпизод уже назван соседним сегментом (`велосипед`). В том же проходе `plannedEventInference.ts` подчистил вводные оболочки `«думаю, что...»` / `«надеюсь, что...»`, чтобы `description` в `planned_events` ближе отражал сам план, а не модальность фразы пользователя.
- **2026-05-26:** `planned_events` anti-duplicate pass перевёл planning dedupe на единый matcher: `plannedEventInference.ts` теперь экспортирует `matchPlannedEventAgainstExisting`, который одинаково используется в augment/filter/persist. `route.ts` загружает open rows не только для текущей беседы, но и user-wide на горизонте `today/tomorrow`; совпадение `same` обновляет существующую row, а конфликт `same topic + same local day + другое explicit time` больше не вставляет вторую запись и уходит в `planningPersistence.skipped` как `ambiguous_time_conflict`. Параллельно history inference сузили до последних user-turns, а debug-export получил отдельный список `planningPersistence.updated`, чтобы QA видел merge existing row вместо молчаливого исчезновения кандидата.
- **2026-05-26:** QA follow-up по planning + practice parsing: `markers.ts` больше не подставляет duration из жизненного контекста, если рядом есть `короткая / минимальная` практика другого типа. Фразы вроде `«через 15 минут пойду в магазин, предложи короткую медитацию»` теперь дают `1 мин meditation`, а не conflict на `15 мин`. Параллельно `plannedEventInference.ts` расширил stopword-слой для identity matching (`пойду`, `собираюсь`, `сходить` и т.п.), чтобы общие уточнения `«в магазин я пойду»` схлопывались с уже найденным shopping-эпизодом вместо второго `planned_event`.
- **2026-05-26:** QA follow-up по mixed summary/planning и практике: `markers.ts` теперь трактует `короткая` / `minimal` practice request как нижнюю границу каталога (медитация 1 мин, дыхание 5 мин, асаны 20 мин), а при нескольких типах в одной реплике ориентируется на последнее явное предпочтение пользователя, чтобы фразы вроде `«...практики йоги, но сейчас хочу короткую практику дыхания»` не срывались обратно в асаны. Параллельно `route.ts` получил дополнительный retry standard-хода, если единственное due-событие уже явно подытожено пользователем, но модель не вернула `[SUMMARIZE_EVENT]`.
- **2026-05-26:** QA follow-up по planning/summarizing: `plannedEventInference.ts` перестал создавать новый `planned_event` из past-tense summary-фраз с daypart (`«я хорошо выспался этой ночью»`), а time-less leisure-планы вроде `«хочу посмотреть фильм»` теперь могут получить implicit evening `timeNorm` вместо тихого пропуска. Одновременно `dialogArcOrchestrator.ts` явно фиксирует правило `йога >= 20 минут = асаны` без отдельного переподтверждения в `inquiry`.
- **2026-05-26:** QA follow-up по planning-ветке: `ru`-resolver в `timeParser` больше не цепляет `завтра` внутри слов вроде `позавтракать`, `plannedEventInference.ts` получил implicit morning/evening defaults для `завтрак` / `театр`-подобных планов и склейку topic-preface (`«Относительно встречи с клиентом…»`) с уточняющим сегментом. Параллельно `route.ts` перестал вставлять новый `planned_event` при уточнении уже открытого события в той же беседе: вместо дубля обновляется существующая row с новым временем/клетками.
- **2026-05-25:** QA regression fix for planning/summarizing test mode: `lifeMatrixPersistence.ts` больше не сжимает 36-часовое окно доступности `planned_events` через `TEST_MODE_FAST_INTERVALS` — anti-replan и resume TTL остаются ускоренными, но cleanup / due-events живут реальные 36 часов после `expected_at`. В том же проходе `plannedEventInference.ts` перестал тащить хвосты запроса на практику в `description` планируемого события (`"…а пока предложи 12 минут дыхания"`), а `timeParser`-регрессия на `11.30` зафиксирована тестами.
- **2026-05-25:** QA follow-up по ветке `summarizing`: `route.ts` теперь сохраняет branch markers из standard-шага при эскалации `standard -> premium`, чтобы `[SUMMARIZE_EVENT]` / `[PLANNED_EVENT]` не терялись перед `persistDialogArtifacts()`. Параллельно `formatDueEvents(...)` отдаёт в prompt локализованное время пользователя вместо сырого UTC, `time_phrase_raw` при insert trim-ится, а `effectiveDialogNowLocal(...)` согласует forced phase с разбором planning-time фраз (`сегодня в 11.30`) во время тестирования.
- **2026-05-25:** QA regression fix for daily dialog planning: `route.ts` теперь может пропустить лишний standard-ход `inquiry` и сразу уйти в premium `final_recommendation`, если planning-событие уже извлекается из user history (`inferPlannedEventsFromUserHistory`) или открытых `planned_events`, а `validateHistoryHasDurationAndType` уже даёт `confident`. Параллельно `dialogArcOrchestrator.ts` ужесточил `inquiry`: на этапе planning не добирать сферу жизни/подтекст события и не требовать точную минуту внутри валидного диапазона (`10-15 минут дыхания`), а `markers.test.ts` зафиксировал этот кейс отдельным регрессионным тестом.
- **2026-05-25:** Planning branch больше не зависит только от `[PLANNED_EVENT]` модели: `plannedEventInference.ts` (`inferPlannedEventsFromUserHistory`, `mergePlannedEventMarkers`, `filterNewPlannedEvents`) извлекает timed-события из user-реплик через `timeParser` (в т.ч. «через N минут» в ru-resolver), `route.ts` через `augmentPlannedMarkers` мержит их с маркерами, `loadOpenPlannedEventsForConversation` отсекает дубликаты, `hasRequiredBranchArtifacts` учитывает открытые rows беседы перед финальной рекомендацией и server-side escalation.
- **2026-05-25:** `spec.md` — ссылка на индекс хранения daily dialog в `MAP.md` (§3); клиентский канон текста — `communicator/spec.md` §3.1.
- **2026-05-25:** GET session sync: при `conversationId` в query беседа не возобновляется, если `ended_at` задана или TTL истёк (кроме `debugExport=1`); фильтр сообщений по TTL применяется ко всем ответам без `debugExport`, а не только при явном id. **`route.ts`**, **`spec.md`**.
- **2026-05-25:** SSE `complete.practicePicked` и persist `messages.meta.practice_picked` теперь заполняются только на финальных turn_mode (`isFinalMode`: `final_recommendation`, `final_recommendation_with_validation_warning`, `forced_final`, `fast_track_final`); на нефинальных ходах — `null` / отсутствует. Это предотвращает преждевременный рендер карточки практики до финальной рекомендации. **`route.ts`**, **`spec.md`**, **`CHANGELOG.md`**.
- **2026-05-25:** Уточнён контракт `conversation_summaries`: `summary_text` = `[branch:phaseTime]`, upsert только вне `free`/`none`; профильный snapshot именован (`buildLifeMatrixReportSnapshot`, `PROFILE_REPORT_SNAPSHOT_VERSION`).
- **2026-05-25:** Daily dialog storage ужат под профильные отчёты. Сервер больше не держит текст беседы в `messages.content`: клиент присылает `turnHistory`, а после `closeConversation(...)` message-строки удаляются. `planned_events` теперь используются только как live-очередь открытых эпизодов; при `[SUMMARIZE_EVENT]` итог сразу merge-ится в `daily_matrices`, затем пересобирается `profile_report_snapshots`, а raw planned-row удаляется. Запись в `user_profile_memory` отключена, `conversation_summaries` оставлены как минимальный служебный след для anti-replan/TTL.
- **2026-05-23:** Для QA planning/summarizing export теперь фиксирует per-turn `planning_persistence` в `messages.meta` и snapshot планов на старт/конец диалога в `dialog_state_after` (`planning_snapshot_at_start`, `planning_open_now`, `planning_closed_recent_48h`, `planning_created_in_this_conversation`).
- **2026-05-23:** Детектор session-wide отказа от практики расширен ещё на обратный порядок формулировки `времени для практик нет` и аналогичные фразы, где отрицание стоит после упоминания практики. Это закрывает QA-кейс, в котором пользователь уже сообщил, что времени на практику нет, но `inquiry` ошибочно делал ещё один refusal-check вместо немедленного перехода в `practice_declined`.
- **2026-05-23:** Финальные premium-инструкции (`final_recommendation`, `final_recommendation_with_validation_warning`, `forced_final`) ужесточены: после выбора практики финальный текст больше не должен задавать пользователю вопросов и не должен просить ничего дополнительно уточнять. Это выравнивает видимый ответ с UX-контрактом карточки: если под сообщением уже показан `PracticeCard`, значит текст этого хода завершает рекомендацию, а не открывает новый inquiry-цикл.
- **2026-05-23:** `markers.ts` ещё раз усилен после QA-прогонов: выбор длительности теперь якорится на упоминание **того же типа практики**, а не любого practice-like слова в длинной реплике, поэтому фразы с бытовым «подышу» внутри описания дня не перетирают финальный запрос `три минуты медитации`. Дополнительно парсер поддерживает десятичные минуты (`2,5` / `2.5`) и дробный формат `3/4 часа`, а session-wide refusal теперь ловит формулировки вроде «не до практик», «нет времени выполнять асаны, пранаяму», «не нужно предлагать практику`.
- **2026-05-23:** Парсер `validateHistoryHasDurationAndType` больше не привязывает длительность практики к первому попавшемуся времени из реплики. `markers.ts` теперь выбирает минутность, ближайшую к упоминанию практики, поэтому фразы вроде «через полчаса вебинар, предложи три минуты медитации» остаются `3 min meditation`, а подтверждения вида «ладно, пять минут медитации, раньше говорил три» фиксируют именно согласованные 5 минут. Параллельно клиент и серверный shared-модуль выровняли диапазон медитации до **1–5 минут**, чтобы `PracticeCard` не спорил с active prompt `dialog_system_v3`.
- **2026-05-23:** Ветка `practice_declined` доведена до терминального поведения: при явном отказе от практики daily dialog больше не уточняет «совсем или не сейчас», не предлагает альтернативный формат и не тянет разговор новыми вопросами. Backend закрывает такую сессию одним финальным сообщением с рекомендацией на день без практики и с пожеланием по времени суток. Параллельно `markers.ts` расширил детектор явного отказа, а `timeParser` научился понимать bare-hour формулировки вроде просто `10` как ближайшее будущее время в контексте дня.
- **2026-05-23:** После первых QA-диалогов усилен guard ветки planning/summarizing. `inquiry` теперь прямо требует сначала доводить активную ветку до `PLANNED_EVENT` / `SUMMARIZE_EVENT`, а server-side ready escalation (`shouldServerEscalateToFinalRecommendation`) блокируется, пока эти branch-артефакты не появились. В `route.ts` добавлен repair-path для преждевременного `[READY_FOR_RECOMMENDATION]`, fallback времени без явного указания смещён на дневное окно (`16:00`), а `persistDialogArtifacts()` отсекает дальние планы за пределами горизонта `сегодня/завтра`, сохраняя skip в `skipped_planned_events`.
- **2026-05-23:** Исправлена подсказка `openingDayQuestionForContext(...)` для случая, когда одновременно активны `summarizing` и `planning`: opening больше не перескакивает сразу в планирование, а сперва ведёт к событию, которое пора подытожить. Одновременно `dialogDebugExport.ts` расширил `dialog_state_after`: в экспорт теперь попадают `context_snapshot`, `context_snippets` / `outcome_text` у `planned_events` и `conversation_summary_for_exported_conversation`, чтобы отладка цикла planning → summarizing показывала не только разговор, но и реально сохранённые артефакты.
- **2026-05-22:** Исправлен продовый crash после финального текста без карточки практики: ветка `planning` могла дойти до `persistDialogArtifacts()` и попытаться вставить `planned_events.time_resolution = "daypart"`, тогда как БД принимает только `explicit | daypart_default | fallback_default`. В `timeParser/index.ts` добавлена защитная `canonicalizeTimeResolution(...)`, а `route.ts` нормализует значение перед insert. Дополнительно `monitoring.ts` теперь сериализует plain-object ошибки в читаемый JSON вместо `"[object Object]"`.
- **2026-05-22:** Opening-инструкция daily dialog ужесточена: первый ход теперь должен звучать как простое человеческое приветствие без чакр/планет/астрологии и без поэтических метафор; `openingDayQuestionForContext` для `summarizing` привязывает вопрос к событию, которое пора подытожить, а не к абстрактному «как день». `prompts-reference.md` синхронизирован с кодом.
- **2026-05-22:** `fast_track_final` больше не включается на любую первую реплику с согласованными типом и минутами. В `dialogArcOrchestrator.ts` добавлен `isPracticeOnlyFirstTurn(...)`: короткий premium-ответ остаётся только для действительно practice-only запроса, а богатая первая реплика про день + практику идёт через `inquiry` → `final_recommendation`. В `route.ts` усилена диагностика финального выбора практики (`choosePractice returned null`, пустой каталог/результат селектора в `practiceSelection.ts`) и увеличен лимит retry на восстановление `[PRACTICE_PICK]` с 200 до 320 токенов. Зафиксировано, что `matrix_ready=false` не блокирует daily dialog: target chakra падает в `astro_primary`. Тесты: `dialogArcOrchestrator.test.ts`, `markers.test.ts`, `practiceSelection.test.ts`, `route.test.ts`.
- **2026-05-21:** Карточка практики: server-side ready escalation — при `inquiry` + `validation.confident` без `[READY_FOR_RECOMMENDATION]` автоматический premium `final_recommendation`; opening: `forcePlanningOnOpening` + `openingDayQuestionForContext` по ветке/фазе.
- **2026-05-21:** Оркестратор: `userAnsweredPracticeRequest` отделяет «ответил про тип/минуты» от `confident`; при конфликте каталога — `catalog_reconciliation` (не `practice_refusal_check`); `resolvePracticePublic` выбирает практику по истории без маркера модели при `confident`.
- **2026-05-21:** `validateHistoryHasDurationAndType` — `confident` требует согласованности тип/длительность по диапазонам v6 (медитация 1–5, дыхание 6–20, асаны >20); конфликт («медитация 15 мин») больше не включает `fast_track_final`.
- **2026-05-21:** pre-push doc-sync — `prompts-reference.md` §1 (зеркало v6 inactive), §2 (`practice_declined`, `PRACTICE_REFUSAL_CHECK_INSTRUCTION`), §3; `spec.md` — режим `practice_declined`, GET `debugExport`, `dialogDebugExport.ts`; `MAP.md`.
- **2026-05-21:** v6 (inactive) усилен: диапазоны 1–5 / 5–20 / >20, переспрос при конфликте + фиксация «Тогда {тип}, {N} мин» после «да»; отказ на сессию («не сейчас» = не нужна); оркестратор `practice_declined` + `userDeclinedPracticeInHistory`; test mode — `sessionResumeTtlMs` сжат только для GET sync (reopen), `sessionTtlMs` 2ч для POST внутри диалога.
- **2026-05-21:** Режим тестирования временных интервалов диалога: `_legacy_web/app/api/_utils/testMode.ts` (`hoursToMs`, `forcedPhaseOrNull`, `logTestModeStartupWarning`); обёртки в `dialogBranching.ts` (4ч anti-replan, `phaseTimeFor`), `lifeMatrixPersistence.ts` (36ч окно `planned_events`), `dialogHelpers.ts` / `route.ts` (2ч TTL). Env: `TEST_MODE_FAST_INTERVALS`, `TEST_MODE_TIME_DIVISOR`, `TEST_MODE_FORCE_PHASE`. Справочник `docs/04_reference/test_mode.md`, тесты `testMode.test.ts`.
- **2026-05-19 (pre-push doc-sync):** **`route.ts`** — импорт шагов длительности карточки из **`@shared/assistantSelectableDurations`** (`_legacy_web/shared_core/assistantSelectableDurations.ts`, копия клиентского модуля для Vercel-only deploy) вместо прямого `@/modules/practices/core/...`. **`dependencies.md`**, **`practices/spec.md`**, **`MAP.md`**, **`CHANGELOG.md`**.
- **2026-05-18:** pre-push doc-sync — миграция **`20260519030000_dialog_system_v3_v5_activate.sql`** активирует `dialog_system_v3` **v5** в продакшене; `PRACTICE_REFUSAL_THRESHOLD` (дефолт 1) управляет подстановкой `practice_refusal_check` в `inquiry`; branch-aware hard-cap через `effectiveDialogMax`. Обновлены **`prompts-reference.md`** (§1–3), **`spec.md`**, **`MAP.md`**, **`CHANGELOG.md`**.
- **2026-05-16:** HARMONIZER v2 foundation: daily dialog переведён на двухфазный контур `summarizing -> planning` с новым day-context loader (`dialogDailyContext.ts`), parser-ом времени (`timeParser`) и матричным слоем (`lifeMatrix.ts`, `lifeMatrixPersistence.ts`). Миграция **`20260516132000_life_matrix_foundation.sql`** добавила `planned_events`, `daily_matrices`, расширила `conversation_summaries` и `user_daily_forecasts.day_target_*`; `route.ts` теперь фиксирует `day_target_chakra`, пишет `planned_events`, обновляет `daily_matrices`, `conversation_summaries`, `user_profile_memory`. Prompt **`dialog_system_v3`** версии **v5** подготовлен миграцией **`20260516150000_dialog_system_v3_v5.sql`**, но оставлен **неактивным** до ручной вычитки владельцем; активной остаётся **v4**. Новые invisible markers: `PLANNED_EVENT`, `SUMMARIZE_EVENT`, `PLAN_TOMORROW`, `MATRIX_CELLS`. Порог прямого вопроса о практике переведён на env **`PRACTICE_REFUSAL_THRESHOLD`** с дефолтом 1. Тесты: **`timeParser.test.ts`**, **`markers.test.ts`**, **`dialogArcOrchestrator.test.ts`**. **`assistant/spec.md`**, **`daily_forecast/spec.md`**, **`profile/spec.md`**, **`communicator/spec.md`**, **`subscription/spec.md`**, **`CHANGELOG.md`**.
- **2026-05-15 (пятый):** `[PRACTICE_PICK]` расширен полем `**card_blurb`** для карточки практики, генерируемой той же premium-моделью в том же финальном вызове. В `**dialogArcOrchestrator.ts**` финальные и `**fast_track_final**` инструкции требуют 2/3-сentence `card_blurb` и используют новую переменную `**{{chakra_label_accusative}}`**; в `**route.ts**` эта переменная подставляется через словарь склонений, а валидный `card_blurb` используется в `**resolvePracticePublic**` только если итоговая практика совпала с маркером, иначе остаётся server fallback. `**markers.ts**` получил quote-aware парсинг `card_blurb`; `**practiceCardSummary.ts**` — валидацию длины/markup и fallback к каталожным описаниям (`modules/practices/core/catalog.ts`). DTO `**practice_picked`** расширен опциональным `**card_blurb`** без изменения SSE-структуры. Тесты: **`dialogArcOrchestrator.test.ts`**, **`markers.test.ts`**, **`practiceCardSummary.test.ts`**. **`prompts-reference.md`**, **`spec.md`**, **`communicator/spec.md`**, **`CHANGELOG.md`**.
- **2026-05-15 (четвёртый):** Пакет `B` для daily dialog v3: `**decideTurnMode**` расширен на **первый ответ ассистента после автоприветствия** (`opening`) — `fast_track_final` теперь срабатывает и на первой содержательной реплике пользователя после initiate; добавлен временный dev-лог **`[FAST_TRACK_DIAG]`** под тем же флагом, что и `[DIALOG_V3_DEBUG_PROMPT]`. Для `**inquiry**` оркестратор считает два подряд `opening` / `inquiry` без уверенного ответа о длительности и типе и подставляет `**practice_refusal_check**` с прямым вопросом о том, нужна ли практика вообще. Новая миграция **`20260515030000_dialog_system_v3_pack_b.sql`** активирует `dialog_system_v3` **v4**: антизатягивание после готовности (`[READY_FOR_RECOMMENDATION]` без «дожёвывания»), немедленное принятие явного отказа от практики и правило **не описывать технику выполнения** в тексте ответа. Тесты: **`markers.test.ts`**, **`dialogArcOrchestrator.test.ts`**. **`prompts-reference.md`**, **`spec.md`**, **`CHANGELOG.md`**.
- **2026-05-14 (doc-sync):** Уточнён публичный контракт в **`spec.md`**: явный `id` маркера только после **`.trim()`**; в результате **`choosePractice`** для логов — **`markerCatalogPracticeKind`**; в **`resolvePracticePublic`** — запасные минуты до клипа из **`durationSec`** выбранной практики либо **10**/**3**; раздельно описаны JSON логов **`[PRACTICE_CARD_MISMATCH]`** на сервере и в **`PracticeCard`** (`**source: "practice_card_client_sync"**`). **`dependencies.md`** — импорт **`route.ts`** → **`assistantSelectableDurations.ts`**. **`MAP.md`**, **`CHANGELOG.md`**.
- **2026-05-15 (третий):** Карточка практики в диалоге: **`resolvePracticePublic`** — единый источник **`overrides.durationMin`** с историей при **`confident`**, клип к шагам каталога (`**assistantSelectableDurations.ts**`), диагностика **`[PRACTICE_CARD_MISMATCH]`**; **`choosePractice`** — при конфликте типа маркер vs уверенная история сбрасывается id маркера (`historyKindConflictResolved`). Клиент **`PracticeCard`** — клип override. Тесты **`practiceSelection.test.ts`**. **`spec.md`**, **`practices/{spec,history}.md`**, **`CHANGELOG.md`**.
- **2026-05-15 (второй):** Окно недавних практик для `**selectPracticeCandidate**` — только **канонические `id`** строк каталога: подсказки из `practice_sessions` и `practice_picked` нормализуются через `**resolvePracticeKeyToCatalogId**`; убран хардкод `BREATH_PRACTICE_SLUGS` — классификация дыхания по slug опирается на строки `kind: breath` в загруженном каталоге. В `**selector.ts**` недавность сравнивается только с `practice.id`. Тесты **`practiceSelection.test.ts`**, **`selector.test.ts`**. **`spec.md`**, **`CHANGELOG.md`**.
- **2026-05-15:** **`practiceSelection.ts` / `choosePractice`** — убран ранний `return` при успешном резолве `[PRACTICE_PICK]` по каталогу: иначе **не** применялись `recentOfferedPracticeIds` + `selectPracticeCandidate`, и модель могла дважды подряд предложить ту же дыхательную практику при неизменном маркере. Теперь маркер сопоставляется с кандидатами **после** исключения недавних id (как для завершённых сессий). При расхождении `practiceKind` из эвристики и `kind` практики из маркера окно недавних и пул кандидатов берутся по **типу из маркера** (как при прежнем резолве «карточка = маркер»). Тест **`practiceSelection.test.ts`**. **`spec.md`**, **`CHANGELOG.md`**.
- **2026-05-14:** Пакет «Б» (`_legacy_web/packet_3`): режим оркестратора **`fast_track_final`** — при первом ходе (`iteration === 1`), без `practice_picked` в истории, если в сессии **ровно одна** пользовательская реплика до ответа ассистента и по ней **`validateHistoryHasDurationAndType`** на одном сообщении даёт `confident: true`, выбирается **premium** сразу (`streamGeminiText`), короткая инструкция с `[PRACTICE_PICK]` и лимитом текста 250 символов; иначе — `opening`. Код: **`dialogArcOrchestrator.ts`** (`decideTurnMode(..., pendingUserContent?)`, `soleFirstUserMessageText`), **`communicator/v2/dialog/route.ts`** (`ResponseMode`, `isFinalMode`, вызов `decideTurnMode` с `userMessage`); подписи фазы в UI: **`modules/communicator/i18n/communicator.ts`**. Документация: **`prompts-reference.md`**, **`spec.md`**, **`CHANGELOG.md`**.
- **2026-05-14:** **`practiceSelection.ts`** / **`choosePractice`** — для не-`default` маркера `[PRACTICE_PICK]` сначала резолвится **`id` / `slug`** по **полному** активному каталогу (все `kind`) плюс статические «Вспышка» и когерентное дыхание; раньше запрос к БД сразу фильтровался по **`practiceKind`** из **`validateHistoryHasDurationAndType`** (только реплики пользователя), из-за чего id дыхания из маркера премиум-модели исчезал из выборки и подставлялась другая практика. Откат дублирующей эвристики **`inferExplicitKindChoice`** в **`markers.ts`** (она не нужна при каноническом маркере). Лог при промахе id: **`[PRACTICE_SELECTOR] marker_id_not_in_catalog`**. Тест **`practiceSelection.test.ts`**. Обновлены **`spec.md`**, **`CHANGELOG.md`**.
- **2026-05-13:** Миграция **`20260513224500_dialog_system_v3_polish_pack_a_fix.sql`**: активный `dialog_system_v3` **v3** — секция **«ЕСЛИ ПОЛЬЗОВАТЕЛЬ ОТКАЗЫВАЕТСЯ ОТ ПРАКТИКИ»** вынесена на верхний уровень (после «КАК ТЫ ПОНИМАЕШЬ…», до «ЕСЛИ ПОЛЬЗОВАТЕЛЬ ИЗМЕНИЛ КОНТЕКСТ ДНЯ»); v2 деактивируется. **`prompts-reference.md` v1.3**, **`spec.md` v2.9**, **`CHANGELOG.md`**.
- **2026-05-13:** Пакет «А» (`_legacy_web/packet_a`): миграция **`20260513223000_dialog_system_v3_polish_pack_a.sql`** (активный `dialog_system_v3` v2 — день недели, чакра, тональный окрас `{{tonal_register}}`, отказ от практики); **`dialogTonalRegisters.ts`**; обновлены **`ORCHESTRATOR_INSTRUCTIONS`** и **`route.ts`** (`tonal_register`, `expandOrchestratorInstruction`, `[DIALOG_V3_DEBUG_PROMPT]` при dev/флагах). Справочник **`prompts-reference.md` v1.2**, **`spec.md` v2.8**, **`CHANGELOG.md`**.
- **2026-05-13:** В **`prompts-reference.md`** восстановлен корректный YAML-frontmatter и выровнена markdown-таблица §3; **version 1.1**.
- **2026-05-13:** Добавлен справочник дословных текстов daily dialog v3 — **`docs/02_modules/assistant/prompts-reference.md`** (`dialog_system_v3` из миграции **`20260511161000_dialog_system_v3.sql`**, все ключи **`ORCHESTRATOR_INSTRUCTIONS`**, таблица **`{{...}}`** из **`buildDialogSystemInstruction`**). В **`spec.md`** — cross-link; в **`MAP.md`** — путь к файлу в колонке точек входа; в **`scripts/docs-sync/sync.sh`** и **`docs/03_rules/documentation_update_rules.md`** — явное исключение для doc-sync и правил обновления. **`CHANGELOG.md`**.
- **2026-05-13:** **`stripDialogScaffoldMarkdown`**: вместо разворачивания `**…**` в текст — **полное удаление** блоков между двойными звёздочками (заголовки секций в финальной рекомендации); `---` по-прежнему; функция **экспортируется** из **`markers.ts`**. Убраны точечные правила по словам «Зеркало»/«Темы» и связанные regex. **`inferDurationSecFromText`**: «три четверти часа» → 45 мин, среднее для диапазонов минут; **`inferKindFromText`**: порядок медитация → йога → параллельное «дыхание или …» (`**null**`) → дыхание. **`resolvePracticePublic`**: для **yoga** объект **`overrides`** в payload клиенту **не включается**. Тесты **`markers.test.ts`**, **`practiceCardSummary.test.ts`**. Уточнены **`assistant/spec.md`**, **`communicator/{spec,history}.md`**, **`CHANGELOG.md`**.
- **2026-05-13:** В **`markers.ts`** исправлено распознавание «параллельного» выбора дыхание vs тело/йога: ранее **`isBreathVersusOtherParallelOffer`** использовал **`\b`** вокруг русских слов («тело», «или»), а в JavaScript границы слова для кириллицы не срабатывают — сообщение «подышать или через тело» ошибочно классифицировалось как только **дыхание** и перетирало **йогу** из предыдущей реплики, из-за чего **`choosePractice`** фильтровал каталог по `kind: breath` при тексте рекомендации про асаны. Заменены границы на явные разделители (пробел/пунктуация). Регрессия: **`markers.test.ts`**. Обновлены **`docs/02_modules/assistant/spec.md`**, **`CHANGELOG.md`**.
- **2026-05-13:** В **`dialog/route.ts`** (`resolvePracticePublic`) текст **`reason`** в `practice_picked` для клиента формируется **`practiceCardSummary.ts`**: 1–2 предложения по правилам `kind` (йога — имена чакр из `chakraIds` + акцент на теле; медитация — общий смысл; дыхание — slug-ключи семи практик, строки ru/en в коде). Длинный `reason` из маркера `[PRACTICE_PICK]` в карточку не передаётся. Тесты: **`practiceCardSummary.test.ts`**. Обновлены **`docs/02_modules/assistant/{spec,history}.md`**, **`CHANGELOG.md`**.
- **2026-05-12:** Логика вывода длительности и типа практики консолидирована: локальные `inferPreferredDurationSec` / `inferPreferredPracticeKind` удалены из `practiceSelection.ts`; `validateHistoryHasDurationAndType` в `markers.ts` расширен полями `durationSec: number | null` и `practiceKind: PracticeKindInferred | null` (новый экспортируемый тип `PracticeKindInferred`). `choosePractice` теперь использует единый `validateHistoryHasDurationAndType` вместо собственных инферов. Расширен набор русских фраз и числительных для распознавания.
- **2026-05-12:** В `practiceSelection.ts` интерфейс **`ChoosePracticeResult`** расширен полями `chakraId` и `preferredDurationMin`; **`inferPreferredDurationSec`** теперь обрабатывает **всю** историю сообщений пользователя (конкатенация `history` + текущее сообщение), а не только текущий ввод. В `route.ts` объект **`overrides`** в ответе `resolvePracticePublic` для **дыхания и медитации** задаётся с fallback-цепочкой: `durationMin` = marker → inferred → null, `chakraIndex` = marker → planet_of_the_day. С **2026-05-13** для **yoga** `overrides` в payload не включается (каталог). Удалены диагностические поля `fullTextLast200` и `hasPracticeMarker` из лога `[DIALOG_V3_DIAG]`.
- **2026-05-12:** В `dialog/route.ts` значение **`maxOutputTokens`** для основных вызовов standard и premium моделей увеличено с 1500 до **2500** (ранее документированный пол 2048 был промежуточным); добавлены диагностические поля `fullTextLast200` и `hasPracticeMarker` в лог `[DIALOG_V3_DIAG]`.
- **2026-05-12:** Маркер `[PRACTICE_PICK]` перемещён с конца финальной рекомендации на **первую строку** ответа в инструкциях `final_recommendation`, `final_recommendation_with_validation_warning`, `forced_final` (`dialogArcOrchestrator.ts`). В `dialog/route.ts` добавлен retry-вызов Gemini при отсутствии маркера после premium-ответа в финальных режимах (`temperature: 0.3`, `maxOutputTokens: 200`).
- **2026-05-12:** В `[PRACTICE_PICK]` маркер добавлены обязательные поля `duration_min` и `chakra`; парсер (`markers.ts`) извлекает их и прокидывает через `choosePractice` → `meta.practicePicked.overrides` → SSE `complete` → фронт `PracticeCard`. В `selector.ts` добавлен `markerIdResolved: boolean | undefined` — `false` когда model-generated id не найден в каталоге (лог `[PRACTICE_SELECTOR] marker_id_not_found`). `PracticeCard.tsx` принимает `overrideDurationMinutes` / `overrideChakraIndex` и отображает их вместо каталожных значений.
- **2026-05-11:** Daily dialog переведён на v3: в **`communicator/v2/dialog/route.ts`** удалена фазовая оркестрация через `dialogue_phases` / `orchestrator_decision`, вместо неё используются единый prompt **`dialog_system_v3`** из БД, rule-based **`dialogArcOrchestrator.ts`**, structured Gemini request (`systemInstruction` + `contents`) и двухступенчатая схема **standard → premium** по маркеру **`[READY_FOR_RECOMMENDATION]`**. В **`gemini.ts`** добавлены structured-вызовы, `AI_MODEL_FALLBACK`, `ensureDialogCache(...)` для explicit context caching и обратная совместимость со строковым `prompt`. Удалены **`softCap.ts`** и **`data/information_axes.json`**; добавлен **`dialogConfig.ts`** (`MAX_DIALOG_LENGTH`). В **`practiceSelection.ts`** default marker резолвится в coherent breathing 10 мин на чакру дня. Добавлена миграция **`20260511161000_dialog_system_v3.sql`**.
- **2026-05-11:** Удалены **`_legacy_web/app/api/_utils/explicitSignals.ts`** и **`_legacy_web/data/dialog_signals.json`**; в **`communicator/v2/dialog/route.ts`** убраны импорт и вызов **`detectExplicitSignals`**, в шаблон оркестратора передаётся **`explicit_signals_json: "[]"`** до **`dialog_v3_unified_prompt`**. В **`insightDetection.ts`** сужены списки **`COGNITIVE_WORDS_RU`** и **`PREPARATION_MARKERS_RU`**. Обновлены **`spec.md`**, **`history.md`**, **`MAP.md`**, **`CHANGELOG.md`**, **`dependencies.md`**.
- **2026-05-11:** Из репозитория удалён файл миграции **`20260509120000_dialog_quality_v4.sql`** (откат промптов в БД — **`20260511140000_revert_dialog_quality_v4.sql`**); обновлены **`code_refs`** в **`spec.md`**, **`history.md`**, **`MAP.md`**, запись в **`CHANGELOG.md`**.
- **2026-05-09:** Уточнён **`docs/02_modules/assistant/spec.md`** под фактический **`gemini.ts`**: порядок цепочки моделей, повтор при **404**/NOT_FOUND, лог **`[GEMINI FALLBACK]`**, финальное сообщение только для «перегрузочных» ошибок; в таблице **`authorVoice`** — поле **`assistant_should_NOT_say`** в **`few_shot_examples`**; в **`code_refs`** добавлен **`gemini.ts`**.
- **2026-05-09:** Добавлены утилиты **`explicitSignals.ts`** / **`softCap.ts`**; в **`communicator/v2/dialog`** интегрированы **`detectExplicitSignals`** (подстановка **`explicit_signals_json`** в оркестратор), **`getSoftCap`** в payload осей и в промпт, метрики ходов для оркестратора и бюджетов астрологии/чакр, расширенный рендер **`responder_main`**. В **`orchestrator.ts`**: поле **`user_register`**, расширенные **`responder_hints`**, нормализация подсказок из сырого JSON. Миграция **`20260509120000_dialog_quality_v4.sql`** — новые активные версии **`responder_main`**, **`orchestrator_decision`**, **`phase_collect_state`**, **`phase_deepen_inquiry`**, **`phase_offer_insight`**, **`phase_contextual_greeting`**.
- **2026-05-09:** **`_legacy_web/app/api/_utils/authorVoice.ts`** и **`authorVoice.test.ts`** синхронизированы с **`data/author_voice.json` v2**: **`AuthorVoiceProfile`** — **`usage_note`**, **`openers_neutral` / `openers_observational`**, удалены **`structural_patterns`** и **`somatic_language`**; **`formatAuthorVoiceForPrompt`** выводит примечание к профилю и две группы зачинов.
- **2026-05-09:** В **`_legacy_web/app/api/_utils/gemini.ts`**: переменные **`AI_MODEL_STANDARD_FALLBACK`** / **`AI_MODEL_PREMIUM_FALLBACK`**, расширенный **`getModelByHint(..., { fallback: true })`**, автоматическая цепочка primary→fallback при 503/429 и др. перегрузках (лог **`[GEMINI FALLBACK]`**), итоговое сообщение пользователю «Сервис временно недоступен…» после исчерпания попыток. В **`userModelTier.ts`** — ссылка в комментарии, что резолв модели живёт в **`gemini.ts`**.

- **2026-05-08:** В **`/api/communicator/v2/dialog`** для стрима ответа респондера задан нижний предел **`maxOutputTokens` = 2048** (поверх значения из `prompts`), чтобы реже обрывать длинные реплики по лимиту вывода модели.

- **2026-05-08:** В **`_legacy_web/app/api/_utils/gemini.ts`** для **`gemini-2.5*`** в `generationConfig` передаётся **`thinkingConfig: { thinkingBudget: 0 }`**, чтобы внутренний thinking не съедал бюджет **`maxOutputTokens`** и не обрывал видимый текст посреди фразы. Для **`gemini-3*`** отдельный `thinkingConfig` не задаётся — сохраняется глубина рассуждений по умолчанию API.

- **2026-05:** Зафиксирован канон кода: таблица **`scenarios`** и **`scenario_cache`** применены (миграция `20260501173500_scenarios_architecture.sql`); точки входа **`/api/ai/monologue`** и **`/api/ai/dialog`** (реэкспорт на общий обработчик диалога) — целевая унификация PATCH 12; параллельно сохранён **`/api/communicator/v2/dialog`** для клиентов без `scenario_id`. Исторические тексты PATCH 12 перенесены в `docs/05_archive/migrated/assistant/`.

- **2026-05:** Расхождение с линейным описанием MODULE_4 «калибровка только через оркестратор»: в проде одновременно (а) голосовой диалог **`use_case = calibration`** с фазами из **`dialogue_phases`** и промптами **`phase_*`**, (б) пайплайн **`/api/calibration/extract`** с усреднением состояния (`calibration` модуль). Оба пути используют общие данные профиля; закрытие Product по голосу может идти через фазу **`acknowledge_and_close`** без повторного вызова extract в том же turn — см. сценарии в приложении.

- **2026-05:** Insight Engine (PATCH 10) в коде: **`insightDetection`** считает CSI/TTM/ETV; **`blockedPhasesForInsight`** блокирует **`ask_practice_intent`** и **`suggest_practice`** для **`daily_dialog`**, если **`isReadyForPractice`** ложно; **`enforceInsightPhaseGuards`** принудительно смещает фазу в **`deepen_inquiry`** или **`offer_insight`** при попытке оркестратора вывести в практику на стадиях **`preconcept`** и **`concept`**. Решение оркестратора по-прежнему на промпте **`orchestrator_decision`** с подстановкой **`insight_metrics_json`**.

- **2026-05:** PATCH 8 / PATCH 9 в данных: активный респондер — **`responder_main` версии 2** (голос автора); фазы деактивируются массово и заменяются библиотекой v2 в **`20260501170500_phase_prompts_v2.sql`** — **11** активных строк с **`prompt_type = 'phase'`** для **`calibration`** и **`daily_dialog`**. Поле **`users.address_form`** добавлено миграцией автора; **`author_voice.json`** подключается через **`authorVoice.ts`**.

- **2026-05:** PATCH 13 в данных и коде: **`monologue_morning_recommendation`** активна версия **2**; сценарий и схема **`morning_recommendation`** допускают **`math_level`**; сценарий **`deep_explanation`** и промпт **`monologue_deep_explanation`** **деактивированы** (`20260501185700_monologue_prompts_v2.sql`). Утренний монолог собирает топ-3 лепестка по **`ranked_planets`** или сортировке **`importance`**.

- **2026-05:** Оптимизации MODULE_4 частично совпадают с кодом: **`greetingBypassDecision`**, **`contextSimilarity`**, **`shouldForceFreshDecision`**, **`decision_source`** / **`cache_similarity`** в объекте решения и в **`user_event_log`**. В **`messages.meta`** хранится полный **`orchestrator_decision`**, не только `decision_source` отдельным полем.

- **2026-05:** Продуктовое напряжение: **`choosePractice`** берёт чакру из **`planet_of_the_day`**, монолог утра — топ-3 по importance (**`topPetals`**). История решения — зафиксировать при следующей ревизии UX практики vs утреннего текста.

Исторические ТЗ (полный текст): **`docs/05_archive/migrated/assistant/`** (PATCH 8, 9, 10, 12, 13, MODULE_4). **`MODULE_3_Calibration_TZ.md`** остаётся в **`docs/05_archive/migrated/calibration/`**; смешанный бриф **`assistant_practice_recommendation_brief.md`** — в **`docs/05_archive/migrated/practices/`**.
