---
id: 02_modules/practices/history
title: Practices History
version: 1.67
updated: 2026-07-29
depends_on: [01_foundation/product_model, 02_modules/subscription/spec, 02_modules/biofeedback/spec, 02_modules/audio/spec, 02_modules/bindu/spec]
code_refs:
  [
    modules/practices/core/catalog.ts,
    modules/practices/core/vimeo.ts,
    modules/practices/ui/PracticeCard.tsx,
    modules/mandala/experiments/SacredSymbolStreamScreen.tsx,
    app/asana-practice.tsx,
    supabase/migrations/20260429051600_calibration_dialogue_orchestrator.sql,
  ]
---

## Decision Log

- **2026-07-29 (breath card help «?»):** На всех дыхательных `PracticeCard` — `SurfaceCardTitleRow` + `SurfaceHelpModal` (как «Рекомендации на день»); общий copy `breathHelp*` в `practices` i18n + typed overlays; padding карточки выровнен на `SURFACE_CARD.padding`.

- **2026-07-29 (catalog footer):** Убрана строка «Всего в каталоге: N…» с каталога (и test, и prod) — лишний UI без отдельного DB-запроса (счёт локальный).

- **2026-07-29 (interpretation bilingual):** Results «Interpret» при EN UI показывал EN error + RU timeout detail (`__DEV__` + хардкод в `breathPracticeInterpretation`). Fix в communicator client + UI — только локализованная строка; silent retry на timeout.

- **2026-07-29 (entry-time warn):** Не показываем warning «Время вхождения не определено…» — в строке остаётся «—»; расчёт/exportMeta без изменений. Мигание guidedOnly от Polar HR-only пакетов оставлено как есть.

- **2026-07-29 (results UI by sensor):** Без пульсометра — «Практика завершена» + новый текст; камера — белый комментарий, без метрик-блока/«Шкалы времени»/подсказки про интерпретацию; BLE — без «Шкалы времени». Алгоритм entry-time не менялся.

- **2026-07-29 (activation cancel):** «Отменить» на активации пульса (камера/BLE) → `returnToPracticeOrigin` (каталог / День), а не legacy idle с выбором типа дыхания.

- **2026-07-29 (breath HR race):** Probe `available===false` больше не перезаписывает `fingerCamera`/`none` в `none` (гонка после выбора «пульс с телефона»). Demote только из `ble`. Пары: `biofeedback`.

- **2026-07-29 (breath HR default):** Если last BLE недоступен — combo по умолчанию «без пульсометра» (не Polar); старт с мёртвым BLE → alert, не чёрный экран. `CoherenceBreathScreen`: prep UI + cancel на iOS, hang watchdog. Пары: `biofeedback`.

- **2026-07-29 (breath test gate):** `BREATH_TESTING_MODE` = `HARMONIZER_TEST_MODE`; live footer diagnostics / activation export / Export JSON только в test; Close на results центрируется, если одна.

- **2026-07-23 (TV standalone HTML):** WordPress Custom HTML на `/tv/` не сохранял новый сниппет (оставался старый browser-locale build → `?fr` игнорировался). Канон: `web_cabinet/tv/` (+ симметрично `web_cabinet/cabinet/`) через ISPManager; WP-страницу `/tv/` снять. `docs/remote-play/wordpress-snippet.html` удалён; модульные docs — `docs/remote-play/README.md`.

- **2026-07-22 (TV URL + remote layout):** Ссылка короче — `https://zamkovoi.yoga/tv?pt` (ru без query). На пульте: практика `dialogTitle`, URL крупным `screenTitle`, подпись `openOnTvCaption` обычным. wordpress-snippet уже понимает `?pt`.

- **2026-07-22 (TV locale + asana i18n):** Пульт/Connect TV и кнопка «Завершить практику» были только RU/EN (`inlineBaseLocale` → EN для PT). Добавлены typed modules `asanaScreen` + `remotePlay` с overlays для de/fr/it/es/pt/nl. Ссылка на ТВ несёт locale (`tvPageUrl`); wordpress-snippet читает URL (не browser lang), audiotrack из сессии/URL, unmute через gesture + overlay. Нужен re-paste `docs/remote-play/wordpress-snippet.html` на zamkovoi.yoga/tv.

- **2026-07-22 (Android asana embed page):** Flash «нет Vimeo ID» = загрузка metadata; `vimeoId` в launch + spinner. Чёрный плеер Android: WebView грузит `https://zamkovoi.yoga/asana-embed.html` (`web_cabinet/asana-embed.html`); iOS остаётся на `vimeoEmbedHtml`+baseUrl. Fallback html+`originWhitelist:*` если страница 404.

- **2026-07-22 (Android asana + TV + BLE copy):** Pixel — чёрный Vimeo WebView (explicit play + android WebView props); Connect TV — подъём над IME как onboarding; picker connectedHint; scan-busy не пугает; Android probe без `devices()`-false-positive после Forget. TV unmute — `wordpress-snippet.html` (нужен re-paste на zamkovoi.yoga/tv).

- **2026-07-22 (BLE/Android PPG QA):** Чёткие статусы picker; «Подключен» после устойчивого HR; без голого чёрного prep; Android camera fps clamp. См. biofeedback 2026-07-22 (6).

- **2026-07-22 (BLE picker-centric Android):** `PracticeCard` — Connect в модалке ждёт live HR; «Подключен» не по remembered id; старт практики только при `androidOsLinkReady`; отсутствующий ремень → «без пульсометра»; короче чёрный wait при warm hold. См. biofeedback 2026-07-22 (5).

- **2026-07-22 (BLE live link):** Android «Начать»/«Подключить» → `ensureWearableLiveLink` (ждать HR); fail → Retry, практика не стартует. См. biofeedback 2026-07-22 (4).

- **2026-07-22 (BLE start warm):** «Начать практику» с выбранным Polar ждёт `warmWearableConnection` (кнопка «Подключаем…»), затем открывает breath; prep-экран минимальный. См. biofeedback 2026-07-22 (2).

- **2026-07-22 (BLE picker):** `PracticeCard` паузит remembered-probe на время picker; строки «Подключен»/«Отключить»; см. `biofeedback/history` 2026-07-22.

- **2026-07-21 (Day practice optimistic UX):** Выбор/отмена pending-практики на «День» больше не ждут сеть + `loadDayPlan` — карточка появляется/исчезает сразу; см. `daily_forecast/history`.

- **2026-07-21 (Day tab launch gate):** Pending `PracticeCard` на вкладке «День» запускал практику без tier-check (Oracle мог стартовать йогу/дыхание/медитацию). Fix: `app/(tabs)/day.tsx` — `tryLaunchDayPractice` + `AccountGateDialog` по kind (`meditations` / `breath_practices` / `asana_practices`), паритет с `PracticeCatalogScreen`.

- **2026-07-15 (catalog browsable for all tiers):** `PracticeCatalogScreen` — каталог (медитации/дыхание/асаны) теперь **полностью browsable для всех уровней**: переключение групп не вызывает гейт, карточки видны всем. Гейт срабатывает только на «Начать практику»/«Открыть на телефоне»/«Открыть на ТВ», по kind: `meditations` / `breath_practices` / `asana_practices` (раньше гейт срабатывал уже при тапе по группе, а `practice_catalog` был единым ключом для всех). Удалены `catalogAllowed`/`locked` проверки из onPress групп и `masterOnly`-подпись на карточке группы — всегда показывается счётчик.

- **2026-07-13:** `PracticeCard` перешёл на общий **`ComboBox` / `ComboBoxRow`** (`modules/ui/ComboBox.tsx`): панель опций под рядом триггеров (соседи в ряду не переносятся), тап вне комбо сворачивает без смены значения.

- **2026-07-12 (3):** `loadDailyPracticeStatsInRange` — календарный диапазон `user_daily_stats` для профиля («Статистика практик»); limit-API `loadDailyPracticeStats` остаётся для day-health baseline.

- **2026-07-12 (2):** Breath recent-stack was a no-op on chakra-2 days: catalog maps only `chandra-bhedana` to chakra 2, hard filter left one candidate, exclusion fell back to the same pick. `selectPracticeCandidate` now widens the breath pool when chakra matches &lt; 2 and soft-prefers the day chakra in ranking.

- **2026-07-12:** Launch/exit UX: Communicator handoff no longer double-schedules overlay dismiss; breath results close with `router.replace("/day")` for `launchSource` `assistant|day`. Day pending flash after practice: `recordPracticeSession` marks Day plan stale and clears cache/prefetch.

- **2026-07-11 (2):** QA: дыхание «без пульсометра» всё ещё монтировало `FingerPpgCameraSource` (фонарик + ложный finger confidence) и показывало метрики на results; pending-карточка на «День» не закрывалась после completed session из‑за `GET /api/day` условия `started_at >= offer.created_at` (re-save offer bump'ает `created_at`). Fixes: `disableOpticalHardware` не монтирует камеру; results — «Отличная практика!» + рекомендация phone/BLE (`noSensorGreatPracticeTitle` / `noSensorResultsRecommendation`); auto-complete offer по `practice_slug` в тот же локальный день без сравнения timestamps.

- **2026-07-06 (34):** Доработка TV Remote по полевому фидбеку (3 нюанса) — завершение прерванной работы после сбоя модели. (1) **Закрытие вкладки / потеря ТВ** — `wordpress-snippet.html`: `beforeunload`/`pagehide` теперь PATCHит `status="stopped"` (не `"closed"`), pairing сохраняется; `sessionStorage` (`hrp_session_id`) восстанавливает ту же `tv_sessions` строку при reload в том же браузере → тот же код, телефон остаётся paired. На телефоне: `status="stopped"` → «Запустить заново» + подсказка `tvStoppedHint` + «Подключить ТВ заново» (без confirm); `session=null` (unlink/expired) → панель «Связь с ТВ потеряна». `linkDevice` закрывает прочие active-сессии пользователя (`status="closed"`) перед привязкой нового кода — убирает stale pairing при fresh `/tv/` tab. (2) **Засчитывание в отчётах** — порог `elapsedSec >= durationSec - 10` (`COMPLETION_TAIL_SEC`); `maybeRecordTvSession` идемпотентна (`recordedRef`), вызывается из confirmFinish, при `status→stopped` (tab close near end), и при connection loss; ранняя остановка не пишется. (3) **«Отключить ТВ» через confirm** — `pendingAction`: `"stop"` (✕/Стоп → stop + record? + back, TV paired) vs `"disconnect"` (→ stop + record? + disconnect + `/connect-tv`). `spec.md` (v1.38), `dependencies.md`, `docs/remote-play/README.md`, `CHANGELOG.md`. tsc + vitest чисты. **Напоминание:** обновить WordPress Custom HTML на `https://zamkovoi.yoga/tv/`.

- **2026-07-06 (33):** Три нюанса TV Remote по полевому фидбеку. (1) **Потеря связи с ТВ** — когда вкладка браузера закрывается / ТВ выключается, сниппет PATCHает `status="closed"` на `beforeunload`, `RemotePlayProvider.applySession` → `session=null`, `connected=false`. Раньше пульт показывал застывшие disabled-кнопки без объяснений, а при reopening вкладки (новый pairing_code) телефон не переподключался → «запутанная логика». Теперь `TVRemoteScreen` детектит переход connected → null (`wasConnectedRef`) и показывает панель **«Связь с ТВ потеряна»** + подсказку + кнопку **«Подключить ТВ заново»** → `router.replace("/connect-tv", { …params })`: пользователь вводит новый код, видит «Начать практику» (параметры прокидываются) → `playVimeo` запускает ту же практику на новой вкладке. (2) **Не засчитывать прерванную, засчитывать досмотренную**: порог завершения изменён с `0.95 * durationSec` на `durationSec - COMPLETION_TAIL_SEC` (`COMPLETION_TAIL_SEC = 10` сек — «осталось ≤10 сек → выполнена», для `durationSec ≤ 10` fallback `0.9 * durationSec`); запись `maybeRecordTvSession` теперь идемпотентна через `recordedRef` и вызывается **не только из `confirmFinish`, но и автоматически при потере связи** — если пользователь закрыл ТВ за 5 сек до конца, сессия всё равно засчитается; ранняя остановка (порог не пройден) — не записывается. (3) **«Отключить ТВ» через подтверждение**: `requestDisconnect` → `pendingAction="disconnect"` → общий `PracticeStopConfirmDialog`; «Завершить» → `stop` + `maybeRecordTvSession` + `disconnect` + `/connect-tv` (pairing разрывается); «Стоп»/✕ → `pendingAction="stop"` → `stop` + `maybeRecordTvSession` + `router.back()` (TV остаётся paired). Новые i18n строки `connectionLostTitle`/`connectionLostHint`/`reconnectButton` (ru/en) + `finishingButton` уже была. `spec.md` (v1.38), `history.md`, `docs/remote-play/README.md`, `CHANGELOG.md`. tsc + vitest + i18n-sync check чисты.

- **2026-07-06 (32):** Запись `practice_sessions` для ТВ-просмотра (только при завершении). Параметры практики (`practiceId`, `slug`, `chakraIds` через запятую, `launchSource`) теперь прокидываются через весь ТВ-путь: хук `useAsanaRemotePlayLauncher(strings, launchSource)` (коммуникатор передаёт `"assistant"`) и `onRemotePlay` каталога (`"catalog"`) → `/connect-tv`|`/tv-remote` → `ConnectTVScreen.startPractice` → `/tv-remote` → `TVRemoteScreen.disconnect` → `/connect-tv`. В `TVRemoteScreen.confirmFinish` после `remotePlay.stop()` вызывается `maybeRecordTvSession()`: **строка `practice_sessions` пишется только если практика дошла до конца** — `durationSec` известен и `elapsedSec >= durationSec * 0.95` (95 %); иначе запись не создаётся (ранняя остановка не учитывается). `context`: `{ source: "asana", launch_source, practice_kind: "yoga", vimeo_id, playback_mode: "tv", audiotrack }`; `completionPct: 100`; `chakraFocusIds` из `chakraIds`; `startedAt = endedAt − elapsedSec·1000` (реальное время просмотра). Запись best-effort (ошибка проглатывается, навигация всё равно идёт). **Таймер `elapsedSec` переписан** — теперь аккумулирует секунды только пока `status === "playing"` (`setElapsedSec((s) => s + 1)` каждый тик), замораживается на паузе/стопе, сбрасывается в `replay()`; прежний расчёт от `startedAt` считал wall-time включая паузы → на resume `elapsedSec` прыгал, и порог завершения был ненадёжным. Состояние `startedAt` убрано. Решение владельца: ТВ-просмотр учитывается в отчётах `practice-by-chakra` / `user_daily_stats`, но только досмотренный до конца. `spec.md` (v1.37), `history.md`, `dependencies.md`, `docs/remote-play/README.md`, `open_questions.md` (вопрос снят), `CHANGELOG.md`. tsc + vitest + i18n-sync check чисты.

- **2026-07-06 (31):** Подтверждение завершения практики на пульте ТВ. Раньше ✕ закрывал окно пульта без предупреждения, а «Стоп» только ставил `status="stopped"` и оставлял окно открытым. Теперь **и ✕, и «Стоп»** открывают общий `PracticeStopConfirmDialog` (тот же, что у телефонной асаны/дыхания/медитации — строки `getCoherenceBreathStrings(locale)`: «Завершить практику?» / «Завершить» / «Продолжить»). По «Продолжить» — диалог закрывается, практика продолжает играть. По «Завершить» — `remotePlay.stop()` (best-effort) + `router.back()`: видео на ТВ останавливается (ТВ уходит в `connectedIdle`, pairing сохраняется для следующего запуска), окно пульта закрывается, пользователь возвращается туда, откуда запустил (Каталог / День / коммуникатор — `router.back()` по стеку push-роута). Добавлены состояние `finishing` (блокирует повторный нажатие и переключает `finishLabel` на `finishingButton` — «Завершаем…»/«Finishing…», новая строка `TvRemoteStrings.finishingButton`) и `showStopConfirm`. «Пауза»/«Продолжить»/«Запустить заново» работают без подтверждения как и раньше. Запись `practice_sessions` для ТВ-просмотра **не ведётся** (у пульта нет `practiceId`/`chakra` — только `vimeoId`/`title`/`durationSec`/`audiotrack`) — вынесено в open_questions. `spec.md` (v1.36), `history.md`, `docs/remote-play/README.md`, `docs/04_workspace/open_questions.md`, `CHANGELOG.md`. tsc + vitest + i18n-sync check чисты.

- **2026-07-06 (30):** Подсказка пульта ТВ (`TvRemoteStrings.openOnTvHint`) — ссылка заменена с `https://zamkovoi.yoga` на `https://zamkovoi.yoga/tv/` (ru/en + fallback в `TVRemoteScreen`), чтобы совпадала с экраном подключения `/connect-tv` (там та же `/tv/`). `spec.md` (v1.35), `history.md`, `docs/remote-play/README.md`, `CHANGELOG.md`. tsc + i18n-sync check чисты.

- **2026-07-06 (29):** Фикс залоченного пульта ТВ + локализация статусов. (1) **Залипание**: `stopRemotePlayback` стирал `vimeo_id` при стопе — после стопа `resume()` ставил `status="playing"`, но без `vimeo_id` ТВ-сниппет (`status === "playing" && next.vimeo_id`) не монтировал плеер → на ТВ idle, на телефоне «playing», при этом «Пауза» была залочена по `status==="stopped"`, а «Стоп» делал no-op. Перестал чистить `vimeo_id` в `stopRemotePlayback` (только `status: "stopped"`); ТВ-сниппет на «stopped» и так зовёт `destroyPlayer`+`connectedIdle`, так что чистка была избыточной. (2) **Конечный автомат пульта** (`TVRemoteScreen`): главная кнопка стала динамической — `status==="stopped"` → **«Запустить заново»** (`remotePlay.playVimeo(params.vimeoId, params.audiotrack)` + сброс `startedAt`/`elapsedSec`), `"paused"` → «Продолжить» (`resume`), иначе → «Пауза» (`pause`); `pauseOrResume` игнорирует нажатия в состоянии `stopped`. «Стоп» disabled при `stopped`. Цикл «стоп → запустить заново → стоп» теперь замкнут. (3) **Локализация статусов**: `TvRemoteStrings.status(value)` больше не подставляет сырой английский статус — добавлена карта `playing→запущено / paused→пауза / stopped→остановлено / waiting→ожидание / closed→закрыто` (ru/en); fallback `statusFn` в `TVRemoteScreen` дублирует карту. Добавлены `replayButton` («Запустить заново»/«Replay») и `replayFailed`. `spec.md` (v1.34), `history.md`, `docs/remote-play/README.md`, `CHANGELOG.md`. tsc + vitest + i18n-sync check чисты.

- **2026-07-06 (28):** Фикс пустого экрана `/tv-remote` + замыкание цикла disconnect. (1) **`TVRemoteScreen` не рендерил текст** — на устройстве модальный шелл `ModalScreenLayout` + карточка в `View content flex:1` отрисовывали `View`-дети и кнопку ✕, но **ни один `<Text>`** (даже raw `<Text>` с явным красным цветом) не был виден; логи подтвердили, что строки (`titleFallback=Практика на ТВ`, `pauseButton=Пауза`) и тема (`scheme=light`, `textPrimary=#0f172a` на `surfaceElevated=#ffffff`) корректны — т.е. дело не в i18n и не в цвете, а в модальном шелле. Экран переписан на тот же шелл, что и рабочий `app/asana-practice.tsx`: `StackScreenLayout` + `StackScrollView` + `SurfaceCardView tone="elevated"` + `FloatingCloseButton` (прямым ребёнком, не через `overlay`); роут `tv-remote` в `app/_layout.tsx` возвращён к обычному push (`headerShown: false`, `presentation: "fullScreenModal"` убран). (2) **Цикл «Отключить ТВ → переподключить → запустить»**: раньше `TVRemoteScreen.disconnect` делал `router.replace("/connect-tv")` без параметров → после повторного подключения кнопка была «Закрыть», а не «Начать практику». Теперь параметры практики (`vimeoId`, `title`, `durationSec`, `audiotrack`) прокидываются через весь ТВ-путь: хук `useAsanaRemotePlayLauncher` и `onRemotePlay` каталога → `/connect-tv` (нет связи) или `/tv-remote` (подключён); `ConnectTVScreen.startPractice` → `/tv-remote` с полным набором; `TVRemoteScreen.disconnect` → `router.replace("/connect-tv", { vimeoId, title, durationSec, audiotrack })`. После переподключения снова появляется «Начать практику» → `playVimeo` + `/tv-remote`. Убран красный debug-маркер и verbose-логи из `TVRemoteScreen`; защитный `str`-fallback для подписей оставлен. `spec.md` (v1.33), `history.md`, `docs/remote-play/README.md`, `CHANGELOG.md`. tsc чисто.

- **2026-07-06 (27):** Правки полевого фидбека по п. (26). (1) **ТВ-страница — полная локализация, включая eyebrow.** В `docs/remote-play/wordpress-snippet.html` добавлена строка `remotePlay` для всех 8 локалей (ru «Удалённый просмотр», en «Remote Play», de «Fernwiedergabe», fr «Lecture à distance», it «Riproduzione remota», es «Reproducción remota», pt «Reprodução remota», nl «Weergave op afstand»); eyebrow рендерится как `${t("appName")} ${t("remotePlay")}` (раньше «Remote Play» был захардкожен латиницей → на русском глазу торчал английский хвост). Локализован и текст `unavailable` в каждой локали. Статичный fallback-HTML в карточке приведён к английскому (фолбэк-локаль). (2) **`app/asana-practice.tsx`** — убран подзаголовок «Выберите, где смотреть практику…» под названием (`ScreenHeader subtitle` удалён; `AsanaScreenStrings.subtitle` убран из типа и ru/en). (3) **`TVRemoteScreen` рендерился пустым каркасом** — роут `tv-remote` в `app/_layout.tsx` был обычным push-экраном, а `ModalScreenLayout` рассчитан на модальное представление; добавлен `presentation: "fullScreenModal"` (как у `connect-tv`), теперь контент (заголовок, `openOnTvHint`, прогресс, кнопки, «Отключить ТВ») отрисовывается. (4) **`ConnectTVScreen` — «Начать практику» после подключения.** Хук `useAsanaRemotePlayLauncher` и `onRemotePlay` каталога теперь валидируют `vimeoId` ДО навигации и при отсутствии подключения идут на `/connect-tv` с параметрами практики (`vimeoId`, `title`, `durationSec`, `audiotrack`). `ConnectTVScreen` читает эти параметры через `useLocalSearchParams`; после успешного подключения (`showLinkedLine`) при наличии `vimeoId` кнопка «Закрыть» заменяется на **«Начать практику»** (`ConnectTvStrings.startPracticeButton`, ru/en), нажатие → `remotePlay.playVimeo(vimeoId, audiotrack)` + `router.replace("/tv-remote", { title, durationSec })`: окно подключения закрывается, на ТВ-браузере запускается видео, на телефоне открывается пульт. Без параметров практики кнопка остаётся «Закрыть» (обратная совместимость). `spec.md` §2/§5, `history.md`, `dependencies.md`, `docs/remote-play/README.md`, `CHANGELOG.md`. tsc чисто; 600/600 тестов; i18n-sync green. **Деплой-шаг (вне кода):** обновить WordPress Custom HTML блок на `zamkovoi.yoga/tv` свежим `wordpress-snippet.html`.

- **2026-07-06 (26):** Переработка UX асан/ТВ по полевому фидбеку (6 пунктов). (1) **ТВ-страница — язык браузера, не приложения.** `docs/remote-play/wordpress-snippet.html` теперь определяет язык интерфейса по `navigator.language` с **английским** fallback (был `ru`), `applyLocale`/`row.locale` убраны; `select`-запросы и `fingerprint(row)` больше не включают `locale`. Колонка `tv_sessions.locale` **удалена** миграцией `20260705203000_remote_play_drop_locale.sql`; `linkDevice`/`playVimeoOnRemote`/`RemotePlayProvider` перестали принимать/писать `locale` (`useAppLocale` убран из провайдера); `services/supabase-types.ts` и `ACTIVE_SELECT` обновлены. `audiotrack` остаётся — язык звуковой дорожки видео следует за локалью приложения, которую браузер ТВ не знает. (2) **Каталог:** удалена панель «TV connected · code» + «Сменить ТВ» над фильтрами асан (`remotePanel`/`remoteText`/`remoteButton` стили и `AppButton`-импорт убраны; deps `listHeader` почищены). (3) **Карточка асаны — всегда две кнопки.** `PracticeCard` для `kind="yoga"` всегда показывает «Открыть на телефоне» (primary) + «Открыть на ТВ» (secondary), независимо от `connected` и контекста; `remotePlayConnected`-пропс убран. Новый хук **`useAsanaRemotePlayLauncher(strings)`** (`modules/practices/ui/useAsanaRemotePlayLauncher.ts`) инкапсулирует ТВ-путь: не подключён → `/connect-tv`; подключён → `playVimeo(vimeoId, vimeoAudiotrackForLocale(locale))` + `/tv-remote`. Каталог передаёт `onRemotePlay` с tier-проверками и per-practice busy; коммуникатор (`Communicator.tsx`) теперь тоже передаёт `onRemotePlay`/`remotePlayDisabled` через тот же хук (раньше в коммуникаторе кнопки «Открыть на ТВ» не было). `onRemotePlay` каталога и хук передают `audiotrack` (раньше каталог звал `playVimeo(vimeoId)` без дорожки → на ТВ всегда `ru`). (4) **`app/asana-practice.tsx` — только телефон.** Убраны сегмент «Телефон/ТВ», `TvPanel`, `mode`-state, SecureStore-персист `harmonizer.asana.playback_mode.v1`, `useRemotePlay()`, `launchOnTv`/`openRemote`. Экран: `ScreenHeader` + `PhonePlayer` + «Завершить практику» + `PracticeStopConfirmDialog`. `context.playback_mode` теперь константа `"phone"`. Это чинит баг «странного окна с ТВ-кодами» при нажатии «Открыть на телефоне» (раньше persisted `mode="tv"` показывал TvPanel с кодом пары). (5) **`ConnectTVScreen`** не автосабмитится на 4 символах (`onSubmitEditing` убран) — только по кнопке; после успеха (`showLinkedLine`) кнопка меняет подпись на «Закрыть» и закрывает окно (`router.back()`) как ✕; диагностическое сообщение («ТВ подключен»/ошибка) остаётся между полями и кнопкой. (6) **`TVRemoteScreen`** переделан: показывает `openOnTvHint` («Откройте на телевизоре или на компьютере страницу https://zamkovoi.yoga»), кнопки пауза/продолжить + стоп, и новую кнопку **«Отключить ТВ»** → `remotePlay.disconnect()` + `router.replace("/connect-tv")`. (7) **`RemotePlayProvider`** получил `disconnect()`: best-effort `stopRemotePlayback` + локальный сброс сессии (`connected` → false), чтобы окно подключения снова показало ввод кода. (8) **`AppDialog.actionsRow`** `justifyContent: "space-between"` → `"center"` (`gap` уже был) — кнопки в `PracticeStopConfirmDialog` (медитация/дыхание/асаны) теперь центрированы с небольшим интервалом, а не разведены к краям. Строки: `ConnectTvStrings.closeButton`, `TvRemoteStrings.openOnTvHint`/`disconnectButton`/`disconnectFailed` (ru/en inline, typed-модуль). `spec.md` §2/§5, `history.md`, `dependencies.md`, `docs/remote-play/README.md`, `MAP.md` (v1.51), `CHANGELOG.md`. tsc чисто; 600/600 тестов; i18n-sync green. **Деплой-шаги (вне кода):** применить миграцию `20260705203000_remote_play_drop_locale.sql` к Supabase и обновить WordPress Custom HTML блок на `zamkovoi.yoga/tv` свежим `wordpress-snippet.html`.

- **2026-07-05 (25):** ТВ-страница Remote Play теперь говорит на языке приложения. Раньше весь текст `docs/remote-play/wordpress-snippet.html` был русским хардкодом, а язык приложения никак не передавался на ТВ. (1) Миграция `20260705193000_remote_play_locale.sql` добавляет nullable `locale text` в `tv_sessions`. (2) `modules/remote-play/RemotePlayProvider.tsx` читает `useAppLocale().locale` и пробрасывает её в `linkDevice(pairingCode, userId, locale)` и `playVimeoOnRemote(sessionId, vimeoId, audiotrack, locale)` (публичный API `useRemotePlay().linkDevice`/`playVimeo` не изменился — локаль подтягивается внутри); `services/supabase-types.ts` и `ACTIVE_SELECT` обновлены под `locale`. (3) Сниппет получил компактную инлайн-таблицу `STRINGS` для 8 локалей (только ресурсы — `appName`, статусы, ошибки; полного дублирования страницы нет), `pickLocale`/`detectLocale`/`stringsFor`/`t`/`applyLocale`/`renderStatic`; язык определялся так: `row.locale` (после pairing) → `navigator.language`-детект (до pairing) → `ru` fallback; `locale` входил в `fingerprint(row)`. **Откатано в (26)** — колонка `locale` удалена, страница берёт язык из браузера (fallback `en`); см. 2026-07-06 (26). `spec.md` §2, `history.md`, `docs/remote-play/README.md`, `MAP.md` (v1.50), `CHANGELOG.md`.

- **2026-07-05 (24):** Языковая настройка практики асаны теперь следует локали приложения. Раньше телефонный `PhonePlayer` звал `vimeoEmbedHtml(vimeoId)` без `audiotrack` → всегда дефолт `"ru"`, а ТВ-путь вообще держал дорожку хардкодом в WordPress-сниппете (`VIMEO_AUDIO_TRACK = "ru"`), `tv_sessions` не хранил дорожку. По данным импорта (`scripts/import-vimeo-asanas.mjs` `vimeo_embed.audiotrack_by_locale`) у видео ровно две дорожки — `ru` и `en`; контракт: RU → `ru`, любая другая из 8 локалей → `en`. (1) Новый **`vimeoAudiotrackForLocale(locale)`** в `modules/practices/core/vimeo.ts` (`ru → "ru"`, остальное → `"en"`); тест на 8 локалей. (2) `app/asana-practice.tsx` считает `audiotrack` из `useAppLocale().locale` и передаёт его в `vimeoEmbedHtml(vimeoId, audiotrack)` (телефон) и в `remotePlay.playVimeo(vimeoId, audiotrack)` (ТВ); в `context` завершённой сессии добавлено `audiotrack`. (3) ТВ-инфраструктура: миграция `20260705190000_remote_play_audiotrack.sql` добавляет nullable `audiotrack text` в `tv_sessions`; `services/supabase-types.ts` обновлён; `playVimeoOnRemote(sessionId, vimeoId, audiotrack?)` и `useRemotePlay().playVimeo(vimeoId, audiotrack?)` пробрасывают дорожку в строку; `ACTIVE_SELECT` += `audiotrack`. (4) `docs/remote-play/wordpress-snippet.html` читает `row.audiotrack` в `vimeoEmbedUrl(vimeoId, audiotrack)` (fallback `VIMEO_AUDIO_TRACK = "ru"` для legacy/null строк), `audiotrack` входит в `fingerprint(row)` и в `wantKey` `mountPlayerIfNeeded`, поэтому смена дорожки на том же `vimeo_id` (пользователь сменил локаль и перезапустил) перемонтирует плеер; select-списки createSession/fetchLatestSession включают `audiotrack`. **Деплой-шаги (вне кода):** применить миграцию к Supabase (`supabase db push`) и обновить WordPress Custom HTML блок на `zamkovoi.yoga` свежим `wordpress-snippet.html` — пока сниппет не обновлён, ТВ остаётся на русском. `spec.md` §2/§5, `history.md`, `docs/remote-play/README.md`, `MAP.md` (v1.49), `CHANGELOG.md`. tsc чисто; practices+remote-play тесты 23/23.

- **2026-07-05 (23):** Полировка экрана асаны по фидбеку полевого теста. Убран весь блок метаданных под плеером (`PracticeID` / `Vimeo ID` / `Длительность` / `Чакры` / `Дата записи` / `Источник запуска`) и компонент `MetaRow`; убрана кнопка «Назад к каталогу». Под видео/ТВ-панелью теперь одна кнопка **«Завершить практику»** — прямо под плеером. Прежнее поведение, сбивавшее с толку (тап по «Завершить практику» сразу менял подпись кнопки на «Практика сохранена» и оставался на экране), заменено на единый с дыханием/медитацией flow: и кнопка «Завершить практику», и `FloatingCloseButton` (✕) открывают **`PracticeStopConfirmDialog`** с теми же строками `getCoherenceBreathStrings(locale)` (`stopConfirmTitle`/`stopConfirmMessage`/`stopConfirmNo`/`stopConfirmYes`), что и у дыхания/медитации. По подтверждению «Завершить» экран пишет сессию (`recordPracticeSession`, `completionPct: 100`, `context.playback_mode`) и уходит через новую локальную `exitAfterPractice(launchSource)`: `launchSource = "assistant" | "day"` → `router.replace("/day")`, иначе `router.back()` (в каталог). Пока идёт запись, finish-кнопка диалога показывает `completingButton` («Сохраняем...»), `completionSaved`-state удалён. Из `modules/practices/i18n/asanaScreen.ts` убраны ставшие неиспользуемыми строки (`metaPracticeId`/`metaVimeoId`/`metaDuration`/`metaChakras`/`metaRecordedAt`/`metaLaunchSource`/`valueUnknown`/`valueNotSelected`/`formatDurationMinutes`/`backToCatalogButton`/`completedButton`) и хелперы `paramsRecord`/`durationMinutes` из роута. `spec.md` §2/§5, `CHANGELOG.md`. tsc чисто; practices-тесты 21/21.

- **2026-07-05 (22):** Регрессия (21) на устройстве: WebView открывал Safari на `https://zamkovoi.yoga` вместо рендера плеера. Причина — я переопределил `originWhitelist={["https://*/*"]}` (с path-сегментом), а top-level навигация WebView при `source={{ html, baseUrl }}` идёт по baseURL `https://zamkovoi.yoga` (без path) и под шаблон `https://*/*` не попадает → `react-native-webview` считает её внешней и вызывает `Linking.openURL(baseURL)`. Дефолтный `originWhitelist` (`['http://*','https://*']`, без path) подходит. Фикс: убрал переопределение `originWhitelist` в `app/asana-practice.tsx` `PhonePlayer`. Universal Links не при чём (`app.json` без `associatedDomains`). tsc чисто; vimeo-тесты 8/8.

- **2026-07-05 (21):** Фикс `PrivacyError` на телефоне. Поле-тест показало, что прямой `WebView source={{ uri: vimeoEmbedUrl(vimeoId) }}` отдаёт Vimeo-ошибку «Because of its privacy settings, this video cannot be played here»: видео доменно-привязаны к `zamkovoi.yoga`/`zamkovoi.ru` (Vimeo «Where can this be embedded?» = specific domains, см. `scripts/import-vimeo-asanas.mjs` `EMBED_ORIGIN`, `docs/remote-play/README.md`), а WebView телефона грузил `player.vimeo.com` напрямую без `Referer` с разрешённого домена. ТВ-страница работает именно потому, что её iframe живёт на `zamkovoi.yoga`. Фикс без деплоя новой страницы: `modules/practices/core/vimeo.ts` получил **`vimeoEmbedHtml(vimeoId, audiotrack)`** — полный HTML-документ с тем же iframe (`src` = `vimeoEmbedUrl`, `allow="autoplay; fullscreen; encrypted-media"`), который `app/asana-practice.tsx` грузит через `WebView source={{ html, baseUrl: VIMEO_EMBED_BASE_URL }}`, где **`VIMEO_EMBED_BASE_URL = "https://zamkovoi.yoga/"`**. На iOS `WKWebView.loadHTMLString:baseURL:` и на Android `loadDataWithBaseURL` делают документ исходящим с `https://zamkovoi.yoga/`, поэтому запрос iframe к `player.vimeo.com` несёт `Referer: https://zamkovoi.yoga/` и проходит privacy-чек. Тест `vimeo.test.ts` (+3 на `vimeoEmbedHtml`/`VIMEO_EMBED_BASE_URL`); `spec.md` §2/§5, `CHANGELOG.md`.

- **2026-07-05 (20):** Асаны можно смотреть и на телефоне, и на ТВ. `app/asana-practice.tsx` перестал быть заглушкой: сверху — сегмент «Телефон / ТВ» (UI-state, persists в SecureStore ключ `harmonizer.asana.playback_mode.v1`, не query-параметр). **Телефон** — встроенный Vimeo-плеер на `react-native-webview` (пакет уже был в зависимостях, текст «dev-client не содержит native WebView» оказался устаревшим); embed URL строит новый **`modules/practices/core/vimeo.ts`** → `vimeoEmbedUrl(vimeoId)` = `https://player.vimeo.com/video/<id>?audiotrack=ru` (без других query-параметров — иначе ломается русская аудиодорожка, см. `docs/remote-play/README.md`); тест `vimeo.test.ts`. **ТВ** — переиспользование `modules/remote-play`: если `useRemotePlay().connected` — «Запустить на ТВ» вызывает `playVimeo(vimeoId)` и навигирует на `/tv-remote` с params `{ title, durationSec }`; если не подключён — «Подключить ТВ» → `/connect-tv`; при живом `session.vimeo_id` — доп. кнопка «Открыть пульт». В `context` завершённой сессии добавлено `playback_mode`. Обновлены `modules/practices/i18n/asanaScreen.ts` (новые строки `modePhone`/`modeTv`/`tvConnectedMeta`/`tvStatus`/`launchOnTvButton`/`connectTvButton`/`openRemoteButton`/`phoneVideoMissing`, убран устаревший `unavailableNote` → нейтральный `subtitle`; ru/en inline как у соседних typed-модулей). `spec.md` §2/§5, `dependencies.md` (+ `remote-play`), `MAP.md` (+ модуль `remote-play`), `open_questions.md` (закрыт пункт про Vimeo на мобильном), `CHANGELOG.md`. tsc чисто; practices-тесты 18/18; i18n-sync green.

- **2026-07-05 (19):** Five meditation-panel parity fixes vs the breath panel from a field test. (1) **Translucency** — `PracticeOverlayPanel` card switched from `theme.colors.surface` (`rgba(30,32,38,0.92)`, near-opaque) to a dedicated `PANEL_CARD_BACKGROUND = "rgba(20,22,28,0.72)"`; on meditation the black canvas + dark mandala behind made 0.92 read as solid, while the breath panel over the lighter footer looked semi-transparent. Both panels are now equally translucent. (2) **Auto-show timing** — meditation overlay was `initialVisible: true` (appeared on screen open); now `initialVisible: false` + a one-shot `OVERLAY_HINT_DELAY_MS = 5_000` timer calls `showOverlay()`, so the screen opens bare and the placard slides up 5 s in, then auto-hides after the same `OVERLAY_AUTOHIDE_MS = 4_000` as breath. (3) **Smoother slide** — `PANEL_SLIDE_DURATION_MS` 220 → 320 and easing from `Easing.out/in(Easing.cubic)` to `Easing.bezier(0.22,1,0.36,1)` (show) / `Easing.bezier(0.5,0,0.75,0)` (hide); applies to both panels (kept identical). (4) **Close-button alignment** — meditation `FloatingCloseButton` style now pins `top: 54, right: 18` (was default `insets.top + 18` ≈ 77 px on notched phones), matching the breath screen's fixed `topCloseButton`. (5) **Stop-confirm text unified** — the meditation `PracticeStopConfirmDialog` now renders the **breath** stop-confirm strings (`getCoherenceBreathStrings(locale)`) for title/message/continue/finish, so the dialog is literally the same text in both practices. The shared message was reworded in all 8 locales: RU "Практика будет остановлена, а результаты по прошедшему отрезку не будут засчитаны." (was "...по текущему отрезку не будут рассчитаны.") and equivalent translations in EN/DE/FR/IT/ES/PT/NL. i18n sync green (en); tsc clean; module tests 26/26.

- **2026-07-05 (18):** Fix two meditation-panel regressions from (17), found in field test. (1) **Light/white panel on a light-mode phone**: `app/_layout.tsx` derives the UI theme from the system color scheme; the breath screen forces dark via `<ThemeProvider value={defaultTheme}>` at the screen level but the meditation screen did not, so `PracticeOverlayPanel`'s `useTheme()` returned the light palette. Fixed at the **base element** (`PracticeOverlayPanel`): it now uses `defaultTheme` directly — always dark, self-sufficient, independent of the surrounding ThemeProvider (per the user's instruction to fix the base element, not per-screen). The meditation screen is also wrapped in `<ThemeProvider value={defaultTheme}>` so the `FloatingCloseButton` and the "Завершить" `AppButton` (outside the panel) are dark too. (2) **Tap on the meditation screen did nothing**: the GL canvas captures touches, so wrapping it in `<Pressable onPress>` never fired. Replaced with a transparent tap-backdrop `Pressable` (`absoluteFillObject`, transparent, `zIndex: 10`) rendered over the canvas and below the panel (40) / close button (30) — same pattern as `BreathPracticeShell.tapBackdrop`. tsc clean; module tests 39/39.

- **2026-07-05 (17):** Unified meditation ("Вспышка") control panel with the breath practice panel via a shared shell `modules/ui/PracticeOverlayPanel.tsx`. The shell owns the slide-up/down animation, themed card, title block, time row, 4px progress track, and a `controls: ReactNode` slot. `BreathOverlayControlPanel` is now a thin wrapper that delegates to the shell and passes the close-X + beats pill + chevrons as the slot (breath visuals unchanged). `SacredSymbolStreamScreen` rewritten to use the same shell with a "Завершить" button slot; the top header (title/description) removed (fully black screen + canvas); the test-mode block (density chips, new-line/next-mandala/pause) removed; the mood/rating dialog removed. Behavior parity with breath: same `useImmersiveOverlayAutohide({ autoHideMs: 4000, initialVisible: true })` (tap to show, auto-hide after 4 s, tap again to hide), same `FloatingCloseButton` shown while the panel is visible. **Session-completion mark moved to practice end**: `recordPracticeSession({ selfRating: null, completionPct: 100 })` now fires in the elapsed-tick effect when `elapsed >= durationMs` (guarded once by `completionSavedRef`), not in a mood dialog — previously closing via X without rating left no completion row in `practice_sessions`. At completion the screen fades to black (600 ms) then returns to origin (`assistant`/`day` launchSource → `router.replace('/day')`, else `router.back()` to the catalog card or Day tab). Added `minutesShort` to `SymbolStreamStrings`; updated `stopConfirmMessage` to drop the rating reference. i18n sync green (en); tsc clean; module tests 39/39.

- **2026-07-04 (16):** Remove mood-picker screen + fade-in results from black + LLM interpretation refactor. Field test `1783174144609` (BLE 20 min, algoVer 1.2.27). (1) The "стало лучше/хуже" smiley screen between practice and results is removed — `ResultsView` has no `stage="mood"` state; the session auto-records on results mount with `selfRating: null`. Removed `ResultsMood` type, `moodRef`, `handleMoodSelect`, mood JSX + styles. (2) Results now fade in from black: the black curtain (zIndex 80, previously sensor→running only) fires on `phase==="results"` — snaps to opacity 1, then `withTiming(0, 600 ms)` after a 60 ms mount delay, so results emerge from darkness. (3) LLM interpretation no longer receives subjective feedback: `subjectiveMood` removed from `BreathPracticeInterpretationRequest` and the server prompt's "Subjective feeling after practice" line. (4) Only positive/neutral metrics are sent to the LLM: `buildInterpretationOutcomePayload` filters `seriesInsights` by direction — coherence/RMSSD/RSA included only if not declined (Δ ≥ −0.1), stress only if not risen (Δ ≤ 0.1), pulse always. A worsened metric is omitted from `seriesInsights`, and the server prompt forbids the model from mentioning metrics absent from `seriesInsights` or reading them elsewhere in the payload. (5) Server prompt now explicitly forbids claiming the user is a beginner / learning a new technique (the model had invented "не редкость при первых попытках освоить новую технику" from a falling RMSSD; with RMSSD now omitted when it declines, the trigger is gone too). Tone set for a woman 35–60, encouraging. Server prompt change is paired with communicator history. The RMSSD/RSA spike at ~4 min in the same export was diagnosed as a Polar H10 single-packet HR-field glitch (HR 81→61→46→81 at t=273), self-corrected in ~10 s — not an algorithm bug, left untouched. algoVer → 1.2.28 (paired with biofeedback history 2026-07-04 (18) stress rescale).

- **2026-07-04 (15):** Keep-awake fix — phone no longer sleeps during practice. Field test `1783165022235` (BLE 5 min, sensor removed 110→150 s, algoVer 1.2.26) showed the phone entering sleep ~41 s after start and again at ~113 s (two pulseLog timestamp gaps +8.3 s / +10.0 s with no beats between), which suspended the JS/VC loop and stretched the sensor-off gap from the intended 40 s to ~108 s (sensor returned at 150 s but the wearable reconnected only at 221 s). Root cause: keep-awake used a manual `activateKeepAwakeAsync(tag)` in `useEffect` with an async `deactivateKeepAwake(tag)` cleanup — under React 18 Strict Mode (dev-client) the mount→cleanup→mount cycle let the first cleanup's async `deactivateKeepAwake` resolve after the second mount's `activateKeepAwakeAsync`, dropping the tag so iOS's 30 s idle timer fired. Fix: replaced the manual effect with a conditional `<PracticeKeepAwake>` component that calls the `useKeepAwake(tag)` hook (expo's tag-lifecycle-safe hook) and is mounted only while `phase ∈ {warmup, qualityCheck, running}`; unmount on phase exit releases the tag. An `AppState` listener re-runs `activateKeepAwakeAsync` on `active` (iOS can reset the idle timer after backgrounding). Same round confirms 1.2.26 algorithm work holds: `1783164624005` (camera 5 min, 3×3 s + 1×20 s lifts) — all advanced metrics correctly withheld (camera computes no RMSSD/coherence/RSA/stress); only nonLive periods are the deliberate lifts (4.8 s at 130 s, 30.5 s at 200 s), no spurious breaks; guidance chart flat-interpolates through the lifts as designed. `1783165720980` (BLE 10 min, ~1 min push-ups at 100 s) — the trend-aware dense-HRV gate yields continuous RMSSD/stress/RSA through the 70→131 bpm ramp (only 0.9 s nonLive at startup, no metric gaps during push-ups); small coherence gaps during exercise are honest (FFT window lacks stable breathing coverage while HR swings); RMSSD trough + stress peak during exercise recovery is physiologically correct. algoVer → 1.2.27 (paired with biofeedback history 2026-07-04 (17)).

- **2026-07-04 (14):** Trend-aware dense-HRV gate + RSA cap 20→28 + BLE reconnect reliability + equal-width picker buttons. Field tests `1783158512492` (BLE 10 min, push-ups mid) and `1783157506376` (BLE 5 min, sensor removed 100→140 s): (1) large RMSSD + stress gaps during the push-up HR ramp despite continuous RR — the 1.2.22 sequential-deviation gate (`max(110, median·0.22)` over a 12-RR context median) rejected the whole HR-ramp RR run because the context median lagged at the pre-ramp ~816 ms while RR fell to 460 ms; <8 accepted RR in many 30 s windows → RMSSD/stress gaps (sim: 63 windows). Fix: trend-aware escape hatch in `buildDenseHrvSeriesFromBeats` — a rejected RR is admitted when the last 4 accepted RR form a monotonic ramp (each step ≥ `DENSE_HRV_TREND_MIN_STEP_MS = 8` ms same direction) and the candidate continues it; a lone 576/499 among stable 730 has no preceding ramp, so motion noise is still rejected (sim: 0 windows <8). (2) `RSA_RESULTS_OUTLIER_BPM` 20 → 28 — a genuine 21 bpm RSA point during the push-up breath swing was deleted, punching a small RSA gap where HR rose; 28 keeps real exercise peaks and still rejects off-body/synthetic garbage. (3) BLE reconnect loop fixed: `useWearableScanner.startScan` and `useRememberedWearableProbe` now also consult `manager.devices([HR_SERVICE_UUID])` (OS-known peripherals) — a remembered Polar H10 that is not advertising (held by another Central / post-reconnect settle) is found immediately instead of looping "не найден"; stale `isConnectable === false` entries skipped. (4) Picker dialog buttons now wrapped in `<View flex:1 flexBasis:0 minWidth:0>` with inner `AppButton width:"100%"` → identical widths regardless of label length (the previous `flex:1` on `AppButton` let intrinsic content size override, "Повторить поиск" wrapped to 3 lines while "Закрыть" stayed wide). algoVer → 1.2.26 (paired with biofeedback history 2026-07-04 (16)).

- **2026-07-04 (13):** Dense-HRV motion-RR gate (results charts). Field test `1783116290771` (BLE 10 min, push-ups at 100 s) showed an RMSSD needle spike to 56 ms and a stress-index V-dip to ~0 right at the push-up burst, coinciding with the first RSA gap. Root cause: the Polar H10 emits motion-corrupted RR during movement (R-peak detection errors — the series jumps `576↔717↔499↔691 ms`, ΔRR ±148–192 ms, while the real rhythm is ~730 ms). These pass the dense-HRV hard range (300–1500 ms) and are not caught by Hampel (they arrive in bursts, so the running MAD stays large and the median shifts with them), injecting huge ΔRR → fake RMSSD spike and inflating MxDMn → SI → 0 → stress plunges. Fix: a sequential-deviation gate in `buildDenseHrvSeriesFromBeats` (reject RR deviating more than `max(110 ms, median·0.22)` from the running context median of the last 12 accepted RR) applied before Hampel — same rule the `PulseBpmEngine` uses to clean wearable BPM. Simulation on the field export drops the RMSSD peak 56 → ~25 ms and rejects only 13/770 RR (the motion noise); resting RSA variability (±100 ms ≈ 14%) is preserved; the stress V-dip resolves with it. The two RSA gaps during the push-up burst are honest and retained: per-second RSA there is 27–40 bpm (real HR swing 60→135 over a breath), exceeding `RSA_RESULTS_OUTLIER_BPM = 20`, so the outlier filter gaps it — RSA is not meaningful during exercise. Confirmed no other bug-caused RSA gap sources. Paired with biofeedback history 2026-07-04 (12) wearable BPM responsiveness (the 131 plateau fix). algoVer → 1.2.22.
- **2026-07-03 (12):** Results-chart header window + coherence auto-scale + pulse-source persistence + seriesInsights aligned to chart headers. Three polish fixes from field tests `1783096820335` / `1783098346952` (BLE) and the post-BLE-practice catalog behavior. (1) **Header `start → end` window**: `summarizeSeries` previously took the mean of the first/last **third** of plotted points — on a 10-min session that is ~3–4 min, which does not represent "the value at the start/end of measurement". Now it takes the mean of the first and last **60 s** of the measured series (`RESULTS_HEADER_WINDOW_MS`), so `7% → 9%` etc. actually reflects the beginning and end of the real measurement window (the user's suggestion, 1–2 min; 60 s is the balance between locality and stability against a single noise spike). This applies to **all** charts (pulse, coherence, RSA, RMSSD, stress), and the same `startMean`/`endMean`/`endMinusStart` are what the LLM interpretation receives via `seriesInsights`. (2) **Coherence chart amplitude**: the coherence chart was pinned to a fixed 0–100% Y-axis, so real sessions living at 0–30% used only the bottom third of the panel. The Y-axis now auto-scales with 25% headroom (`autoMaxHeadroom` prop on `ResultsMetricChart`, floor stays 0% — 0 is a meaningful "no coherence"), so 0–30% sessions fill the panel and beat-to-beat variation is visible. The header % and the headline coherence avg/max are unchanged (still real percent). (3) **Pulse-source persistence bug**: after closing a BLE practice the catalog reset the pulse source to "phone camera" — `PracticeCard`'s probe-fallback effect wrote `preferredSensorMode="fingerCamera"` to SecureStore whenever the 4 s remembered-device probe failed, and the probe routinely fails transiently right after `BleHeartRateSource` disconnects on practice exit (the strap doesn't re-advertise within 4 s). The persisted preference is now the user's INTENT and is never overwritten by a probe failure: the effect only adjusts the in-memory `selectedSensorMode` (camera while the strap is genuinely unavailable, `ble` once the probe succeeds again). Manual dropdown changes still write the preference. So returning from a BLE practice keeps "ble" selected once the strap is seen again, and only falls back to camera UI while the strap is truly unavailable. (4) **seriesInsights → LLM alignment**: `buildInterpretationOutcomePayload` previously ran `summarizeSeries` on the raw `resultsGraphs` (which for coherence includes the leading zero seconds before the FFT window fills), so the `startMean` sent to the Gemini interpretation was dragged toward 0 even though the chart header (computed from `prepareSeriesForDisplay` with `trimZeroEdges`) starts at the first real measurement. It now runs `summarizeSeries` on the same display-prepared point arrays that feed the chart headers, so the LLM's `startMean`/`endMean`/`endMinusStart` per metric exactly match the `start → end` values the user sees — the interpretation reflects real start-vs-end change for every metric. The server prompt already instructs the model to compare start/mid/end from `seriesInsights`. algoVer → 1.2.21 (paired with biofeedback history 2026-07-03 (11) off-body frozen-RR detection).

- **2026-07-03 (11):** Finger-off bridge cap (engine/pipeline). Field test `1783095121380` (algoVer 1.2.19) showed a flat ~67.1 bpm "live" plateau for ~12 s before the 20 s-lift gray bar — the bridge held the baseline while the finger was deliberately off, masking the loss as fake live pulse. Fix is biofeedback-engine-only (see biofeedback history 2026-07-03 (10)): `OPTICAL_FINGER_OFF_BRIDGE_CAP_MS = 4 s` cuts the bridge to gray when the finger is off beyond the cap, while preserving the full 12 s re-lock budget when the finger is on. Breath-screen behavior is unchanged; the effect is that a deliberate long finger-off no longer draws a long flat live plateau before going gray. algoVer → 1.2.20.

- **2026-07-03 (10):** Bridge holds baseline through the gap window; 12 s budget. Field test `1783093877906` (algoVer 1.2.18) showed a sharp ~84.7 bpm edge spike right after the 130 s 3-finger-lift (pre-gap ~71.6) and a wide gray band after the 160 s lift (interpolation did not fire, unlike 100/130 s). Both are biofeedback-engine artifacts (see biofeedback history 2026-07-03 (9)): the bridge flag stayed true while the gap was in the 10 s window but the candidate BPM was the mixed median (synthetic + real post-gap beats), so artifactualy short post-gap RR pulled the guidance BPM up to ~84; and the 160 s re-lock took ~15 s on marginal PPG, exceeding the 8 s bridge budget. Fixes are engine-only: during `bridging` the candidate is anchored to `60_000/stableRr` (the bridge actually holds the baseline), and `OPTICAL_INTERP_MAX_MS`/`OPTICAL_OPEN_BRIDGE_MAX_MS` go 8 s → 12 s. Breath-screen behavior is unchanged; the effect is that camera guidance BPM no longer spikes at gap boundaries and short lifts stay bridged longer. algoVer → 1.2.19.

- **2026-07-03 (9):** Start-of-running emulated grace. Field test `1783091581263` (algoVer 1.2.17) showed a ~24 s start-of-session gray band: the peak detector lost lock in settle's tail (last beat ~11 s before `running`), so beat-staleness at t=0 was already ~11 s, and the emulated fallback waited the full `BREATH_CAMERA_EMULATED_FALLBACK_MS = 20 s` → ~9 s of gray non-pacing measurement at start. The product spec says a long loss must switch to a synthetic sine wave so breathing continues; at start there is no prior live running beat to "recover" to, so a short grace is safe. Fix: new `runningLiveBeatSeenRef` (false at each running start, flips true when a trusted beat arrives IN running, reset in beginFromIdle/background/cleanup). While false, `liveBeatAbortReady` uses `BREATH_CAMERA_EMULATED_START_GRACE_MS = 4 s` instead of 20 s; once a trusted beat arrives, the 20 s threshold resumes (mid-session brief losses stay on bridge/hold). Paired with biofeedback history 2026-07-03 (8) peak-detector re-acquire sweep, which shortens the underlying re-lock so real pulse returns faster too. The emulated seed already uses the plausible baseline (history 2026-07-03 (8)), so the start synthetic pacing reflects the user's real ~80 bpm. algoVer → 1.2.18.

- **2026-07-03 (8):** Camera start + emulated seed now use the plausible baseline when no coherent baseline exists. Field test `1783091581263` (algoVer 1.2.16) still showed a bogus guidance-graph "floor" at ~44.9 bpm at t=0 and short spontaneous gray bands through the session. On marginal PPG the coherence gate almost never passes, so `pipeline.getLastStableRrMs()` was 0 for the whole session; the camera-hold guidance value (`holdStableBpm`) fell through to the noisy `snapshot.pulseRateBpm ≈ 44.9`, and the planner seed / emulated seed fell to the 65 default. Biofeedback history 2026-07-03 (7) added a looser `getLastPlausibleBpm()` (last slew-limited `displayBpm` in 50–110, noise excluded). This UI now uses it as the second-tier fallback in three places: the camera-hold guidance value (above the 65 default — eliminates the 44.9 floor), the `running`-phase planner seed (above `snapshot.pulseRateBpm`), and `switchToEmulatedPulse`'s camera seed. Net effect on camera: short marginal-PPG flickers bridge at the user's real ~80 bpm (no gray, no 44.9 floor), and the start emulated ramp seeds at ~80 instead of 65. The ~24 s start gray band remains honest (peak detector lost lock ~11 s before `running`; 8 s bridge budget can't cover it). algoVer → 1.2.17.

- **2026-07-03 (7):** No practices-code change; `COHERENCE_ALGORITHM_VERSION` → 1.2.16 to track the paired biofeedback pipeline safety net (history 2026-07-03 (6): `computePipelineBridgeOverride`). The breath-runtime already treats `bridgingShortGap = true` as live, so the safety net — which makes the pipeline emit `bridgingShortGap` on short marginal-PPG losses even when the engine bridge was blocked by noisy residual beats — directly removes the spontaneous short gray bands (e.g. t≈14–16 s, t≈72–73 s in export `1783089776509`) without any UI-side change. The 20 s finger lift still correctly shows a gray tail after the 8 s bridge budget.

- **2026-07-03 (6):** No practices-code change; `COHERENCE_ALGORITHM_VERSION` → 1.2.15 to track the paired biofeedback engine fix (history 2026-07-03 (5): empty-window bridge rescue). The breath-runtime already treated `bridgingShortGap = true` as live (`cameraReadyForLog`, `isPulseLogEntryLiveForMeasurement`), so the rescue — which makes the engine emit `bridgingShortGap` on short marginal-PPG losses even when the RR filter rejected the whole window — directly removes start-of-session gray bands on camera sessions without any UI-side change. Field test `1783088279299`: the ~22 s start gap's first ≤ 8 s now stays live at the coherent baseline (~80 bpm) instead of painting gray.

- **2026-07-03 (5):** Camera start + camera-hold guidance now seed from the coherent baseline, not warmup noise. Field test `1783086748164` (algoVer 1.2.13; same 3×3 s + 1×20 s lift protocol) confirmed the 2026-07-03 (4) coherence-gating worked — noise no longer paced breathing and the 75.8–86.5 bpm range showed no edge spikes through any lift — but exposed two residual polish issues at the boundaries of the loss window. (a) On a marginal-PPG start the warmup peak-detector left `snapshot.pulseRateBpm ≈ 54.7` (a noise value; the user's real pulse was ~80, and a coherent warmup window HAD set `lastStableRrMs = 750 ms`). At `running` the planner was seeded from `snapshot.pulseRateBpm` (`seedBpm = snapshot.pulseRateBpm > 0 ? snapshot.pulseRateBpm : INITIAL_SEED_BPM`), and the camera-hold guidance branch fell back to `snapshot.pulseRateBpm` when `lastLoggedGuidanceBpmRef` was still 0 — so for the first ~16 s (until the 20 s emulated fallback fired) the practice breathed at ~54.7 and the guidance graph painted a bogus "floor" at 54.7 before jumping to 80. (b) The 20 s lift's emulated fallback seeded at 65 (default) instead of ~80 because the finger-off `shouldHardReset → softReset()` had wiped `lastStableRrMs` (see biofeedback history 2026-07-03 (4)). Fixes: (1) the `running`-phase planner-seed effect now prefers `pipeline.getLastStableRrMs()` (coherent baseline, only updated on low-jitter windows) over `snapshot.pulseRateBpm`, clamped to the camera-seed range 50–90 bpm; wearable mode still trusts `snapshot.pulseRateBpm` directly (strap HR is not noise-polluted). (2) The camera-hold guidance value (`cameraHoldGuidance` branch in the `pulseBpm` log subscription) now prefers the same coherent baseline before falling back to `snapshot.pulseRateBpm`; mid-session holds already have `lastLoggedGuidanceBpmRef` set so this only participates at start. (3) The camera-seed constants (default 65, clamp 50–90) were promoted to module level so the planner seed and the emulated seed use one definition. The measurement-graph gray band at start is honest (no live beat yet); the guidance graph now starts at the real ~80 and transitions seamlessly into live. algoVer → 1.2.14.

- **2026-07-03 (4):** Camera noise-tracking no longer paces breathing — emulated fallback now wins on incoherent signal. Field test `1783085782371` (algoVer 1.2.12, finger-on 5-min practice) showed the peak detector flickering `tracking` on marginal-PPG noise (amplitude 0.006–0.037, `signalQuality` 0.73–0.87, `fingerPresenceConfidence` 0.74–0.81 — all "good") for the first ~2.5 min, producing a bogus climbing BPM 44→77 that paced breathing and rendered as `liveMeasurementActive` on the charts, while the user's real pulse was ~88–94 (only visible once amplitude rose to ~0.037 at t≈126). The 2026-07-03 (3) emulated-fallback fix never fired because each noise `tracking` flicker reset `lastFreshBeatSourceTsRef` (the staleness clock) before 20 s could elapse. The discriminator is `looksCoherent` (low jitter, ≥5 RR): noise has huge jitter (BPM 44→77 in 7 s is physiologically impossible), real pulse is stable. Three coordinated fixes: (1) `CoherenceBreathScreen.trustedFreshBeat` now requires `looksCoherent || bridgingShortGap` — bare `tracking` no longer resets the loss clock, so incoherent noise accumulates staleness and the practice falls to the emulated synthetic sine wave within 20 s; the measured chart honestly grays out noise instead of plotting it as live pulse. (2) `BiofeedbackPipeline` only updates `lastStableRrMs` on a `looksCoherent` window (was: any `trackingNow`), so the emulated seed reflects a genuinely coherent baseline rather than the last noisy median (~46 bpm). (3) `switchToEmulatedPulse` camera seed is sanitized: when no coherent baseline exists it falls back to a 65 bpm resting pace and clamps the result to 50–90 bpm, so a bogus low/high outlier can't drive synthetic breathing absurdly. The pipeline `lockState` still uses the loose `liveWindowUsableForTracking` (anti-freeze design preserved); only the breath-screen trust gate and the baseline-RR recording now require coherence. Recovery from emulated likewise requires a coherent beat, preventing bounce-back onto noise. algoVer → 1.2.13.

- **2026-07-03 (3):** Camera emulated-fallback now fires on pure beat staleness, not only on finger absence. Field test `1783084493476` (algoVer 1.2.11, 5-min finger-on practice, no lifts) collapsed: the peak detector lost lock at ~38 s and never sustainably recovered (marginal PPG amplitude 0.007–0.013, finger-presence confidence stayed high), so `measuredPulseRateBpm` went to 0 for 87 % of the session while `emulatedActive` stayed `false` the whole time — the practice ran on a stale held ~76 bpm guidance value for 4.5 min instead of a synthetic sine wave. Root cause: `CoherenceBreathScreen`'s `liveBeatAbortReady` required `(!fingerOk || fingerAbsentWallMs ≥ 3 s)` alongside the staleness threshold, so when the finger was detected but produced no beats (cold finger / weak perfusion / marginal signal — exactly the "палец есть, пульс не считывается" case the spec says must switch to synthetic), the fallback was blocked forever. Fix: the camera-time `liveBeatStaleMs ≥ BREATH_CAMERA_EMULATED_FALLBACK_MS (20 s)` path now triggers emulated on its own, regardless of finger presence; the wall-clock variant keeps the finger-absence guard to stay immune to bg/fg wall-clock jumps. Brief hiccups are still absorbed upstream — the engine bridges gaps ≤ ~8 s and holds BPM through reacquire, and 20 s is well past that, so the staleness-only path is an unambiguous real loss. Verified across scenarios (3 s lift → bridged, no fallback; 20 s lift → fallback; good signal → no fallback; finger-on-no-beats → fallback). algoVer → 1.2.12.

- **2026-07-03 (2):** Bridged camera gaps are first-class live pulse in practice results. Together with the source-level optical reconstruction (`bridgingShortGap`, see biofeedback history 2026-07-03 (2)), `CoherenceBreathScreen` and `breath-results-series` now treat a bridged short gap as a live measurement even when the last REAL beat is older than the 5 s freshness window: the engine already bounds the bridge (≤ 8 s), and the reconstructed pulse is exactly what guidance breathes to. `pulseLog` entries record the new `bridgingShortGap` flag, and `isPulseLogEntryLiveForMeasurement` honors it, so a 3 s finger lift (plus optical re-lock) draws **no** gray band and no boundary spikes on the results charts — only genuinely long losses (→ emulated) remain gray. Verified on the `1783079328709` export replay.

- **2026-07-03 (1):** Camera guidance/runtime now matches the intended “delay a few beats, then decide” behavior. Field test `1783032655745` showed that one 3 s finger lift already behaved correctly, but two other 3 s lifts still turned into a second gray segment after the finger had returned, because the runtime eventually received `reacquiring` from the source layer and zeroed measured pulse again. At the same time the 20 s lift fell into `emulated`, but the camera never restored before session end because recovery only got a series of short 2.5 s optical probes. User-visible fix: short gaps that are still plausibly only a few missed beats no longer trigger the hard reacquire path at the source, so practice guidance/results keep the pre-gap BPM context and slide back onto real pulse instead of dropping into a fake post-return `holding + 0 bpm` band. For real long-loss fallback, camera recovery probes now retry sooner and can stay alive continuously while the optical path is clearly seeing finger/signal signs, so emulated guidance has a realistic chance to hand back to live pulse before the practice ends.

- **2026-07-02 (6):** Camera short-gap behavior in practice results/runtime now follows the intended interpolation contract. Field test `1783030499402` showed that 3 s finger lifts still produced wide gray bands even after the low-fps bandpass fix, because `CoherenceBreathScreen` zeroed `measuredPulseRateBpm` too early: it only treated a camera sample as live when a very fresh beat had just arrived, while the runtime still legitimately sat in `holding`/recent `tracking` with a usable held BPM. The practice layer now treats recent camera `holding`/`tracking` (non-emulated, non-reacquiring, `event.bpm > 0`, beat age ≤ 5 s) as a live measured pulse. In practice this means short finger lifts ride through as interpolation/hold instead of immediately drawing a large gray band, while truly stale camera data still degrades to non-live and then emulated pacing. The export/runtime contract was also tightened so emulated fallback writes `pulseSource="emulated"` + `pulseLockState="searching"` instead of leaking probe-only internal camera states into result logs.

- **2026-07-02 (5):** Camera fallback/runtime follow-up. Field test `1783029200609` showed two UX-level rough edges in `CoherenceBreathScreen` after the earlier emulated→live plateau fix: (1) during long camera fallback the runtime could briefly claim `tracking` / `0 bpm` (or even flip back to live too early) before a real optical beat had returned, because the recovery probe accepted stale pre-probe snapshot state; (2) the runtime status during emulated fallback exposed the synthetic beat-stream as if it were real camera tracking. Fixes: the camera recovery probe now records the start of each probe window and returns from `emulated` only after a **fresh real beat** has arrived after that moment; failed probes clear the marker and pause optical again. Separately the biofeedback snapshot adapter now reports `pulseLockState = searching` and `hasFreshBeat = false` while `pulseSource=emulated`, so the practice UI no longer says “tracking” when the app is only guiding by synthetic fallback. The underlying low-fps optical recovery fix (matched 6/8 Hz bandpass buckets) lives in `biofeedback/history.md` 2026-07-02 (13); practices history records only the user-visible runtime contract.

- **2026-07-02 (4):** Field-test follow-up #4 (re-run of the 3 practices after #3). Charts again surfaced real bugs; fixed at source, self-verified on the new exports (`modules/breath/core/__field_verify.test.ts`, `modules/biofeedback/signal/ppg-bandpass.test.ts`). (1) **Optical pulse barely locked (`holding` most of the session).** Regression from #3's fps 15→10: the PPG bandpass had no coefficients below 15 Hz, so a 10 Hz stream was mis-filtered and the peak detector lost lock. Added matched 10/12 Hz SOS and set the camera long-run to **12 Hz** (see biofeedback history 2026-07-02 (3)). (2) **BLE5 "two gray bands from one removal":** a short island of Polar buffered beats between the removal and reconnect is now fused into a single band (`mergeWearableReconnectIslands`). (3) **RMSSD spikes (135/89 then snap back):** dense RMSSD/stress now Hampel-clean each window and exclude RR that fall inside a gap, so the window never reaches across a gap into the strap's frozen off-body RR; the analysis also replaces flagged RR with the local (not session) median. (4) **RSA inflated first point / drift:** per-second RSA skips a startup warmup and measures the range of the linearly-detrended breath window, and RSA/coherence result series are gated to the same gray bands as the pulse. (5) **Metric start offsets** (coherence ~1 min, stress ~15–30 s) confirmed inherent (window math) and left as-is; RMSSD/stress already start from t=0. (6) **LLM interpretation terseness** on the gapped 5-min session is data-driven (sparser/artifact-laden series), not a payload bug; the cleaner metrics improve the model's input. No prompt change.

- **2026-07-02 (3):** Field-test follow-up #3 (optical 5 min, Polar H10 5 min w/ removal, Polar H10 10 min w/ push-ups). Charts drove out real algorithm bugs, fixed at the source and self-verified on the exports (`modules/breath/core/__field_verify.test.ts`). (1) **Measured == guidance for wearables:** `CoherenceBreathScreen` no longer logs the quantized integer `wearableHeartRateBpm` for the measured chart while guidance used the smooth RR-derived `event.bpm` — both now log the same live RR-derived value, so the "Пульс (измерение)" plateaus and the divergence between the two charts outside gray zones are gone. (2) **No fake gaps on a stably-worn strap:** short wearable non-live runs (< `WEARABLE_MIN_REAL_GAP_MS` = 8 s) are no longer shaded, and the measured line holds the last live value across a transport blip flanked by live readings instead of dipping to 0 (`breath-results-series.ts`); the underlying BLE RR-gate fixes (see biofeedback history 2026-07-02 (2)) stop those blips from being recorded at all going forward. (3) **RSA/coherence breaks reconstructed:** the two RSA breaks in the push-up run were single Polar RR-buffer artifacts (6.0 s / 8.7 s voids at a continuous HR); `bridgeArtifactRrGaps` rebuilds the missing beats so the metric line stays continuous where the pulse never stopped, while true removals stay honest gaps. (4) **Dense RMSSD/stress:** result RMSSD & stress are now dense per-second (`buildDenseHrvSeriesFromBeats`) over windows that grow from t=0, gated to the same signal-loss intervals as the pulse chart — RMSSD is no longer a 3-segment line and stress no longer starts at 1:00. (5) **Optical heat:** long-run camera fps 15→10 Hz (guidance-only; metrics are BLE-only).

- **2026-07-02 (2):** Field-test follow-up #2. (1) **Crash fix:** camera guidance-only practices threw `CoherenceEngine.finalize() called without active session` at 5:00, because the 2026-07-02 overheating fix stopped calling `coherenceEngine.startSession()` in `cameraGuidanceOnlyMode` but the finalize path still called `finalize()`. The camera branch now uses the engine result only when a session is actually active, otherwise it derives an empty (all-suppressed) result via the side-effect-free `analyzeWindow` — no session is opened, so the perf/heat win stands. (2) **Gap-band alignment:** `collectNonLiveIntervalsFromLog` / `collectFlaggedIntervalsFromLog` now anchor a non-live interval's LEFT edge to the last live sample's timestamp instead of the first *logged* non-live sample. When the sensor drops, the pulse log can go silent for several seconds before a non-live sample is recorded (BLE reconnect: last live RR at 105.4 s, first emulated log entry at 113.3 s); the old code left that slice unshaded so the measured line "ended before the gray zone." The band's left edge is now exclusive (`tMs > startMs`) so the boundary live sample keeps its value and the line touches the band edge exactly. (3) **Guidance continuity:** the guidance chart is always defined (live → hold → emulated), so it now draws as one continuous line across the gray band (`continuousAcrossGaps` → `maxGapMs = ∞`, breaking only at true zeros); the silent-log slice no longer splits the flat hold line off from the pre-gap live line. Measured and metric charts still break honestly at time gaps. (4) **Warm-up is inherent, not legacy:** confirmed the late metric starts are mathematical, not optical-era filtering — coherence needs a 60 s sliding FFT window (`windowSeconds: 60`) to resolve the ~0.1 Hz breathing peak, so it cannot produce a value before ~50–60 s regardless of sensor precision; RMSSD/stress need ≥ `HRV_MIN_VALID_BEATS_FOR_METRICS` (30) beats (~30 s); RSA needs ~one breath cycle (~10 s). The mid-graph gaps are the real 40 s strap removal plus one window's refill and are shown truthfully.

- **2026-07-02:** Field-test follow-up (camera + Polar H10 exports). **Charts must be honest, not pretty.** (1) `sanitizeBreathGuidanceBpm` was rewritten from a latching filter into a pure step-limiter (`maxStepBpm`, default 6): the old «recent-accepted spread ≤ 3 → freeze» branch produced the phantom guidance plateau near 4:30 while measured pulse kept moving, so it was removed together with the `recentGuidanceBpmRef` state. (2) `dropPostGapMeasuredArtifacts` now only folds a sample when it is *actually* preceded by a near-zero gap sample (`prev != null && isNearZero(prev.value)`); the previous `prev == null` branch wrongly treated the very first session sample as a post-gap recovery and dropped it (deviation threshold also tightened 5→4 BPM). This removes the plunge at the first grey band and the low→high jump at ~140 s. (3) Metric result charts (coherence / RMSSD / stress / RSA) stopped calling `bridgeSeriesAcrossNonLiveGaps` and now pass `extendEnd: false`: instead of drawing straight bridges across signal-loss windows and an artificial flat tail to 5:00, `splitPulseChartSeriesSegments` breaks the polyline at zero samples **and** at any time gap wider than an adaptive limit (≈3.5× median spacing, ≥6 s). The long straight coherence line and the horizontal RMSSD/stress tails were both bridging artifacts. (4) RSA amplitude is now sourced from the new dense per-second `rsaAmplitudeBpm` (≈one point/second, ~222 pts on a 5-min run) instead of the sparse ~10 per-cycle summary that rendered as 5 straight segments. (5) `resultChartYAxis` was given a fixed 52 px width with right-aligned labels so every chart's plot area starts at the same x — the gap bands / gridlines now line up across all graphs. The late start of metric graphs is genuine engine/analysis warm-up (a full window of clean beats is required before the first honest value) and is now shown truthfully rather than back-filled to 0:00.

- **2026-07-01 (8):** Breath result charts no longer re-sanitize guidance or draw post-gap camera artifacts. `buildPulseSeriesFromLog` stopped re-running `sanitizeBreathGuidanceBpm` over the already runtime-sanitized `guidancePulseRateBpm`: after a hold/gap the sanitizer's «recent accepted» history collapsed to one repeated value (spread 0) and then permanently rejected every real ≥3.5 BPM change, freezing the guidance line at the pre-gap value (the phantom horizontal plateau). The logged guidance is now plotted faithfully. A new `dropPostGapMeasuredArtifacts` folds the first unreliable live sample right after a measurement gap back into the gap when it deviates > 6 BPM from the stabilized run that follows, killing the false ~58 BPM point wedged between a gap and the real ~69 BPM. Guidance during emulated fallback now reads as a flat hold at the last live rate (matching the synthetic beat-train pacing) instead of following a decaying value, removing the phantom downward dip inside the gap band. The coherence result series now indexes `perSecond[secondIndex - 1]` (it is 0-indexed with 1-based `secondIndex`); the old off-by-one mislabeled boundary-second coverage. Breath also honors the new `pulseBpm.reacquiring` flag: a reacquiring camera frame is not treated as a live measurement or guidance beat.

- **2026-07-01 (7):** Breath guidance spike filtering no longer freezes the old BPM during a large real pulse change: small isolated jumps are still suppressed, but larger shifts are step-limited so camera guidance does not form a false plateau while measured pulse is moving. During short camera hold/interpolation windows, non-live `pulseBpm` no longer updates either the visible guidance series or `BreathPhasePlanner`; guidance holds the last accepted live BPM until real pulse or emulated fallback returns. Camera long-loss fallback now requires a stable recovery window before clearing the hard-loss accumulator, and result pulse charts apply the same 8-second stable-recovery merge, so a brief false `tracking` blip cannot split a ~20 s finger removal into multiple short gaps. Result pulse charts also replay guidance through the current sanitizer and remove short PPG recovery spike-runs, while HRV/RSA/coherence calculations remain unchanged. Advanced result charts now share the same fixed `0…practiceTotalMs` x-axis as pulse charts, so 5-minute BLE reports keep the correct 0:00→5:00 scale even when the first real metric point appears later.

- **2026-07-01 (6):** Pulse chart shading now uses one shared non-live interval on measured and guidance graphs with a fixed 0…session-duration time axis; BLE reconnect retries after failed connect instead of stopping at `failed`; interpretation button appears when partial metric series exist even if the session ended emulated.

- **2026-07-01 (5):** Breath results pulse polylines disappeared after decimation because segment splitting treated ~2.5 s chart sampling as signal loss; charts now break only at zero measured samples. Wearable search hides «Повторить поиск» while scanning.

- **2026-07-01 (4):** Breath results pulse charts now split measured vs guidance data at the source: measured series breaks on non-live samples and highlights gap bands with vertical edges + translucent shading; guidance keeps hold/emulated pacing and filters isolated BPM spikes before they reach the planner. BLE `guidedOnly` sessions treat stale RR the same as `fullMetrics` when RR was ever seen, JSON export adds `debug.pulseLockTransitions`, BLE prep uses the shared black-curtain fade into practice, and the wearable not-found dialog puts primary retry + close on one row.

- **2026-07-01 (3):** BLE prep→practice transition no longer flashes the full sensor-activation screen for one frame; RMSSD/stress/RSA result charts drop single-point garbage spikes from off-body beats, RSA stops bridging across signal-loss gaps, and camera/BLE measured pulse charts render vertical edges at zero plateaus so loss duration reads correctly.

- **2026-07-01 (2):** Field-test follow-up for camera/BLE breath exports.

- **2026-07-01:** Breath results pulse/metric charts were reworked around explicit live-vs-synthetic intervals. Camera short finger-off gaps now stay on interpolation/hold instead of triggering emulated fallback at 12 s, measured pulse only zeroes during true emulated/long-loss windows, BLE measured pulse zeroes when RR is stale even if Polar keeps streaming HR-only packets, metric charts bridge non-live gaps with straight lines (fixing RSA spikes), BLE prep waits for the first live HR/RR before `running`, and practice interpretation now times out instead of hanging silently.

- **2026-06-30 (6):** Breath BLE startup no longer jumps straight into `running` with an unavoidable first `0 bpm` sample. The screen now waits in the wearable activation/preparation path until the strap actually reaches `guidedOnly/fullMetrics` readiness, remembered chest straps are re-validated by a short live BLE probe instead of `manager.devices()` cache alone, and results charts stop fabricating left-edge plateaus for coherence/RSA/RMSSD/stress before those metrics have really started computing.

- **2026-06-30 (5):** Breath results charts were hardened against warmup/recovery artifacts. BLE metric series no longer disappear just because the final aggregate session later fails a trust/coverage gate, chart x-axes are extended to the full practice duration instead of stopping at the last sparse sample, and camera pulse graphs no longer freeze forever after an `emulated -> live` return caused by mixed source time bases.

- **2026-06-30 (4):** Breath results stopped tying time-series charts to the exact same final gates as the aggregate summary. BLE sessions can now still show the captured `RMSSD` / stress / RSA / coherence graphs for the reliable part of the run even if the final aggregate row and interpretation CTA are withheld later, and pulse charts split into `measured` vs `guidance` whenever fallback pacing diverges from the real live sensor trace.

- **2026-06-30 (3):** Camera guidance-only long-loss fallback was tightened again after fresh field exports: once the camera path has already crossed the hard “signal lost” conditions, `CoherenceBreathScreen` now switches to emulated pulse immediately instead of waiting through a second identical timeout. This keeps a ~20-second finger removal on continuous pacing while still allowing short 3-second detachments to stay on the honest live/holding graph.

- **2026-06-30 (2):** Breath results became graph-first. Camera guidance-only sessions now show only a pulse chart and an explicit note that LLM-style biometric interpretation requires a BLE RR source; BLE results render pulse plus every reliably available metric chart (coherence, RSA, RMSSD, stress) and only show the `Интерпретация` CTA when at least some real biometrics survived the session.

- **2026-06-29 (3):** Breath practice camera mode was simplified into guidance-only runtime: the phone camera now drives breathing from live pulse/BPM, long camera-signal loss falls back to emulated pulse instead of aborting the session, and advanced HRV/coherence/RSA results are reserved for BLE devices that actually deliver RR. Running/results copy now explicitly tells the user to switch to a Bluetooth heart-rate sensor for detailed biometrics.

- **2026-06-29 (2):** Practice cards now probe whether a remembered BLE chest strap is actually advertising before showing it as a selectable pulse source; unavailable devices fall back to phone camera and the picker shows actionable troubleshooting when scan finds nothing.

- **2026-06-29:** Breath final trust now tolerates shaky finger placement at the very beginning better than before. Grace-window logic: no blind discard of minute one — start evaluation after last early recovery or from minute two if failures persisted; hybrid start/end measurement windows each use `applyInitialGraceWindow: true` like the whole-session path.

- **2026-06-29 (2):** Breath running-flow learned a bidirectional fallback. After sustained camera or BLE signal loss `CoherenceBreathScreen` can seed emulated pulse from the latest stable BPM, keep the practice continuous, and then automatically return to the live source once camera recovery-probe or wearable HR/RR recovery confirms that the real signal is back.

- **2026-06-29 (4):** Camera guidance-only UX stopped surfacing multiple low-level signal states during the practice. Instead, `CoherenceBreathScreen` now shows one soft reminder at most once per session, and only after about 5 seconds of sustained camera-signal loss/instability, so users still see that pulse guidance is real without being distracted by technical banner churn.

- **2026-06-29 (5):** Camera guidance-only reminder/fallback now keys off the age of the last real detected beat, not only `fingerDetected`. This fixes the case where frames kept arriving after the finger was removed, but the app failed to show the soft reminder or enter `emulated` fallback because contact heuristics stayed ambiguous while no fresh live pulse was actually coming in.

- **2026-06-29 (6):** BLE running UX was simplified for production breathing. The practice screen no longer shows a misleading “strap is reconnecting” banner during the initial connect path, and transient `guidedOnly` downgrades (for example while a full-metrics strap is re-establishing contact) no longer flash an HRV-paused warning over the breathing session.

- **2026-06-29 (7):** Breath results no longer hand off to a dead communicator queue. The old `Обсудить` path was replaced with an inline `Интерпретация` state inside the same results modal: it sends the already-saved `outcomeToCommunicatorPayload(...)` plus the user's post-practice mood to a dedicated `POST /api/communicator/v2/practice-interpretation` endpoint and swaps the metrics table for a short STANDARD-model summary without navigating away from the practice.

- **2026-06-30:** BLE sleep/resume follow-up: the shared background guard in `CoherenceBreathScreen` no longer treats wearable sessions like camera sessions by checking `fingerDetected` after a background round-trip. On BLE this could surface the “Практика приостановлена” flow even without a JS crash; resume now leaves recovery to the wearable HR/RR path and emits better runtime diagnostics for the next export.

- **2026-06-28 (5):** Breath results no longer expose finger-camera biometrics as all-or-nothing. `CoherenceBreathScreen` now reads `SignalTrustLevel` from the biofeedback pipeline: `guided_limited` keeps the practice on real pulse/baseline guidance and stores only RMSSD/stress, while `pulse_only` withholds final biometrics instead of pretending that degraded finger signal can still support coherence/RSA.

- **2026-06-28 (6):** Breath results gained a late-session fallback for finger-camera HRV. If the overall session ends in `pulse_only`, but the tail of the measurement still contains a sufficiently long clean segment, `CoherenceBreathScreen` now preserves `RMSSD` / stress from that reliable tail instead of dropping them to `—`; coherence and RSA stay hidden.

- **2026-06-28 (7):** Breath results no longer hide coherence/RSA by default once the full session degrades. `CoherenceBreathScreen` now performs a second, stricter fallback pass for coherence/RSA on the latest continuous 180/150/120-second tail window; only windows with no local gap events and with a passing coherence-analysis gate are allowed to surface recovered coherence/RSA.

- **2026-06-27 (4):** `PracticeCard` now collapses inline duration/chakra/pulse selectors on screen focus (`useFocusEffect`), so returning from a prematurely stopped breath session no longer leaves the duration grid expanded in the catalog or communicator card.

- **2026-06-27 (3):** BLE breathing launch was tightened after device QA. The remembered/just-picked strap path no longer shows the old warmup / quality-check screen: `CoherenceBreathScreen` goes straight to the running practice, while Bluetooth failures within the first seconds now reopen the same shared `WearablePickerDialog` that the catalog card uses. This also removed the duplicated BLE picker UI between the card and the practice screen.

- **2026-06-27 (2):** Breath-card BLE UX moved one step earlier. The pulse-source labels were renamed to `Пульс с телефона` / `Пульсометр Bluetooth` / `Без пульсометра`, and choosing BLE without a remembered strap no longer jumps straight into practice start. `PracticeCard` now swaps the CTA to «Найти пульсометр», opens a small in-card modal scanner, lets the user pick a found model, persists that device into wearable preferences, and only then returns to the normal «Начать практику» flow.

- **2026-06-27:** Breath launch contract moved from binary `usePulseSensor` toward explicit sensor selection: `sensorMode`, remembered BLE device identity (`deviceId` / `deviceName` / `provider`), `capabilityTier`, `autoReconnect`, with `usePulseSensor` kept only for backward-compatible deep links. `PracticeCard` now surfaces `camera / Bluetooth / without sensor`, persists the preferred mode through wearable preferences, and the route `app/breath-coherence.tsx` forwards the richer query payload to `CoherenceBreathScreen`.

- **2026-06-24 (4):** Non-tab and fullscreen practice surfaces moved onto shared UI ownership. `app/asana-practice.tsx` now uses shared `StackScreenLayout`, `StackScrollView`, `SurfaceCardView`, `ScreenHeader`, and `FloatingCloseButton`; assistant fullscreen wrappers on Home/Day are routed through one shared `AssistantModalShell`; immersive practice chrome starts converging on shared close / stop-confirm / overlay-autohide primitives instead of per-screen copies.

- **2026-06-24:** Practices catalog yoga path now keeps a per-locale local snapshot (`services/practiceCatalogCache.ts`) and seeds `loadPracticeCatalog({ initialYoga })` from it before hitting Supabase. Result: повторное открытие вкладки «Практики» показывает уже известные асаны мгновенно, фоновой revalidate больше не сбрасывает список в пустое состояние, а object-shaped ошибки Supabase сериализуются в читаемый текст вместо `"[object Object]"`.

- **2026-06-24 (2):** Названия асан сведены к одному алгоритму. Вместо RU-only / EN-only хвостовых замен `catalog.ts` теперь использует общий `resolveYogaPracticeTitle(...)`: helper убирает импортный Vimeo suffix (`_и3`, `_i3`, …) и подставляет locale-native `yogaTitlePrefix` (`Практика`, `Practice`, `Übung`, `Pratique`, …). Тот же helper подключён в `app/asana-practice.tsx`, чтобы каталог и экран самой асаны не расходились. В `PracticeCatalogScreen` нижний отступ списка стал зависеть от реальной высоты tab bar, чтобы контент не уходил под нижнюю навигацию.

- **2026-06-24 (3):** `PracticeCatalogScreen` переведён на общий tab-shell UI-слоя: `TabScreenLayout`, `ScreenHeader`, `StateCard`, `useTabScreenContentProps`. Цель — чтобы safe area, header и state-блоки для вкладки «Практики» больше не эволюционировали отдельно от Home/Day/Profile.

- **2026-06-22:** Assistant practice handoff v4: вернули opaque cover (`AssistantPracticeHandoffCover`) — родительский Modal (Home/Day) показывает его по `onPracticeLaunchStart` до dismiss; min-delay overlay сокращён **700→200 ms** (двойной `requestAnimationFrame` в `useAssistantPracticeOverlayDismiss`); Day tab использует тот же delayed dismiss, что Home. Причина: flash вкладки под Modal без cover; 700 ms давали лишнюю задержку после paint practice route.

- **2026-06-19:** Practices-tab regression follow-up. Root cause was split across data shape and deferred-load semantics: the lighter yoga query had dropped the persisted thumbnail/chakra fallback path, and timeout/error still surfaced to `PracticeCatalogScreen` as a plain empty array. Fix: `catalog.ts` now selects only the needed jsonb slices (`video_thumbnail`, `chakra_ids`, `primary_chakra_id`, `recorded_at`) instead of the full `params` blob, and `onLateYogaPractices` reports `state: "ready" | "timeout" | "error"` so the screen can keep late-loading or show a partial-catalog error instead of silently rendering «нет асан». `PracticeCatalogScreen.tsx` also stopped prefetching Vimeo thumbnails for cards that already have a persisted thumbnail, so the DB path stays authoritative and does not get pointlessly overwritten by null live fetches.

- **2026-06-18 (2):** Yoga catalog again reads `params` from Supabase so `PracticeCard` gets persisted `video_thumbnail` immediately. Root cause: the lighter select without `params` made the catalog depend almost entirely on live thumbnail fetches to Vimeo/backend; when those timed out, covers disappeared even though thumbnails were already stored in DB.

- **2026-06-18:** Yoga catalog load: Supabase select без `params`, таймаут отложенной йоги 12→30 с, `loadYogaPractices` пробрасывает ошибку Supabase; fallback чакр из `params.chakra_ids` при пустом `practice_chakras`. Каталог: typed i18n для empty/late-loading/hint (`emptyPracticesHint`, `yogaLateLoadingTitle`, `invalidChakraFilterHint`) вместо hardcoded RU в `PracticeCatalogScreen`.

- **2026-06-16 (3):** Assistant practice i18n: `practiceCardSummary.ts` — generic card/reason fallbacks для всех 8 локалей (без chakra-name leakage); `practiceSelection.ts` локализует `practice_picked.name`. `SacredSymbolStreamScreen` — density chip labels через `getSymbolStreamStrings` (`densityAiry`/`densityBalanced`/`densityDense`).

- **2026-06-16 (2):** Typed pluralization/footer: `PracticeCatalogStrings` — `practiceCountOne`, `practiceCountWithTotal`, `catalogFooterTemplate`; `getPracticeCatalogStrings` wires `practiceCount`/`catalogFooter` from overlays (EN singular via `practiceCountOne`). `PracticeCatalogScreen` — group tab titles: `numberOfLines={2}`, `adjustsFontSizeToFit`, tighter grid padding.

- **2026-06-16:** i18n follow-up: `PracticeCatalogStrings` gained `durationMinUnit` / `durationFromPrefix` (typed gate); `PracticeCard` uses them instead of locale ternaries. `catalog.ts` `localizedText` reads yoga jsonb by full `AppContentLocale`; `displayYogaTitle` uses `inlineBaseLocale` + `SOURCE_LOCALE` for RU-only title transforms.

- **2026-06-19 (4):** Assistant overlay dismiss v3: откат cover/`onPracticeLaunchStarted`; закрытие overlay только после `launchPractice` + focus practice route + min-delay 700 ms (без «возврата на Главную» между шагами).
- **2026-06-19 (3):** Assistant overlay dismiss v2: *(отменено в v3)*
- **2026-06-19 (2):** Assistant `practice_picked` теперь держит согласованные `launch.params` с card overrides: `applyPracticeCardOverridesToPayload` в `practiceSelection.ts` запекает выбранные `durationMin`/`chakraIndex` в `durationSec`, `chakraIds` и query-параметры launch до SSE/export (раньше overrides жили отдельно от catalog-default launch).
- **2026-06-19:** Assistant practice dismiss: route-обёртки медитации/дыхания/йоги вызывают `useAssistantPracticeOverlayDismiss(launchSource)` — overlay коммуникатора закрывается только после mount экрана практики (см. `assistantPracticeOverlayDismiss.ts`), убирая flash Home/Day под full-screen Modal.
- **2026-06-14:** Каталог практик локализован: `modules/practices/i18n/practices.ts` (`getPracticeCatalogStrings`, typed gate); `loadYogaPractices(locale)`; `PracticeCatalogScreen`/`PracticeCard` используют `useAppLocale().locale` и `chakra/i18n` для tag labels.

- **2026-06-11 (2):** Assistant practice text снова разделён на два разных слоя: visible `reason` мотивирует выполнить практику сейчас, а `card_blurb` описывает саму карточку. RU yoga fallback в карточке использует падежную форму `зона шестой чакры`, чтобы не было фразы `зона шестая чакра`.
- **2026-06-11:** Server fallback для assistant practice reason убрал падежные конструкции с русскими названиями чакр (`через шестая чакра`). Видимый bubble practice-pick теперь может опираться на безопасный `card_blurb`, а reason остаётся нейтральной связью практики с фокусом дня.
- **2026-06-09:** `SacredSymbolStreamScreen` стал учитывать источник запуска при завершении практики. Если медитация была открыта из ассистента или со вкладки «День» (`launchSource = "assistant" | "day"`), завершение теперь переводит пользователя на `/day`, а не возвращает назад на Home-экран поверх уже закрытого assistant overlay.
- **2026-06-08 (3):** `practiceCardSummary.ts` перевёл русские chakra labels в fallback-текстах карточки на нумерованную форму (`первая чакра` … `седьмая чакра`). Это касается прежде всего йога-card blurbs, которые раньше могли показывать санскритские имена чакр при серверном fallback.
- **2026-06-08 (2):** `services/dayHealthContext.ts` использует `user_daily_stats` как baseline для финального подытоживания дня: ассистент получает минуты/количество практик за день и сравнение с обычной практикой пользователя. Это не меняет запись `practice_sessions`: как и раньше, учитываются только завершённые практики.
- **2026-06-08:** `PracticeCard` больше не сбрасывает выбранную пользователем длительность медитации/дыхания после локального выбора, пока карточка остаётся той же практикой. Дефолт чакры для дыхания/медитации теперь берётся из `primaryChakra` / `chakraIds`, а вкладка «День» перед сохранением pending-практики принудительно подставляет `user_daily_forecasts.day_target_chakra` для meditation/breath offer.
- **2026-06-07:** Вкладка «День» получила одну ожидающую карточку практики на локальный день через таблицу `day_practice_offers`; это не меняет инвариант `practice_sessions`: отчёты и статистика по-прежнему учитывают только завершённые практики. `GET /api/day` автоматически закрывает pending offer как `completed`, если после создания карточки появилась matching completed session.
- **2026-06-07 (2):** Ассистентская `PRACTICE_PICK`-карточка теперь тоже становится pending-практикой вкладки «День»: `Communicator.onPracticeOffered(...)` передаёт `PracticeSummary`, а вкладка сохраняет его через `POST /api/day`.
- **2026-05-26:** Assistant-facing длительности выровнены по нижней границе каталога: серверный `markers.ts` теперь трактует `короткую` / `minimal` практику без числа минут как `1` мин для медитации, `5` мин для дыхания и `20` мин для асан. Это убирает лишний уточняющий вопрос перед карточкой и синхронизирует server inference с шагами `assistantSelectableDurations.ts`.
- **2026-05-23:** Диапазон медитации в карточке практики и в статическом каталоге выровнен с daily dialog: `assistantSelectableDurations.ts` и `PracticeCard.tsx` теперь допускают только **1–5 минут** (не 1–10), а `STATIC_MEDITATIONS` в `catalog.ts` ограничивает `maxDurationSec` тем же потолком. Это убирает расхождение между серверным `catalogDurationRangeForKind("meditation")`, активным prompt `dialog_system_v3` и клиентским выбором минут.
- **2026-05-19 (pre-push doc-sync):** Серверный **`route.ts`** импортирует шаги длительности карточки через **`@shared/assistantSelectableDurations`** (`_legacy_web/shared_core/assistantSelectableDurations.ts`, копия **`modules/practices/core/assistantSelectableDurations.ts`** для Vercel-only deploy); клиентский канонический файл без изменения контракта. **`spec.md`**, **`dependencies.md`**, **`assistant/dependencies.md`**, **`MAP.md`**, **`CHANGELOG.md`**.
- **2026-05-14 (doc-sync):** **`spec.md`** — лог **`[PRACTICE_CARD_MISMATCH]`** в **`PracticeCard`**: добавлено поле **`source: "practice_card_client_sync"`**; **`dependencies.md`** — явная связь **`route.ts`** ↔ **`assistantSelectableDurations.ts`**; **`MAP.md`**, **`CHANGELOG.md`**.
- **2026-05-15 (карточка ассистента):** **`assistantSelectableDurations.ts`** — общий список минут для дыхания (5–20) и медитации (1–10) + **`clipDurationMinutesToSelectableMinutes`**. Сервер: **`resolvePracticePublic`** — приоритет **`overrides.durationMin`** у **`validateHistoryHasDurationAndType` при `confident`**, иначе маркер; клип; лог **`[PRACTICE_CARD_MISMATCH]`**. Клиент: **`PracticeCard`** клипит `overrideDurationMinutes` и синхронизирует состояние в **`useEffect`**. **`choosePractice`**: при `confident` и конфликте типа маркер vs история — сброс id маркера, поле **`historyKindConflictResolved`**. Тесты **`practiceSelection.test.ts`**. **`assistant/spec.md`**, **`communicator` по ссылке на карточку**, **`CHANGELOG.md`**.
- **2026-05-15:** Недавние практики для ассистента: нормализация к **`id`** каталога (`resolvePracticeKeyToCatalogId`), селектор сравнивает только по **`id`**; классификация дыхания по сессиям — по slug из каталога `breath`, без отдельного списка slug в коде. **`spec.md`**, **`assistant/history.md`**, **`CHANGELOG.md`**.
- **2026-05-15:** Серверный **`choosePractice`**: явный `[PRACTICE_PICK]` больше не обходит **`selectPracticeCandidate`** — окно недавних предложений/сессий того же `kind` применяется и при валидном id в каталоге (исправление повторов дыхания подряд). См. **`assistant/history.md`**.
- **2026-05-14:** Документирован контракт сервера: в **`practiceSelection.ts`** для не-`default` **`[PRACTICE_PICK]`** сначала резолвится **`id` / `slug`** по полному активному каталогу — карточка в приложении совпадает с выбором премиум-модели. **`spec.md`** (§2 интеграция с ассистентом).
- **2026-05-11:** `modules/practices/ui/PracticeCard.tsx` стал единым компонентом для каталога и коммуникатора. Assistant-launch больше не идёт через отдельный `launchPracticeFromAssistant` на home: `Communicator.tsx` сам адаптирует `PracticePicked` в `PracticeSummary`, даёт пользователю override duration/chakra и вызывает `launchPractice(..., { launchSource: 'assistant' })`. На сервере default assistant marker теперь явно резолвится в coherent breathing 10 мин на чакру дня.
- **2026-05-08 (второй проход):** **`PracticeCatalogScreen` + отложенная йога.** (1) Колбэк мог отработать **раньше** продолжения `load()` — сброс **`yogaLateLoading`** и перезапись **`setState`** пустым `yoga` из возврата каталога. (2) Чаще наоборот: продолжение **`load()`** шло **раньше** микрозадачи с **`onLateYogaPractices`**, слот **`lateYogaSlotRef`** оставался пустым → в UI «нет асан» при живых данных. Исправления: слот **`lateYogaSlotRef`**, **`mergedYoga`**, **`yogaLateLoading`** до ожидания, после **`await loadPracticeCatalog`** — **`await Promise.resolve()`**, чтобы слисть очередь микрозадач перед чтением слота. (3) При **медленном** Supabase колбэк мог прийти, пока экран ещё в **`loading`**: ранний **`return current`** в **`setState`** отбрасывал асаны, затем основной поток фиксировал **`ready`** с пустой йогой. Сейчас: **`catalogMeditationBreathRef`** + **`pendingLateYogaRef`**, колбэк всегда собирает **`{ meditation, breath, yoga }`** из ref среза (или откладывает до записи среза). Код: `modules/practices/ui/PracticeCatalogScreen.tsx`.

- **2026-05-08:** **`loadPracticeCatalog` + `onLateYogaPractices`.** Ранее колбэк вешался на «сырой» промис йоги без таймаута и без вызова при `reject` — экран «Практики» мог бесконечно показывать отложенную загрузку асан. Сейчас для этой ветки используется тот же **`withTimeout` (12 с)**, ошибка мапится в пустой список, при срабатывании таймаута после фактического ответа возможен второй вызов колбэка с данными. Код: `modules/practices/core/catalog.ts`.

- **2026-05-07:** Миграция документации по `migration_protocol.md`: канон — код. Исторические briefs из `docs/tmp_docs/02052026/` перенесены в `docs/05_archive/migrated/practices/`. Файлы **`assistant_practice_recommendation_brief.md`** (ассистент + выбор/запуск практики) и **`access_tiers_navigation_brief.md`** (тарифы, табы, в т.ч. `practice_catalog` / `asana_practices`) архивированы **как смешанные** с соседними модулями; для сценариев ассистента см. также будущую/текущую документацию `assistant`, для гейтов — `subscription`.

- **2026-05-07:** **Vimeo и экран асан.** ТЗ предполагало проигрывание ~200 видео; фактически `app/asana-practice.tsx` показывает заглушку (нет WebView в dev-client) и опирается на `video_external_id` из БД для будущего Remote Play / нативного плеера. Каталог и запись завершения сессии реализованы.

- **2026-05-07:** **Биометрия медитации.** В старых формулировках допускалась неопределённость; в коде **`SacredSymbolStreamScreen`** не используется biofeedback и в `recordPracticeSession` уходит **`metrics: {}`**. Пульсометр и расчёт итоговых метрик — только **`CoherenceBreathScreen`**.

- **2026-05-07:** **Каталог дыхания vs таблица `practices`.** Дыхательные карточки собираются из констант **`BREATH_PRACTICES`** в коде, а не из строк Supabase с `kind = 'breath'` (таблица при этом допускает `breath` по схеме init — для серверных сценариев/импорта). Йога-асаны — из БД.

- **2026-06-18:** **Yoga catalog load reliability.** `loadYogaPractices` no longer pulls heavy `params` blobs (Vimeo embed JSON) on the catalog path, raises the deferred-load timeout to 30s, and surfaces Supabase errors instead of silently returning an empty list. Practice catalog empty/late states use locale-native strings (removed hardcoded RU dev copy in `PracticeCatalogScreen`).

- **2026-05-07:** **Дефолт длительности «Вспышка».** В `modules/practices/core/catalog.ts` для launch по умолчанию задано **3 мин** (`defaultDurationSec` / `durationMs` в карточке); в **`SacredSymbolStreamScreen`** при отсутствии пропсов используется **5 мин** (`DEFAULT_DURATION_MS`). Поведение зависит от пути входа (каталог передаёт `durationMs`, прямой диплинк — может не передавать).

- **2026-05-07:** **`user_practice_preferences`.** Триггер `practice_sessions_update_prefs` наращивает счётчик только если **`practice_id` не null**; клиент задаёт UUID **`practice_id` для асан**, для дыхания и медитации в типичном пути — нет, поэтому автоматическое ведение предпочтений по UUID для них не срабатывает.
