type Message = {
  role: "user" | "assistant" | "system";
  meta?: Record<string, unknown> | null;
};

export const ORCHESTRATOR_INSTRUCTIONS = {
  opening: `[Инструкция оркестратора:
это первый ход разговора. Поздоровайся в соответствии со
временем суток. Сделай короткий мост от темы дня к разговору —
не пересказывай дневную рекомендацию, возьми один живой нерв
из темы дня. Пригласи пользователя рассказать, что у него
сегодня происходит — что волнует, какие планы, что важного.
В этом же сообщении мягко выясни длительность практики и тип
(асаны, дыхание или медитация). Всё в одном естественном
сообщении, не в трёх вопросах подряд.]`,
  inquiry: `[Инструкция оркестратора:
это уточняющий ход. Слушай пользователя, иди за его языком.
Если он что-то сказал о себе — поддержи коротко. Не давай
развёрнутых советов сейчас. Мягко уточни то, чего ещё не
хватает из трёх вещей (КОНТЕКСТ, ДЛИТЕЛЬНОСТЬ, ТИП). Один
вопрос за ход. Если все три собраны и согласованы — выведи
только маркер [READY_FOR_RECOMMENDATION]. Если пользователь
явно просит практику без разговора — сразу выясняй длительность
и тип, не настаивай на разговоре.]`,
  final_recommendation: `[Инструкция оркестратора: пользователь созрел для рекомендации. Сейчас ты ОБЯЗАТЕЛЬНО выдаёшь развёрнутый финальный ответ, строго в следующем порядке и без сокращений:

1) Зеркало — короткое отражение того, с чем пользователь пришёл (2–4 предложения).
2) Темы — что в его рассказе главное, что вторично (2–4 предложения).
3) Главная тема через призму чакры дня — связь его ситуации с чакрой и её сегодняшней гармоничностью (3–6 предложений).
4) Штрих глубины — один абзац из нейрофизиологии, или Юнга, или философии (на выбор, не смешивать регистры) (2–5 предложений).
5) Мостик к практике — почему именно эта длительность и этот тип практики ему сейчас (2–4 предложения).
6) В САМОМ КОНЦЕ, на отдельной строке: маркер [PRACTICE_PICK: id="..." duration_min="..." chakra="..." reason="..."]. Поля duration_min (запрошенная пользователем длительность в минутах, целое число) и chakra (номер чакры дня, число от 1 до 7) обязательны в маркере.

Запрещено: отвечать только маркером, пропускать любой из блоков 1–5, ставить маркер в середине текста. Длина финального ответа — не меньше 1200 символов до маркера.]`,
  final_recommendation_with_validation_warning: `[Инструкция оркестратора: standard-модель сигнализировала готовность, но автоматический валидатор не нашёл явных признаков длительности или типа в истории.

Если ты можешь дать рекомендацию с разумным дефолтом (когерентное дыхание 10 минут на активную чакру дня) — давай. Если нет — мягко уточни недостающее прямо в тексте, НЕ выдавая маркер [PRACTICE_PICK].

В случае выдачи рекомендации — ОБЯЗАТЕЛЬНО следуй полной структуре:
1) Зеркало — короткое отражение того, с чем пользователь пришёл (2–4 предложения).
2) Темы — что в его рассказе главное, что вторично (2–4 предложения).
3) Главная тема через призму чакры дня — связь его ситуации с чакрой и её сегодняшней гармоничностью (3–6 предложений).
4) Штрих глубины — один абзац из нейрофизиологии, или Юнга, или философии (на выбор, не смешивать регистры) (2–5 предложений).
5) Мостик к практике — почему именно эта длительность и этот тип практики ему сейчас (2–4 предложения).
6) В САМОМ КОНЦЕ, на отдельной строке: маркер [PRACTICE_PICK: id="default" duration_min="..." chakra="..." reason="..."]. Поля duration_min (запрошенная пользователем длительность в минутах, целое число; если не указана — разумный дефолт 10) и chakra (номер чакры дня, число от 1 до 7) обязательны в маркере.

Запрещено: отвечать только маркером, пропускать любой из блоков 1–5, ставить маркер в середине текста. Длина финального ответа — не меньше 1200 символов до маркера.]`,
  forced_final: `[Инструкция
оркестратора: диалог дошёл до максимума ходов. Дай финальную
рекомендацию прямо сейчас на основе того, что уже собрано.
Информация может быть неполной — используй разумные дефолты
(когерентное дыхание 10 минут на чакру дня), если данных не
хватает. Это финальный ход, после него диалог закрывается.

Следуй структуре финального ответа из режима
final_recommendation (зеркало, темы, главная через призму
чакры, штрих глубины, мостик и [PRACTICE_PICK: id="..." duration_min="..." chakra="..." reason="..."]). duration_min и chakra обязательны.

Если контекста почти нет — пропускай части зеркала и тем,
переходи сразу к мостику и практике. Не выдумывай за
пользователя то, чего он не сказал.]`,
  post_recommendation: `[Инструкция
оркестратора: практика уже выбрана в предыдущем ходу. Веди
себя кратко по правилам из системного промпта (раздел «если
практика уже выбрана»). Маркер [READY_FOR_RECOMMENDATION] не
используешь.]`,
} as const;

export interface ArcDecision {
  mode: "opening" | "inquiry" | "forced_final" | "post_recommendation";
  modelTier: "premium" | "standard";
  instruction: string;
}

function hasPracticePicked(message: Message): boolean {
  const practicePicked = (message.meta as { practicePicked?: unknown; practice_picked?: unknown } | null)?.practicePicked
    ?? (message.meta as { practice_picked?: unknown } | null)?.practice_picked;
  return practicePicked != null;
}

export function decideTurnMode(
  history: Message[],
  iteration: number,
  maxDialogLength: number,
): ArcDecision {
  if (history.some((message) => message.role === "assistant" && hasPracticePicked(message))) {
    return {
      mode: "post_recommendation",
      modelTier: "standard",
      instruction: ORCHESTRATOR_INSTRUCTIONS.post_recommendation,
    };
  }

  if (iteration >= maxDialogLength) {
    return {
      mode: "forced_final",
      modelTier: "premium",
      instruction: ORCHESTRATOR_INSTRUCTIONS.forced_final,
    };
  }

  if (iteration === 1) {
    return {
      mode: "opening",
      modelTier: "standard",
      instruction: ORCHESTRATOR_INSTRUCTIONS.opening,
    };
  }

  return {
    mode: "inquiry",
    modelTier: "standard",
    instruction: ORCHESTRATOR_INSTRUCTIONS.inquiry,
  };
}
