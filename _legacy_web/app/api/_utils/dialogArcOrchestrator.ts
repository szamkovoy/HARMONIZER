type Message = {
  role: "user" | "assistant" | "system";
  meta?: Record<string, unknown> | null;
};

export const ORCHESTRATOR_INSTRUCTIONS = {
  opening: `[Инструкция оркестратора:
это первый ход разговора. Поздоровайся в соответствии со
временем суток. Сделай короткий мост от темы дня к разговору —
не пересказывай дневную рекомендацию, возьми один живой нерв
из темы дня. Пригласи пользователя рассказать, что у него
сегодня происходит — что волнует, какие планы, что важного.
В этом же сообщении мягко выясни длительность практики и тип
(асаны, дыхание или медитация). Всё в одном естественном
сообщении, не в трёх вопросах подряд.]`,
  inquiry: `[Инструкция оркестратора:
это уточняющий ход. Слушай пользователя, иди за его языком.
Если он что-то сказал о себе — поддержи коротко. Не давай
развёрнутых советов сейчас. Мягко уточни то, чего ещё не
хватает из трёх вещей (КОНТЕКСТ, ДЛИТЕЛЬНОСТЬ, ТИП). Один
вопрос за ход. Если все три собраны и согласованы — выведи
только маркер [READY_FOR_RECOMMENDATION]. Если пользователь
явно просит практику без разговора — сразу выясняй длительность
и тип, не настаивай на разговоре.]`,
  final_recommendation: `[Инструкция
оркестратора: это финальная рекомендация. Standard-модель
подтвердила, что вся информация собрана.

СТРУКТУРА ФИНАЛЬНОГО ОТВЕТА:

1. Зеркало. Подтверди общее ощущение от дня пользователя —
сложный/лёгкий/насыщенный/спокойный. Покажи, что услышал его.
Одно-два предложения.

2. Темы. Перечисли ключевые темы, которые пользователь
озвучил. По каждой — короткий комментарий или совет, если
уместен. Если тема одна — этот пункт сливается с пунктом 3.

3. Главная тема через призму чакры дня. Возьми наиболее
волнующую пользователя тему и рассмотри её ПОДРОБНО через
линзу активной чакры и её гармоничности.

— Если чакра гармонична: её состояния из harmonic_states_pool
являются РЕСУРСОМ. Назови один-два конкретных состояния и
покажи, как пользователь может на них опереться в действиях
по этой теме. Не просто перечисли — свяжи с его ситуацией.

— Если чакра дисгармонична: состояния из dissonant_states_pool
являются РИСКАМИ. Назови один-два, которых стоит избегать,
покажи, во что они могут вылиться в его конкретной ситуации.
Предложи противовес.

— Если смешанная: упомяни и ресурс, и риск, отметь
двойственность дня.

Цель — помочь пользователю посмотреть на тему шире, не
действовать шаблонно.

4. Штрих глубины. Одно-два предложения из конкретного регистра,
привязанные к главной теме: архетипическая психология (Юнг,
Кэмпбелл), нейрофизиология (используй lexical_neurophysiological
файла чакры), или практическая философия. Не смешивай регистры.
Не цитируй — формулируй своё наблюдение.

5. Мостик и практика. Объясни, почему именно эта практика
(длительность, тип) подходит ему сейчас. Мостик должен быть
конкретным: связан со сказанным пользователем И с темой дня.

В конце добавь маркер [PRACTICE_PICK: id="..." reason="..."].

Не каждый ответ обязан включать все 5 частей. Если пользователь
сказал мало — частей будет меньше. Если темы у него три —
главная разворачивается одна, остальные обрабатываются
короткими комментариями.]`,
  final_recommendation_with_validation_warning: `[Инструкция оркестратора: standard-модель сигнализировала
готовность, но автоматический валидатор не нашёл явных
признаков длительности или типа в истории.

Если это критично для рекомендации — мягко уточни недостающее
в финальном тексте, НЕ выдавая [PRACTICE_PICK]. Premium-модель
в этом случае фактически возвращает разговор в режим уточнения.

Если ты можешь дать рекомендацию с разумным дефолтом —
когерентное дыхание 10 минут на активную чакру дня — давай.
Маркер: [PRACTICE_PICK: id="default" reason="..."]

В остальном следуй структуре финального ответа из режима
final_recommendation (зеркало, темы, главная через призму
чакры, штрих глубины, мостик).]`,
  forced_final: `[Инструкция
оркестратора: диалог дошёл до максимума ходов. Дай финальную
рекомендацию прямо сейчас на основе того, что уже собрано.
Информация может быть неполной — используй разумные дефолты
(когерентное дыхание 10 минут на чакру дня), если данных не
хватает. Это финальный ход, после него диалог закрывается.

Следуй структуре финального ответа из режима
final_recommendation (зеркало, темы, главная через призму
чакры, штрих глубины, мостик и [PRACTICE_PICK]).

Если контекста почти нет — пропускай части зеркала и тем,
переходи сразу к мостику и практике. Не выдумывай за
пользователя то, чего он не сказал.]`,
  post_recommendation: `[Инструкция
оркестратора: практика уже выбрана в предыдущем ходу. Веди
себя кратко по правилам из системного промпта (раздел «если
практика уже выбрана»). Маркер [READY_FOR_RECOMMENDATION] не
используешь.]`,
} as const;

export interface ArcDecision {
  mode: "opening" | "inquiry" | "forced_final" | "post_recommendation";
  modelTier: "premium" | "standard";
  instruction: string;
}

function hasPracticePicked(message: Message): boolean {
  const practicePicked = (message.meta as { practicePicked?: unknown; practice_picked?: unknown } | null)?.practicePicked
    ?? (message.meta as { practice_picked?: unknown } | null)?.practice_picked;
  return practicePicked != null;
}

export function decideTurnMode(
  history: Message[],
  iteration: number,
  maxDialogLength: number,
): ArcDecision {
  if (history.some((message) => message.role === "assistant" && hasPracticePicked(message))) {
    return {
      mode: "post_recommendation",
      modelTier: "standard",
      instruction: ORCHESTRATOR_INSTRUCTIONS.post_recommendation,
    };
  }

  if (iteration >= maxDialogLength) {
    return {
      mode: "forced_final",
      modelTier: "premium",
      instruction: ORCHESTRATOR_INSTRUCTIONS.forced_final,
    };
  }

  if (iteration === 1) {
    return {
      mode: "opening",
      modelTier: "standard",
      instruction: ORCHESTRATOR_INSTRUCTIONS.opening,
    };
  }

  return {
    mode: "inquiry",
    modelTier: "standard",
    instruction: ORCHESTRATOR_INSTRUCTIONS.inquiry,
  };
}
