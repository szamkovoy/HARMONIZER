/** Shared layout constants for marketing email chrome (safe for client + server). */

/** Content column width — keep preview iframe aligned with this. */
export const MARKETING_EMAIL_MAX_WIDTH_PX = 560;

/** Inner content width after 28px padding on each side of the column. */
export const MARKETING_EMAIL_CONTENT_INNER_WIDTH_PX =
  MARKETING_EMAIL_MAX_WIDTH_PX - 56;
