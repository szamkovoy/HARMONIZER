diff --git a/node_modules/@epubjs-react-native/core/lib/commonjs/View.js b/node_modules/@epubjs-react-native/core/lib/commonjs/View.js
index 0de0fef..b491bd5 100644
--- a/node_modules/@epubjs-react-native/core/lib/commonjs/View.js
+++ b/node_modules/@epubjs-react-native/core/lib/commonjs/View.js
@@ -370,6 +370,7 @@ function View({
   return /*#__PURE__*/_react.default.createElement(_GestureHandler.GestureHandler, {
     width: width,
     height: height,
+    enableSwipe: enableSwipe,
     onSingleTap: () => {
       onPress();
       onSingleTap();
diff --git a/node_modules/@epubjs-react-native/core/lib/commonjs/template.js b/node_modules/@epubjs-react-native/core/lib/commonjs/template.js
index 889bb46..d7a5ca9 100644
--- a/node_modules/@epubjs-react-native/core/lib/commonjs/template.js
+++ b/node_modules/@epubjs-react-native/core/lib/commonjs/template.js
@@ -23,12 +23,19 @@ var _default = exports.default = `
       }
 
       #viewer {
-        height: 100vh;
-        width: 100vw;
+        /* Use % not vw/vh — iOS WKWebView 100vw is often the device width, not the
+           inset WebView width, so side margins looked different in scrolled vs paginated. */
+        height: 100%;
+        width: 100%;
         overflow: hidden !important;
         display: flex;
         justify-content: center;
-        align-items: center;
+        align-items: stretch;
+      }
+      html, body {
+        width: 100%;
+        height: 100%;
+        margin: 0;
       }
 
       [ref="epubjs-mk-balloon"] {
@@ -165,20 +172,32 @@ var _default = exports.default = `
         });
       }
 
+      function hzSafeProgress(loc) {
+        try {
+          if (!loc || !loc.start || !loc.start.cfi || !book.locations) return 0;
+          var p = book.locations.percentageFromCfi(loc.start.cfi);
+          return typeof p === "number" && isFinite(p) ? p : 0;
+        } catch (e) {
+          return 0;
+        }
+      }
+
       book.ready
         .then(function () {
           if (initialLocations) {
             return book.locations.load(initialLocations);
           }
 
-          book.locations.generate(1600).then(function () {
+          // HARMONIZER: await generate so onReady/onLocationsReady always fire (scrolled remount).
+          return book.locations.generate(1600).then(function () {
+            var loc = rendition.currentLocation();
             reactNativeWebview.postMessage(JSON.stringify({
               type: "onLocationsReady",
               epubKey: book.key(),
               locations: book.locations.save(),
               totalLocations: book.locations.total,
-              currentLocation: rendition.currentLocation(),
-              progress: book.locations.percentageFromCfi(rendition.currentLocation().start.cfi),
+              currentLocation: loc,
+              progress: hzSafeProgress(loc),
             }));
           });
         })
@@ -192,7 +211,14 @@ var _default = exports.default = `
               type: "onReady",
               totalLocations: book.locations.total,
               currentLocation: currentLocation,
-              progress: book.locations.percentageFromCfi(currentLocation.start.cfi),
+              progress: hzSafeProgress(currentLocation),
+            }));
+          }).catch(function () {
+            reactNativeWebview.postMessage(JSON.stringify({
+              type: "onReady",
+              totalLocations: book.locations && book.locations.total ? book.locations.total : 0,
+              currentLocation: null,
+              progress: 0,
             }));
           });
 
@@ -256,7 +282,7 @@ var _default = exports.default = `
       });
 
       rendition.on("relocated", function (location) {
-        var percent = book.locations.percentageFromCfi(location.start.cfi);
+        var percent = hzSafeProgress(location);
         var percentage = Math.floor(percent * 100);
         var chapter = getChapter(location);
 
diff --git a/node_modules/@epubjs-react-native/core/lib/commonjs/utils/GestureHandler.js b/node_modules/@epubjs-react-native/core/lib/commonjs/utils/GestureHandler.js
index 9d403dd..50e1daa 100644
--- a/node_modules/@epubjs-react-native/core/lib/commonjs/utils/GestureHandler.js
+++ b/node_modules/@epubjs-react-native/core/lib/commonjs/utils/GestureHandler.js
@@ -18,8 +18,22 @@ function GestureHandler({
   onSwipeUp,
   onSwipeDown,
   onLongPress,
+  enableSwipe = true,
   children
 }) {
+  if (!enableSwipe) {
+    return /*#__PURE__*/_react.default.createElement(_reactNativeGestureHandler.GestureHandlerRootView, {
+      style: {
+        flex: 1
+      }
+    }, /*#__PURE__*/_react.default.createElement(_reactNative.View, {
+      style: {
+        width,
+        height,
+        flex: 1
+      }
+    }, children));
+  }
   const singleTap = _reactNativeGestureHandler.Gesture.Tap().runOnJS(true).maxDuration(250).onStart(onSingleTap);
   const doubleTap = _reactNativeGestureHandler.Gesture.Tap().runOnJS(true).maxDuration(250).numberOfTaps(2).onStart(onDoubleTap);
   const longPress = _reactNativeGestureHandler.Gesture.LongPress().runOnJS(true).onStart(onLongPress);
diff --git a/node_modules/@epubjs-react-native/core/lib/module/View.js b/node_modules/@epubjs-react-native/core/lib/module/View.js
index 63f3c24..653ecb5 100644
--- a/node_modules/@epubjs-react-native/core/lib/module/View.js
+++ b/node_modules/@epubjs-react-native/core/lib/module/View.js
@@ -361,6 +361,7 @@ export function View({
   return /*#__PURE__*/React.createElement(GestureHandler, {
     width: width,
     height: height,
+    enableSwipe: enableSwipe,
     onSingleTap: () => {
       onPress();
       onSingleTap();
diff --git a/node_modules/@epubjs-react-native/core/lib/module/template.js b/node_modules/@epubjs-react-native/core/lib/module/template.js
index dadb7a6..705836c 100644
--- a/node_modules/@epubjs-react-native/core/lib/module/template.js
+++ b/node_modules/@epubjs-react-native/core/lib/module/template.js
@@ -15,12 +15,19 @@ export default `
       }
 
       #viewer {
-        height: 100vh;
-        width: 100vw;
+        /* Use % not vw/vh — iOS WKWebView 100vw is often the device width, not the
+           inset WebView width, so side margins looked different in scrolled vs paginated. */
+        height: 100%;
+        width: 100%;
         overflow: hidden !important;
         display: flex;
         justify-content: center;
-        align-items: center;
+        align-items: stretch;
+      }
+      html, body {
+        width: 100%;
+        height: 100%;
+        margin: 0;
       }
 
       [ref="epubjs-mk-balloon"] {
@@ -157,20 +164,32 @@ export default `
         });
       }
 
+      function hzSafeProgress(loc) {
+        try {
+          if (!loc || !loc.start || !loc.start.cfi || !book.locations) return 0;
+          var p = book.locations.percentageFromCfi(loc.start.cfi);
+          return typeof p === "number" && isFinite(p) ? p : 0;
+        } catch (e) {
+          return 0;
+        }
+      }
+
       book.ready
         .then(function () {
           if (initialLocations) {
             return book.locations.load(initialLocations);
           }
 
-          book.locations.generate(1600).then(function () {
+          // HARMONIZER: await generate so onReady/onLocationsReady always fire (scrolled remount).
+          return book.locations.generate(1600).then(function () {
+            var loc = rendition.currentLocation();
             reactNativeWebview.postMessage(JSON.stringify({
               type: "onLocationsReady",
               epubKey: book.key(),
               locations: book.locations.save(),
               totalLocations: book.locations.total,
-              currentLocation: rendition.currentLocation(),
-              progress: book.locations.percentageFromCfi(rendition.currentLocation().start.cfi),
+              currentLocation: loc,
+              progress: hzSafeProgress(loc),
             }));
           });
         })
@@ -184,7 +203,14 @@ export default `
               type: "onReady",
               totalLocations: book.locations.total,
               currentLocation: currentLocation,
-              progress: book.locations.percentageFromCfi(currentLocation.start.cfi),
+              progress: hzSafeProgress(currentLocation),
+            }));
+          }).catch(function () {
+            reactNativeWebview.postMessage(JSON.stringify({
+              type: "onReady",
+              totalLocations: book.locations && book.locations.total ? book.locations.total : 0,
+              currentLocation: null,
+              progress: 0,
             }));
           });
 
@@ -248,7 +274,7 @@ export default `
       });
 
       rendition.on("relocated", function (location) {
-        var percent = book.locations.percentageFromCfi(location.start.cfi);
+        var percent = hzSafeProgress(location);
         var percentage = Math.floor(percent * 100);
         var chapter = getChapter(location);
 
diff --git a/node_modules/@epubjs-react-native/core/lib/module/utils/GestureHandler.js b/node_modules/@epubjs-react-native/core/lib/module/utils/GestureHandler.js
index 58e8611..2626dec 100644
--- a/node_modules/@epubjs-react-native/core/lib/module/utils/GestureHandler.js
+++ b/node_modules/@epubjs-react-native/core/lib/module/utils/GestureHandler.js
@@ -1,6 +1,11 @@
 import React from 'react';
 import { I18nManager, Platform, View } from 'react-native';
 import { GestureHandlerRootView, GestureDetector, Gesture, Directions, TouchableWithoutFeedback } from 'react-native-gesture-handler';
+
+/**
+ * HARMONIZER patch: when `enableSwipe` is false (vertical scroll mode), do not
+ * install Exclusive fling/tap detectors — they steal touches and freeze scrolled-doc.
+ */
 export function GestureHandler({
   width = '100%',
   height = '100%',
@@ -11,8 +16,23 @@ export function GestureHandler({
   onSwipeUp,
   onSwipeDown,
   onLongPress,
+  enableSwipe = true,
   children
 }) {
+  if (!enableSwipe) {
+    return /*#__PURE__*/React.createElement(GestureHandlerRootView, {
+      style: {
+        flex: 1
+      }
+    }, /*#__PURE__*/React.createElement(View, {
+      style: {
+        width,
+        height,
+        flex: 1
+      }
+    }, children));
+  }
+
   const singleTap = Gesture.Tap().runOnJS(true).maxDuration(250).onStart(onSingleTap);
   const doubleTap = Gesture.Tap().runOnJS(true).maxDuration(250).numberOfTaps(2).onStart(onDoubleTap);
   const longPress = Gesture.LongPress().runOnJS(true).onStart(onLongPress);
